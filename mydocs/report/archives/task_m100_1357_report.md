# 최종 결과보고서 — Task #1357

## 이슈
미주 다단 페이지네이션: body 하단 초과 줄을 다음 쪽으로 미분리 (#1357, #1336 잔여)

대상: `samples/3-09월_교육_통합_2024-구분선아래20구분선위20.hwp` p21 col0
(para 1156~1159, overflow 최대 82px).

## 결정: **바운드 유지 (deferred-tracked)**

정밀 수정(A)을 권장 경로(시각 회귀 하니스 선구축 → 수정)로 끝까지 시도했으나, 안전한
착지가 **실증적으로 불가**함을 확인하고 작업지시자 판단으로 **바운드 유지**로 마무리.

## 근본 원인 (확정)
typeset 누적기 `current_height` 가 layout 실제 렌더 y 보다 **전역 과소누적**
(이 변형 col0 미주에서 ~91px). en_fit 이 작아 fit 판정 → 본문 하단 초과 배치.
#1082 vpos-delta↔layout 정합의 잔여.

## 시도와 회귀 (실증)
default col0 tail 에 HeightCursor 시뮬 렌더-y 예측 advance 블록 추가 →
- p21 오버플로 해소되나, 넘긴 과충전이 **col1 로 전이** → page16/18/19 col1 신규 오버플로
- 전 문서 총합 50.1px → **72.9px 악화**, `issue_1082`(대상) FAIL
- **advance 로는 문제 이동만, 해소 불가** 실증 → 롤백(베이스라인 복구)

## 수확 (보존)
- `scripts/task1274_visual_sweep.py` 에 대상 샘플 타겟(`2024-09-below20above20`) 추가 →
  exam 시각 회귀 세트 확장. cascade 회귀를 정확히 검출(세트 가치 입증).
- 근본 원인·수정 지점·검증 요건·회귀 양상을 stage1/A/BC 문서에 확정 기록
  (다음 착수 시 재조사 불필요).

## 추적
- `tests/issue_1082_endnote_multicolumn_drift.rs` 가 전 문서 오버플로를 **60px 바운드**로
  추적(현 50.1px 이내). 회귀 시 자동 검출.
- 근본 정정은 **typeset 누적기 정합(deep #1082)** 전용 대형 타스크 필요 — 전 exam 회귀
  위험이 커 별도 착수 권장.

## 영향
소스 무변경(시도분 롤백). 회귀 가드 하니스·문서만 추가.
