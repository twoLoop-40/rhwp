# PR #983 / #984 / #985 최종 보고 — dependabot 의존성 업데이트 일괄

## 1. 결정

**3건 모두 merge** — cherry-pick 일괄 처리, 전 검증 통과.

| PR | 대상 | 버전 | 결정 |
|----|------|------|------|
| #984 | `vite` | 8.0.11 → 8.0.13 | ✅ merge (저위험 patch) |
| #983 | `puppeteer-core` | 24.43.0 → 25.0.2 | ✅ merge (dev 한정, API 호환) |
| #985 | `quick-xml` | 0.39 → 0.40 (실제 0.40.1) | ✅ merge (HWPX 회귀 없음) |

작성자: dependabot. base `devel`. 처리: cherry-pick → `pr-deps-batch`
브랜치 (#971/#976 동일 패턴).

## 2. 검증 결과

| 항목 | #984 | #983 | #985 / 통합 |
|------|------|------|-------------|
| cherry-pick | ✅ ebe639f7 | ✅ c901ffd2 | ✅ ee028323 (충돌 없음) |
| `npm run build` | ✅ 성공 | ✅ install OK | — |
| puppeteer API | — | ✅ 25.0.2 launch/connect 함수 호환 | — |
| `cargo build --release` | — | — | ✅ quick-xml 0.40.1 컴파일 |
| 전체 `cargo test` | — | — | ✅ 1484 passed, 0 failed |
| `cargo clippy -D warnings` | — | — | ✅ 0 warnings |
| `cargo fmt --check` | — | — | ✅ 위반 0건 |
| HWPX 파싱/ir-diff | — | — | ✅ 정상 (dump + ir-diff 동작) |
| WASM 빌드 (Docker) | — | — | ✅ 성공 (4.82MB, quick-xml WASM 호환) |

## 3. 위험 평가 결과

### #984 vite (저위험 → 확인)
8.0.11 → 8.0.13 patch. `npm run build` 성공. 런타임/파서 무관.

### #983 puppeteer-core (메이저 → dev 한정 확인)
24 → 25 메이저. **E2E 전용 (puppeteer-core)**, 프로덕션 번들 무관.
핵심 API(`launch`/`connect`) 함수 시그니처 호환 확인. 메이저
변경에도 rhwp E2E 사용 범위 영향 없음.

### #985 quick-xml (런타임 핵심 → HWPX 회귀 없음 확인)
HWPX 파서/시리얼라이저 14파일 의존. 0.39.2 → **0.40.1** minor.
- 14파일 사용에도 컴파일 에러 없음 → API breaking 없음
- HWPX dump + ir-diff 정상 동작 (실제 파싱 회귀 없음)
- WASM 타깃에서도 quick-xml 0.40.1 정상 컴파일
- 전체 test 1484 passed (HWPX 파싱/직렬화 회귀 가드 포함)

> 비고: 본 저장소는 `Cargo.lock` 을 git 추적하지 않음 (라이브러리
> 관행). dependabot 이 `Cargo.toml` 만 갱신한 것이 정상.
> 로컬 `cargo update -p quick-xml` 로 0.40.1 해석·검증 완료.

## 4. 처리

- cherry-pick 3건 → `pr-deps-batch` → 검증 → `local/devel` merge
- PR #983/#984/#985 close (cherry-pick 처리 명시)
- `pr_983_984_985_review.md` / `_report.md` → `pr/archives/`
- 작은 단위 일괄 처리로 위험 분산
  (`feedback_small_batch_release_strategy` 부합)
