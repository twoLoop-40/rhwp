# PR #1111 / #1112 검토 — dependabot dev-dep 업데이트 일괄

> dependabot 자동 PR 2 건. 본질이 단순 (dev-dependency patch level upgrade) 하여
> CLAUDE.md PR 4단계 절차 간소화 — 통합 검토 문서로 일괄 처리.
> 선례: `pr/archives/pr_983_984_985_review.md` (동일 dependabot 일괄 패턴).

## 1. 대상 PR

| PR | 대상 | 버전 | 종류 | 위험도 | 변경 |
|----|------|------|------|--------|------|
| #1111 | `puppeteer-core` | 25.0.2 → 25.0.4 | dev (E2E) | **낮음** (patch) | rhwp-studio/package*.json |
| #1112 | `vite` | 8.0.13 → 8.0.14 | dev (빌드) | **낮음** (patch) | rhwp-studio/package*.json |

공통:
- author: app/dependabot, base: `devel`
- mergeable: MERGEABLE, mergeStateStatus: BEHIND (devel 가 앞섬 — rebase 또는 cherry-pick 필요)
- **CI 전부 pass**: Build & Test, CodeQL, Canvas visual diff, Analyze (js/py/rust)
- 변경 파일: 2 (`rhwp-studio/package.json` + `rhwp-studio/package-lock.json` 만)
- 코드 변경 없음 — 의존성 버전만 patch level 상향

## 2. 위험 분석

### #1111 puppeteer-core (저위험 — patch)

25.0.2 → 25.0.4. 두 patch:
- **25.0.3**: `@puppeteer/browsers` 3.0.2 → 3.0.3 (dev dep 의 dev dep)
- **25.0.4**: "Throw TargetCloseError when session ID not found" 버그 fix
  ([puppeteer#15002](https://github.com/puppeteer/puppeteer/issues/15002))

→ **rhwp-studio E2E (Puppeteer-core 기반)** 전용 — 프로덕션 번들 무관.
선례 PR #983 (24 → 25 메이저) 도 호환 확인된 이력 — 본 patch 는 더 안전.

### #1112 vite (저위험 — patch)

8.0.13 → 8.0.14 (2026-05-21 release). 주요 변경:
- feat: rolldown 1.0.2 업데이트
- fix: HTML transformIndexHtml trailing slash 처리, dev server error handling, optimizer JSX
- 모두 호환적 버그 fix + 의존성 minor 업데이트

→ rhwp-studio 빌드 도구. 런타임/파서/번들 동작 무관. `npm run build` 통과로 검증 충분.

## 3. 처리 방식

선례 #983/#984/#985 의 cherry-pick 일괄 처리 패턴 적용:

1. `pr-deps-1111-1112` 브랜치 생성 (local/devel 기준)
2. 두 PR cherry-pick 순차 (위험 낮은 순 — vite 먼저 → puppeteer)
3. 검증:
   - `rhwp-studio/npm install` → lockfile 정합 + 의존성 해소
   - `rhwp-studio/npm run build` (vite 빌드 확인 — #1112 검증)
   - puppeteer 사용 e2e 테스트 영역 — install 만으로 검증 충분 (메이저 아님)
4. local/devel merge → devel push → 두 PR close (cherry-pick 처리 명시)
5. `pr_1111_1112_review.md` + `pr_1111_1112_report.md` → `pr/archives/`

`feedback_small_batch_release_strategy` 정합 — 작은 단위 일괄 처리로 위험 분산.

## 4. 검증 계획

| 항목 | 명령 | 기대 |
|------|------|------|
| cherry-pick #1112 | `git cherry-pick <vite-sha>` | 충돌 없음 |
| cherry-pick #1111 | `git cherry-pick <puppeteer-sha>` | 충돌 없음 |
| npm install | `cd rhwp-studio && npm install` | 의존성 해소 |
| vite 빌드 | `cd rhwp-studio && npm run build` | success |
| 통합 push | `git push origin devel` | success |

CI 기존 pass — Build & Test / CodeQL / Canvas visual diff 정합 (Rust 영역 무관).

## 5. 본 PR 작업의 핵심 메모리 룰 정합

- `feedback_assign_issue_before_work` — dependabot PR 은 issue 없는 자동 — 본 review/report 가 처리 기록
- `feedback_check_open_prs_first` — 본 task 자체가 PR 처리
- `feedback_small_batch_release_strategy` — 작은 단위 일괄 처리, dev-dep 위험 분산
- `feedback_pr_comment_tone` — dependabot bot 이므로 차분한 사실 중심 close 명시 (감사 톤 부적절)
- `feedback_release_sync_check` — devel push 전 origin/devel 동기화 확인

## 6. 작업지시자 승인 요청

1. 본 통합 검토 (cherry-pick 일괄 처리, 선례 #983-985 동일 패턴) 승인 여부
2. 처리 순서 (#1112 vite → #1111 puppeteer) 권장 수용 여부
3. 검증 영역 (npm install + npm run build) 권장 수용 여부
