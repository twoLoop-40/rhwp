# PR #983 / #984 / #985 검토 — dependabot 의존성 업데이트 일괄

> dependabot 자동 PR 3건. 본질이 단순(버전 범프)하여 CLAUDE.md PR
> 4단계 절차를 간소화, 통합 검토 문서로 일괄 처리.

## 1. 대상 PR

| PR | 대상 | 버전 | 종류 | 위험도 | 변경 |
|----|------|------|------|--------|------|
| #984 | `vite` | 8.0.11 → 8.0.13 | dev (빌드) | **낮음** (patch) | package*.json |
| #983 | `puppeteer-core` | 24.43.0 → 25.0.2 | dev (E2E) | **중** (메이저 24→25) | package*.json |
| #985 | `quick-xml` | 0.39 → 0.40 | **런타임 (HWPX 파서)** | **중·주의** (minor, 14파일 사용) | Cargo.toml 1줄 |

- 공통: base `devel`, MERGEABLE/BEHIND, CI(Build&Test/Analyze/CodeQL/
  Canvas visual diff) **전부 pass**, 각 단일 커밋, author dependabot.

## 2. 위험 분석

### #984 vite (저위험)
8.0.11 → 8.0.13 patch. rhwp-studio 빌드 도구. 런타임/파서 무관.
`npm run build` 통과로 검증 충분.

### #983 puppeteer-core (메이저, dev 한정)
24 → 25 메이저. E2E 테스트(`rhwp-studio/e2e/`)에서만 사용
(puppeteer-core). breaking change 가능 — E2E 스모크 확인 필요.
프로덕션 번들 무관.

### #985 quick-xml (런타임 핵심 — 중점)
HWPX = ZIP+XML, `quick-xml` 은 HWPX 파서/시리얼라이저 **14개 파일**
의존. 0.39 → 0.40 minor 이나 API 변경 시 파싱/직렬화 영향.
→ `cargo build` + HWPX 파싱 테스트 + ir-diff/HWPX 회귀 필수.

## 3. 처리 방식

#971/#976 과 동일 cherry-pick 패턴. 위험 낮은 순 처리:
**#984 → #983 → #985**. 각 단계 빌드/테스트, #985 는 HWPX
회귀까지 확인. 일괄 처리는 작은 단위 회전 운영
(`feedback_small_batch_release_strategy`) 부합.

## 4. 검증 계획

- [ ] #984: cherry-pick + `npm run build` (rhwp-studio)
- [ ] #983: cherry-pick + E2E 스모크 (text-flow 등) 또는 빌드 확인
- [ ] #985: cherry-pick + `cargo build --release` + `cargo test`
      (HWPX 파싱/직렬화) + `cargo clippy` + `cargo fmt --check`
- [ ] 3건 통합 후 전체 `cargo test` + WASM 빌드

## 5. 판단 (잠정)

- #984: 저위험, 검증 통과 시 즉시 수용.
- #983: dev 한정, E2E 통과 시 수용.
- #985: 런타임 핵심 — HWPX 회귀 없음 확인이 수용 전제.
  API breaking 발견 시 별도 코드 수정 또는 보류.

검증 결과에 따라 `pr_983_984_985_report.md` 작성.
개별 보류 필요 시 PR별 분리 처리.
