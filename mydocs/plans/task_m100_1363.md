# 수행계획서 — Task #1363: 미주 높이 모델 SSOT 리팩터

- **이슈**: #1363 (M100), 관련 #1357/#1336/#1082/#1248/#1302
- **브랜치**: `local/task1363` (base: `stream/devel`)
- **성격**: 대형·다회차 아키텍처 리팩터. 조사·설계 우선, 단계적 검증.

## 1. 문제 (확정)
미주 para 높이를 **두 모델**이 따로 계산:
- typeset `compute_en_metrics → (fit, acc)`: saved-vpos delta `(tb-base)` + caps/floors → `current_height` 누적
- layout `HeightCursor.vpos_adjust` + format 줄높이(순차) → 실제 렌더 y

두 모델이 trailing line-spacing·내부 saved-vpos rewind 처리에서 갈려 typeset 이 과소 누적
→ 단 하단 줄 본문 초과(C×C, #1357). 부분 조정은 모두 회귀(#1357 결론).

## 2. SSOT 원칙
**layout 렌더가 ground truth**(눈에 보이는 결과). typeset 누적은 layout 이 렌더할 높이를
**정확히 예측**해야 한다. → 두 경로가 **동일한 높이 함수**를 공유.

## 3. 설계 방향 (Stage 2 에서 확정)
- 후보 A: typeset 누적을 layout `HeightCursor` 로직으로 대체(동일 코드 → 본질적 SSOT).
  최강 정합, 단 현 exam 튜닝(compute_en_metrics 기준)을 전면 재검증·재튜닝해야 함.
- 후보 C: 공유 함수 `endnote_para_rendered_advance(para, ctx)` 추출 후 양쪽이 호출,
  divergence 를 항목별(trailing-ls/rewind/cap) 점진 제거. 회귀 위험 분산.
- **선호: C(점진)** — 단계마다 exam 가드로 게이트.

## 4. 단계 (잠정, 다회차)
1. **Stage 1 — 모델 매핑**: compute_en_metrics 의 모든 분기(advance_px/min_h/cap/
   stale/rewind)와 HeightCursor.vpos_adjust 의 대응을 1:1 표로 정리. 각 divergence 의
   exam 별 영향(어느 가드가 그 분기에 의존하는지) 식별.
2. **Stage 2 — SSOT 설계**: 공유 높이 함수 시그니처·반환(fit/advance/split-friendly) 확정.
   migration 전략(플래그 A/B, exam 골든값 비교 하니스) 수립.
3. **Stage 3+ — 점진 이행**: divergence 항목 1개씩 SSOT 로 이전, 매 단계 전체 cargo test +
   `task1274_visual_sweep.py` 무회귀 확인. 회귀 시 해당 항목 롤백·재설계.
4. **Stage N — 검증·정리**: C×C(p17)·p22 오버플로 해소, 전 exam 무회귀, issue_1082 바운드
   대폭 감소 확인. 신규 회귀 가드 추가.

## 5. 검증 (전 단계 공통)
- 전체 `cargo test` 0 failed (특히 issue_1082/1139/1274/1284)
- `scripts/task1274_visual_sweep.py --target all` flagged 수 ≤ 베이스라인
- 대상 p17/p22 시각 PDF 정합

## 6. 위험
최고난도 영역(SSOT 부재로 회귀 빈발). 점진+게이트로만 진행. 단일 단계라도 회귀 잡히면
즉시 롤백. 무리한 일괄 변경 금지.

## 7. 베이스라인 우선
Stage 1 착수 전, 현재(stream/devel) exam 가드·sweep flagged·issue_1082 px 를 **골든
베이스라인**으로 기록(회귀 판정 기준).
