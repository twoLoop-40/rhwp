# Task #321 v5 수행 계획서 — pi=0 block-table drift 보정

## 배경

Stage 5 (`mydocs/working/task_m100_321_stage5.md`) 결과:
- 21_언어 page 1 col 0 drift = +76.3 px
- 출처: pi=0 단독 — 4×5 폼 표(wrap=TopAndBottom, vert_rel_to=Paper) 를 포함한 첫 문단의
  block-table layout 이 본문 좌표계와 mismatch
- pi=1..pi=9 본문은 HWP LINE_SEG 위치 와 정확히 일치 (per-paragraph 오차 0)

drift 76.3 px 만 제거되면 pi=9 가 col 0 에 통째로 들어가 PDF 와 일치한다.

## 목표

`typeset_block_table` (또는 호스트 paragraph advance 처리) 에서 다음을 만족하는 보정 추가:

1. 21_언어 page 1: col 0 drift 0 ± 5 px → pi=9 가 col 0 에 통째로 fit, col 1 시작이 pi=10
2. 다른 샘플(exam_math/kor/eng, form-002 등) 회귀 0
3. 시각적 표 위치는 무변경 (block-table 은 이미 본문 위에 그려짐)

## 산출물

- 코드: `src/renderer/typeset.rs` (또는 관련 layout 파일) 의 block-table 호스트 advance 보정
- 보고: `mydocs/working/task_m100_321_stage6.md` (수정 후 측정 결과)
- 최종: `mydocs/report/task_m100_321_v5_report.md`

## 검증 기준

| 항목 | 기준 |
|------|------|
| 21_언어 p1 col 0 | drift ≤ 5 px (현 76.3 → 0) |
| 21_언어 p1 col 1 첫 문단 | "적합성 검증이란..." (pi=10) |
| 21_언어 p1 표 시각 위치 | 변경 없음 (PDF 일치) |
| 페이지 수 | 21_언어 16, exam_math 20, exam_kor 24, exam_eng 10 (현재 값 유지) |
| `cargo test --lib` | 992 passed |
| 골든 SVG 회귀 | `tests/golden_svg/` 무변경 또는 의도된 변경만 |

## 단계 분할

3 단계.

1. **Stage 6a — 가설 검증**: 수정 코드 시안 1 개 작성 (H1: block-table host advance 보정).
   21_언어 p1 단일 측정 후 drift 0 달성 여부 확인. 다른 샘플 페이지 수 변화 확인.

2. **Stage 6b — 회귀 검증**: 골든 SVG diff, lib tests, 4 개 sample 페이지 수 비교. 회귀 발견 시
   조건 정밀화 (vert_rel_to=Paper + wrap=TopAndBottom + col 0 첫 문단 등 더 좁은 가드).

3. **Stage 6c — 보고서**: 측정 표 + 수정 diff + 회귀 결과 + 최종 보고서.

## 위험

- block-table 의 advance 를 줄이면 pi=0 다음 본문이 표와 시각적으로 겹칠 위험 → 표가 절대 좌표로
  그려지는지(=Paper-anchored), block-table 자리 차지(=Para-anchored) 인지 구분 필수.
- col 1 의 `pending_body_wide_top_reserve` 와 양립 검증 필요 — col 1 은 이미 reserve 적용되므로
  col 0 에서만 advance 보정 적용해야 double-fix 회피.
- 다른 샘플의 page 1 첫 문단이 동일 패턴(Paper-anchored block table) 일 경우 페이지 수가 변할 수
  있음 → 수치 변화는 시각 검증 후 의도된 것이면 수용.

## 승인 요청

본 수행 계획서 승인 시 `mydocs/plans/task_m100_321_v5_impl.md` (구현 계획서) 작성으로 진행.
