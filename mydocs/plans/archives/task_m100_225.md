---
타스크: #225 mydocs/manual/MEMORY.md 링크 누락 및 중복 파일
유형: 문서 정리 (C 방안 · 핫픽스 최소 문서)
브랜치: local/task225
작성일: 2026-04-23
기여자 보고: @InsuJeong496
---

# 수행계획서

## 배경

@InsuJeong496 님이 2026-04-20 보고: `mydocs/manual/` 아래에 **MEMORY.md 가 두 군데에 존재** 하며 상위 경로(`mydocs/manual/MEMORY.md`) 의 파일은 **링크가 모두 깨져있음**.

## 현황 분석

3개 MEMORY.md 파일 비교:

| 파일 | 줄수 | 상태 | 링크 |
|------|------|------|------|
| `~/.claude/projects/-home-edward-mygithub-rhwp/memory/MEMORY.md` | 24 | **최신** (AI 메모리 시스템 원본) | 같은 폴더 파일 참조, 정상 |
| `mydocs/manual/MEMORY.md` | 22 | **중복 · 링크 깨짐** | 상대 경로지만 같은 폴더에 파일 없음 (실제는 `memory/` 하위에 있음) |
| `mydocs/manual/memory/MEMORY.md` | 20 | **구버전 공개 사본** | 정상 |

이력 추정:
- 원본은 `~/.claude/.../memory/` (AI 메모리)
- 공개 투명성 차원에서 `mydocs/manual/memory/` 로 수동 사본 (20 항목 시점에서 멈춤)
- 누군가 `mydocs/manual/MEMORY.md` 에 22 항목 시점에서 파일을 별도 저장 (어떤 경위든 중복 발생)

## 변경 내용

### 1. `mydocs/manual/MEMORY.md` 삭제
- 중복 파일, 링크 깨짐
- 정보 손실 없음 — 내용은 두 상대 파일에 모두 있거나 더 최신임

### 2. `mydocs/manual/memory/MEMORY.md` 갱신
- `~/.claude/.../memory/MEMORY.md` (24 항목) 와 동일 내용으로 갱신
- 2026-04-23 오늘까지 추가된 메모리 4개 반영:
  - `feedback_release_sync_check.md`
  - `feedback_release_manual_required.md`
  - `feedback_external_docs_self_censor.md`
  - `feedback_amo_submission_gotchas.md`

### 3. `mydocs/manual/memory/` 에 누락된 4개 메모리 파일 복사
- 위 4개의 **실제 내용 파일** 을 공개 사본에 복사
- `~/.claude/.../memory/feedback_*.md` 4개 → `mydocs/manual/memory/feedback_*.md`

## 위험 요소

- **내용 공개 적합성**: 4개 파일 내용에 비공개 정보가 있는지 확인 필요.
  - `feedback_release_sync_check` — 릴리즈 절차 (공개 적합 ✅)
  - `feedback_release_manual_required` — 매뉴얼 정독 규칙 (공개 적합 ✅)
  - `feedback_external_docs_self_censor` — 자기검열 체크리스트 (공개 적합 ✅ · 이미 이 체크리스트 자체는 투명성 가치 있음)
  - `feedback_amo_submission_gotchas` — AMO 제출 함정 (공개 적합 ✅ · 다른 오픈소스 Firefox 확장 개발자에게 가치)
  - 결론: 4개 모두 공개 적합

## 검증

- [ ] `mydocs/manual/MEMORY.md` 삭제됨
- [ ] `mydocs/manual/memory/MEMORY.md` 24 항목, ~/.claude 와 diff 0
- [ ] 복사된 4개 파일 존재
- [ ] `mydocs/manual/memory/MEMORY.md` 의 모든 `[이름](파일명.md)` 링크가 같은 폴더 파일 가리킴 (작동 확인)

## 추가 조치 — 후속

본 이슈는 **일회성 동기화** 로 해결하되, 근본적 drift 방지책은 별도.

- **권고 (별도 이슈 또는 이번 작업 포함)**: 메모리 등록/갱신 시 공개 사본 동시 갱신을 AI 작업 체크리스트에 포함.
- 본 타스크 범위 외 — 별도 이슈 등록 고려.

## 절차

하이퍼-워터폴 축약 절차 (C 방안):

1. 본 수행계획서 승인
2. 즉시 구현 (MEMORY.md 삭제 + 공개 사본 갱신 + 4개 메모리 파일 복사)
3. 커밋 (타스크 브랜치 · 저자 기여자 크레딧 표기)
4. local/devel merge + devel push
5. 이슈 #225 close (@InsuJeong496 감사 코멘트)
6. 최종 보고서 작성 (`mydocs/report/task_m100_225_report.md`)

## 승인 요청

본 수행계획서 승인 시 즉시 구현 진행.
