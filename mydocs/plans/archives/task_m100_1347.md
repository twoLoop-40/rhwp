# 수행계획서 — 단 구분선 부분 페이지 길이 복원 (M100 #1347)

## 1. 배경

`3-09월_교육_통합_2024-구분선아래20구분선위20.hwpx` 23페이지 가운데 단 구분선이 본문 전체 높이로 그려진다. 한글2022 PDF는 콘텐츠 하단까지만(약 44.7%) 그린다.

PDF 픽셀 측정:
| 페이지 | PDF | 현재 렌더 |
|--------|-----|----------|
| 16 (꽉 참) | 87.7% (≈본문) | 본문 전체 ✓ |
| 23 (부분) | 44.7% (콘텐츠) | 본문 전체 ✗ |

## 2. 근본 원인 (조사 확정)

- `91c5cebe` (#1333): 단 구분선을 콘텐츠 높이(body 하단 캡)로 통일 → p3·8·23 짧음 PDF 정합. (작성: planet6897)
- `4e7f191f` (이후, 작성: edwardkim): "keep page column separator full height" — `zone_layout.is_none()`(일반 2단·연속 페이지)일 때 무조건 body 전체 높이 사용 → 부분 페이지(23쪽) 재퇴행.

`emit_zone_column_separators`(layout.rs:2763-2764)의 body 하단 캡(#1333 v2)은 유지되므로, 콘텐츠 높이를 넘으면 body 하단에서 잘린다. 꽉 찬 페이지는 콘텐츠≈body라 전체로 그려진다 → 콘텐츠 높이 단일 경로면 두 경우 모두 정합.

## 3. 수정 방향

`4e7f191f`가 도입한 `prev_zone_sep_full_body` 분기를 제거하고 #1333의 콘텐츠 높이(`prev_zone_y_end`) 단일 경로로 복원. (사용자 승인: "#1333 콘텐츠높이 복원")

영향 파일: `src/renderer/layout.rs` (단일)

## 4. 범위 및 제약

- HWP3 분기 추가 없음. 공통 layout.rs.
- shortcut.hwp(#874) 혼합 zone 부분 구분선 정합 유지(콘텐츠 높이는 #1333에서 이미 검증).
- body 하단 캡 로직은 그대로 유지.

## 5. 검증

1. `export-svg -p 22`(p23) → 구분선 y 길이 PDF(44.7%) ±1.5% 정합
2. p16(꽉 참) 전체 높이 유지 확인
3. shortcut.hwp 9개 구분선 보존, 중복 0
4. `cargo test --release` 회귀 무결

## 6. 산출물

- 구현계획서 `task_m100_1347_impl.md`, 단계별 `working/`, 최종 `report/`
