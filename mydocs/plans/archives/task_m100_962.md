# 수행 계획서 — Task #962: 시험지 page 2 <보기> textbox content scramble

- 이슈: [#962](https://github.com/edwardkim/rhwp/issues/962)
- 마일스톤: M100 / v1.0.0
- 브랜치: `local/task962` (base = upstream/devel)
- 관련: Task #960 (PR #963) Stage 4 시각 검증 중 발견된 별도 결함

## 1. 증상

`samples/3-11월_실전_통합_2022.hwp` page 2 의 문14 <보기> textbox (pi=118 InFrontOfText TAC 사각형 + 내부 글상자) 내부 content scrambled.

### 한컴 (정답)
```
ㄱ. h(1)=3
ㄴ. 함수 h(x)는 실수 전체의 집합에서 연속이다.
ㄷ. 함수 g(x)가 닫힌구간 [-1, 1]에서 감소하고
    g(-1)=-2이면 함수 h(x)는 실수 전체의 집합에서 최솟값을 갖는다.
```

### rhwp
```
h(1)=h(1)=3            ← ㄱ. prefix 누락 + 중복?
h(x). 함수 h(x)는 실수 전체의 집합에서 연속이다.   ← ㄴ. prefix 누락
g(x[..함수1g(x))가 단힌구간[x])-1, 1]에서 감소하고   ← garbled
    g(-1)=-2이면 함수 h(x)는 실수 전체의 집합에서
    최솟값을 갖는다.
```

## 2. 추정 결함 영역

pi=118 의 구조 (Task #960 dump):
- [0] [사각형] 글앞으로 (InFrontOfText), 글자처럼=true (TAC), z=1735
- 내부 글상자 (text box) paragraphs:
  - p[0] "ㄱ. " + 수식 h(1)=3 (cc=20, ctrls=1)
  - p[1] "ㄴ. 함수 h(x)는 실수 전체의 집합에서 연속이다." (cc=34, ctrls=1)
  - p[2] "ㄷ. 함수 g(x)가 닫힌구간 [-1, 1]에서 감소하고 ..." (cc=83, ctrls=4)

→ TAC 사각형 + 내부 textbox + 다수 inline 수식의 rendering path 결함.

후보:
- A. textbox 내부 paragraph rendering 의 inline TAC 처리 (table_layout.rs / shape_layout.rs)
- B. inline 수식 의 x position 계산 (textbox 안 좁은 영역)
- C. ㄱㄴㄷ prefix 가 inline 수식과 같은 row 에 처리되어 lost
- D. composer.rs 의 textbox 내부 paragraph 처리

## 3. 단계 구성 (5 단계)

- **Stage 1**: 정밀 추적 (textbox 내부 paragraph rendering path 식별 + 시각 비교)
- **Stage 2**: 구현 계획 V2 (fix 위치 + 위험)
- **Stage 3**: Fix 구현 + 단위 검증
- **Stage 4**: 다중 sample 회귀 검증 (특히 InFrontOfText TAC 사각형 + 글상자 + 수식 보유 sample)
- **Stage 5**: 시각 검증 + 최종 보고서 + PR

## 4. 위험 평가

| 위험 | 평가 |
|------|------|
| textbox / 글상자 내부 rendering 변경 → 광범위 회귀 | **매우 큼** |
| inline 수식 위치 계산 변경 → 모든 수식 sample 영향 | **매우 큼** |
| 본 fix 의 본 session 안 완료 | **불확실** |

본 task 도 archive/task936 (9 시도 + 5 revert) 패턴 우려 매우 큰 영역.

## 5. 진행 규칙

- 자동진행 안함 (단계별 명시 승인)
- 회귀 발견 시 즉시 보고 + revert 가능
- 본 session 안 완료 불가 시 Stage 1 (진단) 만 보존 + 새 session 분리

## 6. 참고

- 원 issue #952 + PR #956/#958/#961/#963
- Task #960 의 Stage 4 보고서 (page 2 보기 textbox 발견)
- `RHWP_DEBUG_PARA_TAC` (Task #957/#960 영구화) — 진단 도구
