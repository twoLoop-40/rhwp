# Task #1261 구현 계획

## Stage0: 기준 재현과 원인 분리

- GitHub 이슈 #1261 등록과 `local/task_m100_1261` 브랜치 생성을 기록한다.
- `dump-pages samples/3-10월_교육_통합_2022.hwp -p 4`로 5쪽 `문28)` 주변 문단과 컨트롤 인덱스를 찾는다.
- `dump`와 render tree/SVG 좌표 확인으로 조건 박스 내부에서 겹치는 줄의 bbox를 측정한다.
- 원인을 다음 후보로 분리한다.
  - LINE_SEG 높이 또는 baseline 해석 문제
  - 수식/TAC 컨트롤 높이가 줄높이에 반영되지 않는 문제
  - 표/박스 내부 문단 흐름에서 다음 줄 y 진행이 누락되는 문제
  - 미주 간격 보정과 독립된 본문 문단 배치 문제

## Stage1: 최소 보정

- Stage0에서 확인한 실제 원인에 맞춰 가장 좁은 코드 위치를 수정한다.
- 수식/TAC 높이 보정이 필요하면 해당 문단/줄 조판 경로에서 한컴 기준 높이만 반영하고, 전체 문서 줄 간격을 임의로 키우지 않는다.
- 표/박스 내부 흐름 문제이면 셀/박스 내부 paragraph 진행 경로에서만 y 진행 누락을 보정한다.
- HWP3/HWPX 등 다른 포맷 전용 분기는 추가하지 않는다.

## Stage2: 회귀 테스트와 시각 검증

- `3-10월_교육_통합_2022.hwp` 5쪽 `문28)` 조건 박스 내부 텍스트/수식이 겹치지 않는 좌표 단언 테스트를 추가한다.
- 기존 관련 테스트를 실행해 `문27)`, `문28)`, 미주 흐름 가드가 깨지지 않는지 확인한다.
- 필요하면 SVG 또는 render tree 기반 비교 산출물을 `output/`에 만들고 한컴 기준 화면과 대조한다.
- rhwp-studio 확인이 필요하면 `wasm-pack build --target web --out-dir pkg` 후 로컬 화면에서 확인한다.

## Stage3: 보고와 커밋 준비

- `mydocs/working/task_m100_1261_stage1.md`에 진단, 수정, 검증 결과를 기록한다.
- 최종 통과 후 `mydocs/report/task_m100_1261_report.md`를 작성한다.
- 커밋 제목은 `task 1261:` 접두어로 시작한다.
- PR 생성은 별도 승인 후 진행한다.
