# Task M100-1028 수행 계획서

## 1. 이슈

- 이슈: [#1028](https://github.com/edwardkim/rhwp/issues/1028)
- 제목: HWPX 글상자 세로 쓰기 미구현 (HWP5는 지원, HWPX 미적용)
- 마일스톤: v1.0.0
- assignee: @edwardkim
- 브랜치: `local/task1028` (base: `local/devel = 20b544f8`)

## 2. 배경

`samples/hwpx/tbox-v-flow-01.hwpx` 와 그 한컴 HWP5 변환본 `samples/hwpx/hancom-hwp/tbox-v-flow-01.hwp` 를 동일 렌더링하면:

| 포맷 | text 요소 | rotate transform |
|------|-----------|------------------|
| **HWPX** | 94 | **0** ⚠️ 가로 출력 |
| **HWP5** | 94 | **4** ✓ 세로 출력 |

한글 2022 PDF (`pdf/hwpx/tbox-v-flow-01-2022.pdf`) 는 세로 쓰기 출력. HWPX 만 미정합.

## 3. 가설 (이슈 본문 기준)

- HWP5: `parser/control/shape.rs:169-176` 가 `list_attr` u32 비트 (표 67 의 vertical text direction 영역) 파싱 → IR (`is_vertical`/`rotation`) → 렌더러 rotate transform
- HWPX: `parser/hwpx/section.rs:2239 parse_draw_text` / `:2347 TextBox::default()` 가 동등 XML 속성 (`<hp:tbox vertical="…">` 또는 `<hp:drawText textDirection="…">`) 추출 누락

## 4. 작업 범위

1. **Stage 1 분석**: HWP5 spec 표 67 의 vertical text direction 비트 정확 식별 + HWPX XML 동등 속성 식별 (한컴 HWPX 스펙 / `samples/hwpx/tbox-v-flow-01.hwpx` ZIP 내 section XML 분석)
2. **Stage 2 IR 매핑**: HWPX parser 가 추출한 vertical flag 를 HWP5 와 동일한 Document IR 영역 (TextBox / paragraph) 으로 전달
3. **Stage 3 검증**: HWPX export-svg 출력 rotate transform 4건 동등 + HWP5 sweep 회귀 부재 + 한글 2022 PDF 시각 정합
4. **Stage 4 회귀 가드**: `tbox-v-flow-01.hwpx` 영구 회귀 가드 (tests/issue_1028.rs 등) + golden snapshot 가능 여부

## 5. 검증 기준

- `rhwp export-svg samples/hwpx/tbox-v-flow-01.hwpx` 출력의 rotate transform **4건** (HWP5 동일)
- 한글 2022 PDF (`pdf/hwpx/tbox-v-flow-01-2022.pdf`) 와 시각 정합 (작업지시자 판정)
- HWP3/HWP5 기존 세로 쓰기 fixture sweep 회귀 부재
- `cargo test --release --tests` 전체 통과 + clippy -D + fmt 0
- WASM 빌드 + rhwp-studio 시각 정합

## 6. 메모리 룰 정합

- `feedback_image_renderer_paths_separate` — HWP5 / HWPX parser 두 경로 정합
- `feedback_diagnosis_layer_attribution` — parser 단계 누락 식별 (renderer 변경 불필요 가설)
- `feedback_visual_judgment_authority` — 한글 2022 PDF 시각 판정 게이트
- `reference_authoritative_hancom` — `pdf/hwpx/tbox-v-flow-01-2022.pdf` baseline
- `feedback_assign_issue_before_work` — 이슈 등록 직후 assignee 지정 (완료)
- `feedback_push_full_test_required` — cargo test --tests 전체 + fmt 필수
- `project_output_folder_structure` — 산출물 `output/poc/task1028/`
