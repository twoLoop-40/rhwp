# Task #1022 v2 — trailing-ls 보정 조건부화 (issue_598 회귀 해소)

## 배경

PR #1024(`pr-task1022`)를 stream/devel 위로 재구성하던 중, `tests/issue_598_footnote_marker_nav`
2건이 실패함을 확인. 조사 결과 stream/devel에서는 통과하나 #1024에서 회귀한 것으로,
본 재구성이 유발한 것이 아니라 #1024 자체의 회귀임.

## 원인 (확정)

- `src/renderer/layout.rs:2359` 부근 lazy_base 산출에서 #1024가 제거한
  `[Task #537] trailing-ls 보정`(`+ trailing_ls_hu`)이 단독 원인.
- 이 hunk만 되돌리면 footnote-01 본문 footnote 마커가 y=355.4 → 369.4 로 복원되어
  `issue_598` 통과.
- PDF(한컴 2022, `pdf/footnote-01-2022.pdf`) 대조: "개념"→첫 불릿 간격
  PDF 85px > devel 68px > PR 53px. #1024(보정 제거)는 정답지에서 더 멀어짐 = 진짜 회귀.

## 트레이드오프

- 보정을 단순 전체 복원하면 표 골든 `issue-617`/`issue-677`이 다시 바뀜
  (작성자가 제거한 명분: page-22 18.3px overflow).
- 즉 **일반 문서: 보정 필요 / 표 overflow 연속 상황: over-correction** →
  두 경로를 구분하는 조건부 gate 필요.

## 단계

1. Stage 1 — 판별 조사: lazy_base 진입 시 footnote-01 vs issue-617/677 을 가르는 조건 계측.
   가설: 직전 PageItem 이 정상 본문 단락 경로로 advance 된 경우만 보정 적용,
   PartialTable 연속/셀 마지막줄 경로는 제외.
2. Stage 2 — gate 구현: 조건부 `+ trailing_ls_hu`.
3. Stage 3 — 검증: issue_598 통과 + issue-617/677/form-002 골든 PDF 재판정·갱신 +
   전체 테스트 + LAYOUT_OVERFLOW 회귀 확인.
4. 재구성: #1024 squash 커밋에 합쳐 force push.

## 권위 자료

- `pdf/footnote-01-2022.pdf` (한글 2022) — 비표 문서 정답지
- issue-617/677 각 PDF — 표 문서 재판정
