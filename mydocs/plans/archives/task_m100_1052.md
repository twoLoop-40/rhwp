# Task #1052 수행 계획서 — 글상자 안 각주 본문 누락 정정

- 이슈: [#1052](https://github.com/edwardkim/rhwp/issues/1052)
- 마일스톤: M100 (v1.0.0)
- 브랜치: `local/task1052`
- assignee: @edwardkim
- 일시: 2026-05-21

## 1. 결함 본질 (Stage 0 진단 결과)

### 1.1 샘플

- `samples/hwpx/footnote-tbox-01.hwpx` (HWPX)
- `samples/footnote-tbox-01.hwp` (HWP5)
- [`pdf-large/hwpx/footnote-tbox-01.pdf`](../../pdf-large/hwpx/footnote-tbox-01.pdf) (한컴 PDF 정답지)

### 1.2 한컴 PDF 정답지

```
글상자 내부에 각주가 있는 경우
 여기에 각주1)가 들어있는 경우         ← 글상자 안 본문
와우
사람2)들은
                                         ← (페이지 하단 각주 영역)
1) 글상자 내부 각주
2) 일반 문단내 각주
```

### 1.3 rhwp 현재 동작

| 항목 | 한컴 정답지 | rhwp 현재 |
|------|------------|-----------|
| 글상자 안 각주 마크 "1)" | ✓ 본문에 표시 | ✓ |
| **글상자 안 각주 본문 "1) 글상자 내부 각주"** | ✓ 페이지 하단 각주 영역 | ❌ **누락** |
| 본문 각주 마크 "2)" | ✓ | ✓ |
| 본문 각주 본문 "2) 일반 문단내 각주" | ✓ | ✓ |

## 2. 근본 원인 (Stage 0 정밀화)

`feedback_image_renderer_paths_separate` 정합 — **두 paginator 경로 동기화 누락**:

| 경로 | Body 각주 | TableCell 각주 | **ShapeTextBox 각주** |
|------|-----------|---------------|----------------------|
| `engine.rs` (paginator) | ✓ (line 1430) | ✓ (line 1774) | ✓ **(line 1376-1398, 이미 구현)** |
| `typeset.rs` (typeset) | ✓ (line 1324) | ✓ (line 2317) | ❌ **누락** |

본 sample (HWPX) 가 typeset 경로로 진입하면서 ShapeTextBox 각주 수집이 누락되어 `page.footnotes` 에 추가되지 못함. 이후 `picture_footnote.rs::layout_footnote_area` 가 받는 footnotes 가 본문 각주만 포함하여 글상자 안 각주 본문 누락.

추가 검증 필요: 본 sample 이 어느 paginator 경로 (engine vs typeset) 로 실제 진입하는지. engine 경로로 진입한다면 별도 원인 (FootnoteSource::ShapeTextBox 의 `layout_footnote_area` 처리 누락 가능성).

## 3. 관련 컨텍스트

### 3.1 기존 해결 사례 (Shape 내부 traverse 패턴)

- **Task #696** (closed): 글상자 컨테이너 자식 표/문단 미렌더 — table-in-tbox p1
- **Task #919** (closed): `resolve_table_by_path` Shape 5 variants traverse 일반화 (PR #919 본 환경 직전 머지)
- **Task #974** (closed): 글상자 안 그림 출력 누락
- **Task #930** (closed): 글상자 내부 텍스트 도형 matrix 세로 스케일

### 3.2 메모리 룰 정합

- `feedback_image_renderer_paths_separate` — engine.rs / typeset.rs 두 paginator 경로 동기화 필수
- `feedback_diagnosis_layer_attribution` — 결함 본질 (typeset 경로 누락) vs 증상 (글상자 안 각주 누락) 분리
- `feedback_self_verification_not_hancom` — 한컴 PDF 정답지 (pdf-large/hwpx) 시각 정합 게이트
- `feedback_search_troubleshootings_first` — 트러블슈팅 사전 검색 완료 (`footnote_line_spacing.md` 무관)

## 4. 성공 기준

| 기준 | 내용 | 검증 방식 |
|------|------|----------|
| C1 | 글상자 안 각주 본문 ("1) 글상자 내부 각주") 페이지 하단 각주 영역 표시 | SVG export 후 텍스트 검사 |
| C2 | 본문 직속 각주 (기존) 회귀 부재 | sample 정합 + 회귀 sweep |
| C3 | 글상자 안 각주 마크 (기존) 위치 정합 유지 | SVG export 비교 |
| C4 | 회귀 가드 `tests/issue_1052_footnote_in_textbox.rs` 영구화 | cargo test --tests |
| C5 | 일반 fixture (table-in-tbox / footnote-01 / 2010-01-06 등) 회귀 부재 | BEFORE/AFTER sweep |
| C6 | 자동 검증 통과 (lib + tests + clippy + fmt) | CI 패턴 |
| C7 | 작업지시자 시각 판정 통과 (HWPX + HWP variant 양쪽 한컴 정합) | rhwp-studio + PDF 비교 |

## 5. 단계 계획 (안)

| Stage | 작업 | 결과물 |
|-------|------|--------|
| 1 | 정밀 진단 — 본 sample 의 실제 paginator 경로 (engine vs typeset) 식별 + FootnoteSource::ShapeTextBox 의 layout_footnote_area 처리 확인 | `stage1.md` 진단 보고서 |
| 2 | typeset.rs 에 engine.rs:1376-1398 동등 코드 추가 (글상자 안 각주 수집) + FootnoteSource::ShapeTextBox traverse 정합 | 코드 변경 + 정량 입증 |
| 3 | 회귀 가드 추가 (`tests/issue_1052_footnote_in_textbox.rs`) + 광범위 sweep (table-in-tbox + footnote-01 + 2010-01-06) | 회귀 가드 + sweep 결과 |
| 4 | WASM 빌드 + 작업지시자 시각 판정 + 머지 + close + orders + archives | 최종 보고서 |

## 6. 비범위 / 후속

- 머리말/꼬리말 안 각주 (본 sample 본문 + 글상자만)
- 표 셀 안 각주 (별도 TableCell 분기 이미 구현됨, 회귀 확인만)
- 미주 (Endnote) 의 Shape 내부 처리 (별도 검토 필요 시 후속)

## 7. 위험

- **typeset 경로만 정정 시 engine 경로 회귀 가능성**: engine 경로의 `Control::Shape` 분기 (line 1376) 가 이미 동작 — typeset 정정으로 *추가* 적용만, 회귀 위험 낮음
- **estimate_footnote_area_height vs layout_footnote_area 동기화**: 두 함수 모두 `footnotes: &[FootnoteRef]` 받음 — push 만 정정하면 자동 traverse
- **FootnoteSource::ShapeTextBox 의 fn_paras 조회 (`get_footnote_paragraphs`)**: 기존 함수가 ShapeTextBox source 처리하는지 확인 필요 (Stage 1 진단 항목)

## 8. 작업지시자 결정 요청

| 결정 | 옵션 |
|------|------|
| 진행 여부 | A. 본 계획 승인 (4 단계) / B. 단계 추가/제외 / C. 보류 (추가 진단 필요) |
| 시각 판정 환경 | rhwp-studio + HWPX 로드 / HWPX + HWP variant 양쪽 |
| 회귀 가드 범위 | 글상자 안 각주만 / 표 셀 안 각주 + 미주 양쪽 |
