# 수행계획서 — Task #1082: 빈 문단 다수 페이지 누적 vpos 드리프트 overflow (C군)

- 이슈: edwardkim/rhwp#1082
- 브랜치: `local/task1082` (stream/devel `be2a71c4` 기준)
- 인벤토리 C군 (A군 #1070 / B군 #1073 / D군 #1079 처리 완료) — **가장 깊은 core 레이아웃**

## 목표
빈 문단(텍스트 없는 FullParagraph)이 수십 개 쌓인 페이지에서 렌더가 문단마다 파일 vpos 보다
≈8px(trailing line_spacing) 더 진행해 누적 전진 드리프트로 본문 하단을 최대 ~900px 초과하는
결함을 조사·해소한다(한컴 PDF 정합).

## 재현 / 사전 진단
- `3-09월_교육_통합_2023.hwp` page12(global 11): 빈 FullParagraph **86개**, body 1001.6px.
  `LAYOUT_OVERFLOW_DRAW pi=666 y=1989 col_bottom=1092 overflow=897px`(pi=664→860 점증).
- `RHWP_TYPESET_DRIFT`: 문단당 fmt_total=21.3(lh13.3 + ls8.0) vs 파일 vpos_h=13.3 → **diff +8.0/
  문단**. 다수 문단 누적이 드리프트의 후보.
- typeset used 는 body 내(회계상 fit)인데 렌더가 누적 드리프트로 초과 → **typeset↔렌더 측정
  불일치**(VPOS_CORR/HeightCursor 계열).
- 동일: 3-09'22(838), 3-10'22(337), 3-11'22(561). hwp/hwpx 양쪽.

## 접근 (단계 개요 — 상세는 구현계획서)
1. **조사 + 타당성(go/no-go) (소스 무변경)**:
   - 빈 문단의 typeset 누적(fmt_total, trailing ls) vs 렌더 배치(HeightCursor::vpos_adjust,
     build_single_column) 정독 → 드리프트가 typeset 측 과대인지 렌더 측 과대인지 확정.
   - 파일 vpos(빈 문단 13.3px 간격) vs typeset/렌더 21.3px 간격의 +8px 출처(trailing ls 정책)
     규명. #1046/#1049/#1054 기존 정정과의 관계.
   - 한컴 PDF(`pdf/3-09월_교육_통합_2023.pdf`) 페이지별 빈 줄 간격 측정 대조.
   - **go/no-go**: core 누적 산식 변경 범위(전 문서 영향) 평가 → 범위 합의.
2. **설계**: 드리프트 0 산식(typeset==렌더==파일 vpos) + 비회귀 케이스 점검.
3. **구현**: 최소 변경 + 단위 검증.
4. **회귀검증**: 4파일 overflow 해소 + 전수 sweep 회귀 0 + 골든 + cargo test + 가드.

## 산출물
`plans/task_m100_1082_impl.md`, `working/task_m100_1082_stage{N}.md`,
`report/task_m100_1082_report.md`.

## 리스크
- **누적 vpos/line-spacing 산식은 전 문서 공유 core** → 광범위 회귀 위험 최상. Stage 1 go/no-go
  로 범위 확정 후 진행. trailing ls 정책 변경은 다수 문서 페이지네이션 영향.
- typeset↔렌더 두 모델 동시 정합 필요(#1079 처럼). Stage 1 에서 어느 쪽이 권위(파일 vpos)인지
  확정.
