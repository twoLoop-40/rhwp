# Task #722 수행 계획서

## 1. 이슈 요약

**Issue #722**: hwp3-sample5.hwp wrap=Square 그림 paragraph 시각 정합 결함

**페이지 8 (Stage 1~2 정정 완료)**: paragraph 175 "아래에 디렉토리 트리 각 부분의 역할에 대하여 설명하였다." — 본 환경 col_area 전체 폭 layout (image 영역 침범) → image z-order 후 그려져 가려짐. PDF 권위 자료 정합: image 우측 wrap zone 첫 줄.

**페이지 27 (Stage 3~4 완료)**: paragraph 779 caption 영역 정정 (caption_room 가드 추가) → image 위 자유 영역 정합.

**페이지 48 (Stage 4 갱신 후 미세 차이)**: paragraph 1394 image 우측 wrap zone 정합 회복. 그러나 inter-image-text gap (image outer margin 3mm) 미적용 — 한컴 정합 (image 끝 + 3mm gap) vs 본 환경 (image 끝 + 0mm). Stage 5~6 완료.

**HWP5 변환본 페이지 16 (Stage 8~9 시도 후 rollback, 별도 issue 분리)**: hwp3-sample5-hwp5.hwp paragraph 441 매칭 가드 (`anchor_full_width_match`) 시도했으나, 가드가 paragraph 442/443 까지 broad 매칭 → 페이지 분할 왜곡 (paragraph 599 페이지 22 → 페이지 21 이동) 회귀 발생. 본 task 는 hwp3-sample5.hwp (HWP3 native) 한정으로 종결, HWP5 변환본 결함은 별도 issue 분리.

## 2. 사전 진단 (IR 덤프)

```
pi=175 (paragraph)
  텍스트: "￼아래에 디렉토리 트리 각 부분의 역할에 대하여 설명하였다. "
  ls[0] vpos=12960 cs=24560 sw=26464  (그림 우측 영역)
  ls[1] vpos=14400 cs=24560 sw=26464  (그림 우측 영역)
pi=175 ci=0 Shape (그림)
  bin_id=2, 89.0×46.5mm, wrap=어울림 (Square)
  위치: 가로=용지 12.6mm, 세로=용지 65.9mm
```

## 3. 본질 추정 (Stage 1 검증 영역)

### 가설 A: HWP3 cs/sw 인코딩 정정 영역

paragraph 의 모든 LINE_SEG 가 cs=24560 (그림 우측) 으로 인코딩됨. 한컴은 첫 줄 cs=0, sw=full 로 인코딩하고 그림은 둘째 줄 이후부터 wrap 가능성. HWP3 파서의 cs/sw 인코딩 정정 영역.

### 가설 B: wrap=Square 그림의 paragraph layout 시작 위치 영역

한컴은 그림 시작점 = paragraph 시작점 + 일정 offset (예: 첫 줄 line_height) 으로 처리. 본 환경은 paragraph 시작 = 그림 시작.

### 가설 C: paragraph layout 시 wrap 영역 상단 padding

paragraph_layout 또는 typeset 단계에서 wrap=Square 그림의 위쪽 영역 (그림 vertical_offset 위) 에 텍스트 첫 줄 처리 누락.

## 4. 진행 절차

### 4.1 Stage 1: 본질 진단

- HWP5 변환본 비교 (가능 시) — 한컴이 cs/sw 어떻게 인코딩하는지 확인
- HWP3 파서 의 cs/sw 처리 코드 추적 (`src/parser/hwp3/mod.rs`)
- 그림 vertical_offset 처리 영역 (한컴 vs 본 환경) 비교

### 4.2 Stage 2: 본질 정정

Stage 1 진단에 따라 정정 위치 결정.

### 4.3 Stage 3: 광범위 회귀 sweep + 시각 판정

- 187 fixture 페이지 수 차이 0 검증
- `samples/hwp3-sample5.hwp` 페이지 8 시각 판정 (작업지시자)

## 5. 마일스톤

M100 (v1.0.0) — 조판 엔진 정합성

## 6. 권위 자료

- `samples/hwp3-sample5.hwp` (이미 git tracked)
- 한컴 hwp3-sample5 변환본 (`samples/hwp3-sample5-hwp5-v2018.hwp`, `_v2024.hwp` 도 git tracked) — Stage 1 비교 자료

## 7. 회귀 위험 영역 좁힘 원칙

- HWP3 파서 또는 layout 단의 정정 영역 명시
- 본 case (wrap=Square + line_segs 인코딩) 만 영향
- 다른 fixture 영향 0 (광범위 sweep)

## 8. 의존성

- 선행 의존: 없음 (devel 분기)
- 후행 의존: 없음

## 9. 승인 요청

수행계획서 + Stage 1 본질 진단 진행 승인 요청.
