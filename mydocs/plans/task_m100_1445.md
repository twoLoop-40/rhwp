# Task M100 #1445 수행계획서 — npm README *Ex 안내 + 소비자 편집 API 매뉴얼

- 이슈: #1445 "[docs] npm @rhwp/core README에 *Ex(options) 안내 + 소비자 편집 API 매뉴얼 (#1413 후속)"
- 마일스톤: M100 (v1.0.0)
- 브랜치: `local/task1445` (`local/devel` 분기)
- 작성일: 2026-06-20

## 1. 배경

#1413 에서 고인자 WASM API 26개에 options object 변형(`*Ex`)을 추가했으나, **소비자
(npm `@rhwp/core` 개발자) 문서에 안내가 없다**. 내부 가이드는 메인테이너용이다.

## 2. 현황 (조사 완료)

- `npm/README.md`(= 패키지 README source, prepare-npm.sh 가 pkg/ 로 복사): 읽기/렌더링
  (SVG·폰트·measureTextWidth) 중심. **편집 API 안내 거의 없음**.
- 소비자용 편집 API 매뉴얼 부재(`mydocs/manual/` 에 e2e·options-convention 만).
- `pkg/rhwp.d.ts` 에 *Ex 27개 + doc comment 노출(IDE 자동완성 OK).

## 3. 수정 방향 (구현계획서 없이 수행계획서에서 확정 — 문서 전용)

1. **npm/README.md API 섹션 보강**: 편집 API 개요 + `*Ex`(options object) 안내(예시 1~2개,
   권장 이유: 중간 삽입형 시그니처 변경에 안전, 전체 목록은 d.ts/매뉴얼 참조). 0.x
   breaking change 대응 한 줄.
2. **소비자 편집 API 매뉴얼 신규** `mydocs/manual/consumer_edit_api_guide.md`(또는 npm 에
   동봉 가능 위치): HwpDocumentBuilder 류 래퍼로 편집 API 를 호출하는 개발자 대상 —
   삽입(text/picture)/표/필드/서식 핵심 + `*Ex` 패턴 + 0.x 변경 추적 팁.
3. CHANGELOG 소비자 관점 한 줄 보강(선택).

## 4. 목표와 완료 조건

1. npm README 만으로 소비자가 `*Ex` 존재·사용법을 발견 가능.
2. 편집 API 커스터마이징 매뉴얼로 래퍼 작성 개발자가 참조 가능.
3. 코드 변경 없음(문서 전용). 외부 공개 문서 자기검열 체크리스트 적용.

## 5. 단계 개요 (1단계 — 문서)

1. README 보강 + 소비자 매뉴얼 작성 + (선택)CHANGELOG + 보고서.

## 6. 위험과 대응

| 위험 | 대응 |
|------|------|
| 외부 공개 문서 톤·정확성 | 자기검열 체크리스트(비교/최상급/공공기관 오인 회피), 예시는 d.ts 시그니처와 일치 |
| README 과잉(읽기 중심 흐름 깨짐) | 편집 API 는 간결 안내 + 매뉴얼 링크로 분리 |
| 0.x API 변경 안내 과장 | "0.x 는 변경 가능, *Ex 로 완충" 정도로 절제 |

## 7. 산출물

- 수행계획서(본 문서) / 최종 보고서
- `npm/README.md`(편집 API + *Ex 안내), `mydocs/manual/consumer_edit_api_guide.md`(신규)
