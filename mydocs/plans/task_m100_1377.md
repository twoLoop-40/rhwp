# 수행계획서 — Task #1377: 미주 단 수직배치 렌더 정합 (render↔typeset 발산 제거)

- **이슈**: #1377 (M100). #1363~#1375 의 미주 overflow·placement 근본(render fidelity) 전용 작업.
- **브랜치**: `local/task1377` (base `devel`, 기본 B + #1375 tolerance 수정 포함)
- **성격**: 렌더러 미주 수직배치를 typeset(=hancom)과 정합 — 대형·고위험

## 1. 배경

#1375 결론: 미주 단 overflow(p22 8px)·placement(p17 C×C)는 typeset 게이트로 불가(비단조). 근본은
**렌더러가 미주 단을 hancom 보다 ~15-26px 깊게 그리는** render↔typeset 발산.

## 2. Stage 1 국소화 결과 (완료)

sep2020 p22 단0 render rel vs typeset top 대조:
| pi | typeset top | render rel | 누적 발산 |
|----|------|------|------|
| 1127 | 0 | 0 | 0 |
| 1128(빈) | 18.0 | 18.0 | 0 |
| **1129 (TAC 그림 ci=1)** | 51.7 | 72.1 | **+20px** |
| 1131 (TAC 그래프 309px) bottom | 418.9 | 445.3 | +26px |
| 1140~1156 | — | — | +26→+21 (후반 compact) |

→ **발산 근원 = 단0 TAC 그림/도형**: pi=1129(작은 그림) +20px, pi=1131(큰 그래프) +6px. 렌더가 TAC
그림 뒤 콘텐츠를 typeset/hancom 보다 ~26px 낮게 배치. 후반부는 오히려 compact(렌더<typeset).
**메모리 `tech_endnote_tac_picture_overlap_vs_stack`(#1357/#1363) 영역.**

## 3. 목표

렌더러의 미주 TAC 그림/도형 뒤 콘텐츠 수직배치를 typeset(=hancom) 높이와 정합 → 발산 ~0 →
p22 bleed→0. 전 exam(B·A3) + 전 문서 골든 SVG 무회귀.

## 4. 핵심 가설 (Stage 2 조사 대상)

- pi=1129(작은 inline 그림)에서 +20px: 렌더가 그림 높이/뒤 콘텐츠 시작을 typeset 보다 크게 잡음.
  `shape_jumped`/shape bottom 배치 vs typeset acc 의 TAC 처리 불일치.
- pi=1131(큰 TACstack 그래프)에서 +6px: 그래프 렌더 높이 vs typeset 309px 차이.

## 5. 접근

- Stage 2: pi=1129/1131 의 렌더 배치 코드(`shape_jumped`, shape bottom, `calculate_shape_reserved_heights`)
  와 typeset TAC 누적(TACstack)의 높이 산출을 정확 대조 → 불일치 지점 규명.
- Stage 3: 렌더(또는 typeset)를 정합화. 좁게 — TAC 그림 뒤 콘텐츠 시작 y 를 typeset 높이로.
- Stage 4: 전 exam·골든 SVG 회귀 + p22/p17 시각 검증 + 보고.

## 6. 제약/리스크

- TAC 그림 렌더는 #1357/#1363 이 다룬 delicate 영역(메모리 경고). 전 문서 그림 렌더 영향.
- 골든 SVG 회귀 가드 필수(`tests/golden_svg/`). 비단조 시 좁은 조건.

## 7. 검증

- 렌더 발산 = EN_RY(렌더 y) vs EN_ACC(typeset) para 대조. overflow = `export-svg` `LAYOUT_OVERFLOW_DRAW`.
- SVG↔PDF 픽셀. 전 exam B·A3.
