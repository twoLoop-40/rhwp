# 오늘 할일 - 2026년 5월 10일

## M100 — v1.0.0 조판 엔진 (PR 처리 — 5/9 사이클의 연속)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #720 (refs #536)** | render — replay raw SVG fragments in native Skia PNG output (P6) | **완료 (옵션 A — 1 commit cherry-pick + no-ff merge `70121b1b`)** | 컨트리뷰터 @seo-rii (Seohyun Lee) — **7번째 사이클** (Skia 영역 핵심 컨트리뷰터, PR #165/#419/#456/#498/#599/#626/#720). 본질: PR #599 (P4 PNG raster backend) + PR #626 (P5 equation replay) 영역 의 후속의 P6 단계. native Skia 경로 영역 의 RawSvg leaf 영역 placeholder fallback (`renderer.rs:763`) → 실제 raster (resvg + tiny-skia 영역). 정정: `image_conv.rs` (+82 LOC) `draw_svg_fragment` + `rasterize_svg_fragment_to_png` + `svg_parse_options` / `renderer.rs` (+88/-6) `PaintOp::RawSvg` 영역 placeholder → 실제 raster + invalid SVG fallback 보존 / `Cargo.toml` `native-skia` feature 영역 의 `dep:resvg` 추가. **보안 가드** ✅: MAX_SVG_FRAGMENT_BYTES = 4 MB / MAX_SVG_RASTER_PIXELS = 67M (8192×8192) / `resolve_string = None` (external href 차단 — file/http/https 등) / `resolve_data` (data: URI 만 허용) / `resources_dir = None` (디렉터리 탐색 차단) / Wrapper SVG (`<svg xmlns=... viewBox=...>{fragment}</svg>` 안전 wrap) / Invalid SVG fallback placeholder 보존. **회귀 가드 2건 신규**: `renders_raw_svg_fragment_as_colored_ink` (green rect → 100+ green 픽셀 검증) + `raw_svg_replay_does_not_load_external_file_hrefs` (외부 file href → red 0 픽셀 검증, **보안 가드 작동 입증**). 본 환경 cherry-pick 충돌 0건. 결정적 검증 lib **1173** + 통합 ALL GREEN + clippy `--features native-skia --lib -- -D warnings` 통과 + **native-skia 24/24 PASS** (신규 2건 + 기존 22건) + svg_snapshot 8/8 (form-002 PR #706 영역 보존) + issue_712/713/716 PASS (PR #714/#715/#719 영역 보존). **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅. **시각 판정 게이트 면제** — 결정적 검증 + 회귀 가드 + 보안 가드 명시 영역 모두 통과. **비목표 명시** (PR 본문 정합): WASM/CanvasKit raw SVG / form replay / SVG filter 전체 parity / network/file resource loading / animated SVG / VLM preset (#613) / PNG DPI metadata (#614) 영역 모두 별건. 검토 보고서: `mydocs/pr/archives/pr_720_review.md`. 처리 보고서: `mydocs/pr/archives/pr_720_report.md`. **`feedback_contributor_cycle_check` 정합** — @seo-rii 7번째 사이클 정확 표현. **`feedback_pr_supersede_chain` 권위 사례** — PR #599 (P4) → PR #626 (P5) → **PR #720 (P6)** 단계적 진전 영역 (Issue #536 트래킹 영역의 단계적 진전 영역). **`feedback_image_renderer_paths_separate` 권위 사례** — native Skia 경로 영역 만 변경 (WASM/CanvasKit 영역 무영향 영역). **`feedback_visual_judgment_authority` 정합** — 결정적 검증 + 회귀 가드 + 보안 가드 통과 영역 → 시각 판정 면제 합리. |
| **PR #723 (closes #722)** | Task #722 — hwp3-sample5.hwp wrap=Square 그림 paragraph 시각 정합 | **완료 (옵션 A — 1 commit cherry-pick + no-ff merge `6ced74b0`, exam_science 회귀 정정 영역 PR #732 통합 후속)** | 컨트리뷰터 @jangster77 (Taesup Jang) — **16번째 사이클** (HWP 3.0 파서 영역 핵심 컨트리뷰터). 본질: hwp3-sample5.hwp 영역 페이지 8/27/48 영역 wrap=Square 그림 paragraph 영역 의 image 침범 + image z-order 가려짐 + inter-image-text gap 미적용 정정. 3 영역 동기 정정: `pagination.rs` (+6 LOC) `WrapAnchorRef.anchor_image_margin_right` 필드 / `typeset.rs` (+56 LOC) anchor 다음 paragraph + anchor host self-register, caption_room 가드 (LINE_SEG ≥ 2 / LINE_SEG 1 + caption_room ≤ line_height → wrap zone, > line_height → caption-style) / `paragraph_layout.rs` (+10/-3) wrap_anchor 영역 cs/sw 영역 inter-image-text gap (3mm = 852 HU) 보정. 자기 검증: cargo build/test/clippy 통과, lib **1173** + 통합 ALL GREEN, 광범위 sweep 7 fixture / 170 페이지 / **168 same / 2 diff** (exam_science_001/002.svg). WASM 빌드 4.60 MB. **시각 판정 (작업지시자)**: ⓐ 페이지 8/27/48 PDF 권위본 (`pdf/hwp3-sample5-2022.pdf`) 정합 ✅ 통과. ⓑ exam_science p1/p2/p8 영역 의 5번/8번/12번 문항 지문 왼쪽 경계 글자 클립핑 ⚠️ **회귀 판정**. **처리 결정**: PR #723 머지 유지 (rollback 미진행), exam_science 회귀 정정 영역 영역 PR #732 (Task #724, 동일 컨트리뷰터) 영역 후속 처리 영역 으로 통합 — 본질 정정 (페이지 8/27/48) 영역 보존 + PR #732 영역 영역 동일 본질 (wrap zone) 영역 의 후속 정정 영역 묶음. 분리된 후속: Issue #732 (Task #724) 영역 OPEN — HWP5 변환본 paragraph 441 영역 + exam_science 회귀 정정 통합. 검토 보고서: `mydocs/pr/archives/pr_723_review.md`. 처리 보고서: `mydocs/pr/archives/pr_723_report.md`. **`feedback_contributor_cycle_check` 정합** — @jangster77 16번째 사이클 정확 표현. **`feedback_pr_supersede_chain` 신규 패턴 (c)** — 머지 + 회귀 정정 영역 후속 PR 통합 영역 영역 (PR #723 본질 → PR #732 회귀 정정 + 본질). **`feedback_visual_judgment_authority` 정합** — 작업지시자 시각 판정 게이트 영역 ⓐ 본질 통과 ⓑ 회귀 발견 영역 후속 PR 통합 결정. **`feedback_hancom_compat_specific_over_general` 정합** — LINE_SEG + caption_room 영역 case 가드 영역 좁힘 + Stage 8~9 회귀 영역 rollback 정합 (PR 본문 명시). |
| **PR #725 (closes #717)** | Task #717 — rhwp-studio 표 셀 빈 영역 hitTest 컨텍스트 이탈 정정 | **완료 (옵션 A — 1 commit cherry-pick + no-ff merge `bbf01424`, 시각 판정 면제)** | 컨트리뷰터 @postmelee — **15+ 사이클** 핵심 컨트리뷰터 (rhwp-studio editor 영역, PR #169/#209/#214/#224/#243/#339/#385/#437/#510/#531/#642/#663/#664/#718/#725 등). 본질: `cursor_rect.rs::hit_test_native()` 영역 영역 `cellIndex` 만 매칭 → HWP 영역 영역 여러 표 (자료 표 + 페이지 번호 표 + 바탕쪽 표) 영역 모두 `cellIndex=0` 반복 영역 영역 다른 표 컨텍스트 영역 으로 오염 영역. exam_social.hwp 1/4쪽 자료 표 제목 행 빈 영역 클릭 영역 페이지 번호 표(`32`) 영역 이동. 정정 (cursor_rect.rs +72/-17): RunInfo / CellBboxInfo `table_id: Option<u32>` 추가 / `collect_runs()` `current_table_id` 인자 추가 (Table 노드 `Some(node.id)` 자식 전파) / cell_bboxes meta 보완 영역 `r.table_id == cb.table_id` + `ctx.innermost().cell_index == cb.cell_index` 격리 (중첩 표 정합) / clicked_cell `.min_by_key(w*h)` 가장 작은 bbox 영역 (중첩 표 내부 셀 우선) / 빈 셀 fallback `cb.cell_context` 영역 전체 cellPath 직렬화. **회귀 가드 3건 신규** (`tests/issue_717_table_cell_hit_test.rs` +130 LOC): 자료 표 제목 행 빈 영역 / 회색 헤더 내부표 빈 영역 (cellPath `[(0,0,0),(1,1,0)]` 보존 + path 기반 X 삽입 후 getTextInCellByPath/getCursorRectByPath 정상) / `<보기>` 표 빈 영역. 본 환경 cherry-pick 충돌 0건. 결정적 검증: cargo build/test/sweep 통과, lib + 통합 ALL GREEN + 신규 3 PASS, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅. clippy 48 errors 영역 영역 모두 기존 코드 (devel 시점 52 → 본 PR 영역 4건 정정) — 본 PR 변경 파일 영역 신규 lint 부재. **시각 판정 게이트 면제** — `cursor_rect.rs` query API 만 변경, SVG/PNG 시각 출력 무영향, 회귀 가드 3건 + 광범위 sweep 0 회귀 보장. PR 본문 영역 영역 before/after 영상 첨부 영역. **후속 분리** (PR 본문 명시): 회색 헤더 내부표 영역 극단적 낮은 빈 셀 (~5.1px) 영역 입력 가시성 영역 별건 ("초저높이 빈 셀 편집 진입/표 선택 UX 정책"). 검토 보고서: `mydocs/pr/archives/pr_725_review.md`. 처리 보고서: `mydocs/pr/archives/pr_725_report.md`. **`feedback_contributor_cycle_check` 정합** — @postmelee 15+ 사이클 (rhwp-studio editor 영역 핵심). **`feedback_image_renderer_paths_separate` 정합** — cursor_rect.rs 영역 격리 (다른 query/render 경로 무영향). **`feedback_visual_judgment_authority` 정합** — 시각 출력 무변경 + 회귀 가드 통과 영역 → 시각 판정 면제 합리. **`feedback_hancom_compat_specific_over_general` 정합** — `table_id` 영역 격리 영역 영역 case 가드 영역 (cellIndex 일반화 영역 결함 본질). |
| **PR #728 (Issue #204)** | Task #204 — rhwp-studio 표 편집 Undo/Redo 스냅샷 기반 이력 등록 | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `8db18f58`, 작업지시자 웹 에디터 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (rhwp-studio + WASM API + 디버깅 툴킷 영역, PR #334/#335/#387/#388/#395/#396/#427/#428/#444/#446/#447/#448/#579/#581/#582/#583/#600/#601/#602/#659/#684/#728 등). 본질: rhwp-studio 영역 영역 표 편집 (생성/삭제/행열 삽입·삭제/셀 분할·병합/캡션) 영역 영역 WASM 직접 호출 영역 영역 `history.execute()` 미경유 영역 영역 `Ctrl+Z` Undo 미동작. **Issue #204 의 B안 (스냅샷 + SnapshotCommand 일괄 래핑)** 채택. 정정 (`rhwp-studio/src/command/commands/table.ts` +123/-101): 11 표 커맨드 영역 영역 `executeOperation({ kind: 'snapshot' })` 패턴 일관 적용 (createTable / insertTableRow×2 / insertTableColumn×2 / deleteTableRow / deleteTableColumn / splitTableCell / mergeTableCells / deleteTable / toggleTableCaption) + `safeTableOp` 헬퍼 try/catch 보존 + Copilot 리뷰 반영 (commit `b8477e5f` controlIdx 사용 + 에러 핸들링 복원). **인프라 재사용** (B안 영역 권위 사례): `input-handler.ts:1485` `case 'snapshot'` + `WasmBridge.saveSnapshot/restoreSnapshot` 광범위 사용 (input-handler-table/keyboard/text 영역 11+ 사용 사례) — 신규 인프라 도입 부재 영역 영역 위험 좁힘. 본 환경 cherry-pick 충돌 0건. 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN (Rust 변경 부재), **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅, WASM 빌드 4.60 MB. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 — dev server 영역 영역 11 표 커맨드 영역 영역 Ctrl+Z 영역 영역 정합 입증. 의도적 미정합 (PR 본문 명시): `splitTableCell` / `mergeTableCells` 영역 영역 `exitCellSelectionMode` 영역 영역 snapshot 외부 호출 (셀 선택 모드 영역 Undo 범위 외 UI 상태) / `toggleTableCaption` 영역 영역 캡션 제거 경로 미정합 (paragraph deletion 영역 별도 경로). **후속 분리**: HwpCtrl(hwpctl) API 경로 (`hwpctl/actions/table-edit.ts`) 영역 영역 InputHandler 영역 영역 history 미접근 영역 영역 아키텍처 변경 필요 영역 영역 별건. 검토 보고서: `mydocs/pr/archives/pr_728_review.md`. 처리 보고서: `mydocs/pr/archives/pr_728_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 파일 변경 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 (B안) 영역 영역 위험 좁힘 + 후속 분리 명시. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 인터랙션 검증 영역 영역 시각 판정 권위 영역 (Canvas visual diff CI 일차 가드 + 인터랙션 본질 가드). |
| **PR #746 (Part of #260)** | Task #260 — Ctrl/Cmd+Arrow 커서 이동 (줄/문서 시작·끝) | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `20c0e5d4`, 작업지시자 웹 에디터 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 영역 영역 15번째 PR — PR #728/#729/#730/#734/#735/#737/#738/#739/#740/#742/#743/#744/#745 후속). 본질: macOS 영역 영역 `Cmd+Arrow` 조합 미처리 영역 영역 줄 시작/끝, 문서 시작/끝 영역 영역 이동 부재 (Issue #260). 정정 (1 file, +33/-1): `rhwp-studio/src/engine/input-handler-keyboard.ts` `handleCtrlKey()` 영역 영역 Arrow 키 처리 추가 — Ctrl/Cmd + ← / → (줄 시작 / 끝, `cursor.moveToLineStart` / `moveToLineEnd`) + Ctrl/Cmd + ↑ / ↓ (문서 시작 / 끝, `cursor.moveToDocumentStart` / `moveToDocumentEnd`) + 모든 방향 영역 영역 Shift 조합 영역 영역 선택 범위 확장 (`cursor.setAnchor` / `clearSelection`). Copilot 리뷰 반영 (commit `ad7e6b4d`): updateSelection() 중복 호출 제거. **인프라 재사용** — 기존 Ctrl+Home/End 패턴 정합 + cursor.moveTo* 기존 메서드 재호출 영역 영역 신규 인프라 도입 부재 영역 영역 위험 좁힘. 본 환경 cherry-pick 충돌 0건. 결정적 검증: tsc --noEmit ✅ 통과, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (TypeScript 영역 영역 SVG 무영향 입증), WASM 빌드 4.66 MB. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 (4가지 Arrow + Shift 조합 정합). **후속 분리** (PR 본문 명시): Issue #260 의 첫 단계 — Ctrl/Cmd+Arrow 만 영역 영역 Option+Arrow (단어 단위 이동) 영역 영역 단어 경계 감지 로직 필요 영역 영역 후속 작업 (Issue #260 OPEN 유지). 검토 보고서: `mydocs/pr/archives/pr_746_review.md`. 처리 보고서: `mydocs/pr/archives/pr_746_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 파일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 영역 영역 위험 좁힘 + 후속 분리 (Option+Arrow) 명시. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 인터랙션 검증 ✅ 통과. |
| **PR #748 (closes #158)** | Task #158 — 표 크기 조절 Undo/Redo (SnapshotCommand 적용) | **완료 (옵션 (b) cherry-pick + empty commit + no-ff merge `5ce0bcc9`, PR #728 supersede 흡수, 작업지시자 웹 에디터 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 16번째 PR — PR #728/#729/#730/#734/#735/#737/#738/#739/#740/#742/#743/#744/#745/#746 후속). 본질: 표 크기 조절 (마우스 드래그 / 키보드 셀 / Ctrl+방향키 비율) 후 Ctrl+Z 되돌리기 부재 (Issue #158). 정정 의도: 3 함수 (`finishResizeDrag`, `resizeCellByKeyboard`, `resizeTableProportional`) 영역 `executeOperation({ kind: 'snapshot' })` 패턴 적용 (PR #728 의 SnapshotCommand 패턴 정합). **PR #728 supersede 흡수** — PR #728 (closes #204) 머지 영역 표 편집 11 커맨드 + 표 크기 조절 3 함수까지 모두 SnapshotCommand 적용 완료된 상태 (HEAD 측이 PR #748 의 try/catch + afterEdit 자동 emit 까지 정합). 3 영역 충돌 수동 해결 시도 결과 HEAD 유지 시 변경 부재 → PR #748 정정 본질이 이미 devel 영역 반영 확인. **처리 방식 — 옵션 (b) cherry-pick + empty commit** (작업지시자 결정): 2 commits (`70cc0209` + `a15336d2`) `--allow-empty` 로 cherry-pick → empty commits `312fc4c5` + `d20a6a45` 생성 (컨트리뷰터 author 정보 보존). 결정적 검증: tsc --noEmit ✅ 통과, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (변경 부재 영역 영역 회귀 0 자명), WASM 빌드 4.66 MB. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 — 3가지 표 크기 조절 경로 + Ctrl+Z (PR #728 의 표 Undo/Redo 정합 보존 입증). 검토 보고서: `mydocs/pr/archives/pr_748_review.md`. 처리 보고서: `mydocs/pr/archives/pr_748_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 파일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_pr_supersede_chain` (a + b) 패턴** — PR #728 본질 흡수 (a 패턴) + 옵션 (b) cherry-pick 으로 author 보존. **`feedback_process_must_follow` 정합** — 인프라 재사용 (PR #728 SnapshotCommand 패턴) 영역 영역 신규 인프라 도입 부재. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 인터랙션 검증 ✅ 통과. |
| **PR #749 (Part of #223)** | Task #223 — Ctrl/Cmd+O 파일 열기 단축키 추가 | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `55cc65d6`, 작업지시자 웹 에디터 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 17번째 PR — PR #728/#729/#730/#734/#735/#737/#738/#739/#740/#742/#743/#744/#745/#746/#748 후속). 본질: Ctrl+O (Windows) / Cmd+O (macOS) 파일 열기 단축키 부재 (Issue #223 macOS 단축키 지원 영역 일부). 정정 (2 files, +10/-0): `rhwp-studio/src/command/shortcut-map.ts` (+2) Ctrl+O / Ctrl+ㅐ 매핑 → file:open / `rhwp-studio/src/main.ts` (+8) `setupGlobalShortcuts` 영역 영역 Ctrl/Cmd+O 핸들러 추가 — 문서 미로드 상태에서도 동작 + 한글 IME `ㅐ` 처리. Copilot 리뷰 반영 (commit `0390b9bb`): 한글 IME 매핑 추가. **인프라 재사용** — 기존 Alt+N (새 문서) 패턴 정합 + `file:open` 커맨드 (File System Access API + file-input fallback) 기존 구현 재호출 + 한글 IME `ㅜ` 매핑 패턴 정합 (`ㅐ` 동일 패턴) 영역 영역 신규 인프라 도입 부재 영역 영역 위험 좁힘. 본 환경 cherry-pick 충돌 0건. 결정적 검증: tsc --noEmit ✅ 통과, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (TypeScript 영역 영역 SVG 무영향 입증), WASM 빌드 4.66 MB. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 (Ctrl+O / Cmd+O / Ctrl+ㅐ + 문서 미로드 상태). **후속 분리** (PR 본문 명시): Issue #223 OPEN 유지 — Cmd+Arrow (PR #746 영역 정합) / Opt+Arrow / 한컴 주요 단축키 영역 의 후속 단계 잔존. CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_749_review.md`. 처리 보고서: `mydocs/pr/archives/pr_749_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 (Alt+N 패턴 + file:open 커맨드) 영역 영역 위험 좁힘 + 후속 분리 (Cmd+Arrow / Opt+Arrow) 명시. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 인터랙션 검증 ✅ 통과. |
| **PR #750 (closes #733)** | feat — 다단 설정 대화상자 구현 | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `577befdd`, 작업지시자 웹 에디터 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 18번째 PR — PR #728/#729/#730/#734/#735/#737/#738/#739/#740/#742/#743/#744/#745/#746/#748/#749 후속). 본질: 다단 생성 후 단설정 (Ctrl+Alt+Enter 단축키 / 메뉴) 호출 부재 — `stub('page:col-settings', ...)` placeholder 만 존재 영역 영역 실제 동작 미구현 (Issue #733). 정정 (5 files, +156/-1): `src/wasm_api.rs` (+17) `getColumnDef` WASM API 신규 (조회 전용, `find_initial_column_def` 헬퍼 재사용 + JSON 수동 포맷) / `rhwp-studio/src/core/wasm-bridge.ts` (+5) WASM 바인딩 / `rhwp-studio/src/ui/column-settings-dialog.ts` (+119, **신규 파일**) `ColumnSettingsDialog extends ModalDialog` (단 수 1~8 / 종류 일반·배분·평행 / 너비 동일 / 간격 mm) / `rhwp-studio/src/command/commands/page.ts` (+13/-1) `page:col-settings` stub → 실제 구현 / `rhwp-studio/src/command/shortcut-map.ts` (+1) `Ctrl+Alt+Enter` 단축키 매핑 (한컴 표준 정합). Copilot 리뷰 반영 (commit `1f88877f`): `onOk()` → `ModalDialog.onConfirm()` 정합 + count(1~8) / type(0~2) / spacing(0~32767) 범위 클램프 + `HWPUNIT_PER_MM` 하드코딩 → `7200/25.4` 계산식. **인프라 재사용** — `setColumnDef` WASM API (기존, 설정 적용 영역 직접 호출) + `find_initial_column_def` 헬퍼 (기존, `getColumnDef` 영역 내부 활용) + `ModalDialog` 베이스 (기존) + `SectionSettingsDialog` 패턴 (동일 호출 패턴) + `ColumnDef` / `ColumnType` 기존 IR 영역 영역 신규 인프라 1개만 (`getColumnDef` 조회 전용) 영역 영역 위험 좁힘. 본 환경 cherry-pick 충돌 0건 (auto-merge 정합). 결정적 검증: tsc --noEmit ✅ 통과, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (Rust 조회 API 추가 영역 영역 SVG 무영향 입증), WASM 빌드 4.66 MB. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 (Ctrl+Alt+Enter + 다단 설정 적용 + 기존 다단 설정 영역 표시 정합). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과 (PR 본문 cargo test 1173 통과 명시). 검토 보고서: `mydocs/pr/archives/pr_750_review.md`. 처리 보고서: `mydocs/pr/archives/pr_750_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — wasm_api 1 메서드 + TypeScript UI 영역 영역 Rust 렌더링 경로 무영향 (sweep 170/170 same 입증). **`feedback_process_must_follow` 정합** — 인프라 재사용 (`SectionSettingsDialog` 패턴 + `setColumnDef` API + `ModalDialog`) 영역 영역 신규 인프라 1개 영역 영역 위험 좁힘. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 인터랙션 검증 ✅ 통과. |
| **PR #751 (Part of #223)** | fix — 문단 정렬 Alt+Shift 단축키 한국어 IME 매핑 추가 | **완료 (옵션 A — 1 commit cherry-pick + no-ff merge `cc5177d4`, 작업지시자 웹 에디터 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 19번째 PR — PR #728/#729/#730/#734/#735/#737/#738/#739/#740/#742/#743/#744/#745/#746/#748/#749/#750 후속). 본질: 문단 정렬 단축키 (Alt+Shift+H/C/D) 영역 영역 한국어 IME 자모 매핑 누락 — 한글 입력 상태 영역 영역 오른쪽/가운데/배분 정렬 단축키 미동작 (Issue #223 macOS 단축키 지원 영역 일부). 정정 (1 file, +3/-0): `rhwp-studio/src/command/shortcut-map.ts` 영역 영역 매핑 3개 추가 — Alt+Shift+ㅗ → format:align-right / Alt+Shift+ㅊ → format:align-center / Alt+Shift+ㅇ → format:align-distribute. **인프라 재사용** — 기존 줄간격 (`ㅁ`/`ㅋ`) + 글꼴 크기 (`ㄷ`/`ㄱ`) IME 매핑 패턴 정합 + 기존 정렬 커맨드 재호출 영역 영역 신규 인프라 도입 부재. 본 환경 cherry-pick 충돌 0건 (auto-merge 정합). 결정적 검증: tsc --noEmit ✅ 통과, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (TypeScript 매핑 추가 영역 영역 SVG 무영향 자명). **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 (Alt+Shift+ㅗ/ㅊ/ㅇ 정렬 동작). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. **후속 분리** (PR 본문 명시): Issue #223 OPEN 유지 — Cmd+Arrow (PR #746 영역 정합) / Opt+Arrow / 한컴 주요 단축키 영역 의 후속 단계 잔존. 검토 보고서: `mydocs/pr/archives/pr_751_review.md`. 처리 보고서: `mydocs/pr/archives/pr_751_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 (기존 IME 매핑 패턴 + 정렬 커맨드) 영역 영역 신규 인프라 도입 부재. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 인터랙션 검증 ✅ 통과. |
| **PR #778 (closes #775)** | Task #775 — Task #703 회귀 정정 (다단 영역 InFrontOfText/BehindText 표 컬럼 분배 복원) | **완료 (옵션 A — 3 commits cherry-pick + no-ff merge `103bae74`, 작업지시자 웹 에디터 시각 판정 통과)** | 컨트리뷰터 @planet6897 (Jaeook Ryu) — **12+ 사이클** 페이지네이션/표 핵심 컨트리뷰터 (PR #701/#706/#707/#710/#711/#714/#715/#719/#771/#772/#777/#778). 본질: 자기 PR #707 (Task #703, 5/9 머지) 회귀 정정 — InFrontOfText/BehindText 가드 영역 다단 컬럼 분배 영역 변경 → exam_eng.hwp p4 27번 보기 그림 영역 +446.6 px 회귀 (cell-clip y 277.08 → 723.69). bisect 진원지: `a759a1c2` (Task #703 GREEN). 정정 (typeset.rs +5/-1, 단일 가드 라인): InFrontOfText/BehindText 가드 영역 `&& st.col_count == 1` 조건 추가 — Task #703 fix 영역 단일 컬럼 한정 (calendar_year col_count=1 → push-only 유지 / exam_eng p4 col_count=2 → cur_h 누적 종전 동작 복원). 회귀 가드 (tests/issue_775.rs +73): 신규 통합 테스트. 거버넌스 문서 (총 +702): 수행/구현 계획서 + 단계별 보고서 (Stage 0/1 RED + Stage 2 GREEN + Stage 3/4) + 최종 결과 보고서 영역 단계별 분리 절차 정합 인상적. 본 환경 cherry-pick 충돌 0건 (auto-merge 정합). 결정적 검증: cargo build/test --release ALL GREEN, **회귀 가드 PASS** (`issue_775_exam_eng_p4_pi181_table_at_column_top`), **Task #703 본 케이스 보존 PASS** (`issue_703_calendar_year_single_page`), cargo clippy --release -- -D warnings 통과, **광범위 sweep 7 fixture / 170 페이지 / 168 same / 2 diff** (exam_eng_002.svg ID 순서만 + exam_eng_004.svg 의도된 정정, PR 본문 정확 일치), WASM 빌드 4.68 MB 재빌드. **작업지시자 웹 에디터 시각 판정** ✅ 통과 (exam_eng p4 cell-clip y=277.08 정합 + calendar_year 1 page 유지). **PDF 권위 자료 정합** (PR 본문 명시): macOS 환경 PDF 직접 비교 불가 영역 정합 체인 (Task #703 이전 동작 = PDF 권위 정합). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_778_review.md`. 처리 보고서: `mydocs/pr/archives/pr_778_report.md`. **`feedback_contributor_cycle_check` 정합** — @planet6897 12+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — typeset 영역 단일 영역 정정 (svg/canvas/skia 무영향). **`feedback_pr_supersede_chain` (c) 패턴** — PR #707 (Task #703) 머지 후 회귀 정정 영역 별 PR. **`feedback_hancom_compat_specific_over_general` 권위 사례 강화** — 단일 컬럼 한정 가드 영역 일반화 영역 회귀 위험 좁힘. **`feedback_diagnosis_layer_attribution` 정합** — bisect 진원지 명시 (`a759a1c2`) + 측정 (cell-clip y 277.08 → 723.69) 영역 정정 layer 정확화. **`feedback_process_must_follow` 정합** — 단계별 분리 + 거버넌스 문서 + 회귀 가드. **`feedback_visual_judgment_authority` 정합** — 시각 판정 통과. **`feedback_pdf_not_authoritative` 정합** — macOS 환경 PDF 직접 비교 불가 영역 정합 체인 명시. |
| **PR #769 (Refs #536)** | render — improve Skia text replay parity (P9) | **완료 (옵션 A — P8 skip + P9 2 commits cherry-pick + no-ff merge `1097fbe4`, 시각 판정 면제)** | 컨트리뷰터 @seo-rii (Seohyun Lee) — **9번째 사이클** (Skia 핵심 컨트리뷰터, PR #165/#419/#456/#498/#599/#626/#720/#761/#769). 본질: P9 단계 — native Skia text replay 영역 기존 TextRunNode payload 영역 더 많이 소비 (단순 glyph drawing 영역 layer payload 의 text metadata 영역 가능 범위 영역 replay). Skia native raster Issue #536 트래킹의 단계적 진전 — P4 #599 → P5 #626 → P6 #720 → P8 #761 → **P9 #769**. P9 본질 보강: char overlap (TextRunNode.char_overlap 소비) + tab leader (char + repeat replay) + text decoration (underline/strike/overline) + shade/shadow/outline-style effect + emphasis dot (한글 강조점) + vertical rotation (세로쓰기) + control mark (output options paragraph mark + control codes). **인프라 도입** — text_replay.rs 모듈 분리 (P10 → P9 통합, PR 본문 명시): `src/renderer/skia/text_replay.rs` 신규 (+748) + `renderer.rs` (+438/-353, page/layer replay orchestration 집중) + `src/renderer/skia/mod.rs` (+1, 신규 모듈 등록). **본 환경 처리** — 3 commits 분석: `49b540a9` (P8) → empty (PR #761 머지 완료 영역 동일 본질) → skip / `8f079b12` (P9 본질 1) → cherry-pick (`7a9015f3`) / `c74fb927` (P9 본질 2 split) → cherry-pick (`e275789f`). **Non-goals** (PR 본문 명시): full text source table / glyph-run IR 미포함 + Skia 영역 기본 public render path 전환 부재 + targeted raster test 외 visual/pixel regression infrastructure 추가 부재. 본 환경 cherry-pick 충돌 0건 (auto-merge). **결정적 검증** — cargo build/test/clippy --release ALL GREEN, **`cargo test --features native-skia --lib skia` → 28/28 PASS** (신규 4건 + 기존 24건): renders_char_overlap_text_run_as_ink + renders_decorated_text_as_ink + renders_tab_leader_for_empty_text_run + renders_output_control_marks_as_ink, cargo clippy --features native-skia --lib -- -D warnings 통과, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (skia 만 영역 svg sweep 무영향 입증). **시각 판정 면제** (작업지시자 결정 — targeted raster 테스트 + sweep 통과 + Skia 만 변경 영역 권위 사례). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_769_review.md`. 처리 보고서: `mydocs/pr/archives/pr_769_report.md`. **`feedback_contributor_cycle_check` 정합** — @seo-rii 9번째 사이클 (Skia 핵심). **`feedback_image_renderer_paths_separate` 권위 사례 강화** — Skia 만 변경 영역 svg/web_canvas 무영향 (sweep 170/170 same 입증). **`feedback_pr_supersede_chain` 권위 사례 강화** — PR #599 (P4) → #626 (P5) → #720 (P6) → #761 (P8) → #769 (P9) Issue #536 트래킹 단계적 진전. **`feedback_process_must_follow` 정합** — P10 → P9 통합 + Non-goals 명시 + 결정적 검증. **`feedback_visual_judgment_authority` 정합** — targeted raster 테스트 + sweep 통과 영역 시각 판정 면제 합리. |
| **PR #764 (closes #762)** | fix — hwpeq inf 기호(∞) 우선순위 복원 (PR #729 회귀 정정) | **완료 (옵션 1 — head commit 만 cherry-pick + no-ff merge `4cf08316`, 이전 abandon 후 재시도, 작업지시자 시각 판정 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 27번째 PR, 이전 abandon 후 재시도). 본질: PR #729 (Task #143 LaTeX 호환 확장 1차, 5/9 머지) 영역 회귀 — FUNCTIONS HashMap 영역 ('inf', 'inf') 추가 영역 기존 hwpeq inf → ∞ 매핑이 함수 텍스트 'inf' 영역 대체됨. samples/exam_math.hwp 페이지 14/16 영역 ∞ 기호 → inf 텍스트 출력 회귀 (Issue #762). 정정 (head commit `0eed7a3b`, parser.rs +36/-7): lookup_symbol (Unicode 기호) 영역 is_function (함수명) 영역 먼저 점검 영역 우선순위 조정 — inf → ∞ / deg → ° 기존 hwpeq 동작 복원, sin/cos/log 등 함수 정상 처리. 회귀 가드 테스트 2건 (test_hwpeq_inf_remains_symbol + test_hwpeq_deg_remains_symbol). **본 환경 처리 — 옵션 1 (head commit 만 cherry-pick)**: PR 영역 8 commits 누적 영역 본질 분리 — commits 1~5 (PR #729 머지 완료 영역 devel 동일 본질 영역 skip) + commit 6 (`45fdb335` Task #143 명령어 대폭 확장 3차, 별 본질) + commits 7~8 (`d672f954`+`04187512` overset/underset/stackrel + 이스케이프, 별 본질) → 컨트리뷰터에 별 PR 분리 안내 (PR comment 한국어). **이전 abandon 본질** (5/10 사이클 시점, context summary): Service Worker 캐시 결함 영역 SVG 개선 영역 WASM canvas 영역 미반영 발견 영역 작업지시자 결정 "이번 PR 리뷰는 실패입니다. local/devel 로 스위치하고 local/task762 는 삭제합니다." 영역 abandon. 본 환경 재시도 영역 head commit 만 분리 cherry-pick + WASM 재빌드 영역 정합 회복. 본 환경 cherry-pick 충돌 0건 (auto-merge). 결정적 검증: cargo build/test/clippy --release ALL GREEN (clippy -D warnings 통과), 회귀 가드 2건 PASS, **광범위 sweep 7 fixture / 170 페이지 / 168 same / 2 diff** (exam_math_014/016 — **의도된 시각 변경**: ∞ 기호 복원, PR 본문 정확 일치), WASM 빌드 4.66 MB 재빌드. **작업지시자 웹 에디터 시각 판정** ✅ 통과 (samples/exam_math.hwp 페이지 14/16 영역 ∞ 기호 정합 — 이전 abandon 영역 Service Worker 캐시 결함 영역 본 환경 영역 회피 정합). CI 미실행 (DIRTY 영역). 검토 보고서: `mydocs/pr/archives/pr_764_review.md`. 처리 보고서: `mydocs/pr/archives/pr_764_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — hwpeq parser 영역 영역 svg/canvas/skia 공통 영역 동기 정정. **`feedback_pr_supersede_chain` (c) 패턴** — PR #729 (Task #143 1차) 머지 후 회귀 정정 영역 별 PR (Issue #762) — 동일 패턴. **`feedback_process_must_follow` 정합** — head commit 만 cherry-pick 영역 본질 분리 (Issue #762 만) + commit 6~8 별 PR 안내. **`feedback_visual_judgment_authority` 정합** — 작업지시자 시각 판정 영역 ∞ 기호 복원 정합 확인 (이전 abandon 권위 사례 영역 영역 Service Worker 캐시 결함 영역 회피). **`feedback_self_verification_not_hancom` 정합** — 회귀 가드 2건 + 광범위 sweep + 작업지시자 시각 판정 영역 자기 검증 + 시각 판정 게이트 정합. 잔존 후속: 컨트리뷰터에 commit 6~8 별 PR 안내 완료 (Task #143 추가 LaTeX 확장 영역 별 PR 등록 대기). |
| **PR #761 (Refs #536)** | render — harden PageLayerTree schema and resource keys (P8) | **완료 (옵션 A — 1 commit cherry-pick + no-ff merge `88d7fd38`, 시각 판정 면제)** | 컨트리뷰터 @seo-rii (Seohyun Lee) — **8번째 사이클** (Skia 핵심 컨트리뷰터, PR #165/#419/#456/#498/#599/#626/#720/#761). 본질: P8 단계 — 이후 text/image/cache 작업이 의존할 Layer IR contract 정리 (Skia native raster Issue #536 트래킹의 단계적 진전 — P4 #599 → P5 #626 → P6 #720 → P8 #761). 정정 (10 files, +284/-73): `paint/schema.rs` 신규 모듈 (LayerTreeSchema 구조체 + schema_version=1/resource_table_version=1/unit='px'/coordinate_system='page-top-left' 단일 관리) + `paint/resources.rs` 함수 신규 (blake3 digest + image_resource_key/svg_resource_key, kind:algorithm:byte_len:digest 형식) + `paint/json.rs` LAYER_TREE_SCHEMA 사용 + `paint/layer_tree.rs` 상수 schema.rs 이전 + `paint/mod.rs` 재export + `renderer/svg_layer.rs` LayerOutputOptions 소비 + 테스트 + `renderer/web_canvas.rs` LayerOutputOptions 소비 + `wasm_api.rs` canvas scale guard 공통화 + `wasm_api/tests.rs` 신규 테스트. **인프라 도입**: blake3 crate (Cargo.toml +1, content-addressable resource key digest) + paint::schema 모듈 (신규) + paint::resources 함수 (신규) + 기존 LayerOutputOptions 재사용. **Non-goals 명시** (PR 본문): binary resource interning 미구현 + native Skia text/image/cache replay 변경 부재 + CanvasKit / rhwp-studio resource transport 후속. 본 환경 cherry-pick 충돌 0건 (auto-merge). **결정적 검증** — 신규 테스트 6 PASS: resource_digest_is_stable_and_content_dependent + resource_keys_include_kind_algorithm_length_and_digest + layer_tree_schema_contract_is_stable + normalize_canvas_scale 2건 + render_page_consumes_layer_output_options. cargo build/test/clippy --release ALL GREEN (clippy -D warnings 통과), cargo check --target wasm32-unknown-unknown --lib 통과, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (contract 정리 단계 시각 출력 무영향 입증). **시각 판정 면제** (작업지시자 결정 — contract 정리 단계 + 결정적 검증 + 회귀 가드 6건 + sweep + clippy/wasm32 check 모두 통과). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_761_review.md`. 처리 보고서: `mydocs/pr/archives/pr_761_report.md`. **`feedback_contributor_cycle_check` 정합** — @seo-rii 8번째 사이클 (Skia 핵심) 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — paint 모듈 정리 영역 svg/web_canvas 동기 보강 (output options 직접 호출 경로). **`feedback_pr_supersede_chain` 권위 사례 강화** — PR #599 (P4) → #626 (P5) → #720 (P6) → #761 (P8) Issue #536 트래킹 단계적 진전. **`feedback_process_must_follow` 정합** — contract 정리 + Non-goals 명시 + 결정적 검증 영역 위험 좁힘. **`feedback_visual_judgment_authority` 정합** — contract 정리 단계 + 결정적 검증 + 회귀 가드 + sweep 통과 영역 시각 판정 면제 합리. |
| **PR #760 (Issue 미연결)** | feat — page:hide-current 현재 쪽만 머리말/꼬리말 감추기 구현 | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `60899a3c`, 작업지시자 웹 에디터 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 26번째 PR — PR #759 후속). 본질: index.html 의 쪽 메뉴에 data-cmd='page:hide-current' 등록되어 있으나 커맨드 정의 누락 — 클릭 미동작. 기존 page:hide-headerfooter 와 본질이 다름 (편집 모드에서만 동작) — 본 PR 은 일반 편집 중에도 사용 가능. 정정 (1 file, +22/-0): `rhwp-studio/src/command/commands/page.ts` 에 page:hide-current 커맨드 신규 등록 — `toggleHideHeaderFooter(pageIndex, true)` (header) + `toggleHideHeaderFooter(pageIndex, false)` (footer) 두 번 호출 + 양측 상태 차이 시 footer 다시 토글하여 동기화 (Copilot 리뷰 반영). Copilot 리뷰 반영 (commit `2b5251c3`): 머리말/꼬리말 감추기 상태 동기화. **인프라 재사용** — toggleHideHeaderFooter WASM API (기존) + cursor.rect.pageIndex (기존) + document-changed eventBus (기존), 신규 인프라 도입 부재. 본 환경 cherry-pick 충돌 0건 (auto-merge). 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅, WASM 빌드 4.68 MB 재빌드. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 (메뉴 → 현재 쪽만 감추기 → 머리말/꼬리말 토글 정합). CI ✅ Build & Test + CodeQL + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_760_review.md`. 처리 보고서: `mydocs/pr/archives/pr_760_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역. **`feedback_process_must_follow` 정합** — 인프라 재사용. **`feedback_visual_judgment_authority` 정합** — 인터랙션 검증 통과. |
| **PR #759 (Issue 미연결)** | feat — table:block-formula 블록 계산식 커맨드 구현 | **완료 (옵션 A — 2 commits cherry-pick + 충돌 수동 해결 + no-ff merge `ad8cbba2`, 시각 판정 면제)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 25번째 PR — PR #758 후속). 본질: `table:block-formula` (블록 계산식) stub 활성화 — 기존 `FormulaDialog` 재사용 (한컴 오피스 정합 — 계산식/블록 계산식 동일 대화상자). 정정 (1 file, +21/-14): `openFormulaDialog` 공통 함수 신규 (Copilot 리뷰 영역 영역 추출) + `table:block-formula` stub → 실제 구현 + `table:formula` 영역 영역 `openFormulaDialog` 위임 (중복 제거). Copilot 리뷰 반영 (commit `ffcc1cb9`): 계산식 대화상자 공통 함수 추출. **인프라 재사용** — `FormulaDialog` (기존) + `inTable` 가드 (기존) + `openFormulaDialog` 공통 함수 영역 영역 신규 인프라 도입 부재. 본 환경 cherry-pick 충돌 (2 commits 모두 충돌) — table.ts 영역 영역 PR #754 (blockCalcCommand) + PR #757 (thousand-sep/decimal-add/decimal-remove 활성화) 영역 영역 인접 영역 영역 양측 보존 수동 해결: 첫 commit (HEAD PR #754/#757 보존 + incoming `table:block-formula` 활성화만 추가) + 두번째 commit (HEAD `blockCalcCommand` + incoming `openFormulaDialog` 양측 함수 보존). 결정적 검증: tsc --noEmit ✅ 통과, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅. **시각 판정 면제** (작업지시자 결정 — 단순 stub 활성화 + 기존 `FormulaDialog` 동일 호출). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_759_review.md`. 처리 보고서: `mydocs/pr/archives/pr_759_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 (FormulaDialog + 공통 함수 추출) 영역 영역 신규 인프라 도입 부재. **`feedback_visual_judgment_authority` 정합** — 단순 stub 활성화 영역 영역 시각 판정 면제 (작업지시자 결정). |
| **PR #758 (Issue 미연결, 후속 Issue #792)** | feat — 표 셀 높이 / 너비 균등화 커맨드 구현 | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `b555739a`, 작업지시자 웹 에디터 인터랙션 검증 통과 + 후속 Issue #792 영역 영역 메뉴 hotkey 인프라)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 24번째 PR — PR #757 후속). 본질: 표 메뉴 2 stub 활성화 — table:cell-height-equal (H) / table:cell-width-equal (W). 정정 (1 file, +82/-2): `rhwp-studio/src/command/commands/table.ts` 영역 영역 2 stub → 실제 구현 — `getTableDimensions` → 셀 개수 → 전체 셀 순회 + `getCellInfo` (row/col/rowSpan/colSpan) + `getCellProperties` (height/width) → 행/열 별 평균 (병합 셀 rowSpan>1 / colSpan>1 제외) → 전체 평균 → 각 셀 delta → `resizeTableCells` 일괄 적용. Copilot 리뷰 반영 (commit `c9ebee5c`): 셀 정보 캐싱 영역 영역 이중 순회 제거. **인프라 재사용** — 모든 WASM API 기존 (`getTableDimensions` + `getCellInfo` + `getCellProperties` + `resizeTableCells`) + `inTable` 가드 영역 영역 신규 인프라 도입 부재. 본 환경 cherry-pick 충돌 0건 (auto-merge 정합). 결정적 검증: tsc --noEmit ✅ 통과, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (TypeScript 단일 파일 영역 영역 SVG 무영향), WASM 빌드 4.68 MB 재빌드. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 (커맨드 본질 — 메뉴 → 셀 높이/너비 균등화 동작 정합). 발견 — 단축키 H / W 미동작: `shortcutLabel: 'H' / 'W'` 영역 영역 한컴 메뉴 hotkey (메뉴 열린 후 H 영역 영역 항목 활성) 정합 의도 영역 영역 rhwp-studio 영역 영역 메뉴 hotkey 인프라 미구현. **Issue #792 신규 등록** — 메뉴 hotkey 인프라 + H/W 단축키 활성 영역 별 task (`feedback_pr_supersede_chain` (c) 패턴). 본 PR 영역 영역 `shortcutLabel` 영역 영역 메뉴 라벨 표시 영역 영역 보존 — Issue #792 영역 영역 인프라 도입 후 활성 정합. **점검 항목**: Ctrl+Z Undo 동작 — `resizeTableCells` 직접 호출 영역 영역 PR #748 SnapshotCommand 패턴 영역 영역 미적용 영역 영역 결함 가능 영역 영역 별 후속 task (PR #754 / #757 와 동일 본질 영역 영역 통합 처리 가능). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_758_review.md`. 처리 보고서: `mydocs/pr/archives/pr_758_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 (모든 WASM API 기존) 영역 영역 신규 인프라 도입 부재. **`feedback_pr_supersede_chain` (c) 패턴** — 본 PR 머지 유지 + Issue #792 영역 영역 본질 정정. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 인터랙션 검증 영역 영역 단축키 미동작 발견 영역 영역 후속 task 분리 결정. |
| **PR #757 (Issue 미연결)** | feat — 표 셀 숫자 서식 커맨드 구현 (천 단위 구분, 자릿점 넣기/빼기) | **완료 (옵션 A — 2 commits cherry-pick + 충돌 수동 해결 + no-ff merge `0eac69b2`, 작업지시자 웹 에디터 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 23번째 PR — PR #756 후속). 본질: 표 메뉴 3 stub 활성화 — table:thousand-sep / table:decimal-add / table:decimal-remove. 정정 (1 file, +103/-3): `rhwp-studio/src/command/commands/table.ts` 영역 영역 3 stub → read-modify-write 패턴 변환 (전용 WASM 포맷 API 부재 영역 영역 텍스트 기반) — `getCellParagraphLength` → `getTextInCell` → 정규식 파싱 + 재포맷 (천 단위 구분 영역 영역 `\B(?=(\d{3})+(?!\d))/g, ','`) → `deleteTextInCell` + `insertTextInCell` → `document-changed` emit. 부호(+/-) + 쉼표 + 소수점 보존. Copilot 리뷰 반영 (commit `3da7f204`): 숫자 유효성 검증 강화 + cellParaIndex 사용 (하드코딩 0 → pos.cellParaIndex ?? 0). **인프라 재사용** — 모든 WASM API 기존 + inTable 가드 영역 영역 신규 인프라 도입 부재. 본 환경 cherry-pick 충돌 (1차) — table.ts 영역 영역 PR #754 (blockCalcCommand) 영역 영역 인접 영역 영역 양측 보존 수동 해결 (HEAD blockCalcCommand 3 줄 + incoming 신규 3 구현). 결정적 검증: tsc --noEmit ✅ 통과, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (TypeScript 단일 파일 영역 영역 SVG 무영향 자명), WASM 빌드 4.68 MB 재빌드. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 (3 커맨드 + 부호/쉼표/소수점 보존). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. **점검 항목**: Ctrl+Z Undo 동작 — read-modify-write 영역 영역 SnapshotCommand 미경유 영역 영역 결함 가능 영역 영역 별 후속 task (PR #754 와 동일 본질). 검토 보고서: `mydocs/pr/archives/pr_757_review.md`. 처리 보고서: `mydocs/pr/archives/pr_757_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 (read-modify-write 패턴 + 기존 WASM API + inTable 가드) 영역 영역 신규 인프라 도입 부재. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 인터랙션 검증 ✅ 통과. |
| **PR #756 (Issue 미연결)** | feat — 보기 메뉴 도구 상자 / 서식 도구 모음 표시 토글 구현 | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `fddc3f06`, 작업지시자 웹 에디터 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 22번째 PR — PR #755 close 후속, PR #728/#729/#730/#734/#735/#737/#738/#739/#740/#742/#743/#744/#745/#746/#748/#749/#750/#751/#752/#754 후속). 본질: 보기 메뉴의 기본 (도구 상자) + 서식 (서식 도구 모음) 영역 영역 stub 영역 영역 비활성. 정정 (1 file, +34/-12): `rhwp-studio/src/command/commands/view.ts` 영역 영역 2 stub → IIFE 클로저 토글 변환 — `view:toolbox-basic` (#icon-toolbar) / `view:toolbox-format` (#style-bar) display 토글 + `[data-cmd]` 메뉴 active 클래스 정합. Copilot 리뷰 반영 (commit `4d2a729f`): 첫 호출 시 DOM 영역 영역 `getComputedStyle()` 영역 영역 초기 표시 상태 정확히 복원. **인프라 재사용** — 기존 `view:para-mark` / `view:toggle-clip` 영역 영역 IIFE 클로저 토글 패턴 정합 + `[data-cmd]` 메뉴 active 클래스 영역 영역 신규 인프라 도입 부재. 본 환경 cherry-pick 충돌 0건 (auto-merge 정합). 결정적 검증: tsc --noEmit ✅ 통과, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (TypeScript 단일 파일 영역 영역 SVG 무영향 자명), WASM 빌드 4.68 MB 재빌드. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 (메뉴 → "보기 → 기본 / 서식" 토글 + active 클래스 + 초기 표시 상태 정확 복원). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_756_review.md`. 처리 보고서: `mydocs/pr/archives/pr_756_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 (IIFE 클로저 토글 패턴) 영역 영역 신규 인프라 도입 부재. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 인터랙션 검증 ✅ 통과. |
| **PR #755 (Issue 미연결, ❌ close)** | feat — page:new-page-num (새 번호로 시작) 커맨드 구현 시도 | **close (옵션 1 — cherry-pick reset + PR close + 별 Issue #791 신규 등록)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 22번째 PR 시도). 본질: 쪽 > 새 번호로 시작 stub 활성화 시도 — `setNumberingRestart(sec, para, mode=2, startNum)` WASM API 호출 + `NumberingRestartDialog` 신규. PR #745 (Task #634 NewNumber Page 한컴 호환) 후속 의도. **본질 결함 발견** (작업지시자 웹 에디터 시각 검증 영역 영역): "새 번호로 시작 > 대화창에서 특정 페이지 시작번호를 1로 설정해도 기존 쪽번호 업데이트 되지 않습니다". 본 환경 점검 결과 — `setNumberingRestart` 영역 영역 **paragraph numbering** (1.1.1, 가, 나 자동 번호 카운터) 영역 영역 재시작 API → **페이지 번호 (쪽번호) 영역 영역 무관**. 본 환경 입증: `set_numbering_restart_native` (`formatting.rs:1141`) 영역 영역 `paragraph.numbering_restart = NewStart(N)` 설정만 / `layout.rs:96` `advance` 영역 영역 paragraph numbering counter 사용 / `page_number.rs` 영역 영역 `numbering_restart` 미참조 (페이지 번호 영역 영역 무관 입증) / `NewNumber` 컨트롤 (`control.rs:38`) 영역 영역 model 존재 영역 영역 WASM 삽입 API 부재. **두 본질 분리 — paragraph numbering (1.1.1 자동 번호) vs page number (footer 쪽번호) 영역 영역 다른 메커니즘**. 본 환경 cherry-pick reset (`9cb3e1ae`+`6d28d653` 영역 영역 git reset --hard 6003a02b 영역 영역 제거) + PR #755 close + 컨트리뷰터에 본질 결함 보고 (한국어, 차분 톤, 코드 위치 명시, 별 PR 권장 방향 안내). **Issue #791 신규 등록** — NewNumber 컨트롤 삽입 WASM API + dialog UI (PR #755 의 NumberingRestartDialog UI 인프라 영역 영역 재사용 가능). 자기 검증 영역 영역: tsc ✅ + cargo test ALL GREEN + sweep 170/170 same + WASM 4.68 MB 영역 영역 모두 통과 영역 영역, **작업지시자 웹 에디터 시각 판정 영역 영역 본질 결함 발견** — `feedback_visual_judgment_authority` 권위 사례 강화 (자기 검증 통과 ≠ 사용자 의도 정합). 보존 영역: `NumberingRestartDialog` 클래스 + `ModalDialog` 패턴 (별 PR 영역 영역 재사용 가능). 검토 보고서: `mydocs/pr/archives/pr_755_review.md`. 처리 보고서: `mydocs/pr/archives/pr_755_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_pr_supersede_chain` (a) 패턴** — PR #755 close + 별 Issue #791 영역 영역 본질 정정. **`feedback_process_must_follow` 정합** — 본질 결함 발견 영역 영역 단순 머지 영역 영역 회귀 위험 차단. **`feedback_visual_judgment_authority` 권위 사례 강화** — 머지 전 작업지시자 시각 판정 게이트 영역 영역 회귀 차단. **`feedback_pr_comment_tone` 정합** — 차분 톤 + 사실 중심 본질 진단 + 코드 위치 명시 + 별 PR 권장 방향 안내. |
| **PR #754 (Issue 미연결)** | feat — 표 블록 합계/평균/곱 계산 커맨드 구현 | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `fcb888d7`, 작업지시자 웹 에디터 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 21번째 PR — PR #728/#729/#730/#734/#735/#737/#738/#739/#740/#742/#743/#744/#745/#746/#748/#749/#750/#751/#752 후속, PR #753 jangster77 후속). 본질: 표 > 블록 합계/평균/곱 커맨드 영역 영역 stub 영역 영역 미동작 — table:block-sum (Ctrl+Shift+S) / table:block-avg (Ctrl+Shift+A) / table:block-product (Ctrl+Shift+P). 정정 (2 files, +37/-3): `rhwp-studio/src/command/commands/table.ts` (+34/-3) `blockCalcCommand` 헬퍼 함수 신규 — 대화상자 없이 즉시 계산 (`getCursorPosition` → `getCellInfo` 직접 조회 → `evaluateTableFormula(=FUNC(above), writeResult=true)` → 결과 셀 삽입), 3 stub → blockCalcCommand 호출 변환 (SUM/AVERAGE/PRODUCT) / `rhwp-studio/src/command/shortcut-map.ts` (+3) Ctrl+Shift+S/A/P 단축키 매핑. Copilot 리뷰 반영 (commit `cb966149`): `getTableProperties` cell loop 계산 → `getCellInfo()` 직접 조회 (효율 개선). **인프라 재사용** — `getCellInfo` WASM API (기존) + `evaluateTableFormula` WASM API (기존) + `inTable` 가드 (기존) + FormulaDialog 패턴 (기존, row/col 계산 로직 공유) + EditorContext 영역 영역 신규 인프라 도입 부재 영역 영역 위험 좁힘. 본 환경 cherry-pick 충돌 0건 (auto-merge 정합). 결정적 검증: tsc --noEmit ✅ 통과, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (TypeScript 단일 영역 영역 영역 SVG 무영향 자명), WASM 빌드 4.68 MB 재빌드. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 (Ctrl+Shift+S/A/P + 메뉴). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. **의도적 제한** (PR 본문 명시): `above` 만 — left/right/below 영역 영역 후속 분리 (`feedback_hancom_compat_specific_over_general` 정합). **점검 항목**: Ctrl+Z Undo 동작 — `evaluateTableFormula(writeResult=true)` 영역 영역 SnapshotCommand 미경유 영역 영역 결함 가능 영역 영역 별 후속 task. 검토 보고서: `mydocs/pr/archives/pr_754_review.md`. 처리 보고서: `mydocs/pr/archives/pr_754_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 (getCellInfo + evaluateTableFormula + inTable 가드 + FormulaDialog 패턴) 영역 영역 신규 인프라 도입 부재. **`feedback_hancom_compat_specific_over_general` 정합** — above 만 영역 영역 일반화 후속 분리. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 인터랙션 검증 ✅ 통과. |
| **PR #753 (closes #741)** | Task #741 — hwp3-sample10.hwp HWP3 native 렌더링 8 단계 정정 + Git LFS 구축 | **완료 (옵션 B — Task #741 commits 11개 cherry-pick + 메인테이너 chore 2개 + no-ff merge `a90ecd09`, 작업지시자 웹 에디터 시각 판정 통과)** | 컨트리뷰터 @jangster77 (Taesup Jang) — **HWP3 핵심 컨트리뷰터** (16+ 사이클, PR #451/#486/#487/#499/#506/#553/#556/#589/#605/#609/#673/#675/#678/#723/#732/#753). 본질: hwp3-sample10.hwp (Oracle 기술 문서, 763 페이지) HWP3 native 렌더링 결함 8개 영역 정정 — 한컴 viewer + PDF (한글 2022) 영역 정합 도달 (Issue #741). **8 단계 정정**: Stage 1~4 (HWP3 외부 file path 그림 IR + image placeholder + TAC line_spacing + vpos-reset 후속 가드, `81c16cfa`) / Stage 5 (HWP3 사적 graphic char 0x0080~0x7FFF cross-ref 매핑, 상위 6값=98.5% coverage, `e184718b`) / Stage 6 (**Hwp3TabDef 필드 순서 bug 정정 — 30+ 사이클 미발견 본질 결함**, `1348d939`) / Stage 7 (HWP3 leader 매핑 + 제목차례 자동 장식 inject, 한컴 사적 로직 cross-ref, `d574cf13`+`d2ddb3bc`) / Stage 8 (HWP3 차례 inline page 번호 ch=1 decode, `639088a9`). **후속 영역**: 외부 그림 native CLI + WASM API + Vite middleware + dialog (`02130377`+`dd71507d`+`4bfcee05`) + CI fix (`314bb121`). **PR #732 후속 commit `ab2fa527` skipped** — devel `25299a5b` 영역 영역 이미 적용. **Git LFS 신규 구축** (메인테이너 chore 2개): `fe4676c3` `.gitattributes` (`pdf-large/**/*.pdf filter=lfs`) + `pdf-large/README.md` 폴더 정책 / `80eccfef` CLAUDE.md "예제 폴더" + "PDF 권위 자료 명명 규약" 갱신 — `pdf-large/` 폴더 한정 LFS 격리 (50 MB 초과 PDF), 기존 pdf/ + pdf-2020/ + pdf-2010/ 영역 영역 일반 git 보존. PR 본문 영역 PDF 96 MB → `pdf-large/hwp3-sample10-hwp5-2022.pdf` 영역 영역 LFS pointer 영역 영역 직접 배치 (LFS 업로드 96 MB 완료, push 시). **인프라 도입**: git-lfs 3.5.1 사용자 영역 (~/.local/bin/) 영역 영역 설치 (root 미사용). 본 환경 cherry-pick 충돌: Stage 5 영역 영역 PDF 진입 사전 제외 + 후속 `4bfcee05` modify/delete 영역 영역 pdf-large/ 이동 처리 영역 영역 해결. 결정적 검증: cargo build/test --release ✅ ALL GREEN, tsc --noEmit ✅ (WASM 빌드 후 신규 WASM API 영역 영역 갱신), **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅, WASM 빌드 4.68 MB, hwp3-sample10 native svg export 페이지 1/2 정합 (53/489 KB), `git lfs ls-files` ✅ pointer 정합. **작업지시자 웹 에디터 시각 판정** ✅ 통과 (한컴 viewer + PDF 한글 2022 정합 — 페이지 1 image placeholder / 페이지 2 제목차례 자동 장식 + ▷ markers + 점선 leader + 페이지 번호 / 페이지 28~339 차례 inline page 번호). 검토 보고서: `mydocs/pr/archives/pr_753_review.md`. 처리 보고서: `mydocs/pr/archives/pr_753_report.md`. **`feedback_contributor_cycle_check` 정합** — @jangster77 16+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 권위 사례** — image placeholder 영역 영역 다중 경로 (svg.rs / picture_footnote / table_cell_content / web_canvas) 동기 정정. **`feedback_process_must_follow` 정합** — Task #741 단계별 분리 (Stage 1~8) 영역 영역 위험 좁힘. **`feedback_hancom_compat_specific_over_general` 정합** — Stage 5 사적 char 상위 6값 + Stage 7 제목차례 trigger 조건 영역 영역 일반화 회귀 위험 좁힘. **`feedback_pdf_not_authoritative` 정합** — 한글 2022 PDF 정답지 등급 ✅. **`reference_authoritative_hancom` 정합** — 한컴 viewer + PDF cross-ref 권위. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 시각 판정 ✅ 통과. **`feedback_visual_regression_grows` 정합** — 광범위 sweep 170/170 same 단계별 검증. **신규 정책 도입** — Git LFS pdf-large/ 폴더 한정 격리 영역 영역 향후 50 MB 초과 PDF 영역 영역 자동 LFS 처리. |
| **PR #752 (Issue 미연결)** | feat — edit:delete (지우기) 커맨드 구현 (선택 영역/개체 삭제) | **완료 (옵션 A — 2 commits cherry-pick + 자기 정정 commit + no-ff merge `3eb10f4e`, 작업지시자 웹 에디터 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 20번째 PR — PR #728/#729/#730/#734/#735/#737/#738/#739/#740/#742/#743/#744/#745/#746/#748/#749/#750/#751 후속). 본질: `edit:delete` 커맨드 영역 영역 `canExecute: () => false` stub — 편집 > 지우기 메뉴 + Ctrl+E 단축키 미동작. 정정 (3 files, +42/-2): `rhwp-studio/src/engine/input-handler.ts` (+36) `performDelete()` 신규 메서드 — `performCut()` 패턴 정합 영역 영역 클립보드 복사만 제외 (그림/도형 → snapshot + `deleteObjectControl` / 표 → snapshot + `deleteTableControl` (cellPath.length > 1 가드 — 중첩 표 삭제 차단) / 텍스트 → `deleteSelection`) / `rhwp-studio/src/command/commands/edit.ts` (+4/-2) stub `() => false` → 실제 `canExecute` + `performDelete` 호출 / `rhwp-studio/src/command/shortcut-map.ts` (+2) `Ctrl+E` 단축키 매핑. Copilot 리뷰 반영 (commit `3b5f6965`): `deleteObjectControl()` 헬퍼 사용 (image/equation→deletePictureControl, shape/group/line→deleteShapeControl 분기) + 중첩 표 `cellPath.length > 1` 가드. **자기 정정 commit `2aafebc8`** — devel HEAD 영역 영역 `deleteObjectControl` 래퍼 시그니처 (`input-handler.ts:1841`) + `getObjectProperties` (1831) + `setObjectProperties` (1836) 영역 영역 `'line'` 타입 누락 — `getSelectedPictureRef` (1819) 와 `_picture.deleteObjectControl` 실제 구현은 `'line'` 포함 영역 영역, 래퍼 3개 영역 영역 누락 (devel HEAD 잠재 결함, PR #752 의 신규 `performDelete` 호출 영역 영역 노출됨). 래퍼 3개 영역 영역 `'line'` 추가 영역 영역 정합 (PR #740 자기 정정 패턴 정합). **인프라 재사용** — `executeOperation snapshot` (PR #728 인프라) + `deleteSelection` (기존 private) + `deleteObjectControl` 헬퍼 (기존) + `deleteTableControl/PictureControl/ShapeControl` WASM API (기존) + `cursor.moveOutOfSelected*` (기존) + `EditorContext` 가드 (기존) + `performCut` 패턴 정합 영역 영역 신규 인프라 도입 부재 영역 영역 위험 좁힘. 본 환경 cherry-pick 충돌 0건 (auto-merge 정합). 결정적 검증: tsc --noEmit ✅ 통과 (자기 정정 후), cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅, WASM 빌드 4.66 MB. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 (텍스트 / 그림 / 표 / 중첩 표 + Undo). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_752_review.md`. 처리 보고서: `mydocs/pr/archives/pr_752_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 (`performCut` 패턴 + `deleteObjectControl` 헬퍼 + `executeOperation snapshot`) 영역 영역 신규 인프라 도입 부재. **`feedback_hancom_compat_specific_over_general` 정합** — 중첩 표 가드 (`cellPath.length > 1`) 영역 영역 일반화 영역 영역 결함 위험 좁힘. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 인터랙션 검증 ✅ 통과. **`feedback_diagnosis_layer_attribution` 정합** — devel HEAD 잠재 결함 영역 영역 본 PR cherry-pick 영역 영역 노출됨 영역 영역 자기 정정 commit 으로 해결 (PR #740 패턴). |
| **PR #745 (closes #634)** | Task #634 — 첫 NewNumber Page 발화 전 쪽번호 미표시 (한컴 호환) | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `8ef9c86e`, 두 별 결함 영역 Issue #782/#783 후속 처리)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 14번째 PR — PR #728/#729/#730/#734/#735/#737/#738/#739/#740/#742/#743/#744 후속). 본질: 한컴오피스 PDF 영역 영역 첫 `새번호 (NewNumber Page)` 컨트롤 등장 페이지부터만 쪽번호 표시 영역 영역 rhwp 영역 영역 PageNumberPos 등록 시점부터 모든 페이지 쪽번호 표시 → 표지/요약문/목차 영역 영역 한컴 시각 차이 (Issue #634). 정정 (3 files, +42/-4): `src/renderer/page_number.rs` (+36/-2) PageNumberAssigner 영역 영역 `numbering_started: bool` 추가 + assign() 영역 영역 첫 NewNumber 소비 시점 영역 영역 true 전환 + `should_hide_page_number()` 메서드 + 신규 unit tests 2건 (should_hide_before_first_new_number / should_not_hide_when_no_new_numbers) / `src/renderer/pagination/engine.rs` (+3/-1) finalize_pages 영역 영역 page_number_pos 할당 영역 영역 가드 / `src/renderer/typeset.rs` (+3/-1) TypesetEngine 동일 가드 (Copilot 리뷰 반영, commit `cc1e6541`). **opt-in 정합** — NewNumber 부재 문서 (대부분 문서) 영역 영역 new_page_numbers 빈 벡터 → should_hide_page_number() 항상 false → 기존 동작 100% 보존. **두 경로 동기 정합** (pagination/engine + typeset, `feedback_image_renderer_paths_separate` 권위 사례 강화). 본 환경 cherry-pick 충돌 0건 (auto-merge 정합). 결정적 검증: cargo build/test --release ✅ ALL GREEN, **page_number tests ✅ 8 PASS** (신규 2건 + 기존 6건), **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (opt-in 정합 입증), WASM 빌드 4.66 MB. **작업지시자 시각 검증** — 두 별 결함 발견 (PR #745 본질 영역 영역 무관, 후속 처리): ⓐ **Issue #782** — aift.hwp 페이지 2 영역 영역 일부 표 셀 검정 바탕 (BEFORE/AFTER 동일, HWP 영역 영역 셀 배경 ColorRef 처리 결함 가능, PR #744 영역 영역 동일 패턴 영역 영역 HWP 적용 가능 영역 영역 점검) ⓑ **Issue #783** — aift.hwp 페이지 6 영역 영역 한컴 4 vs rhwp 3 불일치 (페이지 번호 카운터 자체 영역 영역 본 PR 영역 영역 다른 본질, 다중 구역 영역 영역 페이지 번호 처리 흐름 한컴 호환 점검 필요). **처리 결정** (`feedback_pr_supersede_chain` (c) 패턴 정합): PR #745 머지 유지 (단위 테스트 결정적 검증 + opt-in 정합 입증) + Issue #782/#783 영역 영역 별 후속 PR 영역 영역 통합 처리. 검토 보고서: `mydocs/pr/archives/pr_745_review.md`. 처리 보고서: `mydocs/pr/archives/pr_745_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 권위 사례 강화** — pagination/engine + typeset 두 경로 동기 정합. **`feedback_hancom_compat_specific_over_general` 정합** — opt-in case 가드 영역 영역 회귀 위험 좁힘. **`feedback_visual_judgment_authority` 정합** — 한컴 PDF 정합 영역 영역 권위 + 두 별 결함 발견 영역 영역 후속 PR 통합 결정. **`feedback_pr_supersede_chain` (c) 패턴 적용** — 머지 유지 + 별 후속 정정. |
| **PR #744 (closes #606)** | Task #606 — HWPX ColorRef 상위 바이트(alpha) 보존 (표 배경 검정 렌더링 정정) | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `a4b368a5`, PR #607 supersede + 시각 판정 면제)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 영역 영역 13번째 PR — PR #728/#729/#730/#734/#735/#737/#738/#739/#740/#742/#743 후속). 본질: HWPX 문서의 `faceColor="#FF000000"` (alpha=0xFF, 채우기 없음) 영역 영역 `0x00000000` (검정) 으로 파싱되어 표 배경 검정색 렌더링 (Issue #606). `style_resolver` 영역 영역 ColorRef 상위 바이트 비제로 → "채우기 없음" 분기 존재 영역 영역, alpha 소실로 분기 도달 부재. 정정 (3 files, +26/-8): `src/parser/hwpx/utils.rs` (+8/-5) `parse_color_str` 영역 영역 `#AARRGGBB` → `0xAABBGGRR` (alpha 보존) + 단위 테스트 신규 케이스 / `src/serializer/hwpx/header.rs` (+12/-2) `color_hex` alpha 비제로 시 `#AARRGGBB` 출력 + `0xFFFFFFFF` 투명 센티넬 처리 / `src/serializer/hwpx/section.rs` (+6/-1) `color_ref_to_hwpx` 동일 alpha 보존 출력 (양방향 정합). Copilot 리뷰 반영 (commit `2e5e993f`): color_hex 투명 센티넬 처리 + 문서 수정. **PR supersede 체인 (a) 패턴**: PR #607 (@dicebattle 4/29 OPEN, 첫 PR, +9/-5 1 file, parser 만 정정) 영역 영역 본 PR #744 가 본질 (parser alpha 보존) 완전 포함 + serializer 양방향 정합 추가 → close+통합 머지 ((a) 패턴 정합, PR #649 → #650 / PR #738 → #765 동일 패턴). 본 환경 cherry-pick 충돌 0건. 결정적 검증: cargo build/test --release ✅ ALL GREEN, **utils tests ✅ 3 PASS** (`test_parse_color_str_with_alpha` 신규 케이스 포함), **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (HWPX 만 영향, HWP 변환본 sweep 무영향). **시각 판정 면제** 합리 — 단위 테스트 결정적 검증 + 양방향 라운드트립 정합. 검토 보고서: `mydocs/pr/archives/pr_744_review.md`. 처리 보고서: `mydocs/pr/archives/pr_744_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현, @dicebattle 첫 PR. **`feedback_pr_supersede_chain` (a) 패턴 권위 사례 강화** — PR #649 → #650 / PR #738 → #765 동일 패턴. **`feedback_image_renderer_paths_separate` 정합** — parser/utils.rs + serializer/* 격리 (다른 layout/render 경로 무영향, sweep 170/170 same 입증). **`feedback_pr_comment_tone` 정합** — @dicebattle 첫 PR close 시 정중 한국어 댓글 + supersede 명시. |
| **PR #743 (closes #721)** | Task #721 — 중첩 표 내 필드 값 설정 (nested_path 전체 탐색) | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `c4d506d4`, 작업지시자 웹 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 영역 12번째 PR — PR #728/#729/#730/#734/#735/#737/#738/#739/#740/#742 후속). 본질: setFieldValueByName / setFieldValue 호출 시 **중첩 표** (표 안의 표) 내부 ClickHere 필드에서 `field_range 인덱스 초과` 에러 발생 (Issue #721). **컨트리뷰터 제기 문제 타당성 검증 ✅** — 본 환경 소스 점검 결과 명백한 비대칭 결함 확인: `collect_fields_from_paragraph` 는 재귀로 nested_path 누적 (✅ 정합) 영역 영역 `get_para_mut_at_location` (line 252 `// 1단계 중첩만 처리` 명시) + `set_cell_field_text` 는 `nested_path[0]` 만 처리 (❌ 결함). 정정 (1 file, +106/-52): `src/document_core/queries/field_query.rs` get_para_mut_at_location 영역 for 루프 전체 순회 (collect 재귀와 대칭 정합) + set_cell_field_text 동일 패턴 (마지막 항목 직전까지 중첩 탐색 + 마지막 항목에서 셀의 첫 문단 텍스트 교체) + 에러 메시지에 경로 인덱스 명시. Copilot 리뷰 반영 (commit `c187b031`): 에러 메시지 정확성 + char_offsets 영역 영역 naive (0..n) 영역 영역 → rebuild_char_offsets() 사용 (UTF-16 서로게이트 페어 반영). 본 환경 cherry-pick 충돌 0건 (auto-merge 정합). 결정적 검증: cargo build/test --release ✅ ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (field_query 만 변경되어 SVG 무영향 입증), WASM 빌드 4.65 MB. **작업지시자 웹 인터랙션 검증** ✅ 통과 — `samples/k-water-rfp.hwp` 24페이지 분리된 표 > 표 영역에서 hitTest 인덱스 초과 오류 해결 확인 (한국 정부 양식의 중첩 표 ClickHere 필드 정합 동작). 검토 보고서: `mydocs/pr/archives/pr_743_review.md`. 처리 보고서: `mydocs/pr/archives/pr_743_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — field_query.rs 격리 (다른 layout/render 경로 무영향, sweep 170/170 same 입증). **`feedback_process_must_follow` 정합** — 컨트리뷰터 제기 본질이 정확하고, 비대칭 결함 (collect 재귀 ↔ get/set 1단계 만) 영역 영역 정확히 정정. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 인터랙션 검증 ✅ 통과 (k-water-rfp.hwp 분리된 표 > 표 hitTest). |
| **PR #742 (closes #420)** | Task #420 — 문서 전환 시 글꼴 드롭다운 초기화 (이전 문서 폰트 잔존 정정) | **완료 (옵션 A — 1 commit cherry-pick + no-ff merge `0a7ea499`, 작업지시자 웹 에디터 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 영역 영역 11번째 PR — PR #728/#729/#730/#734/#735/#737/#738/#739/#740 후속). 본질: HY헤드라인M 사용 문서 → 텍스트 복사 → 새 문서 → 글꼴 드롭다운 영역 영역 HY헤드라인M 잔존 (Issue #420). 스타일 드롭다운 (`initStyleDropdown` 정합) 영역 영역 글꼴 드롭다운 영역 영역 대응 메서드 부재. 정정 (2 files, +28/-1): `rhwp-studio/src/ui/toolbar.ts` (+26) `initFontDropdown(docFonts?: string[])` 신규 — `replaceChildren()` 통한 완전 초기화 + BASE_FONTS 7개 (함초롬바탕/함초롬돋움/맑은 고딕/나눔고딕/바탕/돋움/궁서) + `docFonts` (현재 문서 fontsUsed) 중복 없이 추가 + `populateFontSetOptions()` + `populateLocalFontOptions()` 재호출 (대표/로컬 글꼴 복원) / `rhwp-studio/src/main.ts` (+2/-1) `initializeDocument()` 영역 영역 `initStyleDropdown()` 직전 영역 영역 `initFontDropdown(docInfo.fontsUsed)` 호출. 인프라 재사용: `initStyleDropdown()` 패턴 정합 + `populateFontSetOptions/LocalFontOptions` 기존 메서드 재호출 + `DocumentInfo.fontsUsed` 기존 IR 활용 (`feedback_process_must_follow` 정합 — 신규 인프라 도입 부재 영역 영역 위험 좁힘). 본 환경 cherry-pick 충돌 0건 (auto-merge 정합). 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (TypeScript 변경 영역 영역 SVG 무영향 입증), WASM 빌드 4.65 MB. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 — HY헤드라인M 문서 → 새 문서 → 글꼴 드롭다운 영역 영역 HY헤드라인M 부재 + BASE_FONTS 7개 + 대표/로컬 글꼴 정합 표시. 검토 보고서: `mydocs/pr/archives/pr_742_review.md`. 처리 보고서: `mydocs/pr/archives/pr_742_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 변경 (Rust 렌더링 경로 무영향, sweep 170/170 same 입증). **`feedback_process_must_follow` 정합** — 인프라 재사용 영역 영역 위험 좁힘. **`feedback_visual_judgment_authority` 정합** — 작업지시자 웹 에디터 인터랙션 검증 ✅ 통과. |
| **PR #740 (Part of #536)** | Task #536 P7 — native Skia form control static replay | **완료 (옵션 A — 4 commits cherry-pick + 자기 정정 + no-ff merge `db76b8dd`, 작업지시자 SVG 시각 판정 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 영역 영역 10번째 PR — PR #728/#729/#730/#734/#735/#737/#738/#739 후속). 본질: native Skia 렌더러 영역 영역 양식 개체 5종 placeholder → 실제 외형 변환 (PR #599 P4 → PR #626 P5 → PR #720 P6 후속의 P7). 정정 (1 file, +245/-2): `src/renderer/skia/renderer.rs` PushButton(RRect+캡션) / CheckBox(체크마크 V자) / RadioButton(원형+점) / ComboBox(드롭다운 화살표+텍스트) / Edit(입력 필드+텍스트) + CSS `#rrggbb` → Skia Color 변환 utility (`parse_css_color`) + glyph 크기 bbox 비례 자동 조정 (8~14px). Copilot 리뷰 반영 (commit `2889d9f4`): RRect import + bg_color 일관 + ComboBox/Edit text-only. CI 수정 (commit `a8093f27`): Path::new() → PathBuilder (native-skia 호환). CJK fallback (commit `85c05bcd`): make_form_font() 메서드 영역 영역 Malgun Gothic / 맑은 고딕 / NanumGothic / 나눔고딕 / AppleGothic 영역 영역 fallback chain — Font::default() 5개소 → self.make_form_font() 대체. **본 환경 자기 정정** (commit `4be49daa`): CI Build & Test job 75178101688 (native-skia tests 실패) 본 환경 재현 — 4번째 commit `85c05bcd` 영역 영역 standalone `fn draw_form_control` → `impl SkiaLayerRenderer { ... }` method 변환 시 impl 닫힘 brace 누락 (line 1025) → impl 닫힘 brace 1줄 추가. 본 환경 cherry-pick 충돌 0건 (auto-merge 정합). 결정적 검증: `cargo build --release --features native-skia` ✅ 통과, **`cargo test --release --features native-skia --lib skia` ✅ 24/24 PASS**, `rhwp export-png samples/form-01.hwp` ✅ form-01.png 13353 bytes 생성. **작업지시자 SVG 시각 판정** ✅ 통과. **PR supersede 체인 — Issue #536 단계적 진전 권위 사례 강화**: PR #599 (P4 PNG raster, @seo-rii) → PR #626 (P5 equation replay, @seo-rii) → PR #720 (P6 raw SVG replay, @seo-rii) → **PR #740 (P7 form control replay, @oksure)** 영역 영역 컨트리뷰터 전환 + 단계적 진전. 검토 보고서: 영역 영역 (review 영역 영역 미작성, 자기 정정 우선 진행). 처리 보고서: `mydocs/pr/archives/pr_740_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_pr_supersede_chain` 정합** — Issue #536 단계적 진전 권위 사례 강화 (P4 → P7 컨트리뷰터 영역 영역 변환 + 단계적 진전). **`feedback_image_renderer_paths_separate` 정합** — native Skia 경로 영역 영역 만 변경 (WASM/CanvasKit 무영향). **`feedback_visual_judgment_authority` 정합** — 작업지시자 SVG 시각 판정 통과. |
| **PR #739 (closes #731)** | Task #731 — 수식 신규 입력 (insertEquation WASM API + 입력 메뉴 항목) | **완료 (옵션 A — 2 commits cherry-pick + 자기 정정 + no-ff merge `d0a75656`, 작업지시자 메뉴 클릭 경로 검증 통과 + Issue #766/#767 별 후속)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 영역 영역 8번째 PR — PR #728/#729/#730/#734/#735/#737/#738 후속). 본질: rhwp-studio 영역 영역 메뉴바 입력 영역 영역 수식 신규 입력 미구현 (Issue #731) — 본문 캐럿 위치 영역 영역 빈 수식 객체 영역 영역 삽입 + 수식 편집 대화상자 (PR #738 EquationEditorDialog) 자동 진입 정정. 정정 (5 files, +152/-0): `src/document_core/commands/object_ops.rs` (+94) `insert_equation_native()` (insertFootnote 패턴 정합 — controls.insert + char_offsets gap 8 + reflow + paginate, treat_as_char: true `project_equation_always_tac` 정합) / `src/wasm_api.rs` (+22) WASM 바인딩 `insertEquation` / `rhwp-studio/src/core/wasm-bridge.ts` (+5) TS 바인딩 / `rhwp-studio/src/command/commands/insert.ts` (+30) `insert:equation` 커맨드 (빈 수식 삽입 후 EquationEditorDialog 자동 open + 표 셀 내부 실행 차단 이중 가드) / `rhwp-studio/index.html` (+1) 입력 메뉴 영역 "수식 (Ctrl+N,M)" 항목. Copilot 리뷰 반영 (commit `e93f60dc`): 셀 내부 실행 차단 + CTRL_EQUATION 상수 + control_mask `1<<11` 비트. **본 환경 자기 정정** (commit `76f9ade6`): EditorContext 영역 영역 `inTableCellEditing` 속성 부재 영역 영역 본 환경 영역 영역 정의 영역 영역 `inTable` 영역 영역 대체 (tsc TS2339 정정). 본 환경 cherry-pick 충돌 0건. 결정적 검증: cargo build/test --release ✅ ALL GREEN, tsc --noEmit ✅ 통과 (자기 정정 후), **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅, WASM 빌드 4.65 MB. **WASM 빌드 영역 영역 caching 한계 학습**: 1차 WASM 빌드 영역 영역 `insertEquation` API 노출 부재 → `cargo clean --target wasm32-unknown-unknown` + Docker 강제 재빌드 영역 영역 정합 (`feedback_self_verification_not_hancom` 영역 영역 별 사례 — 자기 검증 통과 영역 영역 영역 WASM 빌드 cache 영역 영역 노출 부재). **작업지시자 웹 검증** ✅ 메뉴 클릭 경로 정합 동작 + 두 별 결함 발견 영역 영역 별 후속 처리: ⓐ **Issue #766** — 수식 객체 backspace 삭제 시 "지정된 컨트롤이 Shape이 아닙니다" 오류 (input-handler-keyboard.ts picture-object selection ref dispatch 영역 영역 Equation 분기 부재) ⓑ **Issue #767** — Ctrl+N+M 단축키 영역 영역 chordMapN 영역 영역 'm' 매핑 부재 → 브라우저 새 창 발생. **처리 결정** (`feedback_pr_supersede_chain` (c) 패턴 정합): PR #739 머지 유지 (insertEquation API + 메뉴 항목 + 대화상자 자동 진입 본질 보존) + Issue #766/#767 영역 별 후속 PR 영역 영역 통합 처리. 검토 보고서: `mydocs/pr/archives/pr_739_review.md`. 처리 보고서: `mydocs/pr/archives/pr_739_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_pr_supersede_chain` (c) 패턴 정합** — PR #723 → PR #732 동일 패턴 (머지 + 별 후속 PR 영역 영역 정정 통합). **`feedback_image_renderer_paths_separate` 정합** — TypeScript + Rust 격리 (sweep 170/170 same 입증). **`feedback_process_must_follow` 정합** — 인프라 재사용 (insertFootnote 패턴 + EquationEditorDialog). **`project_equation_always_tac` 정합** — treat_as_char: true. **WASM 빌드 영역 영역 caching 한계** — 향후 영역 영역 wasm_api.rs 변경 시 영역 영역 강제 재빌드 권장. |
| **PR #738 (closes #144)** | Task #144 — 수식 편집 UI 개선 (듀얼 모드, 자동완성, 탭 템플릿, 기호 검색) | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `11cb5424`, 작업지시자 시각 판정 통과, PR #765 supersede (a) 패턴)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 영역 영역 7번째 PR — PR #728/#729/#730/#734/#735/#737 후속). 본질: 수식 편집 대화상자 (`equation-editor-dialog.ts`) 영역 영역 UX 전면 개선 — Rust 수식 파서 영역 영역 PR #729 (closes #143) 영역 영역 양 구문 처리 영역 영역 활용 영역 영역 백엔드 변경 부재. 정정 (2 files, +601/-98): `rhwp-studio/src/ui/equation-editor-dialog.ts` (+447/-91) HWP ↔ LaTeX 듀얼 입력 모드 (`InputMode = 'hwp' | 'latex'` + setMode/toggleMode + 타이틀바 모드 토글 버튼 + 기존 스크립트 영역 영역 `\명령어` 존재 시 LaTeX 모드 자동 시작) + 탭 기반 130+ 템플릿 (7 탭: 구조/그리스/연산자/화살표/함수/장식/특수, `symbols.rs` 명령어 체계 정합) + 명령어 자동완성 드롭다운 (2자 이상 입력 활성화, ↑↓ 선택 / Tab+Enter 확정 / Esc 닫기, 기호+이름+그룹 동시 표시) + 기호 검색 (이름/유니코드 → 커서 위치 삽입) + LaTeX 전환 힌트 (HWP 모드 영역 영역 `\` + 영문자 감지 → 안내 배너) / `rhwp-studio/src/styles/dialogs.css` (+154/-7) 신규 UI 요소 영역 CSS. Copilot 리뷰 반영 (commit `f4636679`): LaTeX 모드 자동완성 정정 — 모드별 insert 분리 + 빈 name 방지 + 백슬래시 중복 제거. 본 환경 cherry-pick 충돌 0건. 결정적 검증: tsc --noEmit ✅ 통과, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (TypeScript 변경 영역 영역 SVG 무영향 입증), WASM 빌드 4.66 MB. **작업지시자 시각 판정** ✅ 통과 — dev server 영역 영역 5개 영역 (모드 토글 + 자동완성 + 탭 템플릿 + 기호 검색 + LaTeX 힌트) 모두 정합. **PR supersede 체인 (a) 패턴 권위 사례**: PR #738 영역 영역 PR #765 (closes #763, 5/9 19:35 등록, 동일 컨트리뷰터, hwpeq/LaTeX 모드 토글 + 30개 템플릿 + Ctrl+M 작은 PR) 영역 영역 의 본질 (UI 모드 토글) 영역 영역 완전 포함 영역 영역 → close+통합 머지 ((a) 패턴 정합 — PR #649 → #650 동일 패턴). PR #765 close + Issue #763 close 후속 처리. 검토 보고서: `mydocs/pr/archives/pr_738_review.md`. 처리 보고서: `mydocs/pr/archives/pr_738_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_pr_supersede_chain` (a) 패턴 권위 사례** — PR #738 영역 영역 PR #765 supersede (close+통합 머지). **`feedback_image_renderer_paths_separate` 정합** — rhwp-studio TypeScript 영역 영역 격리 (Rust 렌더링 경로 무영향, sweep 170/170 same 입증). **`feedback_process_must_follow` 정합** — 백엔드 변경 부재 영역 영역 위험 좁힘 + Rust 수식 파서 영역 영역 양 구문 처리 영역 영역 의 활용 (인프라 재사용). **`feedback_visual_judgment_authority` 정합** — 작업지시자 dev server 인터랙션 검증 ✅ 통과 영역 영역 시각 판정 권위. |
| **PR #737 (closes #615)** | Task #615 — pua_oldhangul U+F53A 매핑 제거 (hwpspec '매핑 표 외' 정합) | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `6ea6f851`, 작업지시자 SVG 시각 판정 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 영역 영역 6번째 PR — PR #728/#729/#730/#734/#735 후속). 본질: PR #600 (closes #513, @oksure) Supplementary PUA-A 시각 판정 중 발견된 별개 결함 영역 영역 별 PR 분리 후속 — `samples/pua-test.hwp` 영역 영역 U+F53A 처리 영역 영역 한컴 정답지 (hwpspec "Basic-out, 매핑 표 외", 빈 공백) 영역 정합 부재 영역 영역 본 환경 영역 영역 임의 매핑 (자모 시퀀스 ᄒᆞᆫ, [\\u{1112}, \\u{119E}, \\u{11AB}]) 영역 영역 옛한글 자모 글리프 미렌더 영역 영역 박스 표시. 정정 (1 file, +24/-4): `src/renderer/pua_oldhangul.rs` U+F53A 매핑 항목 제거 (line 5022) + 매핑 수 5660 → 5659 동기화 (헤더 doc + `test_map_size`) + 자동 생성 안전성 코멘트 (`scripts/gen_pua_oldhangul_rs.py` 재생성 시 주의) + **회귀 가드 신규** `test_hwpspec_unmapped_codepoints_not_in_table` (재삽입 차단). Copilot 리뷰 반영 (commit `efe1867e`): 헤더 매핑 수 5660→5659 + 재생성 안전성 코멘트. 본 환경 cherry-pick 충돌 0건. 결정적 검증: cargo build/test --release ✅ ALL GREEN, **pua tests 13 PASS** (신규 1건 + 기존 12건, map_size 5659), **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (U+F53A 영역 영역 7 fixture 영역 영역 사용 부재 영역 영역 영향 부재 입증), WASM 빌드 4.66 MB. **작업지시자 SVG 시각 판정** ✅ 통과 — `samples/pua-test.hwp` 영역 영역 빈 공백 정합 (한컴 PDF 정답지 영역 영역 정합). 단일 코드포인트 영역 영역 case 가드 (`feedback_hancom_compat_specific_over_general` 정합) — 다른 PUA 매핑 5658개 보존 + HWP3 한자 영역 영역 (`johab_map.rs:4969` U+F53A → 慽) 영역 영역 별 포맷 영역 영역 무관. 검토 보고서: `mydocs/pr/archives/pr_737_review.md`. 처리 보고서: `mydocs/pr/archives/pr_737_report.md`. **`feedback_pr_supersede_chain` 정합** — 별 패턴 (PR #600 시각 판정 중 발견된 별 결함 영역 영역 별 PR 분리 후속, 동일 컨트리뷰터). **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — pua_oldhangul.rs 영역 영역 격리 (다른 layout/render 경로 무영향). **`feedback_visual_judgment_authority` 정합** — 한컴 PDF 정답지 영역 영역 정합 권위 + 작업지시자 SVG 시각 판정 ✅ 통과. **`feedback_process_must_follow` 정합** — 자동 생성 영역 영역 안전성 코멘트 + 회귀 가드 영역 영역 재삽입 차단 영역 영역 위험 좁힘. |
| **PR #735 (closes #613)** | Task #613 — export-png VLM 프리셋 확장 (GPT-4V / Gemini / Qwen-VL / LLaVA) | **완료 (옵션 A — 2 commits cherry-pick + 수동 충돌 해결 + no-ff merge `56fe3a74`, 시각 판정 면제 + 통합 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 영역 영역 5번째 PR — PR #728/#729/#730/#734 후속). 본질: PR #599 (Task #588 후속) 영역 export-png CLI 영역 영역 VLM 친화 프리셋 영역 영역 첫 단계 영역 영역 Claude Vision 만 구현 영역 영역 다른 VLM provider 프리셋 미지원. 정정 (2 files, +71/-8): `src/document_core/queries/rendering.rs` (+61/-3) `VlmTarget` enum 1 → 6 variants 확장 (Claude / Gpt4vLow / Gpt4vHigh / Gemini / QwenVl / Llava) + `constraints()` 메서드 각 provider 한도 + `from_str()` 하이픈/밑줄 정규화 + 축약 별칭 (gpt4v / qwen) + `all_names()` 헬퍼 + 신규 unit tests 2건 (`vlm_target_from_str_all_variants` 12 케이스 + `vlm_target_constraints_are_sane` 6 variants sanity check) / `src/main.rs` (+10/-5) CLI 도움말 영역 6종 프리셋 한 줄 설명 + 에러 메시지 영역 `VlmTarget::all_names()` 동기화. 추가 프리셋: gpt4v-low (512 px / 262K), gpt4v-high (2000 px / 1.54 MP, 별칭 gpt4v), gemini (3072 px / 9.44 MP), qwen-vl (2240 px / 5.02 MP 28×28 patch, 별칭 qwen), llava (672 px / 452K OSS CLIP). Copilot 리뷰 반영 (commit `28837339`): from_str 정규화 후 dead arm 제거 + 별칭 도움말 동기화. **본 환경 cherry-pick 영역 영역 PR #734 (--dpi) 영역 영역 동일 파일 영역 영역 누적 변경 영역 영역 충돌 발생 영역 영역 → 수동 통합** (PR #734 의 `--dpi` 보존 + PR #735 의 VLM 6종 보존). 결정적 검증: cargo build/test --release ✅ ALL GREEN, VLM unit tests ✅ 2 PASS, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅. **PR #734 + PR #735 통합 검증** ✅ (`samples/통합재정통계(2010.11월).hwp` 영역 `--vlm-target gemini` 단독 / `--vlm-target gpt4v --dpi 200` 동시 — pHYs ppm 7874 = 200 DPI 정합, 두 옵션 독립 동작). opt-in (`--vlm-target` 미지정 시 동작 보존). 시각 판정 면제 합리 (CLI + enum 확장 영역 영역 픽셀 데이터 무영향). 매뉴얼 (`mydocs/manual/export_png_command.md`) 영역 VLM 프리셋 표 + 비목표 영역 영역 갱신. 검토 보고서: `mydocs/pr/archives/pr_735_review.md`. 처리 보고서: `mydocs/pr/archives/pr_735_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — native CLI / SkiaLayerRenderer 영역 영역 만 변경 (WASM/CanvasKit 무영향). **`feedback_process_must_follow` 정합** — opt-in 영역 영역 위험 좁힘 + 외부 의존성 부재. **`feedback_visual_judgment_authority` 정합** — CLI 옵션 + enum 확장 영역 영역 시각 출력 무영향 영역 영역 시각 판정 면제 + 통합 검증 ✅. |
| **PR #734 (closes #614)** | Task #614 — export-png --dpi 옵션 PNG pHYs chunk 메타데이터 | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `d76e86fc`, 시각 판정 면제 + 작업지시자 실 sample 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 영역 영역 4번째 PR — PR #728/#729/#730 후속). 본질: PR #599 (Task #588 후속) 영역 export-png CLI 영역 영역 PNG raster 출력 지원 영역 영역 본 단계 영역 영역 옵션 영역 영역 VLM 입력용 (scale / max-dimension) 영역 한정 영역 영역 인쇄 워크플로우 영역 DPI 메타데이터 지정 미지원. 정정 (2 files, +164/-5): `src/main.rs` (+21/-1) CLI `--dpi <값>` 옵션 추가 (양수 실수 검증) + 도움말 갱신 + `PngExportOptions::dpi: Option<f64>` 필드 / `src/document_core/queries/rendering.rs` (+143/-4) scale 결정 우선순위 갱신 (`--dpi` 만 지정 시 `scale = dpi/96.0` 자동) + `inject_png_phys` 후처리 함수 (PNG IHDR 직후 + 첫 IDAT 직전 영역 pHYs chunk 삽입, PNG spec §11.3.5.3 정합) + CRC32 자체 구현 (외부 의존성 없음) + 신규 unit tests 2건 (`inject_png_phys_inserts_after_ihdr` + `png_crc32_known_value`). Copilot 리뷰 반영 (commit `9c3a6257`): PNG 시그니처 8바이트 검증 + IHDR chunk 타입/data length 검증 + `checked_add` 오버플로우 방지. **opt-in** — `--dpi` 미지정 시 기존 동작 100% 보존. 본 환경 cherry-pick 충돌 0건. 결정적 검증: cargo build/test --release ✅ ALL GREEN, 신규 unit tests ✅ 2 PASS, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅. **작업지시자 실 sample 검증** ✅ 통과 — `samples/통합재정통계(2010.11월).hwp` 영역 영역 4 패턴 (baseline / `--dpi 96` / `--dpi 300` / `--scale 2 --dpi 150`) 영역 pHYs chunk byte 영역 영역 검증: PPM 영역 영역 정확 변환 (3780/11811/5906 ppm = 96/300/150 DPI ✓) + IHDR 폭 영역 영역 auto-scale 정합 (794/2481/1588 px = 1.0/3.125/2.0 scale ✓) + opt-in 영역 영역 baseline pHYs 부재 ✓ + pHYs chunk 영역 영역 21 bytes 추가 (4+4+9+4 정합). 시각 판정 면제 합리 (PNG 메타데이터 영역 영역 픽셀 데이터 무영향 + SVG 시각 출력 무변경 + 결정적 검증 통과). 검토 보고서: `mydocs/pr/archives/pr_734_review.md`. 처리 보고서: `mydocs/pr/archives/pr_734_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 정확 표현. **`feedback_image_renderer_paths_separate` 정합** — native CLI / SkiaLayerRenderer 영역 영역 만 변경 (WASM/CanvasKit 무영향). **`feedback_process_must_follow` 정합** — opt-in 영역 영역 위험 좁힘 + 외부 의존성 부재 (CRC32 자체 구현). **`feedback_visual_judgment_authority` 정합** — PNG 메타데이터 영역 영역 (픽셀 데이터 무영향) 시각 판정 면제 + 실 sample 검증 ✅. |
| **PR #732 (closes #724)** | Task #724 — hwp3-sample5-hwp5.hwp paragraph 441 wrap zone + HWP3 파서 IR 정합 + exam_science 회귀 정정 | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `0e419fb8`, 작업지시자 시각 판정 통과, PR #723 (c) 패턴 권위 사례)** | 컨트리뷰터 @jangster77 (Taesup Jang) — **17번째 사이클** (HWP 3.0 파서 영역 핵심, PR #723 16번째 후속). 본질: HWP5 변환본 (한컴 내부 변환, `samples/hwp3-sample5-hwp5.hwp`) 페이지 16 paragraph 441 ("하드디스크는 하나 이상의 둥그런 플래터(platter)로 구성되고...") image 우측 wrap zone 영역 영역 layout 부재 영역. **작업지시자 핵심 지적**: "HWP3 파서가 잘못해석해서 IR 로 잘못 전달, composer.rs 에서 처리하면 너무 예외 — HWP3 파서 ↔ Document IR 표준 정합 영역 본질". CLAUDE.md HWP3 파서 규칙 정합 (composer/layout 미수정). 정정 영역 영역 두 part: ⓐ Task #724 본질 (commit `86eee508`): typeset.rs (+49) anchor host cs=0 caption-style 매칭 가드 (`anchor_image_match`) + Task #321 vpos-reset 가드 영역 wrap_around 강제 종료 (anchor cs=0 한정) / parser/hwp3/mod.rs (+37/-5) 빈 paragraph + page break flag 가드 + wrap_zone 영역 끝 sw=col_area_width 정합화 + wrap_zone 비활성 cs=0/sw=0 정합화 ⓑ PR #732 후속 (commit `25299a5b`) — **PR #723 영역 영역 host_self register 영역 영역 exam_science 회귀 정정**: typeset.rs (+9/-1) `image_margin_right_hu > 0` 가드 추가. 컨트리뷰터 자발 영역 영역 (c) 패턴 통합 영역 영역 — 작업지시자 결정 영역 영역 + 컨트리뷰터 후속 영역 영역. 본 환경 cherry-pick: 2 commits (a8b8effa + ab2fa527) — `ab910d65` (Task #722) 영역 영역 PR #723 머지 영역 영역 skip. 충돌 0건. 결정적 검증: cargo build/test --release ✅ ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / exam_science 2 diff (회귀 정정 입증)** — text 좌표 시프트 영역 영역 정상 복원: "후" 585.34 → 583.18 / "시" 608.23 → 603.93 / "간" 620.80 → 616.50 / 그림 655.87 → 649.87, WASM 빌드 4.65 MB. **작업지시자 시각 판정** ✅ 통과 — HWP3 sample5 페이지 8/27/48 (PR #723 정합 보존) + HWP5 변환본 페이지 16/22 (Task #724 본질) + exam_science p1/p2 (회귀 정정 입증). IR 정합 검증 (HWP3 native vs HWP5 변환본): 171 column_type Page → Normal / 189/191/443 ls sw 0 → 51024 / 435 ls vpos 221760 → 1440 (HWP5 변환본 정합 ✓). 검토 보고서: `mydocs/pr/archives/pr_732_review.md`. 처리 보고서: `mydocs/pr/archives/pr_732_report.md`. **`feedback_pr_supersede_chain` (c) 패턴 권위 사례** — PR #723 (Task #722, 16번째) 머지 유지 + PR #732 (Task #724, 17번째) 영역 영역 회귀 정정 통합 영역 영역 컨트리뷰터 자발 영역 영역 정합. **`feedback_contributor_cycle_check` 정합** — @jangster77 17번째 사이클 정확 표현. **`feedback_hancom_compat_specific_over_general` 정합** — `image_margin_right_hu > 0` 가드 영역 영역 case 가드 좁힘 (일반화 host_self register 영역 영역 회귀 본질). **`feedback_visual_judgment_authority` 정합** — 작업지시자 시각 판정 게이트 ✅ 통과 영역 영역 권위. **`feedback_image_renderer_paths_separate` 정합** — typeset.rs / parser/hwp3 영역 영역 격리 (다른 layout/render 경로 무영향). |
| **PR #730 (closes #172)** | Task #172 — HWPX section.xml 컨트롤 디스패처 표/그림/도형/각주 직렬화 | **완료 (옵션 A — PR HEAD squash cherry-pick + no-ff merge `47a303f2`, 라운드트립 테스트 17 PASS 게이트 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 영역 영역 3번째 PR — PR #728/#729 후속). 본질: Task #164 (PR #170) 영역 영역 텍스트 직렬화 완료 영역 영역 그러나 `Paragraph.controls` IR (표/이미지/도형 등) 직렬화 부재 영역. `section.rs::render_run_content/render_control_slot` 영역 영역 디스패처 확장 — Equation-only 게이트 영역 → `slots.is_empty()` 영역 영역 Table/Picture/Shape/Footnote/Endnote 모두 직렬화. Issue #172 체크리스트 6 항목 모두 커버: 표 (`Control::Table` → `<hp:tbl>`) / 그림 (`Control::Picture` → `<hp:pic>` + BinData) / 도형 (`Control::Shape` 변형별 공통 속성) / 각주/미주 (`<hp:ctrl><hp:footNote/endNote>` + `<hp:subList>` 재귀 문단) / BinData ZIP 엔트리 (mod.rs 기존 활용) / content.hpf manifest 자동 등록 (mod.rs 기존 활용). 정정 (5 files, +619/-37): `section.rs` (+159/-33) 디스패처 확장 + 각주/미주 + Shape 디스패치 + drawText / `mod.rs` (+210/-2) 라운드트립 테스트 4건 신규 / `shape.rs` (+122/-2) drawText 글상자 + Rectangle Writer-based / `context.rs` (+19) 인라인 Table borderFillIDRef 1-pass 사전 등록 / `tests/hwpx_roundtrip_integration.rs` (+109) 4건 신규 (stage5_table_control / stage5_picture_bindata / stage5_footnote_endnote / stage5_tac_img_sample). Copilot 리뷰 반영 (commit `328b00e5`): 에러 로깅 + Cursor 제거 + ShapeObject::Picture 위임. 본 환경 cherry-pick: **PR HEAD squash cherry-pick** 채택 — 5 commits (7a544375/99c8ac6c/328b00e5/815759b2/ec0b509e) 영역 영역 개별 cherry-pick 영역 영역 commits 영역 영역 누적 변경 영역 영역 충돌 발생 영역 영역 PR HEAD squash cherry-pick (`3d2a8eeb`) 영역 영역 단일 commit 영역 영역 적용 (충돌 0건, PR #729 동일 패턴). 결정적 검증: cargo build/test --release ✅ ALL GREEN, **hwpx_roundtrip_integration 17 PASS** (신규 4건 + 기존 13건), **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅, WASM 빌드 4.61 MB. **판정 게이트** (작업지시자 결정): 라운드트립 테스트 17 PASS 영역 영역 채택 — 자기 라운드트립 결정적 입증 + 광범위 sweep 0 회귀 + 직렬화 영역 영역 만 변경 영역 영역 시각 출력 무관 영역 영역 면제 합리. **한컴 호환 검증 후속 작업** — 작업지시자 한컴2020/한컴2022 영역 영역 별건 진행 가능 (`feedback_self_verification_not_hancom` 영역 정합, `picture_save_hancom_compatibility.md` / `task178_first/second_attempt` 영역 영역 사전 검색 영역 영역 본 PR 영역 영역 (HWPX→HWPX) 영역 영역 다른 영역 영역 영역). 검토 보고서: `mydocs/pr/archives/pr_730_review.md`. 처리 보고서: `mydocs/pr/archives/pr_730_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 (5/10 사이클 영역 영역 PR #728/#729/#730 영역 3건 처리). **`feedback_self_verification_not_hancom` 정합** — 자기 라운드트립 영역 영역 게이트 채택 영역 영역 한컴 검증 영역 영역 후속 별건. **`feedback_search_troubleshootings_first` 정합** — 사전 검색 영역 영역 한컴 호환 사례 영역 영역 점검 영역 본 PR 영역 영역 별건. **`feedback_image_renderer_paths_separate` 정합** — hwpx 직렬화 영역 영역 격리 (sweep 170/170 same 영역 영역 입증). **`feedback_process_must_follow` 정합** — 인프라 재사용 (BinData ZIP / content.hpf manifest 기존 활용) 영역 영역 위험 좁힘. |
| **PR #729 (closes #143)** | Task #143 — rhwp 영역 LaTeX 명령어 호환 확장 (2차) | **완료 (옵션 A — PR HEAD squash cherry-pick + no-ff merge `eb3f9fd4`, 작업지시자 시각 검증 + exam_math 회귀 영역 PR #762 통합 후속)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/10 사이클 영역 영역 2번째 PR — PR #728 직후 영역). 본질: 기존 hwpeq 토크나이저/파서 영역 영역 LaTeX 호환 명령어 대폭 확장 (메인테이너 권고 영역 영역 듀얼 토크나이저 → 기존 파서 확장 채택, 신규 인프라 도입 부재). 정정 (6 files, +735/-11): `symbols.rs` (+87/-5) FontStyleKind 5개 추가 (Blackboard/Calligraphy/Fraktur/SansSerif/Monospace) + 기호 별칭 ~80개 (관계/논리/화살표/연산자/큰 연산자/기호/함수/장식/괄호) / `parser.rs` (+599/-3) 구조 명령어 (\\frac/\\dfrac/\\tfrac/\\text/\\operatorname/\\binom/\\overset/\\underset/\\stackrel/\\phantom) + 환경 파싱 (pmatrix/bmatrix/cases/aligned/array/smallmatrix/split) + LaTeX 호환 테스트 41건 신규 / `tokenizer.rs` (+40) \\\\, \\, \\: \\; \\!, \\{ \\} \\| \\# 영역 LaTeX 이스케이프 토큰화 / `canvas/svg/skia` (+9/-3) 3 렌더러 동기 정정 (FontStyleKind 5개 영역 exhaustive match). 본 환경 cherry-pick: **PR HEAD squash cherry-pick** 채택 — 7 commits (04187512/d672f954/45fdb335/0264fe18/69b0b84d/d96ddcc4/d53b7acf) 영역 영역 개별 cherry-pick 영역 영역 commits 영역 영역 누적 변경 영역 영역 충돌 발생 영역 영역 영역 PR HEAD squash cherry-pick (`f65ffd03`) 영역 영역 단일 commit 영역 영역 적용 (충돌 0건). 결정적 검증: cargo build/test --release ✅ ALL GREEN, equation 모듈 **114 PASS** (43 → 84 신규 41건), **광범위 sweep 7 fixture / 170 페이지 / 168 same / 2 diff** (exam_math_014/016.svg) ⚠️, WASM 빌드 4.61 MB. **작업지시자 시각 검증**: ⓐ LaTeX 입력 (`samples/equation-lim.hwp` 예제 영역 LaTeX 형식 수식 입력 테스트) ✅ 통과 ⓑ exam_math p14/16 회귀 (`inf` ∞ 기호 → "inf" 텍스트) ⚠️ 확인. **회귀 본질**: PR 영역 `FUNCTIONS` HashMap 영역 `("inf","inf"), ("sup","sup"), ("lim","lim"), ...` 추가 영역 영역 → `is_function("inf") = true` 영역 영역 lookup 우선 매칭 → 기존 hwpeq script 영역 `inf` 영역 ∞ 기호 영역 영역 영역 의미 영역 영역 충돌. `test_hwpeq_not_regressed` 영역 영역 6 케이스 영역 영역 본 케이스 미커버. **처리 결정** (`feedback_pr_supersede_chain` 영역 (c) 패턴 정합): PR #729 머지 유지 (LaTeX 본질 확장 영역 보존) + exam_math 회귀 정정 영역 영역 **Issue #762** (신규 등록) 영역 영역 후속 PR 영역 영역 통합 처리 영역 — @oksure 후속 PR 영역 영역 통합 권장. 후속 분리: UI 명시적 모드 토글 (rhwp-studio 프론트엔드 범위) 영역 별건 이슈 영역. 검토 보고서: `mydocs/pr/archives/pr_729_review.md`. 처리 보고서: `mydocs/pr/archives/pr_729_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클 (5/10 사이클 영역 영역 PR #728 + #729 영역 2건 영역 처리). **`feedback_image_renderer_paths_separate` 권위 사례 강화** — 3 렌더러 (canvas/svg/skia) 동기 정정 + Skia renderer 영역 누락 정정 영역 fix commit `69b0b84d` 영역 영역 자기 진단. **`feedback_process_must_follow` 정합** — 메인테이너 권고 채택 (듀얼 토크나이저 → 기존 파서 확장) + 후속 분리 (UI 모드 토글 + Issue #762) 명시. **`feedback_pr_supersede_chain` (c) 패턴 적용** — PR #723 → #732 패턴 영역 영역 동일 적용 영역 — 머지 유지 + 회귀 정정 영역 후속 PR 통합. **`feedback_hancom_compat_specific_over_general` 정합** — hwpeq 영역 영역 영역 LaTeX 영역 영역 영역 키워드 충돌 영역 영역 일반화 lookup 영역 영역 case 가드 영역 영역 정합 (Issue #762 영역 영역 후속 정정 방향). |

## 메인테이너 본인 영역 — 5/10

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #781 (closes #779)** | Task #779 — rhwp-studio 스크롤바 드래그 후 마우스 release 시 이전 페이지 자동 복귀 정정 | **완료 (옵션 A — 3 commits cherry-pick + no-ff merge `6c125000`, 작업지시자 웹 에디터 시각 판정 통과)** | 컨트리뷰터 @jangster77 (Taesup Jang) — **HWP3 + rhwp-studio editor 핵심 컨트리뷰터** (16+ 사이클). PR #732 (Task #724) 머지 후속 영역. **본질**: `updateCaret()` ([input-handler.ts:1553](rhwp-studio/src/engine/input-handler.ts#L1553)) 영역 의 `scrollCaretIntoView()` 호출 영역 이 cursor 변경 trigger 없는 mouseup (drag-during-scroll 패턴) 영역 의 caret 원본 위치 자동 복귀 결함 발동. **재현 시나리오**: (1) 다중 페이지 문서 (예: hwp3-sample10.hwp 763 페이지) 로드 (2) 텍스트 클릭 (caret p.1) (3) 마우스 보유 상태 로 scrollbar 까지 drag → page 2+ scroll (4) mouse release → caret 원본 위치 (p.1) 로 자동 scroll back. **Stage 1** (`cead11f`): 본질 진단 — scrollTop 변경 4 영역 (viewport-manager / canvas-view / drag autoscroll PR #718 / scrollCaretIntoView) 중 scrollCaretIntoView 영역 단일 trigger 확정. updateCaret() 호출 30+ 곳 영역 분류 (키보드 영역 20+ / 마우스 영역 9 / programmatic 5+). 가설 A (단순 scrollbar) / B (drag-during-scroll) / C (다른 trigger) 도출. **Stage 2** (`2e6a832`): updateCaret(skipScroll: boolean = false) 시그니처 확장 + onMouseUp 영역 `updateCaret(true)` 변경. **opt-in skip** 영역 좁힘 — 30+ 기존 호출 영역 무영향 (`feedback_hancom_compat_specific_over_general` 정합). 키보드/programmatic/onMouseDown cursor placement / 드래그 selection autoscroll (PR #718, 별도 path) 모두 보존. **Stage 3**: 결정적 검증 (tsc clean / npm build 성공 / cargo test 1217 passed / clippy 0) + 작업지시자 시각 판정 ★ 통과 ("해결 완료") — 시나리오 1~5 (본 결함 해소 + cursor click 정상 + 키보드 정상 + 드래그 selection autoscroll 보존 + wheel scroll 정상). PR #718 (Task #661, 드래그 selection autoscroll) 의 후속 영역 — 동일 patten (`scrollCaretIntoView` 영역) 의 다른 trigger (drag-during-scroll) 영역 정정. **`feedback_pr_supersede_chain` 권위 사례** — PR #718 → Task #779 동일 컴포넌트 영역 의 후속 영역 정정. **`feedback_visual_judgment_authority` 권위 사례** — 작업지시자 dev 서버 직접 시각 판정 으로 본 결함 해소 + 회귀 부재 양측 confirm. 최종 결과 보고서: `mydocs/report/task_m100_779_report.md`. 단계별 보고서 3개 (stage1 / stage2 / stage3). |

## 작업 메모

### 5/9 사이클 종료 + 5/10 사이클 시작

5/9 사이클 영역 의 12 PR 처리 완료 (PR #694/#693/#695/#699/#706/#707/#710/#711/#714/#715/#718/#719). 5/10 사이클 영역 의 PR #720 (Skia 영역 P6) 으로 시작.

### 본 사이클 (5/10) 패턴

- PR #720 (refs #536) — 1 commit cherry-pick + no-ff merge `70121b1b` (옵션 A). native Skia PNG 영역 의 RawSvg fragment 영역 실제 raster (resvg + tiny-skia). 보안 가드 ✅ (4 MB / 67M / external href 차단). 회귀 가드 2건 신규 + 본 환경 직접 측정 24/24 PASS. **시각 판정 면제** (결정적 검증 + 회귀 가드 + 보안 가드 통과). @seo-rii 7번째 사이클 (Skia 영역 핵심). `feedback_pr_supersede_chain` 권위 사례 강화 — PR #599/#626/#720 단계적 진전 영역.
- PR #723 (closes #722) — 1 commit cherry-pick + no-ff merge `6ced74b0` (옵션 A). HWP3 변환본 영역 hwp3-sample5.hwp 영역 wrap=Square 그림 paragraph 영역 시각 정합 (페이지 8/27/48). typeset.rs anchor host self-register + caption_room 가드 + paragraph_layout.rs cs/sw inter-image-text gap (3mm) 보정. WASM 빌드 4.60 MB. **시각 판정 (작업지시자)**: ⓐ 본질 (페이지 8/27/48 PDF 정합) ✅ ⓑ exam_science p1/p2/p8 5번/8번/12번 문항 지문 왼쪽 경계 클립핑 ⚠️ 회귀 판정. **처리 결정**: 머지 유지 + exam_science 회귀 정정 영역 PR #732 (Task #724, 동일 컨트리뷰터 @jangster77) 영역 후속 통합. @jangster77 16번째 사이클. `feedback_pr_supersede_chain` **신규 패턴 (c)** 등록 — 머지 + 회귀 정정 영역 후속 PR 통합 영역 영역.
- PR #725 (closes #717) — 1 commit cherry-pick + no-ff merge `bbf01424` (옵션 A). rhwp-studio editor 영역 표 셀 빈 영역 hitTest 컨텍스트 이탈 정정. cursor_rect.rs 영역 `table_id` 영역 격리 + 중첩 표 cellPath 보존. 회귀 가드 3건 신규. 광범위 sweep 7 fixture / 170 페이지 / **회귀 0** ✅. **시각 판정 면제** (query API + 시각 출력 무영향 + 회귀 가드 통과). @postmelee 15+ 사이클 (rhwp-studio editor 영역 핵심). PR 본문 영역 영역 before/after 영상 첨부.
- PR #728 (Issue #204) — 2 commits cherry-pick + no-ff merge `8db18f58` (옵션 A). rhwp-studio 영역 표 편집 Undo/Redo 스냅샷 기반 이력 등록 — 11 표 커맨드 영역 영역 `executeOperation({ kind: 'snapshot' })` 패턴 일관 적용. 인프라 재사용 (B안) — 신규 도입 부재 영역 영역 위험 좁힘. tsc/test/sweep 통과 + WASM 빌드 4.60 MB + **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과. @oksure **20+ 사이클** (rhwp-studio + WASM API + 디버깅 툴킷 영역 핵심). 후속 분리: HwpCtrl(hwpctl) API 경로 영역 별건.

### 후속 영역

- Issue #536 OPEN 유지 (멀티 렌더러 트래킹 영역) — 후속 P7+ 영역 가능
- **Issue #732 (Task #724) OPEN** — HWP5 변환본 paragraph 441 영역 + **exam_science 회귀 정정 통합** (PR #723 영역 후속) — @jangster77 PR 등록됨
- **Issue #741 OPEN (PR #753)** — hwp3-sample10.hwp HWP3 외부 file path + ParaShape tab + 사적 char + 제목차례 장식 + 차례 page 번호 정합 — @jangster77 PR 등록됨
- Issue #726 OPEN — table-vpos-01 도형 미출력 (5/9 등록)
- Issue #727 OPEN — table-vpos-01 PUA 매핑 (5/9 등록)
- Issue #731 OPEN — rhwp-studio 수식 입력 기능 (5/9 등록)
