# 2026-06-14 오늘 할일

## PR #1401 — HWPX 셀 필드 이름 파싱 복원 (#493 부분, oksure)

- [x] PR 정보 확인 — base `devel`, head `oksure:contrib/fix-493-hwpx-cell-field-name`
- [x] 코드 검토 — `hp:tc name` 파싱과 serializer `write_cell()` 방출 계약 정합 확인
- [x] 로컬 검증 — `cargo fmt --check`, `cargo build --release`, `cargo test --release --lib`, `cargo test --profile release-test --tests`, `wasm-pack build --target web --out-dir pkg` 통과
- [x] PR 전용 회귀 테스트 — `cargo test --test issue_493_hwpx_cell_field_name` 통과
- [x] 검토 문서 `mydocs/pr/archives/pr_1401_review.md` 작성
- [x] 검토 문서/오늘할일 커밋을 PR head에 push해 PR diff에 포함
- [x] GitHub Actions 재확인 — 문서 커밋 push 후 `Build & Test`, CodeQL 확인
- [x] PR merge 준비 완료 보고
- [x] PR merge — merge commit `29310240e7f01bf9b70cf22d4c7a5cfdc6092e06`
- [x] #493은 부분 해결이므로 close하지 않음 확인 — `OPEN` 유지
- [x] PR 감사 코멘트 등록 — https://github.com/edwardkim/rhwp/pull/1401#issuecomment-4699050096

## PR #1404 — 표 생성 직후 F5 포커스 복원 (#1140 케이스 1, oksure)

- [x] PR 정보 확인 — base `devel`, head `oksure:contrib/fix-1140-table-dialog-focus`
- [x] 코드 검토 — 표 만들기/셀 나누기 dialog 닫힘 후 editor textarea focus 복원 패턴 확인
- [x] Studio 검증 — `npm test`, `npx tsc --noEmit` 통과
- [x] 로컬 검증 — `cargo fmt --check`, `cargo build --release`, `cargo test --release --lib`, `cargo test --profile release-test --tests`, `wasm-pack build --target web --out-dir pkg` 통과
- [x] 검토 문서 `mydocs/pr/archives/pr_1404_review.md` 작성
- [x] 검토 문서/오늘할일 커밋을 PR head에 push해 PR diff에 포함
- [x] GitHub Actions 재확인 — 문서 커밋 push 후 `Build & Test`, CodeQL 확인
- [x] PR merge 준비 완료 보고
- [x] PR merge — merge commit `fa1e93a5c56b37e9bafb498ab6071119d382ec83`
- [x] #1140은 케이스 1만 해결이므로 close하지 않음 확인 — `OPEN` 유지
- [x] PR 감사 코멘트 등록 — https://github.com/edwardkim/rhwp/pull/1404#issuecomment-4699140955

## PR #1405 — HWPX 직렬화 무손실 라운드트립 충실도 (first-time contributor, physwkim)

- [x] PR 정보 확인 — base `devel`, head `physwkim:fix/hwpx-lossless-roundtrip`
- [x] first-time contributor 확인 — `author_association=FIRST_TIME_CONTRIBUTOR`
- [x] 코드 검토 — HWPX parser/serializer/model 보존 경로, raw XML splice fallback, golden SVG 변경 확인
- [x] 로컬 검증 — `git diff --check upstream/devel...HEAD`, `cargo fmt --check`, `cargo build --release`, `cargo test --release --lib`, `cargo test --profile release-test --tests`, `wasm-pack build --target web --out-dir pkg` 통과
- [x] GitHub Actions 확인 — `Build & Test`, CodeQL, `Canvas visual diff` 통과
- [x] 검토 문서 `mydocs/pr/archives/pr_1405_review.md` 작성
- [x] 검토 문서/오늘할일 커밋을 PR head에 push해 PR diff에 포함
- [ ] GitHub Actions 재확인 — 문서 커밋 push 후 `Build & Test`, CodeQL 확인. 통과 반영만을 위한 추가 push는 하지 않음
- [ ] PR merge 준비 완료 보고
- [ ] PR merge — 작업지시자 승인 후 진행
- [ ] first-time contributor 감사 코멘트 등록

## PR #1410 — 공식 미주 모양 모델 정규화 (#1293, jangster77)

- [x] PR 정보 확인 — base `devel`, head `jangster77:task_m100_1293`
- [x] 코드/문서 검토 — `FootnoteShape` 공식 UI 의미 접근자, HWP5/HWPX 파서, `typeset`/`height_cursor`/`layout`, visual sweep 보강 확인
- [x] PR head 검증 기록 확인 — `cargo build --release`, `cargo test --release --lib`, `cargo test --profile release-test --tests`, `cargo fmt --check`, `wasm-pack build --target web --out-dir pkg`, full visual sweep 기록 확인
- [x] 검토 시점 재확인 — `git diff --check upstream/devel`, `cargo fmt --check` 통과
- [x] EOF blank line 정리 — `mydocs/plans/task_m100_1293.md`, `mydocs/working/task_m100_1293_stage1.md`, `mydocs/working/task_m100_1293_stage52.md`
- [x] pre-review 문서 `mydocs/pr/archives/pr_1410_review.md` 작성
- [x] pre-review 문서/오늘할일/EOF 정리 커밋을 PR head에 push해 PR diff에 포함
- [x] GitHub Actions 1차 실패 분석 — `Build & Test`의 `cargo clippy -- -D warnings` 3건 확인
- [x] clippy 보정 — `layout.rs` 중복 boolean 조건, `typeset.rs` `bind_instead_of_map`/`let_and_return` 정리 후 `cargo clippy -- -D warnings` 통과
- [ ] GitHub Actions 재확인 — 문서 커밋 push 후 `Build & Test`, CodeQL, `Canvas visual diff` 확인. 통과 반영만을 위한 추가 push는 하지 않음
- [ ] PR merge — GitHub Actions 통과 후 진행
- [ ] #1293 close 여부 확인 — `closingIssuesReferences`가 비어 있어 auto-close 실패 시 수동 확인 필요
