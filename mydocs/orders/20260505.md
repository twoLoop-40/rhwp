# 오늘 할일 - 2026년 5월 5일

## M100 — v1.0.0 조판 엔진 (PR 처리 + 인프라 정합)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #589 (PR #553 재PR)** | Task #511 v2 + Task #554: HWP3 Square wrap 보완6+8 (페이지네이션 안전) + 변환본 식별 휴리스틱 | **완료 (cherry-pick 머지 + push, PR #589/#556 close)** | 외부 컨트리뷰터 @jangster77 (Taesup Jang) — PR #553 close 후 재PR. 메인테이너 회신 3개 요청 모두 응답 — (i) page 4/8 결함 정정 (시각 판정 ★ 통과), (ii) 페이지네이션 안전 (`wrap_around_pic_bottom_px` 등 메커니즘 모두 제외, mod.rs LOC 19배 축소: PR #553 +508 → 본 PR +27), (iii) HWP 3.0 직렬화 round-trip 정합 (`wrap_precomputed` derived 필드 + serializer 미참조, 직접 검증 0 hits). **CLAUDE.md HWP3 파서 규칙 준수** — 렌더러의 `wrap_around_is_hwp3` 등 HWP3 전용 분기 모두 제거, 포맷 독립적 IR 플래그 (`Paragraph.wrap_precomputed`) 로 통합. **A2 전략** — `v_push_before` / `promoted_cps` (LineSeg 단위 직렬화 우려 영역) 모두 제외. 7 commits cherry-pick (충돌 0, author 보존): Task #460 보완6/8 (`bdb51a4`/`ff64387`) + Task #554 Stage 0~2-4 (`1ad07a4`~`3678217`). 검증: cargo test --lib **1129 passed** / issue_554 12 / issue_546 1 (Task #546 회귀 0) / issue_530/505/418/501 / svg_snapshot 6/6 / clippy 0 / build --release / Docker WASM **4,569,773 bytes** (devel baseline 4,582,545 대비 -12,772, LOC 축소 정합). HWP3 native 페이지 수 회귀 0 (64/64). **시각 판정 ★ 통과** — 작업지시자 평가: "1차 리뷰에서 시각판정에서 문제가 되었던 부분이 개선되었습니다. 정확하지도 않은 HWP 3.0 스펙으로 하나 하나 문제를 풀어가는 모습 멋지십니다!" before/after SVG 자료: `output/svg/pr589_before/hwp3-sample5/` ↔ `output/svg/pr589_after/hwp3-sample5/` (page 4/8/16/22/27 + 17/18/48 차이) + PR 신규 fixture `output/svg/pr589_after/hwp3-sample5-hwp5/` (64 페이지). devel merge `5ba7f2e` + push 완료. PR #556 (Task #554 단독, 본 PR 통합) close 처리 (이미 close 상태였음, 통합 안내 댓글 등록). 잔존 trade-off (page 27 그림+텍스트 y / HWP3 변환본 1개 -1 over-correct) 별도 후속 task 권고. 검토 보고서: `mydocs/pr/pr_589_review.md` → archives 이동 예정. 처리 보고서: `mydocs/pr/archives/pr_589_report.md`. |
| **PR #561 Task #548** | 셀 inline TAC Shape margin + indent 정정 (closes #548) | **완료 (핀셋 cherry-pick 머지 + push, PR #561/Issue #548 close)** | 외부 컨트리뷰터 @planet6897 (PR 등록) / Jaeook Ryu = @jangster77 (commit author) — stacked PR (PR #560 위, PR #560 은 이미 close + devel 적용 완료). PR mergeable=CONFLICTING 표시였으나 본질 cherry-pick (3de05051 + a0dad0d3) 충돌 0 — PR base 시점 차이 때문. **본 환경 devel 에 `test_548` 이 RED + #[ignore] 로 이미 존재** (PR #560 cherry-pick 시 들어옴) — fix 적용으로 RED → GREEN 전환 결정적 검증. 본질: `effective_margin_left_line` 헬퍼 (paragraph_layout 산식과 동일 단일 룰) + `table_layout.rs` +79 LOC + 3 분기 (paragraph 시작 / Picture target_line reset / Shape target_line reset) line_margin 가산. 페이지 8 셀 5 line 0 [푸코] inline rect: x=131.04 → **155.60** (PDF 한컴 2010 ≈155.6) 정정. fixup commit (`a0dad0d3`): contributor fork 측정 y≈685~690 → 본 devel 측정 y≈698.43 (Task #479 미적용 trailing-ls 모델), y 범위 [685,690] → [690,710] 조정. 검증: cargo test --lib --release **1130 passed** (test_548 GREEN) / issue_546 1 (Task #546 회귀 0) / issue_554 12 / svg_snapshot 6/6 / clippy 0 / build --release / Docker WASM **4,570,220 bytes** (PR #589 baseline +447 bytes — table_layout.rs +79 LOC 정합). 광범위 회귀 sweep (3 샘플 39 페이지) **10 differ** — 21_언어_기출 1/15 (page 8 권위 영역 정합) + exam_kor 7/20 + exam_science 2/4. **메인테이너 시각 판정 ★ 통과** — 작업지시자 평가: "이제 한국어, 영어 시험지는 제법 볼만하다는 느낌이 듭니다." cherry-pick author Jaeook Ryu 보존 (`bee0c77` + `309cfbf`). devel merge `84bced9` + push 완료. PR #561 close + Issue #548 close (closes #548 키워드 자동 처리). 컨트리뷰터 두 분의 협업 흐름 (@planet6897 fork `9dc40ddb` → @jangster77 핀셋 cherry-pick) 우수. 처리 보고서: `mydocs/pr/archives/pr_561_report.md`. 검토 보고서: `mydocs/pr/archives/pr_561_review.md`. |
| **PR #567 Task #565** | exam_science.hwp 12/15/18/19번 인라인 수식(Equation, treat_as_char) 미렌더 정정 (closes #565) | **완료 (핀셋 cherry-pick 머지 + push, PR #567/Issue #565 close)** | 외부 컨트리뷰터 @planet6897 (PR 등록) / Jaeook Ryu = @jangster77 (commit author). PR mergeable=CONFLICTING 표시였으나 본질 cherry-pick (a35bdbed) 충돌 0 — PR base 시점 차이 때문. **본질 결함**: `paragraph_layout::layout_inline_table_paragraph` 가 인라인 표 + 텍스트 세그먼트만 처리 (인라인 수식/treat_as_char Picture/Shape 무시) → `inline_shape_position` 미등록 → `shape_layout::layout_shape_item` fallback 으로 동일 좌표 (534.8, 1218.106) 에 9개 수식 겹침 (exam_science 12/15/18/19번 본문). **정밀 진단 (Stage 1~2)**: 동일 페이지 두 문단 비교 (0.60 그림 문단 = 정상 8개 수식 분산 / 0.61 본문 = 결함 9개 수식 겹침) 으로 결함 origin 정확히 식별. **본질 정정 (Stage 3)**: `has_inline_tables && !has_other_inline_ctrls` 케이스별 명시 가드 강화 — 인라인 표 단독 케이스는 기존 경로 유지 (회귀 0), 인라인 표 + 인라인 수식/treat_as_char Picture/Shape 동시 케이스만 일반 `layout_paragraph` 로 보냄. 단일 파일 변경 (`src/renderer/layout.rs` +13/-1). 검증: cargo test --lib --release **1130 passed** (회귀 0) / svg_snapshot 6/6 / issue_546 1 (Task #546 회귀 0) / issue_554 12 / clippy 0 / build --release / Docker WASM **4,570,464 bytes** (PR #561 baseline +244 bytes — layout.rs +13/-1 LOC 정합). 광범위 회귀 sweep (3 fixture 44 페이지) **3 differ — exam_science 002/003/004 (PR 본문 100% 재현)** + exam_kor 20/20 + exam_math 20/20 회귀 0 (케이스별 명시 가드 정합성 정량 입증). **메인테이너 시각 판정 ★ 통과** — 작업지시자 평가: "12번 인라인 수식 겹쳐지던 문제 해결. 15번/18번/19번 개선됨." 잔존 영역 (12번 문항의 인라인과 별개 조판) 은 컨트리뷰터가 추가 이슈로 인지. cherry-pick author Jaeook Ryu 보존 (`466c487`). devel merge `bbd4418` + push 완료. 처리 보고서: `mydocs/pr/archives/pr_567_report.md`. 검토 보고서: `mydocs/pr/archives/pr_567_review.md`. |
| **PR #564 Task #521** | TAC 표 outer_margin_bottom 누락 정정 (closes #521) | **완료 (핀셋 cherry-pick 머지 + push, PR #564/Issue #521 close)** | 외부 컨트리뷰터 @planet6897 (PR 등록) / Jaeook Ryu = @jangster77 (commit author). PR mergeable=CONFLICTING 표시였으나 본질 cherry-pick (04eefd99) 충돌 0 — stacked 구조 (PR #560 위) + PR base 시점 차이. **본질 결함**: `src/renderer/layout.rs::layout_table_item` TAC after-spacing 분기 (라인 2497) 가 `outer_margin_bottom` 미적용 (`layout_partial_table_item` 라인 2638-2647 와 산식 불일치). 한컴 명세 인용: `lh = cell_h + outer_margin_bottom` (exam_eng pi=104 lh=22207 = 21607+600). cell_h 만 advance → 다음 paragraph -8 px shortfall. **단일 룰 정정**: 두 분기 동일 산식으로 통일 (`feedback_rule_not_heuristic` 정합). 단일 파일 본질 (layout.rs +11/-1 + integration_tests.rs +80 회귀 테스트 `test_521_tac_table_outer_margin_bottom_p2`). 정량 측정: exam_eng p2 18번 ① **543.95 → 551.95 (+8 px PDF 한컴 2010 정합)**. 검증: cargo test --lib --release **1131 passed** (test_521 RED → GREEN) / svg_snapshot 6/6 / issue_546 1 (Task #546 회귀 0) / issue_554 12 / clippy 0 / build --release / Docker WASM **4,570,615 bytes** (PR #567 baseline +151 bytes — layout.rs +11 LOC 정합). **광범위 페이지네이션 회귀 sweep**: `samples/` 폴더 전체 **164 fixture (158 hwp + 6 hwpx) / 1,614 페이지 / 페이지 수 회귀 0** — TAC 표 outer_margin_bottom 가산 (+8 px / 표) 이 페이지 break 를 트리거하는 케이스 없음. PR 본문 "278 differ / text count 변동 0" 안전성 패턴이 광범위 sweep 으로 정량 입증. **메인테이너 시각 판정 ★ 통과** — 작업지시자 평가: "메인테이너의 광범위한 샘플 파일 페이지네이션 및 분리된 표 시각 검증 통과입니다." WASM 기반 다양한 hwp 문서 + 분리된 표 영역 (`layout_partial_table_item` 경로) 까지 검증. cherry-pick author Jaeook Ryu 보존 (`fc16acc`). devel merge `47aab48` + push 완료. PR #564 close + Issue #521 수동 close (closes #521 키워드는 merge 가 아닌 close 로 자동 처리 안 됨, 안내 댓글 등록). 처리 보고서: `mydocs/pr/archives/pr_564_report.md`. 검토 보고서: `mydocs/pr/archives/pr_564_review.md`. |
| **PR #570 Task #568** | 인라인 표(분수)+수식 단락 우측 편위 정정 (closes #568) | **완료 (핀셋 cherry-pick 머지 + push, PR #570/Issue #568 close)** | 외부 컨트리뷰터 @planet6897 (PR 등록) / Jaeook Ryu = @jangster77 (commit author). PR mergeable=CONFLICTING 표시였으나 본질 cherry-pick (1f187cf9) 충돌 0 — PR base 시점 차이. **본질 결함**: `paragraph_layout.rs::layout_composed_paragraph` L857 `effective_col_x/w` 분기가 인라인 TAC 표 보유 줄의 `comp_line.segment_width` 무시 → col_area.width(31692 HU)로 `available_width` 산출 → Justify slack(~160 px) 과대 → `extra_word_spacing` 80 px/space 부풀음 → 인라인 표 +175 px 우측 편위 (exam_science.hwp pi=61 12번 응답 분수). HWP 가 인라인 TAC 표 줄의 segment_width 를 표 폭+잔여로 좁게 인코딩 (wrap=TopAndBottom 영향) 하는 본질 미반영. **단일 룰 확장 + 케이스별 명시 가드**: `has_picture_shape_square_wrap \|\| line_has_inline_tac_table` (Picture/Shape Square wrap 분기와 동일한 LINE_SEG.cs/sw 사용 패턴 재사용 + 인라인 TAC 표 케이스 추가) + 임계값 보정 `sw < col_w_hu - 200` → `sw + cs < col_w_hu - 200` (단락 들여쓰기 LINE_SEG.column_start 인코딩한 정상 paragraph 의 full-width line 미진입 보장, `feedback_rule_not_heuristic` 정합). 단일 파일 본질 (`paragraph_layout.rs` +25/-2). 정량 측정: pi=61 인라인 분수 x=**739.87 → 584.93 (+175 px → ±5-10 px 잔여)**. 검증: cargo test --lib --release **1131 passed** (회귀 0) / svg_snapshot 6/6 / issue_546 1 (Task #546 회귀 0) / issue_554 12 / clippy 0 / build --release / Docker WASM **4,570,901 bytes** (PR #564 baseline +286 bytes — paragraph_layout.rs +25/-2 LOC 정합). **광범위 페이지네이션 회귀 sweep**: `samples/` 폴더 전체 **164 fixture / 1,614 페이지 / 페이지 수 회귀 0**. exam_science byte 차이: page 1/3/4 byte-identical / page 2 만 의도된 정정 (PR 본문 100% 재현). **메인테이너 시각 판정 ★ 통과** — 작업지시자 SVG 검증 (exam_science 12번 문항 첨부) 으로 인라인 분수 [1g의 A에 들어 있는 중성자수 / 1g의 D에 들어 있는 중성자수] 본문 흐름 정상 배치 회복 확인. cherry-pick author Jaeook Ryu 보존 (`9c6e79f`). devel merge `8719a03` + push 완료. PR #570 close + Issue #568 수동 close (closes #568 키워드는 merge 가 아닌 close 로 자동 처리 안 됨, 안내 댓글 등록). 미해결 영역 (Page 1 header / Page 3/4 보기 셀 / 페이지 쪽번호) 별도 후속 task 후보. 처리 보고서: `mydocs/pr/archives/pr_570_report.md`. 검토 보고서: `mydocs/pr/archives/pr_570_review.md`. |
| **PR #575 Task #573** | 보기 셀 분수 단락 인라인 표 셀 paragraph 라우팅 정정 (closes #573, 인접 효과로 #572 자동 정정) | **완료 (핀셋 cherry-pick 머지 + push, PR #575/Issue #572/#573 close)** | 외부 컨트리뷰터 @planet6897 (PR 등록) / Jaeook Ryu = @jangster77 (commit author). PR mergeable=CONFLICTING 표시였으나 본질 cherry-pick (7d5075bf) 충돌 0 — PR base 시점 차이. **본질 결함**: `table_layout.rs::has_table_ctrl` 가 block table (treat_as_char=false) 와 inline TAC table (treat_as_char=true) 미구분 → 인라인 TAC 표 보유 셀 paragraph 가 ELSE 분기로 빠져 `layout_composed_paragraph` SKIP → 'ㄷ. ', '이다.' surrounding text 미렌더 (exam_science 13/15/16/19번 보기 셀 분수 단락). **Stage 1 정밀 진단의 통찰**: 사용자 보고 "오른쪽 편위" 의 실제 본질이 "surrounding text 미렌더" 임을 확정 (분수가 cell 좌단에 단독 노출되어 우측 편위로 인지). **본질 정정 (단일 파일 +19/-2)**: (1) `has_block_table_ctrl` 신설 (treat_as_char=false 만, 케이스별 명시 가드 — `feedback_hancom_compat_specific_over_general` + `feedback_rule_not_heuristic` 정합), (2) L1461 조건 `!has_block_table_ctrl` 시에만 `layout_composed_paragraph` 호출, (3) L1844 inline TAC table branch 에 `inline_shape_position` 중복 emit 가드 추가 (Equation L1800 패턴 재사용 — 단일 룰 재사용). **인접 효과 — Issue #572 자동 정정**: Page 1 header 외곽 1×1 표 셀 p[3] 의 sub-tables routing 도 layout_composed_paragraph 의 paragraph alignment + Justify slack 분배 경로로 변경 — "AsimD의" x=86.39 → 152.39 (cell 좌단 → 중앙 정렬 가까움). **단일 본질 정정으로 Task #573 + Issue #572 두 결함 동시 해결**. 정량 측정: pi=68 cell[5] p[2] "ㄷ. 이다." surrounding text Before 미렌더 → After 정합 (x=97.07 y=715.56 / 분수 122.07 / "이다." 347.29-389). 검증: cargo test --lib --release **1131 passed** (회귀 0) / svg_snapshot 6/6 / issue_546 1 (Task #546 회귀 0) / issue_554 12 / clippy 0 / build --release / Docker WASM **4,571,604 bytes** (PR #570 baseline +703 bytes — table_layout.rs +19/-2 LOC 정합). **광범위 페이지네이션 회귀 sweep**: `samples/` 폴더 전체 **164 fixture / 1,614 페이지 / 페이지 수 회귀 0**. exam_science byte 차이: 4 페이지 모두 의도된 정정 (page 1 Issue #572 인접 효과 / page 2 pi=61 / page 3 13/16번 / page 4 19번) — PR 본문 100% 재현. **메인테이너 시각 판정 ★ 통과** — 작업지시자 평가: "메인테이너의 시각 판정 통과입니다." cherry-pick author Jaeook Ryu 보존 (`323c1c7`). devel merge `cb8d0a8` + push 완료. PR #575 close + Issue #573 수동 close + Issue #572 수동 close (인접 효과 자동 정정, 메인테이너 시각 판정 통과 확인). 처리 보고서: `mydocs/pr/archives/pr_575_report.md`. 검토 보고서: `mydocs/pr/archives/pr_575_review.md`. **본 사이클 최고 수준 PR 사례 — Stage 1 정밀 진단 + 단일 본질 정정으로 두 결함 동시 해결 + HWP 표준 룰 직접 사용 + 단일 룰 재사용 패턴 모두 정합.** |
| **PR #580 Task #577** | 셀 내부 단독 TopAndBottom 이미지 1라인 오프셋 정정 (closes #577) | **완료 (핀셋 cherry-pick 머지 + push, PR #580/Issue #577 close)** | 외부 컨트리뷰터 @planet6897 (PR 등록) / Jaeook Ryu = @jangster77 (commit author). PR mergeable=CONFLICTING 표시였으나 본질 cherry-pick (0acd13a6) 충돌 0 — PR base 시점 차이. **본질 결함**: 셀 내부 비-TAC `text_wrap=TopAndBottom AND vert_rel_to=Para` Picture 가 `compute_object_position` 호출 시 anchor 시점 좌표로 `para_y` (advance 후) 사용 → image_y - cell_y = pad_top(3.78) + line_height(15.32) = 19.10 px → cell-clip 영역 ~10.81 px 초과 (exam_science.hwp 페이지 1 보기 ⑤ 및 ②④ 이미지 하단 잘림). HWP IR 표준은 anchor 시점이 paragraph 시작 좌표 (`para_y_before_compose`) 이지만 코드는 advance 후 `para_y` 사용 → 1라인 오프셋. **본질 정정 (단일 파일 +22/-3)**: `text_wrap=TopAndBottom AND vert_rel_to=Para` 양 조건에만 `para_y_before_compose` 사용 (조건부 anchor_y 도입) + `compute_object_position` 자체 무변경 (다른 호출처 회귀 차단). **케이스별 명시 가드** (`feedback_hancom_compat_specific_over_general`) + **HWP IR 표준 직접 사용** (anchor 시점 좌표 = paragraph 시작, `feedback_rule_not_heuristic`) + **회귀 위험 영역 좁힘**. 정량 측정: image_y - cell_y **19.10 → 3.78 px** (= pad_top, HWP IR 정합) / LAYOUT_OVERFLOW exam_science **9.5→3.4 px** / mel-001.hwp **8건 (3.5~18.8 px)→0건**. 검증: cargo test --lib --release **1131 passed** (회귀 0) / svg_snapshot 6/6 / issue_546 1 (Task #546 회귀 0) / issue_554 12 / clippy 0 / build --release / Docker WASM **4,571,643 bytes** (PR #575 baseline +39 bytes — table_layout.rs +22/-3 LOC 정합). **광범위 페이지네이션 회귀 sweep**: `samples/` 폴더 전체 **164 fixture / 1,614 페이지 / 페이지 수 회귀 0**. SVG byte 차이: exam_science 4 페이지 중 2 (page 1, page 4) — page 1 보기 ①~⑤ 정정 (PR 본문 권위 영역) + page 4 부수 효과. mel-001 21 페이지 byte 차이 0 (LAYOUT_OVERFLOW 정정이 SVG byte 단위는 무영향, clip 영역 안으로만 이동). **메인테이너 시각 판정 ★ 통과** — 작업지시자 평가: "시각 검증 통과입니다." cherry-pick author Jaeook Ryu 보존 (`50a80ab`). devel merge `ee85631` + push 완료. PR #580 close + Issue #577 수동 close (closes #577 키워드는 merge 가 아닌 close 로 자동 처리 안 됨, 안내 댓글 등록). 처리 보고서: `mydocs/pr/archives/pr_580_report.md`. 검토 보고서: `mydocs/pr/archives/pr_580_review.md`. |
| **PR #584 Task #574** | HY견명조 heavy display 오분류 정정 (closes #574) | **완료 (핀셋 cherry-pick 3 commits + 옵션 C 충돌 통합 머지 + push, PR #584/Issue #574 close)** | 외부 컨트리뷰터 @planet6897 (PR 등록) / Jaeook Ryu = @jangster77 (commit author). PR mergeable=CONFLICTING — PR base 시점 차이 + integration_tests.rs 위치 충돌 (HEAD 의 test_521/test_552 vs ef6583c 의 test_574 추가). **본질 결함**: `is_heavy_display_face` (`src/renderer/style_resolver.rs:601`) hardcoded list 에 "HY견명조" 잘못 포함 → CharShape.bold=false 무시되고 SVG 에 `font-weight="bold"` 강제 적용 (exam_science 페이지 1 쪽번호 "1" 굵기 결함). **Stage 0 정밀 진단의 통찰**: 이슈 본문 "쪽번호 1 출처가 바탕쪽" 가설 → 실제 본문 [6] 표 셀 paragraph[0] Shape (사각형, InFrontOfText) TextBox 내부 literal text "1" 정정 + 이슈 본문 "회색" 가설 → IR 색상 #000000 (한컴 PDF 도 검정), 본질은 굵기만. **본질 정정 (단일 줄 17 글자)**: hardcoded list 에서 "HY견명조" 제거. HY견명조B (명시 Bold variant) 보존 + HY헤드라인M / HY견고딕 / HY그래픽 보존 (Task #146 v4 본질). **TDD 흐름**: Stage 2 RED + Stage 3 GREEN 분리 — 본 사이클 가장 세분화된 흐름 (7-stage + 진단 스크립트 `examples/inspect_574.rs` 보존 가치). cherry-pick 3 commits (`6d193ec9` 진단+스크립트 / `ef6583ce` Stage 2 TDD RED / `0002b496` Stage 3 fix GREEN). 옵션 C 충돌 통합: integration_tests.rs HEAD 영역 (test_521/test_552) 보존 + test_574 추가. 검증: cargo test --lib --release **1132 passed** (test_574 RED → GREEN, baseline +1) / svg_snapshot 6/6 / issue_546 1 (Task #546 회귀 0) / issue_554 12 / clippy 0 / build --release / Docker WASM **4,581,498 bytes** (PR #580 baseline +9,855 bytes). **광범위 페이지네이션 회귀 sweep**: `samples/` 폴더 전체 **164 fixture / 1,614 페이지 / 페이지 수 회귀 0**. **SVG 정량 측정 (`font-weight="bold"` 출현 횟수)**: 4 fixture 합계 1,664 → 423 (**-1,241** 제거) — exam_science -82 / exam_kor -759 / exam_eng -156 / exam_math -244. 페이지 1 쪽번호 "1" 권위 케이스 검증: HY견명조 font-size=44 우상단 `font-weight="bold"` 제거 (CharShape.bold=false 권위 회복). **메인테이너 시각 판정 ★ 통과** — Canvas + SVG 양쪽 통과. **추가 발견 (feedback_image_renderer_paths_separate 자연 발현)**: SVG (`svg.rs`) 는 `is_visually_bold()` 사용 → `is_heavy_display_face` 호출 → 본 PR fix 적용. Canvas (`web_canvas.rs`) 는 `style.bold` 만 사용 → `is_heavy_display_face` 미호출 → hardcoded list 영향 없음 (처음부터 정상). 본 PR fix 효과는 SVG 경로 단독 적용. **별도 task 영역 발견**: SVG 시각 검증 중 HY견고딕 의 "홀수형" 텍스트도 `font-weight="bold"` 강제 적용 — 본 PR 의 의도된 보존 영역 (Task #146 v4 본질), 한컴 PDF 와의 비교 결과에 따라 별도 task 후보로 검토 가능. cherry-pick author Jaeook Ryu 보존 (`c9cc04a` + `0f0dd5e` + `24b1211`). devel merge `4c44a27` + push 완료. PR #584 close + Issue #574 수동 close (closes #574 키워드는 merge 가 아닌 close 로 자동 처리 안 됨, 안내 댓글 등록). 처리 보고서: `mydocs/pr/archives/pr_584_report.md`. 검토 보고서: `mydocs/pr/archives/pr_584_review.md`. **본 사이클 가장 세분화된 흐름 (7-stage + TDD Stage 2/3 분리) + Stage 0 정밀 진단으로 이슈 본문 가설 정정 + 진단 스크립트 보존 가치 + Canvas/SVG 경로 차이 자연 발현 패턴 모두 정합.** |
| **PR #592 Task #588** | exam_eng.hwp p7 #40 글상자 사이 화살표 누락 — PUA U+F003B → ↓ 매핑 추가 (closes #588) | **완료 (핀셋 cherry-pick 머지 + push, PR #592/Issue #588 close)** | 외부 컨트리뷰터 @planet6897 (PR 등록) / Jaeook Ryu = @jangster77 (commit author). **PR mergeable=MERGEABLE — 본 사이클 첫 케이스** (PR #561~#584 모두 CONFLICTING 이었음, PR base 가 본 devel 과 정합한 시점). 본질 cherry-pick (2788fea8) 충돌 0. **본질 결함**: exam_eng.hwp p7 #40 요약형 문항 글상자 사이 ↓ 화살표가 SPUA-A 저영역 (U+F003B, UTF-8 `f3 b0 80 bb`) 미매핑으로 두부(□) 표시 (`map_pua_bullet_char` 매핑 표 밖). **Stage 1 PDF 글리프 외곽 분석 (정밀 진단)**: `samples/exam_eng.pdf` 7쪽의 임베디드 폰트 `AAAAAL+HCRBatang` 의 `uF003B` 1 contour 7 pts × stem 35% × arrowhead 100% × solid filled → ↓ DOWNWARDS ARROW 형태 확정. **본질 정정 (단일 분기 신설, paragraph_layout.rs +32)**: SPUA-A 저영역 (`0xF0000..=0xF00CF`) 분기 신설 + U+F003B → U+2193 매핑 + 단위 테스트 +2 (`supplementary_pua_a_low_range_maps_down_arrow` + `supplementary_pua_a_low_range_unmapped_returns_original`). **디스조인트 설계 (회귀 차단)**: 본 PR (`0xF0000..=0xF00CF`) vs Task #528 (`0xF00D0..=0xF09FF` 책괄호/예시) vs Task #509 (`0xF02B0..=0xF02FF` 원문자) vs Wingdings (`0xF020..=0xF0FF` BMP PUA) 완전 무중첩, 회귀 위험 영역 0. 검증: cargo test --lib --release **1134 passed** (baseline 1132 + 단위 테스트 +2) / svg_snapshot 6/6 / issue_546 1 (Task #546 회귀 0) / issue_554 12 / clippy 0 / build --release / Docker WASM **4,581,527 bytes** (PR #584 baseline +29 bytes — paragraph_layout.rs +32 LOC 정합). **광범위 페이지네이션 회귀 sweep**: `samples/` 폴더 전체 **164 fixture / 1,614 페이지 / 페이지 수 회귀 0**. **SVG 정량 측정**: exam_eng U+F003B 1→0 + ↓ 1 (PR 본문 권위 영역 정확 1건 변환), exam_kor / exam_math / exam_science 모두 0건 (다른 fixture 무영향, 디스조인트 설계 정합 입증). **페이지 7 권위 케이스 검증**: `exam_eng_007.svg:4162` 정확한 1행 변경 (`>󰀻<` → `>↓<`, x/y/font/size/scale/fill 모두 동일) — PR 본문 100% 재현. **메인테이너 시각 판정 ★ 통과** — 작업지시자 평가: "메인테이너가 7 페이지 40번 화살표 출력 확인했습니다." cherry-pick author Jaeook Ryu 보존 (`dd568ed`). devel merge `a213fb8` + push 완료. PR #592 close + Issue #588 수동 close (closes #588 키워드는 merge 가 아닌 close 로 자동 처리 안 됨, 안내 댓글 등록). **잔존 영역 (별도 이슈 후보)**: U+F0090 (img-start-001.hwp) / U+F012B/F081C (복학원서.hwp) / U+F02BA, F02C3~F02C5, F02CE~F02D0, F02FC (exam fixtures). 처리 보고서: `mydocs/pr/archives/pr_592_report.md`. 검토 보고서: `mydocs/pr/archives/pr_592_review.md`. **본 사이클 첫 MERGEABLE PR (PR base 정합 시점) + Stage 1 PDF 글리프 외곽 직접 분석 + 디스조인트 설계 + 단위 테스트 +2 + 시각 변경 정량 측정 (1건 정확 변환) 패턴 모두 정합.** |
| **PR #593 Task #590** | Square wrap 표 horz_rel_to=단 속성 정합 (closes #590) | **완료 (핀셋 cherry-pick 머지 + push, PR #593/Issue #590 close)** | 외부 컨트리뷰터 @planet6897 (PR 등록) / Jaeook Ryu = @jangster77 (commit author). **PR mergeable=MERGEABLE — PR #592 와 함께 본 사이클 두 번째 케이스** (PR base 가 본 devel 과 정합한 시점). 본질 cherry-pick (5d3b3e2d) 충돌 0. **본질 결함**: `src/renderer/layout.rs:2285-2300` (Issue #480 도입 분기) 가 모든 Square-wrap 표를 무조건 문단 좌측 가장자리 (`col_area.x + effective_margin`) 기준으로 강제 배치하며 `horz_rel_to` 속성을 무시 → exam_kor.hwp p17 [A] 글상자 우측 6.5mm 치우침. **수식 검증**: `table_x = col_area.x + effective_margin + h_offset = 117.17 + 24.67 + 9.44 = 151.28 px` (effective_margin 24.67 px = 6.5 mm 강제 적용). **본질 정정 (단일 줄)**: 분기 가드에 `horz_rel_to=Para` 한정 1줄 추가 → Column/Page/Paper 는 `compute_table_x_position` 명세 기반 분기로 위임 (HWP 표준 룰). 단일 파일 본질 (`layout.rs` +5/-4). **케이스별 명시 가드 (`feedback_hancom_compat_specific_over_general`) + HWP 표준 룰 위임 (`feedback_rule_not_heuristic`) + 회귀 위험 영역 좁힘** 정합. **특이 사항**: 단 1 commit 에 본질 + plans + working + report + orders 갱신 모두 통합 (이전 PR 들은 본질 + 보고서 별도 commit, 본 PR 새 패턴) — auto-merge 정합으로 충돌 0. 검증: cargo test --lib --release **1134 passed** (회귀 0) / svg_snapshot 6/6 / issue_546 1 (Task #546 회귀 0) / issue_554 12 / clippy 0 / build --release / Docker WASM **4,581,465 bytes** (PR #592 baseline -62 bytes — 가드 1줄 추가의 LLVM 최적화 효과). **광범위 페이지네이션 회귀 sweep**: `samples/` 폴더 전체 **164 fixture / 1,614 페이지 / 페이지 수 회귀 0**. **exam_kor 4 페이지 의도된 정정 (PR 본문 100% 재현)**: page 17 [A] (Left -24.7 px **사용자 보고 정정**) + page 18 [B] x2 + page 19 [B] (Left 동류 정정) + page 14 [A] x4 (Right -7.55 px **명세 정합 향상** — `inline_x_override` ADD/SUBTRACT 모순 통일). **page 17 [A] 글자 정량 검증**: 셀 내부 `[` translate(152.28→127.61), `A` translate(157.50→132.83), 정확 -24.67 px 좌측 시프트 + 다른 위치 (x=622) `[A]` 변경 0 (회귀 0, 케이스별 명시 가드 정합 입증). **메인테이너 시각 판정 ★ 통과 (SVG + 웹 캔바스 양쪽)** — 작업지시자 평가: "svg, 웹 모두 시각 판정 통과입니다." PR #584 와 달리 Canvas/SVG 경로 차이 의심 영역 없이 양쪽 일관 정합. cherry-pick author Jaeook Ryu 보존 (`3682cff`). devel merge `0e543e6` + push 완료. PR #593 close + Issue #590 수동 close. 처리 보고서: `mydocs/pr/archives/pr_593_report.md`. 검토 보고서: `mydocs/pr/archives/pr_593_review.md`. **단일 줄 분기 가드 + MERGEABLE + HWP 표준 룰 위임 + 명세 정합 향상 + SVG/Canvas 양쪽 일관 정합 패턴 모두 정합.** |
| **PR #599 (refs #536)** | render P4 native Skia PNG raster backend + AI 파이프라인 + VLM 연동 (refs #536, OPEN 유지) | **완료 (commit 단위 cherry-pick 9 commits + 메인테이너 5개 후속 정정 + push, PR #599 close)** | 외부 컨트리뷰터 @seo-rii (이전 PR #498 P3 동일 컨트리뷰터). PR mergeable=MERGEABLE 표시였으나 PR base 가 본 devel 보다 **73 commits 뒤** (PR #571 와 같은 base skew, PR base = `eaac8bd` 5/4 PR #563 후속). 단순 머지 시 본 사이클 cherry-pick 처리분 모두 revert 위험 → **commit 단위 cherry-pick 으로 처리**. 본 PR 의 Skia 본질 영역이 신규 파일 (`src/renderer/skia/` + `layer_renderer.rs` + `image_conv.rs`) 로 격리되어 있어 본 사이클 처리분과 **0 중첩** → 깨끗한 cherry-pick 가능 (PR #571 close 결정과 다른 케이스). 9 commits 모두 author seo-rii 보존. **본질 (P4 단계, refs #536)**: PageLayerTree → native Skia replay → PNG 첫 raster backend, LayerRasterRenderer/RasterRenderOptions/RasterRenderOutput 추가, SkiaLayerRenderer 신규, DocumentCore::render_page_png_native(page) API, native-skia feature gate (기본 빌드 영향 0), raster size guard, CI workflow 자동화. **메인테이너 5개 후속 정정 (`876d820`)**: PR 본질만으로 한컴 fixture 정상 표시 안 됨 → (1) Skia 한글 폰트 fallback chain (Noto Sans KR / Nanum 등 추가), (2) `--font-path` 동적 폰트 로딩 (`with_font_paths` API + `custom_typefaces` HashMap, ttfs 디렉토리의 한컴 전용 폰트 HY견명조 등 동적 로드), (3) char 단위 fallback 렌더링 (NBSP/U+2007/U+200B 등 공백 두부 정정 — `unichar_to_glyph` 검사 + 일반 공백 너비 advance), (4) **VLM 옵션 (AI 파이프라인 + Vision-Language Model 연동)** — `PngExportOptions` + `VlmTarget::Claude` (1568 longest edge / 1.15 MP, Claude Vision 정합) + `--scale` + `--max-dimension`, max_dimension+max_pixels 결합 자동 scale + 0.5% 안전 마진, (5) `export-png` CLI 명령 + 매뉴얼 (한글/영문 동기화 — `mydocs/manual/export_png_command.md` + `mydocs/eng/manual/export_png_command.md`). 검증: cargo test --lib --release **1134 passed** (회귀 0) / cargo test --features native-skia skia **20 passed** / clippy 0 / build --release / build --release --features native-skia / Docker WASM **4,581,465 bytes** (PR #593 baseline +0 — feature gate 정합 입증). **광범위 페이지네이션 회귀 sweep**: 164 fixture / 1,614 페이지 / 회귀 0. **VLM 옵션 게이트웨이**: 5개 조합 모두 정상 (default 1123×1588 / scale 2.0 = 2246×3175 / scale 0.5 = 562×794 / max-dim 1024 = 725×1024 / **vlm-target claude = 898×1269 = 1.14 MP ≤ 1.15 MP ✓**). **메인테이너 시각 판정 ★ 통과** — SVG/PNG 한글 + 공백 정상 표시 + VLM 옵션 5개 조합 + Claude 프리셋 픽셀 한도 정합. 후속 이슈 등록: **#613 (VLM 프리셋 확장 GPT-4V/Gemini/Qwen-VL/LLaVA)** + **#614 (DPI 메타데이터 옵션 --dpi PNG pHYs chunk)**. cherry-pick author seo-rii 보존. devel merge `f7d5563` + push 완료. PR #599 close + 영문 댓글 등록 + **한글 댓글 추가 등록 (작업지시자 안내 정합 — 컨트리뷰터에게 한글 답변 정합)**. Issue #536 (refs) OPEN 유지 (멀티 렌더러 트래킹, P5+ 후속 단계 포함). 처리 보고서: `mydocs/pr/archives/pr_599_report.md`. 검토 보고서: `mydocs/pr/archives/pr_599_review.md`. **DTP 엔진 정체성 (`project_dtp_identity`) 정합 + commit 단위 cherry-pick 으로 base skew 우회 + AI 파이프라인 / VLM 연동 도입 + 매뉴얼 한영 동기화 패턴 모두 정합. 본 사이클 가장 광범위 신규 영역 흡수 PR.** |
| **PR #600 (closes #513)** | Supplementary PUA-A (U+F02B1~F02C4) SVG 출력 정정 | **완료 (commit 단위 cherry-pick 2 commits + push, PR #600/Issue #513 close)** | 외부 컨트리뷰터 @oksure (Hyunwoo Park, oksure@gmail.com) — 4번째 PR (PR #581/#582/#583 모두 cherry-pick 완료). PR mergeable=MERGEABLE 표시였으나 PR base skew (PR #571/#599 패턴) — UI 표시지만 PR base diff 가 본 사이클 cherry-pick 모두 revert 위험. 그러나 본질 commit 영역 (composer.rs F02B1~F02C4 + paragraph_layout.rs ⑩~⑳) 이 본 사이클 처리분과 **0 중첩** (PR #592 의 SPUA-A 저영역 F0000~F00CF 와 다른 코드포인트 영역) → commit 단위 cherry-pick 가능. **본질 결함**: Task #509 의 `map_pua_bullet_char` 매핑이 `convert_pua_enclosed_numbers` (composer) 의 CharOverlap 변환 (border_type=3, 사각형 안의 숫자) 에 막혀 도달 못 함 → SVG 에 ① 대신 "1" (사각형 박스) 렌더링. **본질 정정**: (1) `composer.rs::pua_enclosed_border_type` 에서 F02B1~F02C4 범위 제거 (CharOverlap 제외), (2) `paragraph_layout.rs::map_pua_bullet_char` 에 F02BA~F02C4 (⑩~⑳) 매핑 추가 (전체 20자 완성), (3) F02CE~F02E1 (반전 사각형) CharOverlap 유지 (표준 Unicode 부재). **Copilot review 3개 응답** (`aafe85a`): ⑩~⑳ 테스트 + 주석 갱신 + 폭 계산 정합 (`map_pua_bullet_char` 후 `estimate_text_width`). cherry-pick 2 commits (충돌 0, author Hyunwoo Park 보존): `34f8547` 본질 + `14f30e8` 리뷰 응답. 검증: cargo test --lib --release **1134 passed** (회귀 0) / cargo test --lib pua **12 passed** (`supplementary_pua_a_maps_circled_digits` ⑩~⑳ GREEN) / svg_snapshot 6/6 / issue_546 1 / issue_554 12 / clippy 0 / build --release. **광범위 페이지네이션 회귀 sweep**: `samples/` 폴더 전체 **164 fixture / 1,614 페이지 / 페이지 수 회귀 0**. **SVG 정량 측정 (PR 본문 100% 재현)**: pua-test 1/1 byte 차이 + 원문자 0→9 / mel-001 15/21 byte 차이 + 원문자 20→34 (+14) / kps-ai 1/80 byte 차이 + 원문자 32→34 (+2) / KTX 0/27 byte 차이 + 변경 없음. **메인테이너 시각 판정 ★ 통과** — ① ~ ⑳ 정상 출력 + KTX 회귀 0. devel merge `9768819` + push 완료. PR #600 close + 한글 댓글 등록 (PR #599 학습 적용) + Issue #513 수동 close. **별개 영역 발견** — pua-test U+0F53A 가 `pua_oldhangul.rs:5022` 에서 자모 시퀀스 (ᄒᆞᆫ) 로 임의 변환되지만 한컴 PDF 정답지는 "Basic-out, 매핑 표 외" 빈 공백 처리 (한컴 자체도 매핑 미지원) — 이슈 **#615** 등록 + 정확한 본질 분석 (pua_oldhangul.rs 매핑이 한컴 정답지와 정합 안 함). **PUA 회귀 패턴 인식 트리거** — 작업지시자 안내: "PUA 가 계속 회귀되는 현상이 발생하는군요! 한군데 고치면 계속 다른 곳에서 터져버립니다." → 본 사이클 PUA 정정 누적 (PR #587/#562/#592/#600 + Issue #615) 의 두더지 잡기 패턴 인식. PUA 매핑 표 통합 재검증 별도 광범위 task 후보. 처리 보고서: `mydocs/pr/archives/pr_600_report.md`. 검토 보고서: `mydocs/pr/archives/pr_600_review.md`. **PR #571/#599 와 다른 base skew 처리 (본 사이클 처리분과 0 중첩) + Task #509 본질 후속 정확 식별 + Copilot review 응답 정합 + 한글 댓글 등록 패턴 모두 정합.** |
| **Issue #615** | pua_oldhangul U+F53A 매핑 잘못됨 — 한컴 정답지 "매핑 표 외" 빈 공백 vs 본 환경 자모 시퀀스 ᄒᆞᆫ 임의 변환 | **OPEN (M100, 후속 task)** | PR #600 시각 판정 중 발견. `pua_oldhangul.rs:5022` 의 임의 매핑이 한컴 정답지와 정합 안 함. 작업지시자 시각 검증으로 한컴 PDF "Basic-out, hwpspec, (매핑 표 외)" 영역 확인. PUA 매핑 표 통합 재검증 영역. |
| **Issue #598** | rhwp-studio 각주 삭제 기능 (한컴 정합 UX) | **OPEN (외부 컨트리뷰터 공개)** | 크롬 확장 사용자 별점 리뷰 출처. 한컴 정합 UX 5항목 명세 — (1) 본문 각주 마커 hit test → 페이지 하단 각주 영역 해당 번호 위치로 커서 이동, (2) 좌우 화살표 이동 시 각주 마커가 한 칸 단위로 처리 (현재 건너뜀), (3) 양방향 + 동일 확인창 UI ((3-A) 각주 앞 위치 + Delete (3-B) 각주 뒤 위치 + Backspace, 두 분기 모두 단일 컴포넌트 호출), (4) 각주 번호 재계산, (5) Undo/Redo. 현재 구현 상태: 각주 삽입 + 각주 영역 zone hit test 구현, 본문 각주 마커 hit test + 커서 이동 단위 + Delete/Backspace 분기 + 삭제 다이얼로그 + WASM `delete_footnote` API 모두 미구현. 마일스톤 M100. assignee 미지정 (외부 기여 환영). |

## 인프라 / 정합 작업

| 항목 | 상태 | 비고 |
|------|------|------|
| `local/devel` 브랜치 재생성 | **완료** | 폴더 이동 untracked 305 파일 (mydocs/plans/archives/ 사본) 정정 — 원본은 mydocs/plans/ 에 tracked 그대로 보존 확인. devel switch + local/devel 삭제 + 새로 생성. |
| 클로드 메모리 ↔ mydocs/manual/memory/ 동기화 | **완료** | 1 파일 차이 정합 — `project_output_folder_structure.md` (PR 검토 SVG before/after 패턴 추가, 901B → 1456B). PR #589 검토 보고서 commit 에 동봉. |
| `mydocs/privacy/persona_dump_20260505.md` 작성 | **완료** | 직전 덤프 (5/1) 와의 4일 차분 명시 — 외부 PR 11건 폭주 사이클, 신규 컨트리뷰터 5명, FFI ABI 영역 진입, 협업 PR 권유 첫 시도, base skew 처리 패턴. git 미추적. |

## 외부 컨트리뷰터 작업 (jangster77 fork)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **#604** | Document IR 표준 정합화 — LineSeg 표준 정의 + wrap_precomputed 제거 + HWP3 cs/sw 인코딩 정정 + Stage A+D+D-2 본질 영역 완전 정합 (HWP3/HWP5/HWPX 3개 포맷 변환본 모두 동일 페이지 수 정합) | **완료 (Stage 1~D-2, 11 commits, branch local/task604, upstream/devel rebase 완료)** | PR #589 머지 후 시각 판정 중 발견. 본질: pi=75 첫 3 줄 cs=0/sw=0 (wrap zone 미설정) → 그림 좌측 (x=56.7) 에 텍스트 그려짐. **Stage 1~6** (Stage 1 `40739ae` IR 표준 문서화, Stage 2 `b255540` typeset wrap_anchors 메타데이터 채널, Stage 2b `d71f944` wrap_precomputed 제거 + HWP3 후처리 청산, Stage 3 `d96320d` wrap zone pgy 가드 단방향화, Stage 4 광범위 회귀 검증, Stage 5 line_spacing 100% 정합화, Stage 6 paper_images z-order 정합). **Stage A+D 본질 진단** (`4ba0816`): HWP5 v2024 변환본 정밀 재진단으로 Document IR 표준 미명시 영역 3개 발견 — paragraph 내 LineSeg cs/sw 전환, paragraph 간 vpos 연결, paragraph 내 line wrap vpos reset. **Stage D-2 본질 영역 완전 정합** (`ce9f6e1` + `67d6772`): 8개 정정 영역 — (1) is_page_break 보강, (2) lh/ls HWP5 분리 인코딩, (3) break_flag → tag bit 누설 제거, (4) pgy-based column_type=Page 제거, (5) wrap zone cs/sw 정합 인코딩 (active_wrap_cs_sw outer state), (6) paper-top anchor acc_vpos reset, (7) line_info.break_flag 0x8001 → column_type=Page 변환, (8) paragraph 내 line wrap pgy 감소 시 acc_vpos reset. **HWP3/HWP5/HWPX 3개 포맷 변환본 모두 동일 페이지 수 정합 달성**: hwp3-sample.hwp 16, hwp3-sample4.hwp 36 (HWP5 정합 + paragraph 시퀀스 정합), hwp3-sample5.hwp **64** (HWP5 완전 정합), exam_science.hwp 4 (Task #546 정합). pi=74 picture page 4 / vpos=0 (HWP5 정합), pi=75 ls[0..18] cs=35460/sw=15564 wrap zone 인코딩, pi=1213 ls[1] vpos=0 (paragraph 내 페이지 reset). **결정적 검증**: cargo test --lib **1131 passed** / svg_snapshot 6/6 / issue_546 정합 / **issue_554 12/12** / clippy lib 0 warning. **upstream/devel rebase**: 0 behind / 10 ahead, 충돌 0 (Task #568 PR #570 Stage 3 layout_composed_paragraph 영역과 Task #604 HWP3 파서 영역 독립). **잔존 영역 (별도 task)**: 조판부호 보기 영역의 시각 차이 — sample4 pi=960 HWP3 4 TABs vs HWP5 변환본 2 TABs + indent 1320 (한컴 변환기 paragraph indent 변환 휴리스틱 영역, 본 task scope 외). 보고서: `mydocs/report/task_m100_604_report.md`, `mydocs/working/task_m100_604_stageD2.md`. |

## 작업 메모

### PR #589 본질
- PR #553 close 시 메인테이너 회신 3개 요청에 대한 응답이 본 PR 의 1차 검토 핵심 평가 대상. 모두 정합 응답.
- `wrap_precomputed` 의 round-trip 정합 모델: HWP3 → 임의 포맷 → HWP3 시 본 필드 손실되지만, **HWP3 파서가 다시 결정** 하므로 정합 유지. derived 필드 정합성 모델 (다른 HWP5 round-trip 보존 필드 `char_count_msb` / `has_para_text` / `tab_extended` 등과 동일 패턴).
- 잔존 결함 (page 27 그림+텍스트 y) 은 PR 본문 §"잔존 결함" 1번에 명시된 trade-off — 보완7 시도 시 HWP3 native pagination 회귀 (16→18, 64→65) 발견하여 본 PR 에서 제외. 트러블슈팅 문서 권장 영역.
- `feedback_check_open_prs_first` + `feedback_assign_issue_before_work` 정합 — PR #589 가 OPEN 상태에서 본 환경 작업 진행 결정 (cherry-pick 기반 처리).

### Issue #598 본질
- 작업지시자 명세 정밀화 과정 4회 반복 — (1) 편집 영역 선택 삭제 → (2) 한컴 정합: 커서 이동 단위 + Backspace 삭제 → (3) 각주 hit test 미구현 + 커서 건너뜀 결함 → (4) 본문 각주 hit test 시 각주 영역 커서 이동 + Delete 분기 + 양방향 동일 확인창 UI.
- 한컴 정합 UX 가 권위 기준 (`reference_authoritative_hancom`) — 외부 컨트리뷰터에게 한컴 환경 미보유 시 시각 판정 요청 명시.

### 메모리 동기화 일관성
- 검토 작업 중 `git stash` / 브랜치 전환 과정에서 `mydocs/manual/memory/project_output_folder_structure.md` 가 구버전으로 되돌아오는 부작용 발생 → 다시 동기화 회복.
- 메모리 ↔ 덤프 동기화는 **세션 종료 전** 또는 **branch 전환 직전** 검증 권장.

### 본 사이클 (5/1 ~ 5/5) 외부 PR 처리 누적
- v0.7.9 릴리즈 (5/1) 이후 외부 PR 11건 처리 — PR #553 (rollback) + #551/#544 (A2) + #562/#555 (A 6 commits) + #581 + #582 + #583 + #558/#557 (5 commits 옵션 C) + #571 (close 옵션 C) + #586/#403 (FFI) + #563/#143 (1차 보정) + #587 (HWP 5.0 swap) + **PR #589 (PR #553 재PR, 본 일자)**.
- 다음 patch (v0.7.10 또는 v0.8.0) 또는 v1.0.0 진입 준비 단계로 추정.
