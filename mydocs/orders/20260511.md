# 오늘 할일 - 2026년 5월 11일

## M100 — v1.0.0 조판 엔진 (PR 처리 — 5/11 사이클)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #815 (close, Refs #727)** | test — PUA U+F02B1~F02BC CharOverlap 변환 검증 | **close 완료 (옵션 C — 본질 진단 잘못 + 신규 테스트 cargo test 2/2 FAILED 영역 영역 close + 분리 PR 재제출 요청)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 **14번째 PR**). 본 PR 본질: Issue #727 영역 영역 PUA U+F02B1~F02BC (사각형 안 숫자 1~12) 영역 영역 조사 결과 + 회귀 가드 단위 테스트 추가 (1 file, +37, composer/tests.rs). **본 PR 본문 결론**: convert_pua_enclosed_numbers 메커니즘 "정상 동작 입증" + U+F02B1~F02C4 → CharOverlap(border_type=3) + pua_to_display_text → "1"~"20" 정확히 반환. **⚠️ 본 환경 cherry-pick + cargo test 결과 2/2 FAILED**: test_pua_enclosed_number_becomes_char_overlap (U+F02B1 should produce a CharOverlap run, panic) + test_pua_to_display_text_range (left: None / right: Some("1"), panic). **본질 진단 — PR 영역 영역 잘못된 가정**: `src/renderer/composer.rs` 라인 1122/1171/1184 명확한 주석 — (1) **U+F02B1~U+F02C4 영역 영역 `map_pua_bullet_char` 영역 영역 ①~⑳ 매핑 (CharOverlap 제외)** + (2) **U+F02CE~U+F02E1 영역 영역 `convert_pua_enclosed_numbers` 영역 영역 CharOverlap (border_type=**4**) 반전 사각형 안 숫자**. PR 영역 영역 (1)/(2) 두 본질 영역 영역 혼동 → 가정한 코드포인트 (U+F02B1~F02C4) 영역 영역 CharOverlap 매핑되지 않음 + border_type=3 (PR) 영역 영역 실제 4 (코드) 부정합 + 실제 CharOverlap 범위 (U+F02CE~F02E1) 미언급. **Issue #727 영역 영역 재진단**: 한컴 권위 (pdf/table-vpos-01-2022.pdf) — U+F02B1~F02BC 영역 영역 사각형 안 숫자 1~12 vs 본 환경 출력 — U+F02B1 → `map_pua_bullet_char` 영역 영역 ① (PR #600 Task #509 매핑) — Issue #727 영역 영역 진단 대상 영역 영역 `convert_pua_enclosed_numbers` 정상 동작 여부 아닌 **`map_pua_bullet_char` 영역 영역 U+F02B1~F02C4 → ①~⑳ 매핑 자체** 영역 영역 한컴 권위 영역 영역 부정합 본질. 두 가능성 — (가) 글로벌 매핑이 잘못 (모든 fixture 영역 영역 사각형 안 숫자 일관, mel-001/kps-ai 재검증 필요) / (나) fixture 별 다른 본질 (context 분기 필요). **본 환경 reset**: cherry-pick 후 cargo test 실패 → `git reset --hard origin/devel` 영역 영역 PR commits 제거 → devel 무영향. **컨트리뷰터 안내 [#815#issuecomment-4421677741](https://github.com/edwardkim/rhwp/pull/815#issuecomment-4421677741)** (정중 톤): cargo test 2/2 FAILED 결과 명시 + 실제 코드 명세 vs PR 가정 매핑 비교 + Issue #727 재진단 + 분리 PR 2 가이드 (분리 PR 1: `map_pua_bullet_char` 매핑 정정 본질 정정 / 분리 PR 2: 회귀 가드 테스트 실제 매핑 정합 영역 영역 정정 후 재제출). Issue #727 OPEN 유지 (분리 PR 1 영역 영역 close). 검토 보고서: `mydocs/pr/archives/pr_815_review.md`. 처리 보고서: `mydocs/pr/archives/pr_815_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — 테스트 신규만 영역 영역 Rust 렌더링 경로 무영향 (그러나 잘못된 코드포인트 영역 영역 검증 영역 영역 의미 부재). **`feedback_process_must_follow` 권위 사례 강화** — cargo test 자기 검증 필수 영역 영역 본질 진단 영역 영역 잘못된 점 발견 (CI 결과 부재 DIRTY 영역 영역 본 환경 자기 검증 표준 영역 영역 정합). **`feedback_hancom_compat_specific_over_general` 정합** — Issue #727 영역 영역 `map_pua_bullet_char` 영역 영역 글로벌 매핑 (PR #600) vs fixture 별 한컴 권위 영역 영역 점검 본질적. **`feedback_diagnosis_layer_attribution` 권위 사례 강화** — 두 본질 분리 진단 (`map_pua_bullet_char` U+F02B1~F02C4 → 원문자 vs `convert_pua_enclosed_numbers` U+F02CE~F02E1 → CharOverlap border_type=4). PR 영역 영역 두 본질 혼동 → 잘못된 가정. **`feedback_visual_judgment_authority` 정합** — Issue #727 영역 영역 작업지시자 시각 판정 영역 영역 발견 + 본 환경 cargo test 영역 영역 결정적 검증 영역 영역 PR 가정 반박. **`feedback_pr_supersede_chain` 정합** — PR #600 (Task #509, ①~⑳ 매핑 도입) → Issue #727 (한컴 권위 영역 영역 부정합 발견) → **PR #815** (close, 잘못된 가정 + cargo test FAILED) → 분리 PR 1 (본질 정정) + 분리 PR 2 (회귀 가드 재제출) (a) 패턴. |
| **PR #814 (closes #692)** | fix — searchAllText API + rhwpDev.search() 다중 매치 근본 정정 | **완료 (옵션 A — 2 commits cherry-pick + 충돌 수동 해결 + 중복 import 제거 commit + no-ff merge `306159c3`, 작업지시자 DEV mode 콘솔 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 **13번째 PR**). 본질: PR #684 (Issue #449 후속, 5/8 머지) 영역 4 가드 (wrapped + 가드 + 반복 횟수 + 후진 가드) 적용 후에도 `rhwpDev.search(text)` 다중 매치 미정합 — Issue #692 작업지시자 직접 검증 영역 발견. 근본 결함: `search_text_native` 단건 검색 영역 `h.char_offset > from_char` (strict greater-than) → 위치 0 매치 누락 + 반복 호출 영역 wrap-around/후진/반복 횟수 종료 조건 정합 부재. 정정 (6 files, +202/-0): Rust `src/document_core/queries/search_query.rs` (+45) `search_all_text_native` (search_all 내부 함수 영역 모든 매치 일괄 반환 + include_cells 옵션 + query.is_empty 가드) + `src/wasm_api.rs` (+13) `searchAllText` 바인딩 + TypeScript `core/types.ts` (+16) `SearchHit` 인터페이스 + `core/wasm-bridge.ts` (+5) 래퍼 + `core/rhwp-dev.ts` (+121 재작성, initRhwpDev/search/showAllIds/findNearest/help) + `main.ts` (+2) DEV 모드 initRhwpDev 호출. **리뷰 반영 commit** (`2f4ba942`): 인코딩 깨짐 수정 + SearchHit.para 의미 문서화. **본 환경 충돌 수동 해결 (5 파일)**: rhwp-dev.ts incoming (PR) 측 우선 — searchAllText 기반 재작성 (PR #684 4 가드 + SearchResult 인터페이스 폐기) + 다른 4 파일 (types/wasm-bridge/search_query/wasm_api) auto-merge 성공. **정정 commit** (`4f763f15`): main.ts initRhwpDev 중복 import 제거 — devel 측 이미 라인 24 import 존재, cherry-pick auto-merge 영역 라인 29 영역 중복 추가됨 → 라인 29 제거 + 라인 24 보존. **인프라 재사용** — search_all 내부 함수 (기존) + SearchHit Rust struct (기존) + format_search_hit 패턴 (기존, 수동 JSON 생성) — 신규 인프라 도입 부재 (기존 search_all 영역 영역 WASM 직접 노출). **영역 좁힘** — searchAllText 신규 API (opt-in) 영역 기존 searchText (단건) 동작 보존 + rhwpDev DEV 모드 (`import.meta.env.DEV`) 영역 영역만 등록 (프로덕션 영향 부재) + include_cells 옵션 본문/셀 분리 (기본 false) + query.is_empty 빈 배열 가드. 본 환경 cherry-pick 2 commits 영역 영역 1 충돌 수동 해결 + 1 중복 제거 정정 commit. 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN, cargo clippy --release --lib -D warnings ✅, sweep 면제 (search 영역 영역 layout/렌더링 경로 무관 자명), WASM 빌드 4.6 MB 재빌드. **작업지시자 DEV mode 콘솔 검증** ✅ 통과 (rhwpDev.search "text" 일괄 매치 + 위치 0 매치 포함 정합 + rhwpDev.search "text", true 셀 포함 + rhwpDev.help toolkit 안내 + rhwpDev.findNearest/showAllIds 기존 동작 보존 + 기존 searchText 단건 API 회귀 부재). CI 결과 부재 (DIRTY 영역, 본 환경 자기 검증 + 시각 판정 영역 영역 보완). **후속 요청** (작업지시자): `rhwpDev.goto(hit)` 메서드 — SearchHit 영역 영역 cursor 이동 + 화면 스크롤. 별 Issue + 후속 PR 영역 영역 진행 예정. 검토 보고서: `mydocs/pr/archives/pr_814_review.md`. 처리 보고서: `mydocs/pr/archives/pr_814_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — Rust search 영역 영역 layout/렌더링 경로 무관. **`feedback_process_must_follow` 정합** — 인프라 재사용 + 신규 인프라 도입 부재. **`feedback_hancom_compat_specific_over_general` 정합** — include_cells 옵션 + query.is_empty 가드 영역 영역 영역 좁힘. **`feedback_diagnosis_layer_attribution` 권위 사례 강화** — h.char_offset > from_char strict greater-than 본질 진단 (Issue #692 명시) + PR #684 4 가드 한계 정확 점검. **`feedback_visual_judgment_authority` 권위 사례 강화** — Issue #692 영역 작업지시자 직접 검증 영역 발견 + 본 PR DEV mode 콘솔 검증 통과 (3 단계 패턴: PR #684 도입 → 작업지시자 검증 영역 결함 발견 → 본 PR 근본 정정). **`feedback_pr_supersede_chain` 정합** — PR #684 (Issue #449 후속 + 4 가드, 미정합) → **PR #814** (searchAllText 일괄 호출 영역 근본 정정) (a) 패턴. |
| **PR #813 (close)** | fix — improve PDF export fidelity (LINE_SEG priority + HWPX adapter + font fallbacks) | **close 완료 (옵션 C — 5 정책 위반 + 회귀 위험 영역 close + 재제출 요청)** | 컨트리뷰터 @PhilipKim85 — **2번째 PR** (PR #798 close 후, 본질적으로 첫 머지 시도). 본 PR 본질 3개 (typeset.rs LINE_SEG 우선 + document.rs HWPX adapter from_bytes 호출 + pdf.rs 폰트 fallback 12개) — 본 환경 정책 영역 영역 **5 정책 위반 + 회귀 위험** 점검. **5 문제점**: (1) **base=main 정책 위반** — 외부 컨트리뷰터 워크플로우 (CLAUDE.md) 영역 영역 devel base 영역 영역 PR 생성 정합, main 영역 영역 release cycle 영역 영역 직접 진입. (2) **폰트 라이선스 정책 어긋남 (작업지시자 강조)** — 본 환경 정책 (`mydocs/tech/font_fallback_strategy.md`) 영역 영역 한컴/HY/MS 폰트 재배포 불가 + 오픈소스 (Noto Serif KR / Noto Sans KR / Pretendard) 폴백 권장 영역 영역, PR fallback chain 영역 영역 HCR/Haansoft/HYGothic-Medium/HYmjrE/HYSinMyeongJo-Medium/HYHeadLine-Medium/HYgprM/HYgtrE/휴먼모음T 등 한컴/HY/휴먼 상용 폰트만 영역 영역 — Noto/Pretendard 미포함. 폰트 임베드 부재 영역 영역 라이선스 직접 위반 부재 영역 영역 본 환경 정책 영역 영역 어긋남. (3) **typeset.rs LINE_SEG 가드 회귀 위험 (가장 심각)** — 본 환경 영역 영역 5/6~5/7 사이클 누적 정정 (PR #621 다중 줄 가드 + PR #622 다단 vpos-reset 가드 + PR #627 글상자 ㉠ y 회귀 정정 + PR #632 LAYOUT_DRIFT_SAFETY_PX 2회 차감 정정 + PR #636 aift p4 목차 alignment) 영역 영역 정교한 보호. 본 PR `has_valid_line_segs` 가드 영역 영역 HWP LINE_SEG 무조건 우선 → 누적 Composer 정정 우회 → 광범위 회귀 위험. 본 PR "Content Match ~88% (HWP) / ~64% (HWPX)" 정성 측정만 — 본 환경 표준 영역 영역 광범위 sweep (160+ fixture / 1,000+ 페이지 / 회귀 0) 미달. (4) **Issue 연결 부재** — `closes` 키워드 부재, Issue → 브랜치 → 계획서 → 구현 절차 (CLAUDE.md) 어긋남. (5) **wasm_api.rs `pub(crate) core` 가시성 변경** — API surface 영역 영역 외부 컨트리뷰터 영역 영역 임의 노출. **분리 PR 3 가이드** (컨트리뷰터 안내 [#813#issuecomment-4421118440](https://github.com/edwardkim/rhwp/pull/813#issuecomment-4421118440)): 분리 PR 1 (pdf.rs SVG viewport BBox 확장, 안전, PR #798 close 후속, 우선 권장) + 분리 PR 2 (폰트 fallback, Noto 우선 + 한컴/HY 보조 변형) + 분리 PR 3 (typeset.rs LINE_SEG + HWPX adapter, 광범위 sweep + 회귀 0 입증 필수). 컨트리뷰터 안내 정중 톤 — PR #798 이어 다시 기여 환영 + 5 문제점 점검 + 분리 PR 3 가이드 + 각 PR Issue 연결 권장. 본 PR close + 컨트리뷰터 분리 PR 재제출 대기. 검토 보고서: `mydocs/pr/archives/pr_813_review.md`. 처리 보고서: `mydocs/pr/archives/pr_813_report.md`. **`feedback_contributor_cycle_check` 정합** — @PhilipKim85 2번째 PR (PR #798 close 후). **`feedback_pr_comment_tone` 정합** — 신규 컨트리뷰터 영역 영역 차분 톤 + 정중한 정정 요청. **`feedback_external_docs_self_censor` 권위 사례 강화** — 폰트 라이선스 (한컴/HY 상용 폰트) 영역 영역 본 환경 정책 (오픈소스 우선) 영역 영역 어긋남 점검. **`feedback_hancom_compat_specific_over_general` 권위 사례 강화** — typeset.rs 누적 정정 (PR #621/#622/#627/#632/#636) 영역 영역 LINE_SEG 무조건 우선 영역 영역 회귀 위험 점검. **`feedback_process_must_follow` 정합** — Issue 연결 부재 + 분리 PR 가이드. **`feedback_release_sync_check` 정합** — base=main 정책 위반 점검. **`feedback_visual_judgment_authority` 권위 사례 강화** — "Content Match ~88% / ~64%" 정성 측정 영역 영역 광범위 sweep 결정적 검증 미달. **`feedback_pr_supersede_chain` 정합** — PR #798 (close, viewport 축소 본질 결함) → PR #813 (close, 5 정책 위반) → 분리 PR 1/2/3 (재제출 대기) (a) 패턴. |
| **PR #812 (closes #686)** | fix — master page 글상자 더블클릭 시 첫 페이지 jump 방지 | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `744ca7b0`, 작업지시자 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 **12번째 PR**). 본질: master page 자동번호 글상자 (sec=0, para=0, control=0 anchored — 모든 페이지 반복 표시) 더블클릭 시 viewport 영역 첫 페이지로 점프 (e2e 측정 scroll -11288px, Issue #686). 결함 흐름: enterTextboxEditing 영역 cursor 영역 master page anchor (sec=0, para=0) 이동 → cursor.getRect().pageIndex = 0 → scrollCaretIntoView 영역 page 0 부근 점프. 한컴 정합: master page 글상자 더블클릭 시 텍스트 편집 모드 진입 부재, 본문 마지막 caret fallback. 정정 (`rhwp-studio/src/engine/input-handler-mouse.ts` +12, 1 file): `onDblClick` 영역 영역 `ref.ppi === 0 && cursorPage !== 0` **2중 가드** — master page anchor 식별 (ppi === 0) + 현재 페이지 0 외 영역 영역만 차단 (cursorPage !== 0) + 텍스트 편집 진입 차단 + 선택 해제 + focus 복원 + return. **Copilot 리뷰 반영 commit** (`eab22908`): 주석 정정 (master page anchor 본질 명확화) + updateCaret 제거 (텍스트 편집 진입 차단 영역 영역 caret 갱신 불필요). **영역 좁힘**: 일반 글상자 (ppi != 0) 영역 기존 텍스트 편집 진입 보존 + page 0 자체 영역 정상 진입 정합 (cursorPage === 0 영역 영역 가드 부재) + 다른 객체 (picture / table / equation) 영역 영역 무관. **인프라 재사용** — cursor.getRect / exitPictureObjectSelection / pictureObjectRenderer.clear / eventBus.emit / textarea.focus (모두 기존) — 신규 인프라 도입 부재. 본 환경 cherry-pick 충돌 0건 (auto-merge). 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN, sweep 면제 (TypeScript 단일 영역 SVG 무영향 자명), WASM 재빌드 불필요 (TypeScript 단일 변경). **작업지시자 인터랙션 검증** ✅ 통과 (페이지 16 자동번호 글상자 더블클릭 점프 부재 + 페이지 1 page 0 정상 진입 + 일반 글상자 기존 동작 보존 + 다른 객체 회귀 부재 + PR #795 셀 드래그 회귀 부재). CI ✅ Build & Test + CodeQL (js-ts/python/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_812_review.md`. 처리 보고서: `mydocs/pr/archives/pr_812_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 + 신규 인프라 도입 부재. **`feedback_hancom_compat_specific_over_general` 권위 사례 강화** — ppi=0 + cursorPage!=0 2중 가드 영역 영역 영역 좁힘 — 일반 글상자 + page 0 영역 회귀 부재. **`feedback_diagnosis_layer_attribution` 권위 사례 강화** — master page anchor (ppi=0) 모든 페이지 반복 표시 + cursor pageIndex = 0 본질 정확 진단 (Issue #686 e2e 측정 명시). **`feedback_visual_judgment_authority` 정합** — 작업지시자 인터랙션 검증 통과. **`feedback_pr_supersede_chain` 정합** — PR #795 (셀 드래그 보호) → **PR #812** (master page 글상자 더블클릭 jump 방지) 마우스 인터랙션 단계적 진전. |
| **PR #811 (closes #220 partial — 4단계)** | feat — F5 본문 블록 선택 + F3 영역 확장 | **완료 (옵션 A — 2 commits cherry-pick + 1 충돌 수동 해결 + 중복 제거 commit + no-ff merge `d8e641a4`, 작업지시자 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 **11번째 PR** — PR #786 → #787 → #788 → #794 → #795 → #796 → #807 → #808 → #809 → #810 → **#811**). 본질: 한컴 HWP F5/F3 블록 선택 영역 rhwp-studio 정합. **4단계 정합** (Issue #220 영역 영역 5단계 명시 — **문장 단계 누락**, 별 Issue #839 분리 후속). F5 본문 블록 선택 (본문 F5 → 블록 모드 + anchor 설정 → 화살표 영역 Shift 없이 선택 확장, F5 재입력/Esc → 모드 해제, 표 셀 F5 영역 기존 셀 선택 모드 보존 `isInCell() && !isInTextBox()` 분기). F3 4단계 확장 (F3 1회 단어 `findWordAt` + F3 2회 문단 + F3 3회 구역 + F3 4회 문서, phase 4 cap). 정정 (3 files): `cursor.ts` (+85) `_blockSelectionMode`/`_expandPhase` 상태 + `enterBlockSelectionMode`/`exitBlockSelectionMode`/`expandSelection` 메서드 + `isWordChar`/`findWordAt` 헬퍼 (Hangul AC00-D7AF + Hangul Jamo 3131-318E + Digit + Latin) + `input-handler-keyboard.ts` (+30/-2) F5 토글 (셀/본문 분기) + F3 확장 + Escape 분기. **Copilot 리뷰 반영 commit** (`59a01de4`): `enterBlockSelectionMode` 영역 `anchor = { ...this.position }` 강제 설정. **본 환경 충돌 수동 해결** (1 파일): `cursor.ts` 파일 끝 영역 영역 PR #794 (`findWordBoundaryForward/Backward`, Alt+Arrow 단어 이동 CharClass 5종 분류) + PR #811 (`findWordAt`, F3 단어 선택 isWordChar) 두 유틸 양측 보존. **정정 commit** (`dc6aa7a4`): wasm-bridge.ts 영역 `getSectionCount` 중복 정의 제거 — PR #808 (commit `ca729bdc`) 영역 영역 이미 추가됨 (라인 231 `this.doc?.getSectionCount() ?? 0` 보존), PR #811 측 (라인 409-411) 제거. **인프라 재사용** — isInCell/isInTextBox + enterCellSelectionMode + cursor.anchor/position + selectionRenderer.clear + WASM getParagraphLength/getParagraphCount/getTextRange/getSectionCount (PR #808) 모두 기존 — 신규 인프라 도입 부재. 본 환경 cherry-pick 2 commits 영역 영역 1 충돌 수동 해결 + 1 중복 제거 정정 commit. 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN, sweep 면제 (TypeScript 단일 영역 SVG 무영향 자명), WASM 재빌드 불필요 (TypeScript 단일 변경). **작업지시자 인터랙션 검증** ✅ 통과 (F5 본문 블록 모드 + 화살표 선택 확장 + F5 재입력/Esc 해제 + F3 4단계 확장 + phase 4 cap + 표 셀 F5 기존 셀 선택 모드 보존 + 기존 단축키 회귀 부재). CI ✅ Build & Test + CodeQL (js-ts/python/rust) + Canvas visual diff 통과. **별 Issue #839 분리** — F3 문장 단계 추가 (Issue #220 5단계 영역 영역 단어 → **문장** → 문단 → 섹션 → 문서, 한컴 표준 정합). Issue #220 OPEN 유지 — 별 Issue #839 close 시 동시 close 점검. 검토 보고서: `mydocs/pr/archives/pr_811_review.md`. 처리 보고서: `mydocs/pr/archives/pr_811_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 + 신규 인프라 도입 부재. **`feedback_hancom_compat_specific_over_general` 정합** — `isInCell() && !isInTextBox()` 분기 + Copilot 리뷰 anchor 강제 설정 영역 영역 영역 좁힘. **`feedback_diagnosis_layer_attribution` 권위 사례 강화** — F5 본문 블록 vs F5 셀 선택 두 본질 분리 + F3 4단계 vs Issue #220 5단계 (문장 누락) 점검. **`feedback_visual_judgment_authority` 정합** — 작업지시자 인터랙션 검증 통과. **`feedback_pr_supersede_chain` 정합** — PR #758 (F5 표 셀 도입) → **PR #811** (본문 F5 + F3) → **별 Issue #839** (F3 문장 단계 후속) 단계적 진전. **`feedback_close_issue_verify_merged` 정합** — Issue #220 영역 영역 4단계 정합만 → OPEN 유지 + 별 Issue #839 영역 영역 5단계 완성 시 close 점검. |
| **PR #810 (closes #792)** | feat — 메뉴 열린 상태 단일 키 hotkey 항목 활성 인프라 | **완료 (옵션 A — 2 commits cherry-pick + no-ff merge `e1be8d9e`, 작업지시자 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 **10번째 PR** — PR #786 → #787 → #788 → #794 → #795 → #796 → #807 → #808 → #809 → **#810**). 본질: PR #758 (5/10 머지) 영역 `table:cell-height-equal` (H) + `table:cell-width-equal` (W) 의 `shortcutLabel` 영역 한컴 메뉴 hotkey 의도 — rhwp-studio 영역 영역 메뉴 hotkey 인프라 미구현 영역 영역 단축키 미동작 (Issue #792, PR #758 후속). 정정 (`rhwp-studio/src/ui/menu-bar.ts` +26/-2, 1 file): `setupKeyboardClose` 영역 영역 hotkey 매칭 추가 — 메뉴 열린 상태 영역 영역 modifier 없는 단일 키 영역 `.md-shortcut` 텍스트 매칭 → 해당 `.md-item[data-cmd]` dispatch + 메뉴 닫기. **세 본질 분리** (Issue #792 명시): `shortcutLabel` (UI 라벨, command-palette/context-menu 표시) vs `shortcut-map.ts` (전역 단축키, Ctrl/Alt/Shift modifier) vs **메뉴 hotkey (모달, 메뉴 열린 상태 영역만 활성, 본 PR 영역 신규)**. **영역 좁힘 가드 (5중)**: modifier 키 동반 시 무시 (`e.ctrlKey || e.altKey || e.metaKey || e.shiftKey`) — 전역 shortcut-map 충돌 방지 + 단일 문자 키만 (`e.key.length === 1`) — Tab/Arrow/Function 키 제외 + disabled 항목 제외 (`:not(.disabled)`) + 대소문자 무시 (`.toUpperCase()`) + 메뉴 닫힌 상태 (`!this.openMenu`) early return — 일반 텍스트 입력 영향 부재. **Copilot 리뷰 반영 commit** (`3cdf7a6b`): hotkey 경로 영역 영역 `data-*` params 전달 — 메뉴 클릭 경로 영역 영역 동일 params 일관성 (`{ anchorEl: item, ...el.dataset 영역 cmd 제외 }`). **인프라 재사용** — MenuBar.openMenu + `.md-item[data-cmd]`/`.md-shortcut` DOM 구조 + dispatcher.dispatch + closeAll (모두 기존) — 신규 인프라 도입 부재 (menu-bar 구조 영역 hotkey 매칭 로직 추가만). 본 환경 cherry-pick 충돌 0건 (auto-merge). 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN, sweep 면제 (TypeScript 단일 영역 SVG 무영향 자명), WASM 재빌드 불필요 (TypeScript 단일 변경). **작업지시자 인터랙션 검증** ✅ 통과 (표 메뉴 H/W 항목 활성 + 메뉴 닫힌 상태 일반 텍스트 입력 회귀 부재 + Ctrl+H 전역 단축키 충돌 부재 + disabled 항목 실행 부재 + Escape 기존 동작 보존). CI ✅ Build & Test + CodeQL (js-ts/python/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_810_review.md`. 처리 보고서: `mydocs/pr/archives/pr_810_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 + 신규 인프라 도입 부재. **`feedback_hancom_compat_specific_over_general` 권위 사례 강화** — modifier/length/disabled/case/메뉴-닫힘 5중 가드 영역 영역 영역 좁힘 + 전역 shortcut-map 충돌 부재. **`feedback_diagnosis_layer_attribution` 정합** — shortcutLabel UI vs 메뉴 hotkey 모달 vs 전역 shortcut-map 세 본질 분리 (Issue #792 명시). **`feedback_visual_judgment_authority` 정합** — 작업지시자 인터랙션 검증 통과. **`feedback_pr_supersede_chain` 정합** — PR #758 (H/W shortcutLabel 도입, 단축키 미동작 발견) → **PR #810** (메뉴 hotkey 인프라 정합) (c) 패턴. **`feedback_small_batch_release_strategy` 정합** — 신규 인프라 (opt-in, 메뉴 닫힌 상태 영향 부재) 영역 영역 PATCH cycle 머지 정합. |
| **PR #809 (closes #791)** | feat — 쪽 새 번호로 시작: insertNewNumber WASM API + dialog UI | **완료 (옵션 A — 3 commits cherry-pick + 2 파일 충돌 수동 해결 + no-ff merge `47ef8fca`, 작업지시자 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 **9번째 PR** — PR #786 → #787 → #788 → #794 → #795 → #796 → #807 → #808 → **#809**). 본질: `page:new-page-num` (쪽 > 새 번호로 시작) stub 영역 실동작 구현 — PR #755 close (paragraph numbering `setNumberingRestart` 영역 잘못 매핑) 후속 정정. **두 본질 분리** (Issue #791 명시): paragraph numbering (1.1.1, 가/나) = `setNumberingRestart` (기존, 본 PR 무관) vs page number (쪽번호) = **`NewNumber` 컨트롤 영역 신규 `insertNewNumber` API** (본 PR). 정정 (5 files, +174/-1): Rust `src/document_core/commands/object_ops.rs` (+63) `insert_new_number_native` (find_control_text_positions 영역 삽입 인덱스 + paragraph.controls.insert + ctrl_data_records.insert(None) + char_offsets[safe_offset..] += 8 + control_mask |= 1u32 << 0x0012 + reflow_paragraph → recompose_section → paginate_if_needed → invalidate_page_tree_cache 후처리 + section_idx/para_idx 범위 가드) + `src/wasm_api.rs` (+21) `insertNewNumber` 바인딩 (start_num 0/>65535 영역 Err) + TypeScript `rhwp-studio/src/ui/new-number-dialog.ts` (신규 +65, ModalDialog 패턴, type=number min=1 max=65535 default=1, onConfirm 영역 wasm.insertNewNumber + eventBus.emit document-changed) + `core/wasm-bridge.ts` (+5) `insertNewNumber` 래퍼 + `command/commands/page.ts` (+19/-1) stub 영역 실커맨드 교체 (canExecute = ctx.hasDocument && !ctx.inTable + execute 영역 NewNumberDialog 호출 + 커서 위치 전달). **리뷰 반영 commits**: `05a3f5e6` (start_num 범위 검증 + 이벤트명 정정) + `f285df13` (Copilot 리뷰 !inTable 가드, 표 셀 삽입 금지, 한컴 호환). **PR #745 (Task #634 NewNumber Page 표시) 정합** — typeset.rs 영역 Control::NewNumber 스캔 + new_page_numbers 적재 + 페이지 렌더링 영역 해당 번호부터 표시 인프라 활용. **본 환경 충돌 수동 해결 (2 파일)**: page.ts import 양쪽 보존 (devel ColumnSettingsDialog + incoming NewNumberDialog) + wasm-bridge.ts 두 메서드 모두 보존 (devel getColumnDef + incoming insertNewNumber). object_ops.rs / wasm_api.rs auto-merge 성공 (impl 블록 끝 추가). **인프라 재사용** — find_control_text_positions + Control::NewNumber + AutoNumberType::Page + reflow/recompose/paginate + invalidate_page_tree_cache + ModalDialog + PR #745 typeset.rs NewNumber 표시 (모두 기존) — 신규 인프라 도입 부재. 본 환경 cherry-pick 3 commits 영역 영역 2 파일 충돌 수동 해결. 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN, cargo clippy --release --lib -D warnings ✅, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (신규 API opt-in 영역 영역 기존 동작 무영향 입증), WASM 빌드 4.5 MB 재빌드. **작업지시자 인터랙션 검증** ✅ 통과 (메뉴 → "쪽 → 새 번호로 시작" → dialog → 시작 번호 입력 → 페이지 번호 갱신 + 표 셀 내부 메뉴 비활성 !inTable + 범위 외 입력 dialog 유지 + 기존 페이지 번호 표시 회귀 부재). CI 결과 부재 (DIRTY 영역, 본 환경 자기 검증 + sweep + 시각 판정 영역 영역 보완). 검토 보고서: `mydocs/pr/archives/pr_809_review.md`. 처리 보고서: `mydocs/pr/archives/pr_809_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — Rust paragraph controls 변경 영역 영역 sweep 필수 + 170/170 same 회귀 부재 입증. **`feedback_pr_supersede_chain` 권위 사례 강화** — PR #755 (close, 잘못된 API) → **PR #809** (본질 정정) (a) 패턴 + PR #745 (NewNumber 표시) → 본 PR (NewNumber 삽입) 한컴 호환 단계적 진전. **`feedback_process_must_follow` 정합** — 인프라 재사용 + 신규 인프라 도입 부재. **`feedback_hancom_compat_specific_over_general` 정합** — !inTable 가드 + start_num 범위 검증 + safe_offset 가드 + section_idx/para_idx 범위 가드 영역 영역 영역 좁힘. **`feedback_diagnosis_layer_attribution` 정합** — paragraph numbering vs page number 두 본질 분리 (Issue #791 명시) + PR #755 잘못된 매핑 본질 진단. **`feedback_visual_judgment_authority` 정합** — 작업지시자 인터랙션 검증 통과. **`feedback_small_batch_release_strategy` 정합** — 신규 API opt-in (하위 호환 100%) 영역 영역 PATCH cycle 머지 정합. |
| **PR #808 (closes #784)** | fix — moveToDocumentEnd 다중 구역 문서 지원 | **완료 (옵션 A — 2 commits cherry-pick + 자기 정정 commit (Ctrl+↑/↓ 한컴 표준 정합) + no-ff merge `ca729bdc`, 작업지시자 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 **8번째 PR** — PR #786 → #787 → #788 → #794 → #795 → #796 → #807 → **#808**). 본질: `moveToDocumentEnd` 의 `const sec = 0` 하드코드 → 다중 구역 문서 (예: aift.hwp 3 구역) 에서 구역 0 의 paragraphCount 만 조회 → 잘못된 위치 → cursor rect 미발견 콘솔 오류 (Issue #784, PR #746 시각 검증 중 발견된 오랜 잠재 결함). 정정 (2 PR commits): `wasm-bridge.ts` (+4) `getSectionCount()` TypeScript 래퍼 (WASM 측 `hwpdocument_getSectionCount` 이미 노출 `src/wasm_api.rs:1236`) + `cursor.ts` (+6/-6) `moveToDocumentEnd` 의 `const secCount = this.wasm.getSectionCount(); const lastSec = secCount > 0 ? secCount - 1 : 0;` 영역 마지막 구역 인덱스 도출 + 빈 문서 폴백 가드. **함께 정리 — 자기 정정 commit `f2ab6e0a`**: 작업지시자 본 PR 시각 검증 중 발견 — `Ctrl+↓` 가 한 문단씩 이동이 아니라 문서 끝으로 이동. PR #746 (Task #260) 의 `Ctrl/Cmd+↑/↓` 매핑이 macOS 표준 (Cmd+↑/↓ = 문서 시작/끝) 만 정합, 한컴 표준 (Ctrl+↑/↓ = 이전/다음 문단) 어긋남. `cursor.ts` (+74) `moveToParagraphBoundary(direction)` 신규 — 본문 경로 (현재 문단 charOffset > 0 = 문단 시작 / 0 = 인접 문단 시작 / 구역 경계 = 인접 구역 정합) + 표 셀 경로 (cellParaIndex 이동) + atLineEnd = false 초기화 (PR #807 정합 보존). `input-handler-keyboard.ts` (+14/-2) ArrowUp/Down 분기: macOS Cmd+↑/↓ (e.metaKey && !e.ctrlKey) = 문서 시작/끝 (macOS 표준 보존) / Windows/Linux Ctrl+↑/↓ = moveToParagraphBoundary (한컴 표준). **인프라 재사용** — WASM `getSectionCount`/`getParagraphCount`/`getParagraphLength`/`getCellParagraphCount` 모두 기존, 신규 WASM API 도입 부재. 본 환경 cherry-pick 충돌 0건 (PR #807 atLineEnd 라인 + PR #808 본문 둘 다 보존). 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN, sweep 면제 (TypeScript 단일 영역 SVG 무영향), WASM 재빌드 불필요. **작업지시자 인터랙션 검증** ✅ 통과: (1) aift.hwp Ctrl+End 콘솔 오류 부재 + 마지막 구역 마지막 문단 끝 + 단일 구역 회귀 부재 + PR #807 atLineEnd 정합. (2) Ctrl+↓ 한 문단씩 이동 정상 (자기 정정 후). **별 Issue #837 분리** — 빈 문단 캐럿 미표시: 빈 문단에 마우스 클릭 + 화살표 진입 + Ctrl+↓ 모두 동일 본질 (글자 있는 문단은 정상). 본 PR 무관 **기존 결함** — WASM `getCursorRect(sec, para, 0)` 또는 caret-renderer 측 빈 문단 처리 결함 추정. 후속 정정 별 Issue. CI ✅ Build & Test + CodeQL (js-ts/python/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_808_review.md`. 처리 보고서: `mydocs/pr/archives/pr_808_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 + 신규 WASM API 도입 부재. **`feedback_hancom_compat_specific_over_general` 정합** — 빈 문서 폴백 + macOS/Windows-Linux 분기. **`feedback_diagnosis_layer_attribution` 정합** — 단일 구역 가정 본질 진단 (Issue #784) + Ctrl+↑/↓ 매핑 본질 (macOS 표준만 정합 / 한컴 표준 어긋남) + 빈 문단 캐럿 본질 (본 PR 무관 기존 결함). **`feedback_visual_judgment_authority` 권위 사례 강화** — **3 단계 발견 패턴**: (1) 본 PR 본질 결함 (PR #746 검증) + (2) 자기 정정 commit (PR #808 검증, Ctrl+↑/↓ 한컴 표준 어긋남) + (3) 별 Issue #837 (자기 정정 검증, 빈 문단 캐럿). **`feedback_pr_supersede_chain` 정합** — PR #746 → PR #808 본질 + 자기 정정 (매핑 본질 정정). |
| **PR #807 (closes #785)** | fix — 줄 끝 이동 후 줄 시작 이동 시 커서 미동작 수정 | **완료 (옵션 A — 2 commits cherry-pick + Task #516 진단 로그 제거 commit + no-ff merge `3960b3b6`, 작업지시자 인터랙션 검증 통과 2회)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 7번째 PR — PR #786 → #787 → #788 → #794 → #795 → #796 → **#807**). 본질: soft-wrap 줄 경계 영역 영역 `charEnd(line N) == charStart(line N+1)` 동일 위치 → End → Home 시 `compute_line_info_struct` 영역 영역 `char_offset >= line_char_starts[i]` 영역 영역 다음 줄 판정 → moveToLineStart 영역 영역 현재 위치 동일 → 미이동 (Issue #785). 정정 (`rhwp-studio/src/engine/cursor.ts` +28/-1, 1 file, cursor affinity 패턴 — Issue #785 3 옵션 중 옵션 A 채택): `atLineEnd: boolean` 플래그 도입 + `moveToLineEnd` 영역 영역 true 설정 + `moveToLineStart` 영역 영역 3중 가드 (`atLineEnd && charOffset === charStart && charOffset > 0`) → `getLineInfo(charOffset - 1)` 영역 영역 이전 줄 정보 조회 + `prevLineInfo.charEnd === pos.charOffset` 재확인 안전망 + 8개 이동 메서드 (moveTo / resetPreferredX / moveHorizontal / moveVertical / moveToDocumentStart / moveToDocumentEnd / moveToCellNext / moveToCellPrev) 영역 영역 `atLineEnd = false` 초기화. 표 셀 분기 정합 (`getLineInfoInCell` 사용). **Copilot 리뷰 반영 commit** (`011bab06`): moveVertical 영역 영역 atLineEnd 초기화 누락 정정 — End → ArrowDown → Home 결함 해결. **함께 정리 — Task #516 진단 로그 3개소 제거 commit** (`6aa4afd2`): 작업지시자 본 PR 시각 검증 영역 영역 발견 — 웹 콘솔 영역 영역 `[Task#516] applyOverlays...` / `[Task#516] JSON image ops=...` / `[Task#516] collected behind=...` 3 로그 출력. Task #516 (옵션 C HTML Hybrid 다층 레이어, PR `cc2a376b`) 영역 영역 "시각 판정 통과 후 제거" 명시 잔존 영역 영역 본 PR 머지 cycle 영역 영역 함께 정리. `rhwp-studio/src/view/page-renderer.ts` -7 (3 console.log 제거, 기능 주석 + console.warn JSON parse 실패 보존). **인프라 재사용** — getLineInfo + getLineInfoInCell + cursor affinity 패턴 (편집기 표준) 영역 영역 신규 인프라 격리 (`atLineEnd: boolean` cursor 영역만). WASM 변경 부재 + Rust 변경 부재. 본 환경 cherry-pick 충돌 0건 (auto-merge). 결정적 검증: tsc --noEmit ✅ (본 PR + 진단 로그 제거 후 재확인), cargo test --release ALL GREEN, sweep 면제 (TypeScript 단일 영역 영역 SVG 무영향 자명), WASM 재빌드 불필요 (cursor.ts 단일 변경 영역 영역 PR #799 빌드 pkg 사용). **작업지시자 인터랙션 검증** ✅ 통과 2회: (1) 본 PR — End→Home 본문/표 셀 + End→ArrowDown→Home moveVertical 초기화 + End→클릭→Home moveTo 초기화 + 강제 줄바꿈 (`\n`) 기존 동작 보존 + 기존 단축키 회귀 부재. (2) 진단 로그 제거 후 재검증 — `[Task#516]` 로그 출력 부재 + BehindText/InFrontOfText 그림 정상 표시 (Task #516 기능 영향 부재). CI ✅ Build & Test + CodeQL (js-ts/python/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_807_review.md`. 처리 보고서: `mydocs/pr/archives/pr_807_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 + 신규 인프라 격리 (`atLineEnd: boolean`). **`feedback_hancom_compat_specific_over_general` 정합** — 3중 가드 + charEnd 재확인 안전망 영역 영역 영역 좁힘. **`feedback_diagnosis_layer_attribution` 정합** — `compute_line_info_struct` `>=` 본질 진단 정확 (Issue #785 영역 영역 명시). **`feedback_visual_judgment_authority` 권위 사례 강화** — 본 PR 시각 검증 영역 영역 Task #516 진단 로그 부수 발견 + 함께 정리 (작업지시자 시각 검증 영역 영역 부수 결함 발견 패턴). |
| **PR #799 (분리 PR 1/3 — Issue #571)** | Task #571 — 문서 비교·이력 (compare + history) 및 diff-engine 정합 | **완료 (옵션 A — 5 commits cherry-pick + 충돌 수동 해결 + `.actual.svg` 5 파일 제거 commit + no-ff merge `6b9ad9e0`, 작업지시자 시각 판정 통과)** | 컨트리뷰터 @xogh3198 (thlee2) — **3번째 시도** (PR #571 5/4 close → PR #623 5/6 close → PR #799 5/11 머지, 본질적으로 첫 머지 시도). 작업지시자 "처음 기여" 표현 영역 영역 사이클 점검 결과 3번째 PR — `feedback_contributor_cycle_check` 메인테이너 필수 절차 입증. 본질: Task #571 분리 PR 1/3 — TypeScript/UI 본질만 (Rust 변경 부재, PR #571 base skew 해결). 정정 (20 files, +6202/-6): diff-engine (`src/compare/diff-engine.ts` +3105 정렬/비교 로직) + Compare/History 다이얼로그 (`compare-dialog.ts` +564 + `compare-result-window.ts` +526 + `history-dialog.ts` +530 + `styles/compare-dialog.css` +368) + IndexedDB (`history/idb-store.ts` +217 + `history/types.ts`) + session/types/debug (`compare/session.ts` +49 + `compare/types.ts` +124 + `compare-debug.ts` +28) + diff-location-label (`compare/diff-location-label.ts` +46) + WasmBridge 4 신규 API (`core/wasm-bridge.ts` +93/-2: `hasLoadedDocument`/`releaseDocument`/`getTableSignature`/`getParagraphStableId` graceful fallback) + 메뉴/단축키 (`index.html` +6 + `command/commands/edit.ts` +41 + `shortcut-map.ts` +2: Alt+Shift+V/Ctrl+Shift+H) + 문서 (`diff-engine-readme.md` +471). **본 환경 충돌 수동 해결**: `wasm-bridge.ts` 영역 영역 HEAD (devel) 의 PR #739/#750/#786 누적 변경 (`populateExternalImagesFromDevServer` Task #741 후속) 과 PR #799 신규 `hasLoadedDocument()` 영역 영역 비대칭 충돌 → 두 영역 모두 포함 정합 + `package-lock.json` 영역 ours 보존 (devel 측) → 빈 commit skip. **메인테이너 정정 commit** (`867a9078`): PR 영역 영역 잘못 staging 된 `tests/golden_svg/**/*.actual.svg` 5 파일 (-2611 lines) 제거 — `*.actual.svg` 는 테스트 실행 시 생성된 golden 비교용 임시 출력 파일이라 저장소 미포함. **WasmBridge graceful fallback 설계**: `getTableSignature`/`getParagraphStableId` 영역 영역 typeof 점검 + JS 폴백 — 본 PR 영역 영역 머지 가능 (분리 PR 3/3 영역 영역 `paragraph.rs::stable_id` Rust 측 구현 후속). **인프라 재사용** — Compare/History 영역 영역 신규 모듈 격리, WasmBridge 기존 위치 확장만. 본 환경 cherry-pick 5 commits 영역 영역 1 충돌 수동 해결 + 1 빈 commit skip. 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN, WASM 빌드 4.5 MB 재빌드, sweep 면제 (TypeScript 단일 영역 영역 SVG 무영향 자명). **작업지시자 시각/인터랙션 검증** ✅ 통과 (Alt+Shift+V 문서 비교 + Ctrl+Shift+H 문서 이력 + 메뉴 + WasmBridge graceful fallback 콘솔 오류 부재 + 기존 단축키 회귀 부재). **후속 분리 PR** (PR 본문 명시): 2/3 — Rust pagination/typeset + 3/3 — `paragraph.rs::stable_id` parser + WASM 노출. PR #799 close + Issue #571 영역 영역 후속 분리 PR 영역 영역 close 예정. 검토 보고서: `mydocs/pr/archives/pr_799_review.md`. 처리 보고서: `mydocs/pr/archives/pr_799_report.md`. **`feedback_contributor_cycle_check` 권위 사례 강화** — @xogh3198 3번째 시도. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_pr_supersede_chain` 정합** — PR #571 → #623 → **#799** 점진적 정합 (base skew 해결 + 분리 PR 가이드 정합). **`feedback_process_must_follow` 정합** — 분리 PR 1/3 (TypeScript) 머지 + 2/3, 3/3 (Rust) 후속 정합. **`feedback_visual_judgment_authority` 정합** — rhwp-studio editor 신규 기능 영역 영역 작업지시자 인터랙션 검증 통과. |
| **PR #797 (Refs #536)** | render — add Text IR v2 compatibility contract (P11) | **완료 (옵션 A — 1 commit cherry-pick + no-ff merge `098db015`, 시각 판정 면제)** | 컨트리뷰터 @seo-rii (Seohyun Lee) — **10번째 사이클** (Skia 핵심 컨트리뷰터, PR #165/#419/#456/#498/#599/#626/#720/#761/#769/#797). 본질: P11 단계 — Text IR v2 compatibility contract 추가 (Skia native raster Issue #536 트래킹의 단계적 진전 — P4 #599 → P5 #626 → P6 #720 → P8 #761 → P9 #769 → **P11 #797**). **중요**: 본 PR 영역 영역 GlyphRun 기본 경로 전환 부재 — TextRun fallback 유지 + Additive schema. 실제 GlyphRun 영역 영역 P12+ 영역 영역 분리 (Still designing 명시). 정정 (12 files, +1173/-35): `src/paint/json.rs` (+687/-14, schemaMinorVersion + feature negotiation + text metadata + special visual ops export) + `src/paint/builder.rs` (+151/-10, LayerBuilder 영역 영역 explicit visual op 생성) + `src/paint/layer_tree.rs` (+170/-1, Text IR v2 metadata + text_sources + TextSourceTable) + `src/paint/paint_op.rs` (+45, 4 신규 PaintOp variant: CharOverlap/TextControlMark/TabLeader/TextDecoration) + `src/paint/schema.rs` (+15/-2, schemaMinorVersion + resourceTableMinorVersion) + `src/paint/mod.rs` (+5/-2 재export) + **4 renderer 동기 정정** (svg_layer.rs +13/-4, canvas.rs +4, skia/renderer.rs +4, web_canvas.rs +10/-1 — 신규 special visual op skip + double-painting 방지) + `docs/text-ir-v2.md` (+67 migration contract 문서) + `README.md` (+2/-1, P11 범위 + GlyphRun 후속 분리 명시). **4 본질 원칙** (PR 본문): Compatibility first (모든 backend TextRun fallback 유지) + Additive schema (기존 consumer 미파괴) + Source traceability (text_sources + TextRun.source span) + Placement/cluster metadata (paintStyle/projectionKind/orientation/placement/clusterBasis/clusters/legacyVisuals). **신규 인프라**: PaintOp 4 신규 variant + PageLayerTree.text_sources + TextSourceTable + TextRun 7 신규 metadata + feature negotiation (used/required/optional/known) + docs/text-ir-v2.md migration contract. **Non-goals** (PR 본문 Still designing): GlyphRun eligibility / font resource table / cluster basis / fallback diagnostics 영역 영역 P12+ 분리. 본 환경 cherry-pick 충돌 0건 (auto-merge). **결정적 검증** — cargo build/test/clippy --release ALL GREEN (clippy -D warnings), **`cargo test --features native-skia --lib skia` → 28/28 PASS** (PR #769 인프라 보존), **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (Compatibility first 원칙 입증). **시각 판정 면제** (작업지시자 결정 — contract 정합 단계 + 결정적 검증 + sweep 통과). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_797_review.md`. 처리 보고서: `mydocs/pr/archives/pr_797_report.md`. **`feedback_contributor_cycle_check` 정합** — @seo-rii 10번째 사이클. **`feedback_image_renderer_paths_separate` 권위 사례 강화** — 4 backend (svg/canvas/skia/web_canvas) 동기 정정 영역 영역 special visual op skip (double-painting 방지). **`feedback_pr_supersede_chain` 권위 사례 강화** — PR #599 (P4) → #626 (P5) → #720 (P6) → #761 (P8) → #769 (P9) → **#797 (P11)** Issue #536 단계적 진전. **`feedback_process_must_follow` 정합** — 4 본질 원칙 + Still designing 명시 (P12+ 분리). **`feedback_visual_judgment_authority` 정합** — contract 정합 단계 영역 영역 시각 판정 면제 합리. |
| **PR #795 (closes #669)** | fix — 표 셀 내부 드래그 선택 시 셀 컨텍스트 이탈 방지 | **완료 (옵션 A — 1 commit cherry-pick + 충돌 수동 해결 + no-ff merge `58176ed3`, 작업지시자 웹 에디터 시각 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 5번째 PR — PR #794 후속). 본질: 표 셀 내부 텍스트 드래그 선택 시 hitTest 영역 영역 셀 빈 영역 영역 본문 레벨 위치 반환 → anchor(셀)↔focus(본문) 혼합 → updateSelection 영역 영역 selectionRenderer.clear 호출 → 선택 미렌더링 (Issue #669). 정정 (2 files, +14): input-handler.ts:1014 `updateTextSelectionDragFromPointer` 래퍼 안 영역 영역 셀 가드 13 라인 적용 (anchor 셀 내부 영역 영역 hit 가 같은 셀 컨텍스트 (parentParaIndex/controlIndex/cellIndex 모두 일치) 아니면 cursor.moveTo 건너뜀, 셀 내 선택 유지) + input-handler-mouse.ts 영역 영역 주석 갱신. **본 환경 충돌 수동 해결**: PR #795 base = 30351cdf 영역 영역 onMouseMove 영역 직접 hit + moveTo + 셀 가드 영역 영역 HEAD (devel) 영역 영역 PR #718 (Task #661, 5/9 머지) updateTextSelectionDragFromPointer 래퍼 사용 영역 영역 비대칭 충돌. 본 환경 영역 영역 셀 가드 영역 영역 input-handler.ts 래퍼 안 적용 → PR #718 정합성 보존 + Issue #669 본질 적용. **인프라 재사용** — cursor.getSelection + hitTestFromClientPoint + PR #718 래퍼 영역 영역 신규 인프라 도입 부재. 본 환경 cherry-pick 충돌 1건 수동 해결. 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅, WASM 빌드 4.68 MB 재빌드. **작업지시자 웹 에디터 시각 검증** ✅ 통과 (표 셀 내부 드래그 선택 + 본문 드래그 회귀 부재 + PR #718 자동 스크롤 + PR #781 회귀 부재). CI 결과 부재 (DIRTY 영역). 검토 보고서: `mydocs/pr/archives/pr_795_review.md`. 처리 보고서: `mydocs/pr/archives/pr_795_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용 (PR #718 래퍼 안 적용) 영역 영역 신규 인프라 도입 부재. **`feedback_hancom_compat_specific_over_general` 정합** — anchor 셀 내부 영역 영역 case 가드 영역 영역 회귀 위험 좁힘. **`feedback_diagnosis_layer_attribution` 정합** — 셀↔본문 혼합 선택 영역 영역 selectionRenderer.clear 호출 본질 정확. **`feedback_visual_judgment_authority` 정합** — 시각 검증 통과. **`feedback_pr_supersede_chain` 정합** — PR #718 (자동 스크롤) + PR #781 (drag-during-scroll) → PR #795 (셀 컨텍스트 이탈 방지) 드래그 인터랙션 점진적 진전. |
| **PR #794 (Part of #223)** | feat — Alt/Option+Arrow 단어 단위 커서 이동 | **완료 (옵션 A — 2 commits cherry-pick + 자기 정정 commit + no-ff merge `76e242c3`, 작업지시자 시각 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 4번째 PR — PR #788 후속). 본질: PR #746 (Ctrl/Cmd+Arrow) 후속 영역 영역 Issue #223 의 다음 단계 — macOS 표준 단어 이동 4 단축키 (Option+←/→ 단어 이동, Option+Shift+←/→ 선택 확장, Option+Backspace 이전 단어 삭제, Option+Delete 다음 단어 삭제). 정정 (2 files, +144/-2): `rhwp-studio/src/engine/cursor.ts` (+128) `CursorState.moveToWordBoundary` 신규 (본문 + 표 셀 양 경로 + 문자 클래스 5종 [공백/한글(완성형+자모+첫가끝)/Latin/Digit/Punct] + 슬라이스 50 char cap 성능 가드 + `findWordBoundary{Forward,Backward}` 유틸) / `rhwp-studio/src/engine/input-handler-keyboard.ts` (+16/-2) Alt+Arrow / Alt+Backspace/Delete 분기 + Alt 가드 추가 (`!e.key.startsWith('Arrow')`). Copilot 리뷰 반영 (commit `a32895b1`): 주석 수정 + 셀 텍스트 슬라이스 최적화. **자기 정정 commit** (`7c0418fc`) — 작업지시자 시각 검증 영역 영역 발견된 두 결함 정정: (1) "Alt+Delete 만 동작하지 않습니다" — shortcut-map.ts:97 영역 Alt+Delete → table:delete-col 매핑 (이전 등록) 영역 영역 일반 편집 영역 dispatcher silently fail (canExecute=inTable 차단) + return → switch case 'Delete' 도달 부재 → 단어 삭제 미동작. (2) "칸 지우기와 단어 지우기가 겹치는 군요!" 본질 충돌 영역 영역 작업지시자 결정 옵션 1 (현재 정정 유지) — 표 안/외 분기: 표 안 + Alt+Delete = table:delete-col (칸 지우기, 기존 동작 보존) / 표 외 + Alt+Delete = 다음 단어 삭제 (PR #794 본질) / Alt+Backspace/Alt+Arrow 항상 단어 삭제/이동. **인프라 재사용** — getTextRange + getTextInCell + getParagraphLength + cursor.moveHorizontal + setAnchor/clearSelection + deleteSelection (모두 기존) + 신규 인프라 격리 (cursor.ts 단일 영역 영역 moveToWordBoundary + findWordBoundary 유틸). 본 환경 cherry-pick 충돌 0건 (auto-merge). 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅, WASM 빌드 4.68 MB 재빌드. **작업지시자 시각 검증** ✅ 통과 (자기 정정 후 — 표 외 영역 Alt+Delete 단어 삭제 + 표 안 영역 칸 지우기 기존 동작 보존 + Alt+Backspace/Arrow 동작). CI ✅ Build & Test + CodeQL + Canvas visual diff 통과. **후속 분리** (PR 본문 명시): Issue #223 OPEN 유지 — 한컴 주요 단축키 등 후속 단계 잔존. 검토 보고서: `mydocs/pr/archives/pr_794_review.md`. 처리 보고서: `mydocs/pr/archives/pr_794_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_pr_supersede_chain` 정합** — PR #746 → PR #794 Issue #223 단계적 진전. **`feedback_process_must_follow` 정합** — 인프라 재사용 + 신규 인프라 격리. **`feedback_visual_judgment_authority` 권위 사례 강화** — 작업지시자 시각 검증 영역 영역 Alt+Delete 결함 + 본질 충돌 발견 영역 영역 자기 정정 (PR #740 패턴). **`feedback_diagnosis_layer_attribution` 정합** — shortcut-map dispatcher silently fail 영역 영역 본질 진단 (canExecute 차단 + return). |
| **PR #788 (closes #782)** | fix — 표 셀 검정 배경 렌더링 (pattern_type 검사 누락 수정) | **완료 (옵션 A — 1 commit cherry-pick + no-ff merge `329698f8`, 작업지시자 SVG 시각 판정 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 3번째 PR — PR #787 후속). 본질: PR #745 (5/10 머지) 영역 영역 작업지시자 시각 검증 영역 발견 결함 — `samples/aift.hwp` 페이지 2 일부 표 셀 검정 단색 fill (Issue #782). **비대칭 본질** (`feedback_image_renderer_paths_separate` 권위 사례 강화): layout/utils.rs (도형 경로) 영역 영역 이미 정합된 `pattern_type > 0` 가드 영역 영역 style_resolver.rs (표 셀 경로) 영역 영역 누락 본질을 정확히 발견. 정정 (1 file, +2/-1, 1 라인 가드): `src/renderer/style_resolver.rs:695` `resolve_single_border_style` 영역 영역 `if s.pattern_type > 0 || (s.background_color >> 24) != 0 { None }` 추가 — pattern_type > 0 시 background_color 영역 영역 패턴 배경색 (셀 단색 fill 아님) 영역 영역 검정 단색 fill 차단. **인프라 재사용** — BorderFill.fill.solid.pattern_type (기존 IR) + layout/utils.rs:126 동일 가드 (기존 패턴) 영역 영역 신규 인프라 도입 부재. 본 환경 cherry-pick 충돌 0건 (auto-merge). 결정적 검증: cargo build/test/clippy --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅ (sweep fixture aift 영역 영역 다른 페이지 영역 영역 영향 부재 — 본 결함 페이지 영역 영역 sweep 미포함), aift.hwp 페이지 2 SVG export 영역 영역 `<rect>` 검정 fill 0회 입증 (출력: `output/svg/pr788/aift_002.svg`, 291 KB). **작업지시자 시각 판정** ✅ 통과 — VSCode SVG 뷰어 dark mode 배경 영역 영역 처음에는 검정 셀 영역 영역 보였으나 본 환경 점검 영역 영역 SVG 자체 영역 영역 rect 검정 fill 0회 (정합) + text 검정 fill 783회 (정상 텍스트 색상) + svg root background 미설정 (투명) 입증. CI ✅ Build & Test + CodeQL + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_788_review.md`. 처리 보고서: `mydocs/pr/archives/pr_788_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 권위 사례 강화** — 동일 본질 영역 영역 다른 경로 (도형 정합 + 표 셀 누락) 비대칭 정정. **`feedback_pr_supersede_chain` (c) 패턴** — PR #745 시각 검증 영역 영역 별 결함 영역 별 PR. **`feedback_visual_judgment_authority` 정합** — Issue #782 영역 작업지시자 시각 발견 + 본 PR 정정 입증 (SVG export rect 검정 fill 0회 검증). **`project_output_folder_structure` 정합** — aift_002 SVG export 영역 영역 `output/svg/pr788/` 정합 위치 (초기 `/tmp/pr788_svg/` 위반 영역 영역 정정). |
| **PR #787 (closes #780)** | fix — 도구 모음 클릭 시 마우스 드래그 선택 보존 | **완료 (옵션 A — 1 commit cherry-pick + no-ff merge `51712958`, 작업지시자 웹 에디터 인터랙션 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 2번째 PR — PR #786 후속). 본질: 마우스 드래그로 텍스트 블럭 선택 후 서식 도구 모음 / 도구 상자 영역 영역 빈 영역 / 라벨 클릭 시 브라우저 기본 동작 (focus 이동) 영역 hidden textarea blur → cursor.hasSelection() false → format-char / adjustFontSize 조기 종료 (Issue #780). 대조: 키보드 선택 (Shift+Arrow) 후 동일 마커 클릭 시 정상 동작. 정정 (1 file, +11/-0): `rhwp-studio/src/main.ts` `initialize()` 안 #icon-toolbar / #style-bar 컨테이너 영역 영역 mousedown preventDefault — input/select 만 제외 (글꼴명/크기 입력 사용자 포커스 보존). **인프라 재사용** — #icon-toolbar/#style-bar 컨테이너 (기존) + 개별 버튼 mousedown preventDefault 패턴 (기존) 영역 영역 신규 인프라 도입 부재 (컨테이너 레벨 보강만). 본 환경 cherry-pick 충돌 0건 (auto-merge). 결정적 검증: tsc --noEmit ✅, cargo test --release ALL GREEN, **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅, WASM 빌드 4.68 MB 재빌드. **작업지시자 웹 에디터 인터랙션 검증** ✅ 통과 (마우스 드래그 선택 + 도구 모음 클릭 + input/select 보존 + 키보드 선택 회귀 부재). CI ✅ Build & Test + CodeQL + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_787_review.md`. 처리 보고서: `mydocs/pr/archives/pr_787_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — TypeScript 단일 영역 영역 Rust 렌더링 경로 무영향. **`feedback_process_must_follow` 정합** — 인프라 재사용. **`feedback_visual_judgment_authority` 정합** — 인터랙션 검증 통과. |
| **PR #786 (closes #767, closes #766)** | fix — Ctrl+M,M 수식 단축키 + 수식 삭제 오류 수정 (PR #739 후속, Chrome reserved shortcut 회피 영역 chord 키 Ctrl+N → Ctrl+M 변경) | **완료 (옵션 A — 2 commits cherry-pick + 자기 정정 4 commits + no-ff merge `ea9531d0`, 작업지시자 웹 에디터 시각 검증 통과)** | 컨트리뷰터 @oksure (Hyunwoo Park) — **20+ 사이클** 핵심 컨트리뷰터 (5/11 사이클 1번째 PR — PR #739 후속). 본 PR 본질: PR #739 (수식 신규 입력) 후속 발견 결함 2건 정정 — Issue #767 (chordMapN['m']/['ㅡ'] 매핑 추가) + Issue #766 (deleteEquationControl 신규 + 4개소 영역 equation 분기). cherry-pick 2 commits: `4a8772b6` + `a86668e9` (Copilot 리뷰 — reflow_paragraph_line_segs Equation 높이 반영). **본 환경 자기 정정 4 commits** — 작업지시자 웹 에디터 시각 검증 영역 영역 발견된 결함 영역 영역: (1) `7bc2dc08` setupGlobalShortcuts Ctrl+N chord 시작 보강 시도 — Chrome reserved shortcut 영역 영역 효과 없음 / (2) `fb1a22cb` 1번 revert / (3) `924ca0f4` chord 키 Ctrl+N → Ctrl+M 변경 (작업지시자 결정 — Chrome / Edge 영역 영역 Ctrl+N (새 창) 영역 영역 OS-level reserved shortcut 영역 영역 JS preventDefault() 차단 불가 영역 영역 chord 키 변경 영역 영역 회피, shortcutLabel 4개 갱신: insert/table/edit/page) / (4) `e906c1b8` Ctrl+M chord IME 합성 중 활성화 — 한글 IME 영역 영역 e.key === 'Process' 영역 영역 e.code (KeyM/KeyN/KeyS/KeyF/KeyK) 판별 영역 영역 chord 활성화 (chord 1번째 키 + 2번째 키 모두). 본 환경 cherry-pick 충돌 0건 (auto-merge 정합). 결정적 검증: cargo build/test/clippy --release ALL GREEN, tsc --noEmit ✅ (WASM 빌드 후 신규 deleteEquationControl API type 갱신), **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅, WASM 빌드 4.68 MB 재빌드. **작업지시자 웹 에디터 시각 검증** ✅ 통과 (수식 객체 삭제 + Ctrl+M,M 영문/한글 IME). CI ✅ Build & Test + CodeQL (js/ts/py/rust) + Canvas visual diff 통과. 검토 보고서: `mydocs/pr/archives/pr_786_review.md`. 처리 보고서: `mydocs/pr/archives/pr_786_report.md`. **`feedback_contributor_cycle_check` 정합** — @oksure 20+ 사이클. **`feedback_image_renderer_paths_separate` 정합** — Equation 컨트롤 전용 삭제 API 영역 영역 Shape/Picture 영역 정합. **`feedback_pr_supersede_chain` (c) 패턴** — PR #739 (수식 신규 입력) 머지 후 후속 결함 정정 영역 별 PR. **`feedback_diagnosis_layer_attribution` 정합** — Chrome reserved shortcut 영역 영역 JS 차단 불가 영역 영역 본질 진단 + chord 키 변경 영역 회피. **`feedback_visual_judgment_authority` 권위 사례 강화** — 작업지시자 시각 검증 영역 영역 결함 발견 + 자기 정정 4 commits 영역 영역 정합 회복 (PR #740 자기 정정 패턴). **`feedback_process_must_follow` 정합** — 본질 분리 (cherry-pick 2 commits 영역 본 PR 본질 + 자기 정정 4 commits 영역 후속 정정). |

## 작업 메모

### 5/10 사이클 종료 + 5/11 사이클 시작
5/10 사이클 영역 영역 27 PR (PR #720~#778+#781) 처리 완료. 5/11 사이클 영역 영역 첫 PR — PR #786 (PR #739 후속).

### Chrome reserved shortcut 학습
Chrome / Edge 영역 영역 OS-level reserved shortcut (Ctrl+N / Ctrl+T / Ctrl+W / Ctrl+Shift+N) 영역 영역 JS preventDefault() 차단 불가 — 향후 chord 키 영역 영역 reserved shortcut 회피 영역 영역 의도적 선택 필수.

### 한글 IME chord 키 처리 패턴
한글 IME 영역 영역 `e.key === 'Process'` 영역 영역 `e.code` (KeyM/KeyN/KeyS 등) 영역 영역 판별 — 본 PR 영역 영역 도입 영역 영역 향후 chord 키 영역 영역 정합 패턴.

### 분리 PR 가이드 정합 사례 (Task #571)
PR #571 영역 영역 base skew + 다중 본질 (TypeScript UI + Rust pagination + Rust stable_id) 영역 영역 분리 PR 1/3 (TypeScript) → 2/3, 3/3 (Rust) 가이드. PR #799 영역 영역 분리 1/3 완료. 후속 PR 2/3, 3/3 영역 영역 시각 판정 게이트 분리 + Issue #571 close 예정.

### `*.actual.svg` 잘못 commit 케이스
PR #799 영역 영역 컨트리뷰터 환경 영역 영역 테스트 실행 시 생성된 `tests/golden_svg/**/*.actual.svg` 5 파일 (+2611) 잘못 staging. golden 비교용 임시 출력 영역 영역 저장소 미포함. 차후 컨트리뷰터 안내 — 로컬 `.gitignore` 영역 영역 `tests/golden_svg/**/*.actual.svg` 추가 권장.
