# 오늘 할일 - 2026년 5월 4일

## M100 — v1.0.0 조판 엔진 (PR 처리)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #553** | Task #511: HWP3 Square wrap 렌더링 보완 6-13 + narrow zone 정정 | **Rollback (close + 재 PR 요청)** | 외부 컨트리뷰터 @jangster77 (Taesup Jang) 의 PR. 작업지시자 옵션 C (Task #525 호출 제거 유지 + Task #511 wrap_precomputed 인프라 통합) cherry-pick 진행 (3 commits). 결정적 검증 통과 (cargo test --lib 1118 / svg_snapshot 6/6 / clippy 0 / WASM 4,586,232 bytes). 그러나 **시각 판정에서 hwp3-sample5 page 4 + page 8 의 그림 + 문단 배치 결함** (Square wrap 그림 어울림 텍스트 y위치 영역, Task #546 trade-off) 수용 불가 판정. 작업지시자 결정으로 옵션 2 (rollback) 진행 — `git reset --hard b84c5e9` 로 local/devel 만 복원 (devel push 안 됨, 영향 0). PR #553 close + 컨트리뷰터에게 page 4/8 결함 정정 + HWP3 직렬화 고려 + 재 PR 요청 안내. PR 의 본질 정정 영역 (HWP3 파서 보완 6-13 + narrow zone + composer wrap_ref_span + floating image z-order + samples) 은 정합 — 재 PR 시 보존 권장. 검토 보고서: `mydocs/pr/archives/pr_553_review.md`. |
| **PR #551 Task #544** | 21_언어_기출 passage 글상자 우측 시프트 정정 (옵션 A2 cherry-pick, closes #544) | **완료 (핀셋 cherry-pick 머지 + push, PR #560)** | 작업지시자 보고 "21_언어_기출_편집가능본.hwp 1/2/4/5/7/8/11/13/14 페이지 글상자 우측 시프트". 이슈 #544 (closed) 재오픈 후 PR #551 잔존 cherry-pick. 당초 검토 옵션 A3 (`1934161f` Task #552 + `05beb208` Task #544 v2 + `84d1d4b2` Task #544 v3) 채택했으나 B1 진단 결과 본 devel 의 Task #479 미적용 (pre-#479 trailing-ls 항상 가산 모델) 확인 → #552/v3 cherry-pick 시 #479-style 분기 신설로 광범위 회귀 위험. **옵션 A2 (`05beb208` 단독, #547+#544(1) 통합) 로 변경 채택**. 본질: `paragraph_layout.rs` (1) `box_x = col_area.x` / `box_w = col_area.width` (margin 미적용, paragraph border outline = col_area 전체) + (2) `inner_pad` 분기 제거 (visible-stroke + bs=0 인 경우 `margin_left = box_margin_left + inner_pad_left` 이중 적용 → `margin_left = box_margin_left` 단일 적용). 측정 (페이지 4 [7~9]): 박스 left 128.51→117.17 (PDF 117.0), width 402.5→425.17 (PDF 425.1), 본문 첫 글자 '평' x 153.12→128.51 (PDF 128.5). 9 박스 모두 정합. 검증: cargo test --lib 1120 / test_544+test_547 +2 GREEN / clippy 0 신규 / 5 샘플 58 페이지 광범위 sweep 38 differ (의도된 paragraph border 정합 변경, visible-stroke paragraph text -11.33 px 좌측 시프트 의도) / 회귀 검출 가능 영역 (비-border paragraph text) 0 변경. 작업지시자 시각 판정 통과. cherry-pick `457d5f33` (Task #544 v2 Stage 2 from @planet6897 `05beb208`). devel merge 완료, 이슈 #544 close, PR #560 등록. 처리 보고서: `mydocs/pr/archives/pr_551_v3_544_a2_report.md`. 검토 문서: `mydocs/pr/archives/pr_551_review_v3_544_a2.md`. 잔존 미반영: #552 / v3 (#479 미적용 모델로 발현 안 함, 별도 사이클 결정 대기), #548 Phase C (별개 본질 [푸코] 페이지). |
| **#590** | exam_kor.hwp 17쪽 [A] 글상자 우측 6.5mm 치우침 — Square wrap 표가 horz_rel_to=단 속성 무시 | **완료 (cherry-pick 머지 + push)** | 작업지시자 보고 "exam_kor.hwp 17 페이지 [35~36] 문제 글상자 [A] 위치가 오른쪽으로 치우침". 분석 결과 `src/renderer/layout.rs:2285-2300` (Issue #480 도입 분기) 가 모든 Square-wrap 표를 무조건 문단 좌측 가장자리(`col_area.x + effective_margin`) 기준으로 강제 배치하며 `horz_rel_to` 속성을 무시. 문단 2 ParaShape `margin_left=1700, indent=+2000` → effective_margin = 24.67 px → 우측 24.7 px (=6.5mm) 시프트. 수식 검증: `table_x = 117.17 + 24.67 + 9.44 = 151.28 px` (SVG 실측치 일치). **패치**: 분기 가드에 `horz_rel_to=Para` 만 한정 → `Column/Page/Paper` 는 `compute_table_x_position` 명세 기반 분기로 fall-through. 검증: cargo test --lib 1125 GREEN / clippy 신규 0 / 5 샘플 56 페이지 sweep 차분 4 (모두 exam_kor: p14 [A] halign=Right + p17/18/19 [A]/[B] halign=Left) — 다른 4 샘플 32 페이지 byte-identical / p17 [A] 셀 좌측 151.28 → **126.61 px** (col_left + h_offset, IR 정합) / p14 [A] (halign=Right) 시각 미세 차이 (≈ flush → 2.5mm inset) 는 명세 정합 우위 (이전 동작은 inline_x_override 경로가 Right 정렬 시 h_offset 부호 모순) / hancomdocs PDF p24 와 시각 정합. 최종 보고서: `mydocs/report/task_m100_590_report.md`. |
| **PR #562 Task #555** | 옛한글 PUA → 자모 변환 후 폰트 매트릭스 (글자폭/줄간격) 갱신 (Task #528 후속, closes #555) | **완료 (cherry-pick 머지)** | 외부 컨트리뷰터 @planet6897 의 새 PR. Task #528 후속 결함 — `run.text` (PUA 1글자) 가 폰트 매트릭스 측정 (`estimate_text_width` 등 10건 호출처) 에 사용되어 자모 시퀀스 (3-4 char) 와 정합 안 됨. **옵션 A 적용**: `effective_text_for_metrics` 헬퍼 (`run.display_text.as_deref().unwrap_or(&run.text)`) + 호출처 10건 패치. 인덱싱 불변성 유지. 본질: `composer.rs:920` (LineSeg 검증) + `layout.rs:3444/3510/3516/3522` (Square wrap host / TAC leading) + `table_layout.rs:860/1659/1825/1851/1935` (셀 max width / inline shape text_before / 셀 분할 추적). char-by-char / partial 변환 케이스 5건은 `map_pua_old_hangul` 직접 적용. 검증: cargo test --lib 1123 / Task #555 단위 테스트 +3 GREEN / clippy 0 신규 / 13 fixture 481 페이지 광범위 sweep **481/481 byte-identical** (회귀 0). 결과 해석: visual char positioning 은 IR 기반 → 본 fix 는 conservative (비-PUA fallback + PUA 매트릭스 정확도 향상, 잠재적 결함 차단). 시각 판정 byte-identical auto-pass. 컨트리뷰터의 별도 fork branch (`pr-task555`) push + 새 PR — `feedback_per_task_pr_branch` + `feedback_no_pr_accumulation` 적용. 작업지시자 결정으로 PR #562 의 7 commits 모두 cherry-pick (옵션 A) — Task #544 v2 처리 영역 + Task #555 본질 모두 포함. 최종 보고서: `mydocs/report/task_m100_555_report.md`. |
| **PR #581** | iframe RPC race condition 정정 — `await initPromise` 패턴 (closes #522) | **완료 (cherry-pick 머지)** | 외부 컨트리뷰터 @oksure. `rhwp-studio/src/main.ts` 단일 파일 (+11/-4) — `const initPromise = initialize();` + 모든 RPC method 진입 시 `await initPromise`. 검증: tsc 0 errors. 처리 보고서: `mydocs/pr/archives/pr_581_report.md`. |
| **PR #582** | 분수형 위첨자 (sup) 베이스라인 정정 (closes #532) | **완료 (cherry-pick 머지)** | 외부 컨트리뷰터 @oksure. `src/renderer/equation/layout.rs` (+44/-7) — `sup_shift` 부호 분기 (tall sup 베이스라인 정합). 검증: cargo test --lib 1124 + 신규 회귀 테스트 `test_superscript_fraction_baseline` GREEN + WASM 4,588,360 bytes. 작업지시자 시각 판정: "svg 와 웹 에디터 모두에서 지수 처리가 개선되었음을 메인테이너가 확인". 처리 보고서: `mydocs/pr/archives/pr_582_report.md`. |
| **PR #583** | 그룹 내 그림(Picture) 직렬화 + 라운드트립 테스트 (PR #428 후속, Copilot review 반영) | **완료 (cherry-pick 머지)** | 외부 컨트리뷰터 @oksure. `src/serializer/control.rs:1276` 의 `serialize_group_child` `ShapeObject::Picture` 분기 — `HWPTAG_SHAPE_COMPONENT` + `HWPTAG_SHAPE_COMPONENT_PICTURE` records 추가. 검증: cargo test --lib 1125 + 신규 `test_roundtrip_group_picture_child` GREEN. 옵션 B (시각 판정 생략, 직렬화 영역은 결정적 라운드트립 테스트 충분). 처리 보고서: `mydocs/pr/archives/pr_583_report.md`. |
| **PR #578 vs #579** | 수식 키워드 토큰화 (Issue #576) — 협업 PR 시도 | **OPEN (양 컨트리뷰터 응답 대기)** | @planet6897 (#578: 158 fixtures sweep + 명시 4 키워드 `times/sim/TIMES/SIM`) vs @oksure (#579: `is_known_command()` + `longest_keyword_prefix_len()` 일반 인프라). PR #579 일반 알고리즘 회귀 위험 (`alphabet → alpha + bet`) 식별. **옵션 C (결합)** + **방법 1 (양 PR 동시 댓글로 협업 PR 권유)** — 본 사이클 첫 시도. 두 PR 모두 OPEN 유지. |
| **PR #558 Task #557** | npm/editor RPC + Wrapper 에 `exportHwpx` / `exportHwpVerify` 노출 (followup #407) | **완료 (cherry-pick 머지)** | 외부 컨트리뷰터 @johndoekim — **rhwp 첫 PR**. 6-layer 통합 (WASM → WasmBridge → RPC → Wrapper → 타입 → README). Stage 0 fail-first → Stage 4 green 의 TDD + 하이퍼-워터폴 정합 흐름. 5 commit cherry-pick (`bc4bc1d` ~ `72af00f`). **Stage 1 충돌 옵션 C 통합**: `rhwp-studio/src/main.ts` 의 RPC switch 에서 PR #581 (`await initPromise` 패턴, closes #522) 와 동일 영역 변경 — 단순 채택 시 race condition 회귀 + `case 'ready'` 중복 unreachable. 옵션 C 로 두 신규 case (`exportHwpx`/`exportHwpVerify`) 에 `await initPromise` 추가 + 중복 'ready' 제거. 검증: cargo test --lib 1125 (회귀 0) + tsc 0 errors. 시각 판정 생략 (API 노출 layer, WASM 핵심 + 렌더링 무영향). devel merge `4ae9be3`. Issue #557 close. 처리 보고서: `mydocs/pr/archives/pr_558_report.md`. |
| **PR #571** | "DIFF update" — 문서 비교 (rhwp-studio compare + history) + Rust pagination/typeset 보조 + paragraph.rs stable_id | **close (옵션 C — 분리 PR + base 동기화 권장)** | 외부 컨트리뷰터 @xogh3198 — **rhwp 첫 PR**. 표시 +6,375/-38 (29 file), 실제 base diff +6,423/-4,182 (66 file). PR base 가 `b84c5e9` (devel 9 commit 전) 로 base skew 발생, 컨트리뷰터의 `aae3c89` merge commit 이 Task #555/PR #582/#583 본질 정정을 사실상 revert (composer/tests.rs -69, equation/layout.rs -51, integration_tests.rs -312, serializer/control/tests.rs -79 등 600+ lines deletion). 추가로 컨트리뷰터의 의도된 Rust 변경 7 file 중 (1) `pagination/engine.rs` 의 `should_reserve_picture_height` 신규 분기 + `typeset.rs` 의 `normalize_floating_only_paragraph_height` 신규 함수가 시각 회귀 위험 영역 (Task #546/#510/#511 본질) — 시각 판정 게이트 필요, (2) `paragraph.rs::stable_id` 필드 추가했으나 parser+WASM 노출 부재로 항상 빈 문자열 (identity 전략 미동작). 한편 rhwp-studio TS 영역 (compare + history + ui, +6,000 lines) 본질은 우수 — alignment 다층화 (글로벌 앵커 → 구간 정렬 → 상대 구조 근접 → DP 비용), `CompareStrategy = 'identity'/'alignment'` 도메인 분리, 한국어 주석 정합. 옵션 C 채택: close + 3 개 분리 PR 권장 ((1) rhwp-studio TS 본질 / (2) Rust pagination 시각 판정 게이트 / (3) stable_id parser+WASM 노출 통합) + fork devel 의 base 동기화 권장. 처리 보고서: `mydocs/pr/archives/pr_571_report.md`. |
| **PR #586 Task #403** | "ffi csharp 구현" — Rust C ABI cdylib + C# P/Invoke wrapper (closes #403) | **완료 (cherry-pick 머지)** | 외부 컨트리뷰터 @nameofSEOKWONHONG — **rhwp 첫 PR**. Issue #403 (M100 — AI 워크로드 FFI 인터페이스) 의 첫 번째 사용 사례 (Rust cdylib + C# P/Invoke). 단일 commit `04eef95` cherry-pick (12 신규 파일, +1,137, 충돌 0). 본질: `bindings/Native/` (cdylib 공통 ABI: `rhwp_export_text` / `rhwp_export_markdown` / `rhwp_string_free`) + `bindings/csharp/RhwpNative.cs` (P/Invoke wrapper) + Stage 1-4 + 계획서/보고서 완비. **FFI 안전성 우수**: (1) Null pointer + UTF-8 검증 (`read_utf8`) (2) `std::panic::catch_unwind` 로 Rust panic 의 C 측 unwind 차단 (3) `CString::into_raw()` 반환 + 명시적 `rhwp_string_free` (호출자 책임) (4) NUL byte fallback (5) 에러 모델 단일 (`{"ok":true/false}`). C# 측: DllImport Cdecl + UTF-8 null-terminated + try/finally leak 안전 패턴. 본 환경 API 활용: 기존 `HwpDocument` native 메서드 (`extract_page_text_native`, `extract_page_markdown_with_images_native`, `get_control_image_*_native`, `get_bin_data_image_*_native`) — 본 환경 코드 변경 0. 검증: `bindings/Native/` cargo build (rhwp-native-ffi v0.1.0 + rhwp v0.7.9) + cargo test --lib 1125 (회귀 0) + cargo build --release + CI All SUCCESS. 시각 판정 생략 (FFI ABI 영역). devel merge `c7b7a2d`. Issue #403 close. 잠재 후속: workspace member 등록 / 단독 CI 워크플로 / 다른 언어 binding (Java/Python/Node) — 별도 사이클. 처리 보고서: `mydocs/pr/archives/pr_586_report.md`. |
| **PR #563 Refs #143** | "fix: LaTeX 분수 수식 미리보기 파싱 보정 (#143 1차)" — `\frac{a}{b}` + `\sqrt[n]{x}` 입력 미리보기 지원 (Refs #143) | **완료 (cherry-pick 머지)** | 외부 컨트리뷰터 @cskwork (donga-csk) — **rhwp 첫 PR**. Issue #143 (M100 — LaTeX 듀얼 토크나이저 방식, OPEN) 의 **1차 보정** (전체 듀얼 파서 미구현, `Refs` 만, `closes` 아님). PR 자체 한정으로 hybrid 단일 토크나이저 방식 채택 — 기존 `tokenizer.rs` 에 `read_latex_command` 추가 (백슬래시 + alphabet) + 기존 `parser.rs` 에 `\frac` / `\sqrt[n]` 분기 추가. 단일 commit `d5c7a22` cherry-pick (8 file, +333, **orders 충돌 1 옵션 C 통합** — 본 환경 PR 처리 표 채택, 컨트리뷰터의 "수식 파서" 섹션 사후 PR #563 항목으로 통합). 코드 안전성: (1) 백슬래시 + alphabet 만 LaTeX 인식 — 한컴 동작 보존 (2) 기존 `SQRT`/`OVER`/`MATRIX` 분기 그대로 + `\frac` 만 신규 FRAC 분기 (3) `\sqrt[n]{x}` LBracket 분기만 추가 — `SQRT(n) of x` 동작 보존 (4) 회귀 테스트 4개 (test_latex_command_prefix + test_latex_frac + test_latex_quadratic_slice + test_latex_sqrt_with_bracket_index). Stage 1 + 계획서/구현계획서/최종보고서 + 시각 검증 스크린샷 완비. 검증: cargo test --lib 1129 (+4 GREEN, 회귀 0) / equation 77 / svg_snapshot 6/6 / clippy 0 / CI All SUCCESS (Canvas visual diff 포함). **메인테이너 시각 판정 통과** (`mydocs/working/task_m100_143_stage1/latex_equation_preview.png` — `\frac{1}{2}` 분수선 + `x=\frac{-b \pm \sqrt{b^2}}{2a}` 분수+루트+± 모두 정합 렌더링). base=main 이라 BLOCKED 였으나 cherry-pick 우회 + 다음 PR 시 base=devel 안내. 후속: Issue #143 의 듀얼 토크나이저 (`latex_tokenizer.rs` + `latex_parser.rs` + UI 모드 토글) 는 별도 사이클. 처리 보고서: `mydocs/pr/archives/pr_563_report.md`. |
| **PR #587** | "fix: 문자 컨트롤 0x18/0x1E 매핑 swap (하이픈 ↔ 묶음 빈칸)" — HWP 5.0 스펙 표 7 정합 swap | **완료 (cherry-pick 머지)** | 외부 컨트리뷰터 @planet6897. 본 환경의 0x18/0x1E 매핑이 HWP 5.0 스펙 표 7 과 정반대 swap 되어 있던 결함 정정 (코드 24=하이픈, 코드 30=묶음 빈칸). 결정적 증거: 본 환경 자체 테스트 주석 (`src/parser/body_text/tests.rs:117,120`) 이 이미 정답을 가리키고 구현만 swap 되어 있던 상태. 단일 commit `1cbf7c8` cherry-pick (2 file, +10/-10, 충돌 0). 본질: `body_text.rs:328-348` (`parse_para_text` 의 0x18/0x1E text.push swap) + `tags.rs:128-131` (`CHAR_HYPHEN`/`CHAR_NBSPACE` 상수 swap, 외부 참조 0건). 검증: cargo test --lib 1129 (회귀 0) / parser::body_text 22 / svg_snapshot 6/6 / clippy 0 / CI All SUCCESS. **시각 회귀 quantitative 측정 100% 재현**: `>-</text>` 글리프 출현 횟수 — exam_eng 366→39 (-327), exam_kor 444→33 (-411), exam_science 37→18 (-19), exam_math 42→42 (변화 없음, 0x1E 미사용), exam_social 2→2 (동일) 모두 PR 본문 표와 일치. **메인테이너 시각 판정 ★ 통과** (svg + 웹 에디터 모두) — exam_eng/exam_kor "1." 문제 번호 다음의 잘못된 하이픈 → 묶음 빈칸 정합 회복. WASM 빌드 (4,582,545 bytes, 직전 4,588,360 대비 -5,815) + studio sync 완료. before/after SVG 자료: `output/svg/pr587_before/{exam_eng,exam_kor}/`, `output/svg/pr587_after/{exam_eng,exam_kor,exam_math,exam_science,exam_social}/`. 처리 보고서: `mydocs/pr/archives/pr_587_report.md`. |

## 작업 메모

### PR #553 (rollback)
- PR #553 의 fork devel 동기화 정합 (분기점 `b84c5e9` = 본 환경 devel) — PR #538/#551 의 fork 누적 패턴과 다른 모범 사례. 컨트리뷰터에게 인정.
- Task #546 trade-off 영역 (HWP3 Square wrap 그림 아래 텍스트 y위치) 의 정정이 본 사이클의 잔존 영역 — 페이지네이션 통합 정리 task 의 핵심
- HWP3 직렬화 (HWP3 → HWP3 round-trip) 까지 고려한 본질 정정이 향후 정합한 방향 (작업지시자 의견)
- local/devel = devel = origin/devel = `b84c5e9` 정합 상태 유지

### PR #551 Task #544
- 본 cherry-pick 은 PR #551 의 세 번째 핀셋 (Task #525 / Task #528 다음). PR #551 잔존 ~50 task 중 Task #544 만 우선 처리.
- B1 진단의 핵심 발견: 본 devel paragraph_layout.rs 의 trailing-ls 모델이 컨트리뷰터 fork (#479 적용) 와 다름. 이슈 #544 본문 측정값 (rhwp box top y=224.4) 은 컨트리뷰터 fork 기준 → 본 devel 에서는 box top y=233.97 ≈ PDF 233.8 이미 정합 ((2) y 시프트 발현 안 함). 사용자 보고 "오른쪽으로 밀려있음" 은 (1) box_x/w 단독.
- PR #551 잔존 처리 사이클의 분리 단위: Task #525 (1차) / Task #528 (2차) / Task #544 (3차, 본 사이클). 다음 사이클 후보: Task #547 (이미 v2 에 통합됨 → 별도 처리 불필요), Task #548 (별개 본질), Task #552 / v3 (#479 모델 도입 결정 시 일괄), 기타 Task #517~#523 등.

## 메모리 정합

- `feedback_visual_regression_grows` — 시각 판정 게이트 정합 운영 (미통과 시 머지 안 함, 본 사이클 정합한 운영)
- `feedback_v076_regression_origin` — 작업지시자 직접 시각 판정으로 page 4 / page 8 결함 식별
- `feedback_pdf_not_authoritative` / `reference_authoritative_hancom` — 한컴 2010/2020 + 작업지시자 시각 판정 권위
- `feedback_pr_comment_tone` — PR close 댓글 차분/사실 중심 + 컨트리뷰터의 정합한 영역 인정 + 재 PR 톤
- `feedback_check_open_prs_first` — 사용자 보고 (글상자 시프트) 즉시 PR #551 잔존 + 이슈 #544 매핑
- `feedback_essential_fix_regression_risk` — 옵션 A3 → A2 변경 사례. trailing-ls 모델 전환 위험 인지 후 최소 fix 채택.
- `feedback_rule_not_heuristic` — paragraph border outline = col_area 산식 단일 룰 (분기 없음).
- `feedback_local_task_branches_origin_backup` — `devel-backup` / `origin/local/task544_v[1-3]` 보존 유지.
- `feedback_no_pr_accumulation` — Task #544 처리 후속에 작업지시자 직접 피드백. closed PR (#551) 잔존 cherry-pick 은 새 PR 로 등록. Task #548 (PR #561) / Task #555 (PR #562 예상) 첫 적용.
- `feedback_per_task_pr_branch` — 각 PR 마다 별도 fork branch (`planet6897:pr-task{N}`). `planet6897:devel` 단일 branch 누적 push 시 GitHub "같은 head→base = 단일 PR" 룰로 별도 PR 등록 불가. Task #548 PR 등록 시 발현, force push + 분리 push 후 신설.
