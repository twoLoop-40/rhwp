# 2026-05-20 — 오늘 할일

## M100 — PR 처리 (메인테이너)

### PR #1011 — 쪽 테두리 포맷별 분리 + cover logo overlap 해소 (외부 기여 @jangster77, closes #1006)

- [x] PR 정보 + 사이클 점검 (#997→#999→#1005→#1009(보류)→**#1011** 마지막, paper_based outline 회귀 사이클 종결)
- [x] devel 동기화 (`9190dea8`) + 본질 커밋 식별 (`67f50f43` 단일)
- [x] PageBorderBasis enum + clip 정책 변경 분석 (header 제거 / footer 유지)
- [x] **매직넘버/하드코딩 감사** (작업지시자 요청): 신규 0건 / 제거 3건 / 잔존 1건(`footer_inside = attr & 0x04`, 후속 정리 권고)
- [x] 검토 문서 `pr_1011_review.md` → 작업지시자 옵션 A 승인
- [x] cherry-pick `aa8160c2` (작성자 Taesup Jang, 충돌 없음, fixture test-image.hwp/.hwpx 포함)
- [x] 검증: cargo test 1307, clippy -D, **fmt 0**
- [x] sweep 7 fixture (BEFORE devel `9190dea8` ↔ AFTER): 일반 5종(sample10/exam_kor/exam_math/aift/biz_plan) **diff=0 무회귀**, sample16-hwp5/hwp3 외곽선만 paper-edge 이동 (텍스트 무변동, 비-도형 diff=0)
- [x] 산출물 `output/poc/pr1011/{before,after}/` 규약 배치
- [x] WASM 빌드 (Docker, 4.83MB) + rhwp-studio/public 동기화
- [x] 작업지시자 시각 판정 통과 (cover logo overlap 해소 + HWP3 외곽선 paper-edge 정합 + 페이지 번호 외곽선 바깥 + 일반 HWP5 무회귀)
- [x] no-ff merge → devel push
- [x] PR #1011 close + 이슈 #1006 close (origin/devel 반영 검증 후)
- [x] 검토/보고서 archives 이동, 임시 브랜치 정리

핵심: **paper_based outline 회귀 사이클 종결** (task877→#920→#956→#987→#1005→#1006).
단일 `attr & 0x01` 비트 해석 모호성을 `PageBorderBasis` enum + parser 단계
명시 주입(PaperBased 통합)으로 책임 분리 — interpretation → parser,
사용 → renderer. 작업지시자 Hancom Office close-up 시각 판정 기반으로
spec ≠ 한컴 실제 동작 정책 명문화 (`feedback_visual_judgment_authority`
권위). selective clip(header 제거/footer 유지) case-specific
(`feedback_hancom_compat_specific_over_general`). 매직넘버 감사 결과
**신규 0/제거 3** — 본 PR 은 매직넘버 측면에서 개선 PR.
test-image fixture 추가로 그림 wrap 4종 회귀 가드 영구화.

보고서: `mydocs/pr/archives/pr_1011_report.md`

---

## @jangster77 PR 시리즈 (5/17~5/20) 5/20 시점 현황

| PR | closes | 본질 | 상태 |
|----|--------|------|------|
| #997 | #994 | sample16 line_segs 누락 겹침 (composer word wrap) | 머지 `f44fd446` (5/19) |
| #999 | #998 | sample16 페이지수 HWP3 정합 64 (CHARS_PER_LINE 45 + spacing_before=0) | 머지 `c58c198f` (5/19) |
| #1005 | #1001 | 한컴 정합 종합 격차 A/B/C (D revert) — PageBorderBasis 도입 전단계 | 머지 `f35a0a59` (5/19) |
| #1009 | #1007 | 페이지 강제 나눔 vpos reset | **보류 옵션 B** (base 부정합 64→65 회귀, 5/19) |
| **#1011** | **#1006** | **쪽 테두리 PageBorderBasis enum + header clip 제거 (회귀 사이클 종결)** | **머지 (5/20)** |

- @jangster77 24+ 사이클 + 본 시리즈 5건 (#997~#1011)
- 매 PR 옵션 A 또는 B + cherry-pick (원작자 메타데이터 보존) + sweep + 작업지시자 시각 판정
- #1009 만 base 부정합으로 보류 (#999/#1005 가 이미 67→64 달성, #1009 적층 시 64→65 over-split). 컨트리뷰터 rebase 후 재제출 대기
- 회귀 사이클 종결 (task877→#920→#956→#987→#1005→**#1006**) — 단일 비트 해석을 포맷별 enum 책임 분리로 해소
- 잔존: #1009 rebase, footer_inside 일관성 정리(별도 issue), test-image paragraph 텍스트 라벨 누락 (별도 root cause)

---

### PR #1015 — wrap 옵션 paragraph 라벨 텍스트 y 위치 정정 (외부 기여 @jangster77, closes #1012)

- [x] PR 정보 + 사이클 점검 (#1011 잔존 직접 후속, test-image 라벨 결함 해소)
- [x] devel 동기화 (`850cfb54`) + 본질 커밋 식별 (`002d4d24` 단일)
- [x] paragraph_layout.rs spacing_before=0 + 4중 AND 가드 fallback 분석 (add-only 14줄)
- [x] 검토 문서 `pr_1015_review.md` → 작업지시자 옵션 A 승인
- [x] cherry-pick `b808b996` (작성자 Taesup Jang, 충돌 없음, fixture #1011 중복 자동 해소)
- [x] 검증: cargo test 1307, clippy -D, **fmt 0**
- [x] sweep 10 fixture (BEFORE devel ↔ AFTER): test-image 타깃 diff=1(라벨 y=143.6→346) + **wrap shape 보유 4종(sample16-hwp5/hwp3, hy-001 HWPX/HWP5) 전부 diff=0** + 일반 4종(exam_kor/math/aift/biz_plan) diff=0 — **쟁점 A/B 회귀 0 입증**
- [x] 산출물 `output/poc/pr1015/{before,after}/` 규약 배치
- [x] WASM 빌드 (Docker, 4.83MB) + rhwp-studio/public 동기화
- [x] 작업지시자 시각 판정 통과 (test-image 라벨 Picture 영역 아래 정합)
- [x] no-ff merge → devel push
- [x] PR #1015 close + 이슈 #1012 close (origin/devel 반영 검증 후)
- [x] 검토/보고서 archives 이동, 임시 브랜치 정리

핵심: **#1011 잔존 직접 해소** — 기존 라인 990 분기(spacing_before > 0)
와 대칭 보완 add-only fallback 14줄. 4중 AND 가드(start_line==0 +
spacing_before==0 + is_column_top + para_index==0) 로 영역 매우 좁힘.
검토 단계 우려한 쟁점 A(공통 경로 8+ 호출 사이트)/B(다른 wrap 모드)를
wrap shape 보유 fixture 4종(sample16, hy-001 HWPX·HWP5) 포함 sweep 10
fixture 으로 **회귀 0 입증** (`feedback_fix_scope_check_two_paths` 권위
사례). z-order 부 결함도 본 fix 후 시각 영역 분리로 자동 해소.

보고서: `mydocs/pr/archives/pr_1015_report.md`

---

### PR #1018 — Task #1016: Resolve image payloads in PaintOp::Image (외부 기여 @postmelee, Refs #1016, Follows #976, Related #1017)

- [x] PR 정보 + 사이클 점검 (@postmelee 15+ PR, #976 직접 후속, #1019 시리즈)
- [x] devel 동기화 (`71aedda9`) + 본질 커밋 식별 (`46a54709` 단일, 24파일 +1861/-264)
- [x] PaintOp::Image 스키마 변경 + image_resolver 신규 모듈 분석
- [x] 검토 문서 `pr_1018_review.md` → 작업지시자 옵션 A 승인 (광범위 표면 sweep 신중)
- [x] cherry-pick `3aeaa5ba` (작성자 postmelee 보존, orders/20260520.md `--ours` 충돌 해소)
- [x] 검증: cargo test 1307, `cargo test --test issue_938` 3건, clippy -D, **fmt 0**
- [x] **sweep 10 fixture (BEFORE devel ↔ AFTER): 전부 diff=0** (복학원서/sample16-hwp5/hwp3/hy-001 HWPX/HWP5/exam_kor/math/aift/biz_plan/test-image) — SVG 출력 100% 동일 입증
- [x] 산출물 `output/poc/pr1018/{before,after}/` 규약 배치
- [x] WASM 빌드 (Docker, 4.83MB) + rhwp-studio/public 동기화
- [x] **작업지시자 시각 판정 생략 통과** (sweep diff=0 정량 입증으로 대체)
- [x] no-ff merge → devel push
- [x] PR #1018 close (이슈 #1016 — Refs 관계, OPEN 유지 결정 필요)
- [x] 검토/보고서 archives 이동, 임시 브랜치 정리

핵심: **`feedback_image_renderer_paths_separate` 본질적 해소** —
메모리 룰 도입(Task #514/#516) 이래 가장 직접적 아키텍처 정정. 5+ renderer
별 사본(svg/canvas/web_canvas/skia/paint/json)이 BMP/PCX/JPEG 워터마크
판정을 분산 → `LayerBuilder` 단계 단일 `image_resolver` 진입점으로
통합 + `PaintOp::Image.resolved` 옵션 필드(schema minor 12→13, 하위호환).
광범위 표면(24파일, 7개 renderer)에도 sweep 10 fixture 전부 diff=0 —
**아키텍처 정정 + 시각 호환성 100% 유지** 모범 사례. PR scope 좁힘
(z-order는 #1017로 분리). 회귀 테스트 `tests/issue_938.rs` 3건 영구화
(overlay/svg/PageLayerTree resolved watermark contract).

보고서: `mydocs/pr/archives/pr_1018_report.md`

---

### PR #1019 — Task #975: PageBackground fill mode + RealPic watermark tone (외부 기여 @postmelee, Refs #975) — **보류 (옵션 B)**

- [x] PR 정보 + 사이클 점검 (@postmelee #976→#1018→#1019 시리즈)
- [x] devel 동기화 (`84246b2a`) + 본질 커밋 3개 식별 (e57fdb97 + 2128e9fc + d58450e3)
- [x] 2 fix 분석 (PageBg fill mode + RealPic preset tone 4-param + 3x3 affine)
- [x] 검토 문서 `pr_1019_review.md` → 작업지시자 옵션 A 승인 (조건부 검증)
- [x] cherry-pick 3 순차: 1차(orders --ours), 2차(1줄), 3차(**svg.rs 275줄 충돌** — #1018 helper 4개 재정의 → `-X theirs` + Python 스크립트로 중복 제거 + 신규 2개 보존 + fmt amend)
- [x] 검증: cargo test 1312, realpic_watermark 2, issue_938 3(#1018 정합 보존), svg_snapshot 8, clippy -D, fmt 0
- [x] sweep 10 fixture (BEFORE devel ↔ AFTER): 전부 diff=0 (복학원서 binary identical) ✅
- [x] WASM 빌드 4.99MB (+160KB RealPic correction matrix) + rhwp-studio 동기화
- [x] 작업지시자 시각 판정: **rhwp-studio 워터마크 효과 정상** ✓, **SVG 출력에서 워터마크 효과 미적용 + 흰색 투명 미처리 발견** (BEFORE/AFTER 동일 — devel 기존 SVG 경로 결함)
- [x] 작업지시자 결정 → **옵션 B (수정 요청/보류)**
- [x] cherry-pick 롤백 + WASM devel(4.83MB) 복구 → rhwp-studio 재동기화
- [x] PR #1019 수정 요청 코멘트 (PR/이슈 #975 **OPEN 유지**)
- [x] 검토/보고서 archives 이동, 산출물 output/poc/pr1019 보존

핵심: PR 2 fix 설계(PageBg fill mode + RealPic 4-param + 3x3 affine bake)
+ #1018 적층 호환성 정리(svg.rs 중복 helper 4개 제거) + 자기 검증 견고.
다만 작업지시자 시각 판정으로 **devel 기존 SVG 경로 결함** 발견 — SVG
출력 워터마크 효과 미적용 + 흰색 영역 투명 미처리 (canvas/web_canvas는
정상). PR 본질 "renderer들이 같은 resolved payload 공유 + tone 정합"
이라면 SVG 정합 포함되어야 함 → scope 확장 요청. 컨트리뷰터에게 최신
devel rebase + 신규 helper image_resolver 이동 + SVG 정합 추가 + fixture
명시 요청. `feedback_visual_judgment_authority` 권위 (sweep diff=0 정량
입증에도 시각 판정으로 SVG 결함 발견).

보고서: `mydocs/pr/archives/pr_1019_report.md`

---

### PR #1020 — Task #727: PUA U+F02B1~F02C4 사각 안 숫자 매핑 제거 + 함초롬바탕 fallback chain 확장 (외부 기여 **@HaimLee-4869 첫 기여**, closes #727)

- [x] PR 정보 + 사이클 점검 (@HaimLee-4869 **첫 기여**, PR 본문 매우 충실 — Investigation PR 가이드 정합)
- [x] devel 동기화 (`321ffb0d`) + 본질 커밋 식별 (`51461af1` 단일)
- [x] PUA 매핑 20개 entry 제거(`0xF02B1~F02C4 → \u{2460}-\u{2473}`) + generic_fallback chain 4곳 함초롬바탕 family 6개 추가 분석
- [x] 검토 문서 `pr_1020_review.md` → 작업지시자 옵션 A 승인
- [x] cherry-pick `1588c1e1` (작성자 @HaimLee-4869 보존, 충돌 없음, fmt amend)
- [x] 검증: cargo test 1307, svg_snapshot 8(golden 7개 갱신 정합), clippy -D, **fmt 0**
- [x] sweep 10 fixture (BEFORE devel ↔ AFTER): mel-001(HWP/HWPX/HWP5) + table-vpos-01(HWPX/HWP) + biz_plan/복학원서/exam_kor/aift/sample16-hwp5 — **font-family 패턴만 변경, 텍스트 좌표/내용 무변동, 비-도형 diff=0** 입증
- [x] 산출물 `output/poc/pr1020/{before,after}/` 규약 배치
- [x] WASM 빌드 (Docker, 4.83MB) + rhwp-studio/public 동기화
- [x] 작업지시자 시각 판정 통과 (table-vpos-01.hwpx p.5 사각 안 1~9 한컴 정합 + mel-001 회귀 부재)
- [x] **후속 이슈 #1023 등록** (OVERLAP 10~12 두자리 숫자 글자겹치기 — `<hp:compose composeType="OVERLAP">` SVG 미출력, composer.rs::inject_char_overlap_text 산출물 누락)
- [x] no-ff merge → devel push
- [x] PR #1020 close + 이슈 #727 close (origin/devel 반영 검증 후)
- [x] 검토/보고서 archives 이동, 임시 브랜치 정리

핵심: **첫 기여 @HaimLee-4869** 모범 PR — Root cause + Fix + 회귀 영향 +
검증 + OVERLAP 별건 분리 + 정공법 후보 + Investigation PR 가이드 정합.
PUA 매핑 제거(raw passthrough) + 함초롬바탕 fallback chain 확장(한글/영문
family name 둘 다, `feedback_font_alias_sync` 정합)으로 사각 안 1~9 한컴
정합 달성. 검토 우려한 쟁점 A(mel-001 회귀 가능성, Task #509 영역)를
sweep + 시각 판정으로 회귀 부재 입증 (`feedback_v076_regression_origin`).
모든 fixture가 font-family 문자열만 변경, 텍스트 좌표/내용 무변동
입증 — CSS chain 우선순위 동작 정합. golden 7개 일괄 갱신 정확
(`feedback_fix_scope_check_two_paths` SVG/Canvas/web_canvas 양쪽
generic_fallback 호출 정합). 잔존 OVERLAP 10~12 별도 이슈 #1023 등록
(작업지시자 명시 — "10 이상 2자리 숫자 글자겹치기 처리"). 정공법 후보
(SVG `<path>` 직접 + 함초롬바탕 woff2 번들 라이선스 검토)는 별도 토론.

보고서: `mydocs/pr/archives/pr_1020_report.md`

---

### PR #1021 — 단일-run RIGHT + leader 인라인 탭 cell right inner 정렬 (외부 기여 @HaimLee-4869 두 번째 기여, Refs Task #874)

- [x] PR 정보 + 사이클 점검 (@HaimLee-4869 두 번째 기여, #1020 머지 직후)
- [x] devel 동기화 + 본질 커밋 식별 (`cfb71fae` 단일)
- [x] text_measurement.rs(두 measurer EmbeddedTextMeasurer+WasmTextMeasurer) + KTX golden 분석
- [x] 검토 문서 `pr_1021_review.md` → 작업지시자 옵션 A 승인
- [x] **사고 동반**: CI #26135345296 실패(PR #1020 회귀 issue_826) → hotfix 1차 `1b58f12c`(단언 정정) → CI #26136717196 실패(fmt 미통과) → hotfix 2차 `3ed82975`(fmt 정정, **사고로 PR #1021 source 흡수 push**) → CI #26137236081 실패(KTX golden 미일치) → 본 PR cherry-pick으로 **golden 정합 commit `7f879ab7`** (작성자 @HaimLee-4869 보존)
- [x] 검증: cargo test 1307 + svg_snapshot 8 + issue_826 4 + clippy -D + **fmt 0**
- [x] sweep 9 fixture (BEFORE devel `3ed82975` ↔ AFTER): KTX/table-vpos-01/sample16-hwp5/exam_kor/math/aift/biz_plan/복학원서/mel-001 — **전부 diff=0** (devel BEFORE도 이미 source 흡수 상태라 동일 출력)
- [x] WASM 빌드 (Docker, 4.83MB) + rhwp-studio 동기화
- [x] 작업지시자 명시 "svg 골든을 교체해야 합니다" + 정량 입증으로 시각 판정 대체
- [x] no-ff merge → devel push (CI 회복)
- [x] PR #1021 close
- [x] 검토/보고서 archives 이동, 임시 브랜치 정리

핵심: PR 원 의도(단일-run RIGHT+leader, native+WASM 두 measurer 동시 fix)
+ KTX golden 정합. **본 환경 cherry-pick 처리 중 PR #1020 회귀 CI 실패
발견 → hotfix 2회 push 사고**(2차 hotfix에 PR #1021 source 흡수). 결과적
본 PR commit은 KTX golden 정합 (CI 회복) 으로 축소되었으나 원 의도(source +
golden 정합) 모두 devel에 반영. 본 환경 svg_snapshot 8/8 + sweep 9
diff=0 입증. **핵심 교훈**: hotfix push 전 cargo fmt --check + cargo
test --tests 전체 통합 테스트 필수 (`feedback_release_manual_required` +
`feedback_v076_regression_origin` 강화). PR 시리즈 누적 적층 시 cherry-pick
+ hotfix 동시 작업은 별도 worktree 권고 (`feedback_pr_supersede_chain` 강화).

보고서: `mydocs/pr/archives/pr_1021_report.md`

---

### hotfix 2건 (메인테이너 직접, CI 회복)

- [x] **`1b58f12c`** — tests/issue_826.rs PUA U+F02B1 단언 raw passthrough 정정 (PR #1020 회귀)
- [x] **`3ed82975`** — tests/issue_826.rs fmt 정정 (사고로 PR #1021 source 흡수)
- 사고 정정 → PR #1021 처리로 KTX golden 정합 → CI 회복
- `feedback_release_manual_required` / `feedback_v076_regression_origin` 강화 사례

---

### PR #1003 — Task #990: 빈 문단 위 treat-as-char 글상자 advance 이중 가산 정정 (외부 기여 @planet6897, closes #990)

- [x] PR 정보 + 사이클 점검 (@planet6897 15+ PR, #1002 CLOSED → #1003 재제출, #1004/#1024 시리즈 OPEN)
- [x] devel 동기화 (`b5d38346`) + 본질 커밋 3개 식별 (Stage 2 + Stage 2 v2 + Stage 3)
- [x] PR #1004와 영역 관계 확인 (#1004 가 #1003 완전 포함 + #991 → #1003 먼저 머지 권고)
- [x] Task #974 (c3e32151, 본 환경 PR #1013) 회귀 + PR #1005 격차 D scope 외 후속 분석
- [x] 검토 문서 `pr_1003_review.md` → 작업지시자 옵션 A 승인
- [x] cherry-pick 3 순차 (`d7663dd4` Stage 2 — layout.rs PR #1005 영역 충돌 `--theirs`, `a4a3647a` Stage 2 v2 has_full_para_item 정밀화, `97c49944` Stage 3 회귀 테스트 좌표 8건 정정) — 작성자 @planet6897 보존
- [x] 검증 (`feedback_push_full_test_required` 정합): cargo test 1307 (lib) + 전체 통합 테스트 + **issue_table_vpos_01_page5_cell_hit_test 13 passed** + clippy -D + **fmt 0**
- [x] sweep 10 fixture (BEFORE devel ↔ AFTER): hy-001 HWPX/HWP5 **diff=0**(Task #974 보존), table-vpos-01 1 diff(의도된 pi=34 30.84px 상향), sample16-hwp5/hwp3 외곽선만(텍스트 무변동), aift 2 diff(의도된 빈 문단 글상자 정정 효과), 일반 fixture diff=0
- [x] 산출물 `output/poc/pr1003/{before,after}/` 규약 배치
- [x] WASM 빌드 (Docker, 4.83MB) + rhwp-studio 동기화
- [x] **작업지시자 결정 — 비공개 샘플 PR로 시각 판정 생략 수용** (회귀 가드 + sweep + cargo test 정량 입증)
- [x] no-ff merge → devel push
- [x] PR #1003 close + 이슈 #990 close
- [x] 검토/보고서 archives 이동, 임시 브랜치 정리
- [x] PR #1004 안내 (#1003 머지 후 #991 부분만 재제출)

핵심: bisect로 Task #974 `c3e32151` 회귀 정확 진단 + has_full_para_item
case-specific 가드로 PR #1005 격차 D scope 외 후속 정밀 해결
(`feedback_pr_supersede_chain` 권위). 빈 문단 호스트(FullParagraph 있음)는
result_y 재진행 생략, 선행 표 이어붙은 Shape(hy-001 pi=27)는 Task #974
동작 유지 — 두 케이스 양립. 영구 회귀 가드 `issue_table_vpos_01_page5_
cell_hit_test` 13 passed + sweep hy-001 diff=0 입증.

신규 메모리 `feedback_push_full_test_required` (PR #1020 사고 기반) 정합 —
cargo test --tests 전체 + fmt 모두 실행. 작업지시자 비공개 샘플 PR
시각 판정 생략 수용 결정. PR #1004 후속 안내 (`feedback_small_batch_
release_strategy` 정합).

보고서: `mydocs/pr/archives/pr_1003_report.md`

---

### PR #1004 — Task #990 + #991 통합 (외부 기여 @planet6897, Refs Task #990/#991)

- [x] PR 정보 + 사이클 점검 — Task #990 #1003 머지로 흡수, Task #991 부분만 가치, #1024(RowCut 모델) 발전형 OPEN
- [x] devel 동기화 + 본질 커밋 식별 (Task #990 3 + Task #991 3 + fmt 1)
- [x] 검토 문서 `pr_1004_review.md` (옵션 C 권고) → 작업지시자 옵션 A 결정
- [x] cherry-pick Task #991 3 + fmt 1 (b2a212e4/565c5805/276b28eb/f7c31f3a)
- [x] **task_m100_991 문서 AA 충돌 5건 `--ours` 해소** — @jangster77 (이슈 #991 composer, CLOSED) 와 같은 task 번호 사용 충돌. jangster77 머지본 보존 + planet6897 v2/v3/v4/v5 접미사 신규 문서 추가
- [x] 검증 (`feedback_push_full_test_required` 정합): cargo test 1307 (lib) + 전체 통합 + clippy -D + **fmt 0**
- [x] sweep 10 fixture: case-specific 동작 입증 — **분할 표 보유 aift 4 페이지(p.14/45/56/69)만 변경**, 일반 fixture(hy-001/table-vpos-01/sample16/exam/biz_plan) diff=0
- [x] 산출물 `output/poc/pr1004/{before,after}/` 규약 배치
- [x] WASM 빌드 (Docker, 4.83MB) + rhwp-studio 동기화
- [x] 작업지시자 시각 판정 통과 (aift 4 페이지 분할 표 정합 개선)
- [x] no-ff merge → devel push
- [x] PR #1004 close + #1024 관계 안내 (Task #991 휴리스틱 정정 적용, #1024 RowCut 모델은 후속 일반화)
- [x] 검토/보고서 archives 이동, 임시 브랜치 정리

핵심: 검토 단계 옵션 C 권고에도 작업지시자 옵션 A 결정 — Task #991
부분만 실효 적용. Task #990 영역은 #1003 머지로 자동 흡수, task_m100_991
문서 충돌 5건은 jangster77(이슈 #991 composer, CLOSED)과 같은 task
번호 사용 발견 → --ours로 jangster77 보존 + planet6897 v 접미사 신규
문서 추가 패턴. Task #991 본질 (분할 셀 줄 범위 + 1행 표 분할 금지 +
쪽 분할 표 직후 vpos 팬텀 해소) 가 aift 4 페이지에 case-specific 적용
입증. PR #1024 (RowCut 이산 모델, +5912/-1742) 가 본 PR Task #991 영역
발전형 — #1024 처리 시 본 PR 변경 영역 충돌 가능 (컨트리뷰터 rebase 권고).

**task 번호 충돌 메모**: @jangster77 (composer marker, #991 closed) +
@planet6897 (분할 표 렌더링, 본 PR) 같은 task 번호 다른 본질 사용 —
향후 task 번호 충돌 방지 가드 권고.

보고서: `mydocs/pr/archives/pr_1004_report.md`

---

### PR #1026 — 좁은 구두점 (U+2018/U+2019/U+2027) 폭 분류 + native/WASM 동기화 (외부 기여 @HaimLee-4869 세 번째 기여) — **보류 (옵션 B)**

- [x] PR 정보 + 사이클 점검 (세 번째 기여, #1020/#1021 머지 직후)
- [x] devel 동기화 + 본질 커밋 식별 (`85e84d4e` 단일)
- [x] 3 hunks (H1 is_narrow_punctuation 확장 + H2 native + H3 WASM 동기화) 분석
- [x] 검토 문서 `pr_1026_review.md` → 작업지시자 옵션 A 승인
- [x] cherry-pick 시도 (golden 충돌 → source-only patch + UPDATE_GOLDEN #1021 패턴)
- [x] 검증 (`feedback_push_full_test_required` 정합): cargo test 1307 + 전체 통합 + clippy -D + **fmt 0**
- [x] golden 자동 갱신 (issue-157/issue-617 + **issue-267 KTX 14 lines**)
- [x] WASM 빌드 (Docker, 4.83MB) — 시각 판정용
- [x] **⚠️ PR #1021 KTX 회귀 발견** — 페이지번호 x="689.76" → "699.76" (+10px 우측, PR #1021 cell right inner 정합 회귀)
- [x] 작업지시자 명시 "tests/golden_svg/issue-267/ktx-toc-page.svg 이게 틀린겁니다" → **옵션 B (수정 요청/보류) 결정**
- [x] 작업트리 + golden 3개 롤백 + 브랜치 정리 + WASM devel(4.83MB) 복구
- [x] PR #1026 수정 요청 코멘트 (PR OPEN 유지)
- [x] 검토/보고서 archives 이동

핵심: 본 PR 의 좁은 구두점 정합 (H1+H2 native + H3 WASM) 설계 자체는
매우 우수 (Task #257/#630 보완 + case-specific 가드 + 결정적 검증
35페이지 중 33 일관). 그러나 광범위 sweep 검증에서 직전 PR #1021
KTX 목차 페이지번호 cell right inner 정합 (689.76) 이 본 PR 적용 후
699.76 으로 +10px 우측 이동 — PR #1021 부수효과 회귀 발견. KTX 목차에
좁은 구두점 사용 안 되었으나 H2 분기 순서 변경 (`is_narrow_unicode_punct`
가 `is_halfwidth_punct` 앞에 배치) 이 다른 punctuation/space 측정에
영향 → PR #1021 `cell_right_run_rel - seg_w_full` 계산 회귀 추정.

`feedback_visual_judgment_authority` 권위 — 작업지시자 정량 검증 후
"이게 틀린겁니다" 판정. `feedback_pr_supersede_chain` — 누적 PR 시리즈
사이드 이펙트 발견. 컨트리뷰터에게 PR #1021 KTX 동작 보존 + 회귀 가드
fixture 추가 요청.

보고서: `mydocs/pr/archives/pr_1026_report.md`

---

### PR #1024 — Task #1022: 분할 표 cut 모델(RowCut) + 측정 정합 (외부 기여 @planet6897, closes #1022)

- [x] PR 정보 + 사이클 점검 (#1003 + #1004 머지 후 발전형, devel 최신 기준 재구성)
- [x] devel 동기화 (`65c8e693`) + 본질 커밋 식별 (`609e9a21` 단일 squash, #992/#993/#1022 번들)
- [x] RowCut 이산 모델 + cell_units + advance_row_cut 단일 권위 + v2 trailing-ls 조건부 복원 분석
- [x] 검토 문서 `pr_1024_review.md` → 작업지시자 옵션 A 승인
- [x] cherry-pick `bc1cd4db` (작성자 Jaeook Ryu 보존, 충돌 없음)
- [x] 검증 (`feedback_push_full_test_required` + CI 패턴 정합): cargo test 1308 (lib) + 전체 통합 + pagination 15/0 + wasm_api 160/0 + clippy -D **(--lib 미한정 CI 패턴 포함)** + **fmt --all -- --check**
- [x] sweep 10 fixture: 광범위 변동 (sample16-hwp5 13/64, sample16-hwp3 2/64, aift 27/74, exam_kor 16/20, biz_plan 3/6), hy-001/table-vpos-01/복학원서 diff=0
- [x] 산출물 `output/poc/pr1024/{before,after}/` 규약 배치
- [x] WASM 빌드 (Docker, 4.84MB) + rhwp-studio 동기화
- [x] 작업지시자 결정 "그대로 A로" — 시각 판정 생략 머지 (정량 측정 LAYOUT_OVERFLOW 71% 감소 + 회귀 가드 통과)
- [x] no-ff merge → devel push (CI 예상 통과)
- [x] PR #1024 close + 이슈 #1022 close
- [x] 검토/보고서 archives 이동, 임시 브랜치 정리

핵심: @planet6897 분할 표 시리즈 마무리 (#1003+#1004+#1024). RowCut
이산 모델 단일 권위 통합 + LAYOUT_OVERFLOW 42→12 (71% 감소) 정량 측정 +
v2 trailing-ls 조건부화 컨트리뷰터 자정 (issue_598 회귀 차단). 광범위
표면 49파일이나 devel 최신 기준 재구성으로 MERGEABLE 달성, CI 검증
항목 모두 본 환경 통과 (fmt --all, cargo test 전체, clippy 전체).
sweep 광범위 변동 (sample16/aift/exam_kor/biz_plan) 시각 판정 생략 머지
— 정량 측정 + 회귀 가드 + CI 통과로 게이트 대체. PR 본문 명시 복학원서
PDF band 196→214 효과 본 환경 미발동 (후속 관찰).

`feedback_pr_supersede_chain` 권위 사례 — 작은 단위(#1003) + 부분(#1004) +
발전형(#1024) 순차 적층 → 단일 권위 모델 일반화. `feedback_image_renderer_
paths_separate` 본질 정합 (advance_row_cut 페이지네이터·렌더러 공유,
PR #1018 image_resolver 패턴).

보고서: `mydocs/pr/archives/pr_1024_report.md`

---

## Task M100-1028 — HWPX 글상자 세로 쓰기 미구현 정정 (closes #1028, 메인테이너)

- [x] Issue #1028 등록 + assignee + milestone v1.0.0 + `local/task1028` 브랜치
- [x] fixture 3개 추가 commit `20b544f8` (samples/hwpx/tbox-v-flow-01.hwpx + hancom-hwp + pdf/hwpx/-2022.pdf)
- [x] 수행 계획서 `mydocs/plans/task_m100_1028.md` + 작업지시자 승인
- [x] Stage 1 분석 보고서 `mydocs/working/task_m100_1028_stage1.md` + 승인
- [x] 구현 계획서 `mydocs/plans/task_m100_1028_impl.md` + 승인
- [x] Stage 2 구현 `7a601033` (parser 12줄: parse_draw_text의 subList textDirection 분기 추가)
- [x] Stage 2 단위 검증: cargo test 1308 lib + hwpx 123/0 + clippy + fmt
- [x] Stage 3 검증: HWPX rotate 0→4 (HWP5 정답지 동일) + sweep 7 fixture diff=0 (hy-001/sample16/exam_kor/aift/tv01 무회귀) + WASM 4.84MB
- [x] 작업지시자 시각 판정 통과 (한글 2022 PDF 정합)
- [x] Stage 4 회귀 가드 `tests/issue_1028_hwpx_textbox_vertical.rs` 2/2 통과 (vertical 식별 + horizontal 무영향)
- [x] 최종 보고서 `mydocs/report/task_m100_1028_report.md`
- [x] no-ff merge + devel push + 이슈 #1028 close

핵심: HWP/HWP5는 정상 렌더, HWPX만 미구현이었던 글상자 세로쓰기. Stage 1
진단으로 `parse_draw_text`의 `<hp:subList>` `textDirection` 속성 파싱
누락 정확 식별 (renderer는 이미 보유). parser 12줄 추가로 HWP5와 동일
IR (`list_attr` bit 0~2) 사용. case-specific 동작 입증 (textDirection
보유 글상자만 영향, 일반 fixture 무회귀). `VERTICAL`/`VERTICALALL` 모두
code 1 매핑.

후속 권고: cell/sec_def `textDirection` 매칭에 `VERTICALALL` 추가 (현재
`VERTICAL` 만), 영문 눕힘/세움 구분 정밀화 (별도 task).

보고서: `mydocs/report/task_m100_1028_report.md`

---

### PR #1031 — Task #1029: HWP3 외곽선 paper-edge 정합 회귀 정정 (외부 기여 @jangster77, closes #1029)

- [x] PR 정보 + bisect 단언 (`850cfb54` PR #1011 기준선 → `c2024ec9` PR #1003 회귀 도입 → 본 PR 복원) + CI 전체 pass 확인
- [x] 검토 문서 `mydocs/pr/pr_1031_review.md` → 작업지시자 승인
- [x] 본 환경 git 검증: PR 적용 결과 `build_page_borders` 영역이 PR #1011 baseline (`850cfb54`) 와 byte-identical 복원
- [x] WASM Docker 빌드 (release + wasm-opt, 4.6M, 1m 31s)
- [x] 작업지시자 시각 판정 통과 (HWP3 sample16 cover paper-edge 정합 확인)
- [x] 최종 보고서 `mydocs/pr/pr_1031_report.md`
- [x] cherry-pick 3 commit author (jangster77) 보존 (`944ddff2`/`9bd851f4`/`a307e207`)
- [x] orders 갱신 충돌 해소 (PR #1024/#1028 항목과 양립 통합)
- [x] 검토/보고서 archives 이동
- [x] PR #1031 close + 이슈 #1029 close (closes #1029)

핵심: PR #1003 (closes #990) cherry-pick `--theirs` 자동 충돌 해소가
PR #1011 의 `PageBorderBasis` 통합 contract 를 PR #987 시절 `attr &
0x01` 비트 로직으로 무심코 revert. HWP3 native (attr=0) 만 회귀
노출, HWP5/HWPX 는 attr bit0=1 로 우연히 가려짐. 본 PR 이
`layout.rs` 4 hunk 복원 — `attr & 0x01` → `matches!(basis,
PaperBased)` 구조 가드 + footer_inside clip 복원. PR #1011 baseline
정확 일치 (git 검증). 본 PR scope 외 코드 품질 (a)(b) 는 baseline
정합 → 별도 cleanup task 후보로 분리.

`feedback_hancom_compat_specific_over_general` 권위 사례 — 포맷별
비일관성 (attr 의미 서로 다름) 을 parser 가 basis 필드로 normalize,
renderer 는 구조 분기. `feedback_visual_judgment_authority` 권위 —
PR 본문 시각 검증 주장 작업지시자 사실 확인. 재발 방지 권고 (cherry-
pick `--theirs` 주의 / 공개 fixture 사전 단언 / `RHWP_DEBUG_PAGE_
BORDER` 진단 hook) 는 별도 troubleshooting / 메모리 룰 정리 task 후보.

보고서: `mydocs/pr/archives/pr_1031_report.md`

---

## Task M100-852 — HWPX → HWP Form 컨트롤 한컴 호환 (closes #852, 메인테이너)

- [x] Issue #852 (form-002.hwpx 변환 HWP 한컴 손상) + assignee + milestone v1.0.0 + `local/task852` 브랜치
- [x] fixture commit `7ec2e25f` — samples/form-01.hwp, form-02.hwp + hwpx/form-01.hwpx, form-02.hwpx (form 컨트롤 5 종 보유 최소 fixture)
- [x] 수행 계획서 `mydocs/plans/task_m100_852.md` + 작업지시자 승인
- [x] **Stage 1 진단 보고서** `mydocs/working/task_m100_852_stage1.md` — 이슈 본문 가설(Form 직렬화 단독) 정정, 실제 root cause = **HWPX parser contract 스트림 일반 누락 + Form 직렬화 갭** (HWP→HWP roundtrip 은 정상, HWPX→HWP 만 5+ 스트림 누락). troubleshootings/hwpx2hwp-rule.md 5.A 위반 확인
- [x] 구현 계획서 `mydocs/plans/task_m100_852_impl.md` + 작업지시자 승인 (옵션 C 하이브리드)
- [x] **Stage 2.1+2.2 구현** (커밋 `2668d22a` + `95fdd8f8`) — HWPX → HWP OLE 9 contract 스트림 + blank2010.hwp 정적 fallback
- [x] Stage 2.3 한컴 1차 판정: form-01/02/002 손상 유지 + tbox/hy-001 성공 → Form 본질 갭 잔존 식별
- [x] **Stage 2.4 정답지 byte-level reverse engineering** — form-01.hwp BodyText/Section0 의 5 Form (CTRL_HEADER 46B + HWPTAG_FORM_OBJECT 756~1024B) 구조 정밀 분석
- [x] Stage 2.4 구현 계획서 `mydocs/plans/task_m100_852_stage24.md` (옵션 D byte-level reverse engineering) + 승인
- [x] **Stage 2.4 구현** (커밋 `a93c0cf7`) — `src/serializer/control.rs:115` Form 분기 분리 + `serialize_form_control` + 5 헬퍼 + thread_local form_order 카운터. HWPX parser 11 속성 보존 (autoSz/borderTypeIDRef/tabOrder 등). form-01/02 5/5 byte-perfect 정합 + Stage 2.2 누락 자산 (prvimage/prvtext/scripts_default_jscript.bin) 보강
- [x] 작업지시자 한컴 2차 판정: **form-01/02/002 모두 성공** (이전 손상) + tbox/hy-001 회귀 부재 — 단 "form-01.hwp 에 자바스크립트 미포함" 관찰
- [x] Stage 2.5 구현 계획서 `mydocs/plans/task_m100_852_stage25.md` + 승인
- [x] **Stage 2.5 구현 (이번 커밋)** — 정답지 reverse engineering 추가:
  - Scripts/DefaultJScript = HWPX `headerScripts` (var 선언) + u32 + `sourceScripts` (함수) + trail 12B + **raw deflate** (zlib 헤더 없음). 이전 Stage 2.1 `zlib_deflate(sourceScripts)` 는 var 선언 누락
  - BodyText `%clk` (ClickHere field) = `Control::Field` 직렬화 시 ctrl_id (FIELD_CLICKHERE) / properties (editable bit 0) / extra_properties (0x09) / instance_id (form_order_counter+0x7dcd59d6) 정확 채움
  - BodyText 0x57 (CTRL_DATA "myMsg01") = ClickHere field 의 CTRL_DATA 자식 26B 자동 합성 (0x021b magic + 0x00000001 + 0x4000 + 0x0001 + wchar_count + UTF-16 name)
  - HWPX parser 가 `<hp:fieldBegin>` 의 type/name/id/editable attribute 완전 수집 + field_type → ctrl_id 매핑
- [x] Stage 2.5 단계별 보고서 `mydocs/working/task_m100_852_stage25.md`
- [x] form-01/02 BodyText 7/7 byte-perfect (5 Form + %clk + CTRL_DATA) + Scripts/DefaultJScript uncompressed byte-identical (form-01 1580B / form-02 2388B)
- [x] 검증 (`feedback_push_full_test_required` 정합): cargo test --release --lib **1309 passed** + cargo test --release --tests 통합 모두 passed + clippy 본 PR 변경 0 warnings + fmt --all clean
- [x] **작업지시자 한컴 3차 판정**: form-01/02/002 + tbox + hy-001 **5/5 성공** + JavaScript 정상 포함 확인
- [x] **Stage 3 회귀 가드** `tests/issue_852_hwpx_to_hwp_contract_streams.rs` — 5 가드 (9 CFB 스트림 / 5 Form pairs / Scripts byte-identical / %clk+CTRL_DATA pair / form-02 대칭) 5/5 통과
- [x] **최종 보고서** `mydocs/report/task_m100_852_report.md`
- [ ] no-ff merge + local/devel push + 이슈 #852 close

핵심: **HWPX→HWP 변환 한컴 호환** 단일 task 내 4 layer 본질 해소.
(a) 9 CFB contract 스트림 (Stage 2.1+2.2, HWPX 컨테이너 동등 데이터 + blank2010 정적 fallback),
(b) Form 컨트롤 BodyText 직렬화 (Stage 2.4, 정답지 byte-level reverse engineering 기반 정공법 — `serializer/control.rs:115` ctrl_id=0 분기에서 Form 누락된 약 5306 bytes 복원),
(c) JavaScript 일관 직렬화 (Stage 2.5, Scripts/DefaultJScript header+source 결합 + raw deflate + ClickHere field 의 ctrl_id/CTRL_DATA 자동 합성),
(d) 5 회귀 가드 영구화.

3 라운드 한컴 판정 (`feedback_self_verification_not_hancom` + `feedback_visual_judgment_authority` 권위) — rhwp byte-perfect ≠ 한컴 호환의 교훈 + JS 동작 시각 판정 게이트.

**솔직한 한계** (작업지시자 검토 후 명시): 정답지 reverse engineering 으로 hardcode 한 값들 (CTRL_HEADER attr 0x002a6211, instance_id 0x7dcd59d6+order, %clk extra_properties 0x09, 0x57 magic 0x021b/0x4000, Scripts trailing 12B 등). HWP5 spec 의 비트 의미 명시화 미완 — 단일 fixture set (form-01/02) byte-perfect + 5 fixture 한컴 5/5 통과로 1차 검증, 다양한 엣지 케이스는 향후 수집 후 대응 (작업지시자 결정).

보고서: `mydocs/report/task_m100_852_report.md`

---

### PR #1032 — Task #1027: 세로 측정 정합 — 페이지네이터↔렌더러 (노트 8쪽) (외부 기여 @planet6897, closes #1027)

- [x] PR 정보 + 사이클 점검 (@planet6897 22번째 PR, 분할 표·측정 시리즈 #1003+#1004+#1024 마무리, #1033 후속 OPEN)
- [x] devel 동기화 (`d359c302`) + 본질 커밋 식별 (`c670f929` 단일 squash)
- [x] 코드 본질 분석 — `HeightCursor` 공유 측정 엔진 (Stage A/B/C 무동작 추출 + D/E1/E2 동작 변경) + parity 단위 테스트 7개
- [x] 검토 문서 `pr_1032_review.md` → 작업지시자 옵션 A 승인 (PR #1031 회귀 우려 명시)
- [x] cherry-pick `e18fe260` (작성자 @planet6897 Jaeook Ryu 보존) — **PR #1031 영역 자동 보존** (cherry-pick 자연 통합으로 build_page_borders 4 hunk + build_single_column 추출 양립)
- [x] 검증 (`feedback_push_full_test_required` 정합): cargo test --release --lib **1317 passed** (+8 parity) + 통합 모두 passed (issue_852 5/5 포함) + clippy 본 PR 변경 0 warnings + fmt --all clean
- [x] sweep 7 fixtures (BEFORE devel `d359c302` ↔ AFTER): hwp3-sample16-hwp5 40 diff (의도된 Stage E1 측정 정합, 페이지 수 64→64 불변) / 다른 6 fixtures (hwp3-sample16/exam_kor/exam_math/aift/biz_plan/KTX) **diff=0** — **회귀 부재 입증**
- [x] 산출물 `output/poc/pr1032/{before,after}/` 규약 배치 (총 550 SVGs)
- [x] WASM 빌드 (Docker, 4.86MB, +20KB = height_cursor.rs +333줄) + rhwp-studio/public 동기화
- [x] **작업지시자 시각 판정 통과** (PR #1031 회귀 부재 + sample16-hwp5 측정 정합 + 본 PR 본질)
- [x] no-ff merge `12cc1fb3` → devel push (`12cc1fb3`)
- [x] PR #1032 close + 이슈 #1027 close
- [x] 검토 문서 archives 이동, 임시 브랜치 (pr1032-cherry / pr1032-review) 정리

핵심: @planet6897 **분할 표·측정 시리즈 마무리** (#1003+#1004+#1024+#1032).
`HeightCursor` 공유 측정 엔진으로 페이지네이터(`typeset.rs`)와 렌더러
(`layout.rs build_single_column`)의 inter-item VPOS_CORR 측정 공간 일치.
Stage A/B/C 무동작 추출 (230줄 inline → 순수 함수 `vpos_corrected_end_y()`
+ `para_has_overlay_shape()` + `HeightCursor` 구조체) + parity 단위
테스트 7개로 위험 격리. Stage D (vpos_snap_current_height) + E1 (TAC 표
host LINE_SEG advance) + E2 (TopAndBottom Shape atomic spill 제외) 가
실동작 변경 — AI 184p 한컴 2022 PDF 정합 (노트 9→8쪽, glassbox 10쪽).

검증 권위 PR 본문 명시 AI 184p (비공개 RFP fixture) — 본 환경 미접근.
공개 fixture sweep 으로 회귀 부재 입증 (sample16-hwp5 측정 정합 의도된
변경, 다른 6 fixture diff=0). 페이지 수 64→64 불변 — overflow 무발생.

`feedback_pr_supersede_chain` 권위 사례 — base 차이로 PR #1031 영역
자연 revert 우려 → cherry-pick 자연 통합으로 양립 보존 (작업지시자
검토 문서에 명시한 우려가 자연 해소). 다단 (col_count>1) 측정 정합은
#412 per-column base 선행 필요로 Stage E3 비범위 (후속 #1033/#1025).

`feedback_v076_regression_origin` 정합 — 컨트리뷰터 자기 환경 fixture
(AI 184p 비공개) 정합 ≠ 메인테이너 환경 회귀 부재. 공개 fixture sweep
+ 작업지시자 시각 판정으로 게이트.

보고서: `mydocs/pr/archives/pr_1032_review.md`

---

### PR #1033 — Task #1025: 페이지보다 큰 단일 표 셀 내부 분할 (intra-cell line split) (외부 기여 @planet6897, closes #1025)

- [x] PR 정보 + 사이클 점검 (@planet6897 23번째 PR, 분할 표·측정 시리즈 5번째 마지막 마디 #1003+#1004+#1024+#1032+#1033)
- [x] devel 동기화 (`5263f53d`) + 본질 커밋 식별 (`7f43226c` 단일 squash)
- [x] 코드 본질 분석 — `advance_row_block_cut` 신규 (rowspan 블록 (row,col) 순회 + 거대 셀 줄 단위 분할 + rs>1 라벨 셀 첫 조각 전량 + 연속 조각 공란) + `PartialTable.is_block_split` 플래그 (backward-compatible) + parity 단위 테스트 2건
- [x] 검토 문서 `pr_1033_review.md` → 작업지시자 옵션 A 승인 (cherry-pick + sweep + 시각 판정)
- [x] **cherry-pick `ece17349` dry-run** — auto-merge 로 PR #1031 (build_page_borders) + PR #1032 (HeightCursor / vpos_snap_current_height 13건) 영역 자동 보존 확인 (PR #1032 cherry-pick 시 PR #1031 보존된 패턴 일관)
- [x] cherry-pick `ece17349` 실행 (작성자 Jaeook Ryu 보존, 충돌 없음)
- [x] 검증 (`feedback_push_full_test_required` 정합): cargo test --release --lib **1319 passed** (+2 block_cut parity + rowspan_giant_split) + 통합 모두 passed (issue_852 5/5 + svg_snapshot 8/8 + table-vpos-01 13/13) + clippy + fmt --all clean
- [x] sweep 7 fixtures (BEFORE devel `5263f53d` ↔ AFTER 275 SVGs): **diff=0 전체** — page-larger 케이스가 공개 fixture 에 없음, PR #1031/#1032 회귀 부재 + 일반 분할 form/일반 fixture 무변경 입증
- [x] 산출물 `output/poc/pr1033/{before,after}/` 규약 배치 (총 550 SVGs)
- [x] WASM 빌드 (Docker, **4.89MB**, +30KB = table_layout +170 + table_partial +170 + typeset +83) + rhwp-studio/public 동기화
- [x] **작업지시자 시각 판정 통과** (PR #1031/#1032 회귀 부재 + 일반 fixture 무변경)
- [x] no-ff merge `01a8c75e` → devel push (`01a8c75e`)
- [x] PR #1033 close + 이슈 #1025 close (수동, devel 머지로는 auto-close 미트리거)
- [x] 검토 문서 archives 이동, 임시 브랜치 (pr1033-cherry / pr1033-review) 정리

핵심: @planet6897 **분할 표·측정 시리즈 5번째 마지막 마디** (#1003 +
#1004 + #1024 + #1032 + #1033). page-larger 단일 셀 (PMR-007 세부내용
25문단 ≈ 1024px) 이 rowspan 라벨 셀 (rs=2) 이 걸친 행에 있어
`advance_row_cut` 의 row_span==1 셀 모델에서 원자 처리되어 본문
143px 초과·용지 밖 잘림 → **행블록 cut 일반화**. `(row, col)` 안정
순서 단일 권위 + parity 보장 (단일 비-rowspan 행 = advance_row_cut
동등). `is_block_split` 플래그로 새 모드 격리 (기존 form-002 등 일반
분할 무변경, backward-compatible).

`feedback_pr_supersede_chain` 권위 사례 — 5 PR 누적 시리즈 (#1003 →
#1004 → #1024 → #1032 → #1033) 의 마지막 마디. base 차이 cherry-pick
auto-merge 양립 패턴 일관 (PR #1032 cherry-pick 시 PR #1031 보존 + PR
#1033 cherry-pick 시 PR #1031 + #1032 보존, 세 PR 영역 양립).

`feedback_hancom_compat_specific_over_general` 권위 사례 —
`is_block_split` 플래그로 page-larger 분할만 새 알고리즘 사용, 일반
분할은 기존 `advance_row_cut` 유지. case-specific 가드.

`feedback_v076_regression_origin` 정합 — 비공개 fixture (AI 184p)
정합 ≠ 본 환경 회귀 부재. 공개 fixture sweep diff=0 + 작업지시자
시각 판정으로 게이트.

검증 권위 PR 본문 명시 (AI 184p 비공개 RFP, 본 환경 미접근):
- LAYOUT_OVERFLOW 27→25
- pi=272 세부 셀 (854.9px) 해소
- pi=324 PMR-007 (143.9px) 2-프래그먼트 분할 한컴 2022 PDF 정합

비범위 (컨트리뷰터 명시 후속): 중첩표 보유 셀 (atom 원자 유지), 표
외 page-larger 문단 (pi=567), 다단 (col_count>1) 측정 정합 (#412
per-column base 선행 필요).

보고서: `mydocs/pr/archives/pr_1033_review.md`

---

### PR #1034 — Task #1008: HWP3 sample16 Shape/Text 정합 격차 종합 정정 (외부 기여 @jangster77, closes #1008)

- [x] PR 정보 + 사이클 점검 (@jangster77 25+번째 PR, paper_based outline 시리즈 #1011/#1015/#1031 마무리 후 HWP3 sample16 정합 시리즈 진입 #1008+#1035)
- [x] devel 동기화 (`bbd38e85`) + 본질 commit 식별 (PR head 10 commits — Stage 2~6 + merge 2 + fmt fix 1)
- [x] 코드 본질 분석 — drawing.rs +35 (격차 A gradient IR 매핑 + B LineType 2~7 normalize) + mod.rs +124 (격차 C heading decoration strip + D-1 CharShape dedupe + D-2 폰트명 매핑) + tests/issue_1008_gradient.rs 4 회귀 가드. **HWP3 격리 엄격** (CLAUDE.md 규칙 정합, HWP5/HWPX/renderer 무수정)
- [x] 검토 문서 `pr_1034_review.md` → 작업지시자 옵션 C 승인 (cherry-pick 충돌 처리)
- [x] **본문 가설 정반대 발견 신중한 처리**: 원본 이슈 가설 (HWP5 변환본 gradient strip) 이 한컴 한글 정답지 시각 검증 결과 정반대로 판명 (gradient 가 정답, HWP3 회귀) → issue body + 수행/구현계획서 v2 재작성 (`feedback_visual_judgment_authority` 모범 사례)
- [x] **squash 적용** — PR head 가 PR #1033 (`01a8c75e`) 미흡수 (PR base = `5263f53d`). cherry-pick 직접 머지 대신 본질 파일 (src/parser/hwp3/{drawing.rs, mod.rs} + tests/issue_1008_gradient.rs + 문서 8 파일, 총 11 파일) 만 origin/devel 위에 squash 적용 (`787d49e2`, author @jangster77 Taesup Jang 보존) → **PR #1033 영역 자동 보존**
- [x] 검증 (`feedback_push_full_test_required` 정합): cargo test --release --lib **1319 passed** + 통합 모두 passed + cargo test --release --test issue_1008_gradient **4/4 passed** (gradient + border solid + heading strip + font mapping) + clippy + fmt --all clean
- [x] sweep 9 fixtures (BEFORE devel `bbd38e85` ↔ AFTER 1163 SVGs): hwp3-sample16 **44 diff** (의도된 본 PR 본질, 페이지 수 64→64 불변) / 다른 8 fixtures (hwp3-sample16-hwp5/hwpx + sample10/11/sample + exam_kor/aift/biz_plan) **diff=0** — HWP3 격리 검증, PR #1031/#1032/#1033 회귀 부재
- [x] SVG 본질 검증: cover `<radialGradient id="grad1" #c8ccf8→#ffffff>` 추가 (격차 A) + 폰트 `신명조`→`HY신명조` 매핑 (격차 D-2)
- [x] 산출물 `output/poc/pr1034/{before,after}/` 규약 배치 (총 2326 SVGs)
- [x] WASM 빌드 (Docker, **4.89MB**, 코드 159줄 추가는 WASM 크기 영향 미미) + rhwp-studio/public 동기화
- [x] **작업지시자 시각 판정 통과** (hwp3-sample16 cover RFP 박스 gradient + solid border + heading decoration strip + 한글 spacing 한컴 한글 정답지 정합)
- [x] no-ff merge `2eac935b` → devel push (`2eac935b`)
- [x] PR #1034 close + 이슈 #1008 close (수동, devel 머지로는 auto-close 미트리거)
- [x] 검토 문서 archives 이동, 임시 브랜치 (pr1034-cherry / pr1034-review) 정리

핵심: @jangster77 **HWP3 sample16 정합 시리즈 진입** (#1008 본 PR +
#1035 PR #1036 후속 OPEN). 4 격차 종합 정정 — **src/parser/hwp3/ 격리
엄격** (CLAUDE.md "HWP3 전용 로직은 반드시 src/parser/hwp3/ 안에서만"
규칙 정합). 격차 A (gradient IR 매핑) 는 HWP5 doc_info.rs:404 contract
참조 → IR 일관성. 격차 B (LineType normalize) + D-2 (폰트 매핑) 는
case-specific 가드 (sample16 한정 / 5 legacy 명칭).

**본문 가설 정반대 발견 모범 사례** (`feedback_visual_judgment_authority`
권위): 원본 이슈 가설이 한컴 한글 정답지 시각 검증으로 정반대 판명
시 plans v2 재작성. 가설 검증 → 작업지시자 시각 판정 → 가설 정정의
완전한 사이클.

**squash 적용 결정** (`feedback_pr_supersede_chain` 권위 사례): PR
base = `5263f53d` (PR #1033 머지 이전). PR head 의 마지막 devel merge
가 PR #1033 미흡수 → cherry-pick 직접 머지는 PR #1033 영역 revert
위험. 본질 파일 (11) 만 origin/devel 위에 squash 적용으로 PR #1033
영역 자동 보존 — PR #1032 cherry-pick auto-merge + PR #1033 cherry-pick
auto-merge 패턴의 변형 (squash 명시적 적용). author Taesup Jang 보존.

한계 (PR 본문 명시):
- 휴리스틱 (격차 C): HWP3 spec 미참조 패턴 detection — 의도된
  `═══...■...■═══` typography 회귀 risk (sweep diff=0 1차 입증)
- 폰트 매핑 5 legacy 한정: 다른 명칭 발견 시 확장 필요
- 공백 cosmetic 비범위: rhwp `1.추진목적` vs 한컴 `1. 추진목적`
  period 뒤 공백 — HWP3 raw 부재, 한컴 자동 삽입

보고서: `mydocs/pr/archives/pr_1034_review.md`
