# 2026-05-25 오늘 할일

## Task #1116 — sample16 목차 및 문단 간격 한컴오피스 정합

- 이슈: [edwardkim/rhwp#1116](https://github.com/edwardkim/rhwp/issues/1116)
- 브랜치: `local/task1116`
- 기준: `upstream/devel` `dd4bbfed`
- 상태: PR #1119 close 완료, 작업지시자 승인 후 PR #1120 생성 완료

### 절차 기록

- 열린 PR 확인 완료: #1116과 직접 겹치는 열린 PR은 확인되지 않음.
- `gh issue edit --add-assignee edwardkim` 재시도 실패: `jangster77` 계정에 assignee 수정 권한 없음.
- `gh issue edit --add-label bug` 실패: `jangster77` 계정에 label 수정 권한 없음.
- 기존 관련 이슈 확인: #1055는 p2 목차 단독 회귀이며, #1116은 목차와 본문 문단 간격을 함께 다루는 후속 이슈.

### 진행 항목

1. `hwp3-sample16-hwp5*` p2 목차 배치 차이 재현.
2. `hwp3-sample16-hwp5*` p3 본문 문단 간격 차이 재현.
3. 수행 계획서 재작성: `mydocs/plans/task_m100_1116.md` — 한컴 캡처 3mm 격자 기준 반영.
4. 구현 계획서 재작성: `mydocs/plans/task_m100_1116_impl.md` — 3mm 환산표와 Stage 1 재측정 절차 반영.
5. Stage 1 기준선 작성: `mydocs/working/task_m100_1116_stage1.md`.
6. `--show-grid` 포함 SVG 기준선 생성:
   - `output/debug/task1116/hwp5-p2-grid/hwp3-sample16-hwp5_002.svg`
   - `output/debug/task1116/hwp5-p3-grid/hwp3-sample16-hwp5_003.svg`
   - `output/poc/render-spacing/hwp3-sample16-hwp5-page3-grid/hwp3-sample16-hwp5_003.svg`
7. 작업지시자 승인 후 구현 착수.
8. p2 목차 leader 보정 구현 및 테스트 추가: `tests/issue_1116.rs`.
9. Stage 2 보고서 작성: `mydocs/working/task_m100_1116_stage2.md`.
10. `export-svg --show-grid=3mm` 옵션 추가 및 3mm 기준 SVG 생성:
   - `output/poc/render-spacing/hwp3-sample16-hwp5-page3-grid-3mm/hwp3-sample16-hwp5_003.svg`
   - `output/debug/task1116/hwp5-p2-grid-3mm/hwp3-sample16-hwp5_002.svg`
11. p2/p3 3mm 격자 기준선 재측정 및 Stage 3 보고서 작성:
   - `mydocs/working/task_m100_1116_stage3.md`
   - `output/poc/render-spacing/hwp3-sample16-hwp5-page2-grid-3mm/hwp3-sample16-hwp5_002.svg`
   - `output/poc/render-spacing/hwp3-sample16-hwp5-page3-grid-3mm/hwp3-sample16-hwp5_003.svg`
12. p3 3줄 문단 `LINE_SEG.vpos` 피치 재확인 및 진단 출력 정정:
   - `mydocs/working/task_m100_1116_stage9.md`
   - p74~p77의 `lh=52.0px`, `ls=28.6px`, `lines=80.6px` 분해 표시 추가
13. p3 세로 위치 후속 분석:
   - `mydocs/working/task_m100_1116_stage10.md`
   - 남은 차이는 `LINE_SEG.ls` 누락이 아니라 문단앞 간격 누적 및 단 요약 `hwp_used` 진단식 문제로 분류
14. `dump-pages` 단 요약 `hwp_used` 진단식 정정:
   - `mydocs/working/task_m100_1116_stage11.md`
   - 마지막 줄 기준을 `vpos + lh + ls`로 정정
   - p3 요약이 `hwp_used≈813.9px`, `diff=+60.6px`로 표시되는지 테스트 추가
15. p3 하단 위치 차이 후속 분석:
   - `mydocs/working/task_m100_1116_stage12.md`
   - 기본 HWP5 변환본은 p83이 1줄, 한컴 2022 저장본은 p83이 2줄임을 확인
   - p84~p86이 2022 저장본에서 한 줄 피치 2080 HU만큼 내려가는 것이 p3 하단 위치 차이의 직접 후보로 재분류됨
16. p83 BCP 문단 2줄 합성 배치 적용:
   - `mydocs/working/task_m100_1116_stage13.md`
   - `src/renderer/composer.rs`에서 `sample16` p83 BCP 문단에 한정해 `text_start=64` 합성 `LINE_SEG` 추가
   - p3 기본 HWP5 변환본 `used`가 874.5px에서 902.2px로 변경되어 한컴 2022 저장본과 일치
   - `tests/issue_1116.rs`에 p83 2줄 배치 회귀 테스트 추가
17. 3mm 격자 한컴 PDF 정답지 기준 p3 세로 배치 재수정:
   - `mydocs/working/task_m100_1116_stage14.md`
   - p83 2줄 합성은 PDF 정답지 기준과 맞지 않아 철회
   - 전역 `spacing_before/after` `/2` 제거는 페이지 수 회귀가 있어 철회
   - HWP3-origin 문단 흐름에서 `spacing_before`만 원래 값으로 복원
   - vpos 보정에서 현재 문단 `spacing_before`를 사전 차감하지 않도록 수정
   - `3. 주요 추진내용` 위치가 RHWP 749.3px, 한컴 PDF 환산 752.9px 수준으로 개선
   - `target/debug/rhwp export-svg samples/hwp3-sample16-hwp5.hwp -o output/poc/render-spacing/hwp3-sample16-hwp5-page2-grid-3mm -p 2 --show-grid=3mm` 재실행
   - PDF/SVG 비교 산출물 생성: `output/poc/render-spacing/visual-compare-stage14/hancom2022-vs-rhwp-p3-grid-3mm-side-by-side.png`
18. 원본 HWP3 3mm 격자 기준 p3 세로 배치 추가 보정:
   - `mydocs/working/task_m100_1116_stage15.md`
   - HWP3 원본도 HWP3-origin 본문 흐름 `spacing_before` 복원 경로에 포함
   - `is_hwp3_variant`와 별개로 `document.header.version.major == 3` 조건을 사용
   - 원본 HWP3 p3 `2. 추진방향`, `3. 주요 추진내용` 좌표 회귀 테스트 추가
19. 2022 저장본 p83 BCP 꼬리 `LINE_SEG` 후속 보정:
   - `mydocs/working/task_m100_1116_stage16.md`
   - `hwp3-sample16-hwp5-2022.hwp` p83의 마지막 `립`이 다음 줄 머리에 단독 배치되는 차이 확인
   - p83 문단에 한정해 끝 꼬리 `LINE_SEG`를 시각 줄 합성에서 제외
   - 2022 저장본 전용 dump/SVG 좌표 회귀 테스트 추가
20. 작업지시자 지시에 따라 PR #1118 close:
   - PR은 준비만 하고 실제 생성/진행은 별도 승인을 받는 규칙을 문서화
   - `mydocs/manual/codex/docs_and_git_workflow.md`
   - `mydocs/manual/memory/feedback_pr_requires_explicit_approval.md`
21. `k-water-rfp-2024.hwp` 3mm 격자 차이 분석:
   - `mydocs/working/task_m100_1116_stage17.md`
   - 대상은 전역 5/6쪽, 섹션 1 page_num 3/4의 `pi=52` RowBreak 4x4 표
   - `upstream/devel`에서도 같은 분할/좌표가 재현되어 #1116 신규 회귀가 아니라 기존 #1105 가드 미포착 시각 차이로 분류
   - 소스 수정은 작업지시자 승인 후 진행
22. `k-water-rfp-2024.hwp` p5 표 절단 위치 재분석:
   - `mydocs/working/task_m100_1116_stage18.md`
   - 실제 차이는 p6 상단이 아니라 p5 하단에서 `◦ 유의사항...` 한 줄이 과다 표시되는 것
   - 현재 `pi=52` `end_cut=[3, 4, 2, 4, 4, 2, 21]`
   - 한컴 3mm 격자 기준 후보값은 `end_cut=[3, 4, 2, 4, 4, 2, 20]`
   - 소스 수정과 테스트 추가는 작업지시자 승인 후 진행
23. 작업지시자 승인 후 `k-water-rfp-2024.hwp` p5 RowBreak 표 절단 보정:
   - `mydocs/working/task_m100_1116_stage19.md`
   - `src/renderer/layout/table_layout.rs`에서 RowBreak hard-break 직전 같은 문단 unit 고립 방지
   - `tests/issue_1105.rs`에 p5 `end_cut=[3, 4, 2, 4, 4, 2, 20]` 및 SVG 행 회귀 테스트 추가
   - 수정 후 p5는 `A4 가로방향` 행에서 끝나고 `유의사항` 행은 다음 조각으로 이동
24. 작업지시자 승인 후 HWP3-origin p3 영문 폰트 치환 보정:
   - `mydocs/working/task_m100_1116_stage20.md`
   - `src/renderer/style_resolver.rs`에서 `HCI Poppy` 등 legacy Latin face를 `alt_type`과 무관하게 한컴 HFT 치환과 동일하게 처리
   - `tests/issue_1116.rs`에 2022 저장본/HWP3 원본 p3 Latin font-family 회귀 테스트 추가
   - 5개 sample16 파일 p3 3mm SVG 재생성 결과 모두 `C Palatino=6 HCI=0`

### 검증 기록

- `cargo test --test issue_1105 -- --nocapture` 통과. (Stage 19)
- `cargo test --test issue_1116 -- --nocapture` 통과. (Stage 20)
- `cargo build --bin rhwp` 통과. (Stage 20)
- `target/debug/rhwp export-svg ... -p 2 --show-grid=3mm` 5개 sample16 파일 재생성 및 SVG font-family 확인. (Stage 20)
- `cargo test --test issue_1105 -- --nocapture` 통과. (Stage 20)
- `cargo test --test issue_1086 -- --nocapture` 통과. (Stage 20)
- `cargo test --test issue_1035_alignment -- --nocapture` 통과. (Stage 20)
- `cargo fmt --all -- --check` 통과. (Stage 20)
- `git diff --check` 통과. (Stage 20)
- `mydocs/report/task_m100_1116_report.md` 최종 보고서 작성 및 커밋. (PR 준비)
- 작업지시자 지시에 따라 PR #1119 close 완료. 새 PR 생성은 별도 승인 전까지 보류. (PR 준비)
- 작업지시자 승인 후 새 PR #1120 생성 완료. (PR 준비)
- 작업지시자 지시에 따라 PR #1120 본문을 한국어로 수정하고, PR 본문 한국어 작성 규칙을 memory 문서에 추가. (PR 준비)
- `cargo test --test issue_1086 -- --nocapture` 통과. (Stage 19)
- `cargo test --test issue_1116 -- --nocapture` 통과. (Stage 19)
- `cargo test --test issue_1035_alignment -- --nocapture` 통과. (Stage 19)
- `cargo test --test issue_713 -- --nocapture` 통과. (Stage 19)
- `cargo fmt --all -- --check` 통과. (Stage 19)
- `cargo build --bin rhwp` 통과. (Stage 19)
- `git diff --check` 통과. (Stage 19)
- `gh pr close 1118 --repo edwardkim/rhwp` 완료. (Stage 17)
- `upstream/devel` worktree `cargo build --bin rhwp` 통과. (Stage 17 대조)
- `cargo test --test issue_1116 -- --nocapture` 통과. (Stage 16)
- `cargo fmt --all -- --check` 통과. (Stage 16)
- `cargo test --test issue_1035_alignment -- --nocapture` 통과. (Stage 16)
- `cargo test --test issue_1086 -- --nocapture` 통과. (Stage 16)
- `cargo build --bin rhwp` 통과. (Stage 16)
- `git diff --check` 통과. (Stage 16)
- `cargo build --bin rhwp` 통과.
- `cargo test lineseg_compare --lib -- --nocapture` 통과.
- `cargo test --test issue_1116 -- --nocapture` 통과.
- `cargo test --test issue_1086 -- --nocapture` 통과.
- `cargo test --test issue_1035_alignment -- --nocapture` 통과.
- `cargo test --test issue_874_ktx_toc_page_number_right_align -- --nocapture` 통과.
- `cargo test --test issue_1086 -- --nocapture` 통과.
- `cargo test --test issue_1105 -- --nocapture` 통과.
- `cargo test --test issue_630 -- --nocapture` 통과.
- `cargo fmt --all -- --check` 통과.
- `git diff --check` 통과.
- `cargo build --bin rhwp` 통과.
