# 2026-06-01 — 오늘 할일

## Task #1198 — `exam_social.hwp` 중첩 표 셀 붙여넣기 경로 보존

- [x] 이슈 생성: #1198 `rhwp-studio: exam_social.hwp 중첩 표 셀에서 내부 클립보드 붙여넣기 시 바깥 셀에 삽입됨`.
- [x] 브랜치 생성/정리: `task-1198`.
- [x] 수행계획서/구현계획서 작성: `mydocs/plans/task_m100_1198.md`, `mydocs/plans/task_m100_1198_impl.md`.
- [x] 업스트림 반영: `upstream/devel` `c884205d`, `Cargo.toml` `0.7.13`, `HEAD...upstream/devel = 0 0`.
- [x] 구현: 중첩 표 `cellPath` 기반 내부/HTML 붙여넣기 native/WASM API와 Studio 라우팅 추가.
- [x] 검증: #1198 신규 테스트, #850 회귀 테스트, `cargo test --lib`, `npm test`, `npm run build` 통과.
- [x] PR #1207 머지 (devel e91385ae, closes #1198).

## Task #1211 — rhwp-studio 입력 편집 재렌더 비용 축소

- [x] 이슈 생성: #1211 `rhwp-studio: 텍스트 입력 시 document-changed 기반 전체 visible page 재렌더로 입력 지연 발생`.
- [x] 브랜치 생성: `task-1211` (`upstream/devel` 기준).
- [x] 수행계획서 작성: `mydocs/plans/task_m100_1211.md`.
- [x] 구현계획서 작성: `mydocs/plans/task_m100_1211_impl.md`.
- [x] Stage 1 조사: `document-changed` full refresh 비용과 #865 이후 잔여 병목 정리.
- [x] Stage 2 구현: page-local canvas refresh 경로 추가.
- [x] Stage 3 구현: 셀 내부 단일 insert/delete 텍스트 편집만 narrow invalidation으로 연결.
- [x] Stage 4 검증: `npm test`, `npm run build`, `exam_social.hwp` 수동 입력 확인.
- [x] PR 본문에 선택안 A+C와 근거를 명시할 항목 문서화.
- [x] 커밋 및 PR 생성: #1212.
- [x] Stage 5 보완: 한글 IME 조합 입력의 raw 편집 경로도 page-local invalidation으로 연결.
- [x] Stage 5 커밋 및 PR 브랜치 업데이트.
- [x] PR #1212 머지 (devel, closes #1211).

## M100 — v1.0.0 editing foundation stage

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #1201 | HWPX masterpage XML의 EVEN/ODD 바탕쪽 파싱 및 section 연결 | 진행중 | 브랜치 `task-1201-hwpx-masterpage`, 구현계획서 작성 완료, 다음 단계: Stage 1 승인 |

## PR #1193 (Fix mac window resizing, @chkwon — rhwp 첫 기여자) — 완료

- PR #1193 → **MERGED** (devel `8a2e60ca`). 연결 이슈 #1191 → CLOSED (`closes #1191`, cross-repo `--no-ff` 라 수동 클로즈).
- 본질: `#editor-area` grid 트랙 `1fr`(=`minmax(auto,1fr)` 콘텐츠 최소 래칫) → `minmax(0, 1fr)` 로 변경. macOS 창 넓혔다 줄일 때 가운데 정렬 깨지는 #1191 해소. CSS 정석 해결책, 1파일 +5/-2.
- 검증: 로컬 머지 CLEAN / `npm run build`(tsc+vite) / WASM Docker 빌드. **작업지시자 시각 판정 통과**(창 리사이즈 가운데 정렬 유지).
- **@chkwon 첫 기여 → 따뜻한 환영 코멘트** 게시 (feedback_pr_comment_tone 취지와 별개로 첫 기여 환영은 적절).
- 보고서: pr_1193_report.md / 검토서: pr_1193_review.md

## Task #1189 — task 1139 후속 보정

- [x] Stage6 시작: `3-11월_실전_통합_2022.hwp` 1쪽 `문1)`과 첫 수식 사이 간격 불일치 재분석.
- [x] Stage6 비교: PDF/한컴 기준 산출물과 현재 SVG/PNG 비교.
- [x] Stage6 구현: `U+FFFC` 인라인 객체 placeholder가 텍스트 폭에 포함되어 첫 수식이 오른쪽으로 밀리는 문제 보정.
- [x] Stage6 검증: 대상 회귀 테스트와 시각 산출물 확인.
- [x] Stage6 커밋 전 검증: `cargo test --tests`, `wasm-pack build --target web --out-dir pkg` 통과.
- [ ] Stage6 커밋 및 PR 준비.

- [x] Stage7 시작: `3-10월_교육_통합_2022.hwp` 11쪽 미주 간격 이상 재확인.
- [x] Stage7 비교: PDF/한컴 기준 산출물과 현재 SVG/PNG 비교.
- [x] Stage7 구현: 빈 문단 뒤 compact 미주 제목의 추가 40px 완충 제거로 PR #1194 보정.
- [x] Stage7 1차 검증: `cargo test --test issue_1139_inline_picture_duplicate -- --nocapture`, `cargo fmt --all --check` 통과.
- [x] Stage7 커밋 전 검증: `cargo test --tests`, `wasm-pack build --target web --out-dir pkg` 통과.
- [ ] Stage7 커밋 및 PR #1194 브랜치 push.

## Task #1195 — 표 셀 안 TAC 표 앞 빈 줄 미반영 제목·표 겹침 (hcar-001 1쪽) — 완료

- **결과**: 셀[28] "1.동의[필수]" 제목과 4×1 표 겹침 해소. 표 631.8→643.8(제목 639.8 아래). 시각 판정 통과.
- **원인/보정**: `layout_table_cells`(table_layout.rs) 표 분기가 표를 `para_y_before_compose`(첫 줄) 기준 배치 → 표 앞 공백 줄 누락. 이미지 TAC 분기 동형으로 `has_preceding_text && line_segs.len()>1` 시 표 line_seg vpos 상대오프셋 가산(한컴 line-feed 규칙). 가드로 무회귀.
- **검증**: test --tests 1894 passed 0 failed / 6페이지 회귀 p2~p6 무변경 / WASM 빌드 / 좌표 회귀 테스트(보정 OFF 시 FAIL 입증).
- Stage1~4 + 보고서 task_m100_1195_report.md. hcar 샘플 픽스처 커밋(pdf LFS). issue-267 충돌은 무관(devel 정상본 회귀).

(이하 진행 기록)

- 현상: 신청서 12×7 표 셀[28] "1. 개인정보 수집 및 이용 동의[필수]" 제목과 중첩 표가 세로로 겹침(한컴은 빈 문단 간격으로 분리). 작업지시자 한컴 vs rhwp 캡처로 확정.
- 진단(코드 무수정): IR 정상(HWP=HWPX vpos·lh 동일 → 파서 무결). 중첩 표 텍스트는 렌더되나 위치 겹침 → 셀 내부 다문단 `para_y` 누적이 빈 문단(text_len=0, 90% 글자에 따라) 높이를 충분히 전진 못함. `table_cell_content.rs` 654~702 루프 + `layout_composed_paragraph` 빈 문단 height. 트러블슈팅 line_spacing_lineseg_sync(ParaShape/LineSeg 이중구조).
- 이슈 #1195 생성+assign, 브랜치 local/task1195, 수행계획서 task_m100_1195.md. 디버그 SVG: output/poc/issue1195/hcar-001_001.svg.
- HWPX 크로스 체크 완료(동일 결함). 위험 중간(셀 공통 경로) → 구조 가드 우선. 수행계획 승인 → 구현계획서.

## PR #1206 (Task #1134: HWPX serializer 문단 id 충돌, @Mireutale 첫 기여) — CLOSED

- PR #1206 → **CLOSED** (머지 안 함). 이슈 #1134 → OPEN 유지(미해결).
- 내용: (1)문단 id 충돌 수정(context next_para_id 전역 카운터, 본문·셀 공유 — 적절) + (2)header.rs fillBrush 직렬화 완성(winBrush/gradation/imgBrush — PR 본문 미언급, 무관 변경 혼재).
- 닫는 사유: ① 대상이 미완성 모듈(HWPX 쓰기 serialize_hwpx 미완성, 주력은 exportHwp HWPX→HWP #178 어댑터) → 메인테이너 정비 후 진행. ② 무관 변경 혼재. ③ image_fill_mode 토큰(LEFT_BOTTOM/RIGHT_* 등) 자기 HWPX 파서가 못 읽어 라운드트립 비대칭 + 한컴 스펙 미확인(feedback_self_verification_not_hancom).
- brush 역할 확인: control.rs:1854 serialize_shape_fill(IR→HWP)과 같은 IR 공유. brush=도형/셀 배경 채우기, HWP는 숫자코드/HWPX는 문자열토큰.
- 컨트리뷰터에 첫 기여 환영 + id 수정만 분리 재제출 요청. 보고서: pr_1206_report.md.

## PR #1202 (HWPX 미주/각주 prefixChar — 마커 '문' 복원, @planet6897) — 완료

- PR #1202 → **MERGED** (devel `73034de9`). 연결 이슈 #1199 → CLOSED.
- 결함: HWPX <hp:endNote>/<hp:footNote> prefixChar(47928=0xBB38 '문')를 파서가 안 읽어 미주 마커 '문N)'→'N)' 탈락. 렌더 경로는 접두 지원 → 파서 단일 원인.
- 수정: parse_ctrl_endnote/footnote 에 suffixChar 대칭 prefixChar 분기 추가(section.rs +118). 모델/렌더러 무변경, prefixChar 없으면 0 유지(무회귀). 회귀 2건.
- 검증: merge CLEAN / fmt / build / test --tests 1896 passed 0 failed / 보정 전·후 '문' 9쪽 0→7, 10쪽 0→6 정량 / 한글2022 PDF 9쪽 '문1)~문7)' 정합. **작업지시자 시각 판정 통과(SVG↔PDF)**. WASM 빌드.
- 보고서: pr_1202_report.md / 검토서: pr_1202_review.md. #1200(curve)은 PR #1203 별개.

## Task #1217 — Dependabot 결합 의존성 그룹화 — 완료

- **결과**: `.github/dependabot.yml` 에 결합 그룹 추가. cargo `resvg-stack`(svg2pdf/pdf-writer/usvg/resvg/ttf-parser), npm `vite-stack`(vite/vite-plugin-pwa).
- **원인**: PR #1215(pdf-writer 0.12→0.15) CI FAILURE — svg2pdf 0.13 이 pdf-writer 0.12 에 고정, src/renderer/pdf.rs 가 `pdf_writer::Chunk` 공유 → 단독 bump 시 타입 충돌(로컬 재현 확증). Dependabot 이 결합 크레이트를 개별 PR 로 올려 반복 함정.
- **해결**: 결합 크레이트 1 PR 동반 bump. security-updates 제외/개별 crate 개별 PR 유지(작은 배치 철학). 기존 키 보존.
- **검증**: YAML 문법 / 기존 키 보존 / 그룹 구조·이름 규칙 / 설정 1파일만(소스 무변경). 빌드·테스트 무영향.
- 사후 관찰: 다음 weekly 실행 시 그룹 PR 묶임 확인(머지 후 GitHub 동작). 보고서 task_m100_1217_report.md.

### Dependabot PR 3건 (#1217 범위 밖, 별개 처리 예정)
- #1216 vite 8.0.14→8.0.15 (dev): CI green, `tsc && vite build` 성공 → 머지 후보.
- #1214 puppeteer-core 25.0.4→25.1.0 (dev): CI green, install/build 성공 → 머지 후보.
- #1215 pdf-writer 0.12→0.15 (런타임): svg2pdf 0.13 결합으로 빌드 불가 → close 후보(svg2pdf 동반 시 재시도).

## PR #1203 (HWPX curve <hp:seg> 외곽선 — '다른 풀이' 태그 박스, @planet6897) — 완료

- PR #1203 → **MERGED** (devel, 로컬 --no-ff + 충돌 해소). 연결 이슈 #1200 → CLOSED.
- 결함: 9쪽 "다른 풀이" curve 도형 외곽선 미렌더. geometry 가 `<hp:seg>` 417개로 인코딩됐으나 파서가 `<hc:pt>`만 읽어 CurveShape.points empty → 빈 path.
- 수정: parse_shape_object() 에 `b"seg"` 분기 추가(첫 seg 시작점 + 각 seg 끝점 → 폴리라인). 파서 단일, 순수 추가, curve 전용, 무회귀. 회귀 1건.
- 충돌(CONFLICTING): #1202 머지 전 분기 → tests 모듈 텍스트 인접 충돌(본문 b"seg" 분기는 자동 머지). 두 PR 테스트 3건 전부 보존하여 해소.
- 검증: 마커 0 / fmt / build / test --tests **1897 passed 0 failed** / 9쪽 외곽선 path d_len 15582(417-seg, devel 미존재) stroke="#000000" / 10·11쪽 무관. **작업지시자 시각 판정 통과(9쪽 SVG↔PDF)**.
- 보고서: pr_1203_report.md / 검토서: pr_1203_review.md.

## PR #1208 (HWPX 수식 스크립트 토큰 — root/bar/prime/cdots leak, @planet6897) — 완료

- PR #1208 → **MERGED** (devel --no-ff `77c7c27c` + cdots 보정 후속 커밋). 연결 이슈 #1204 → CLOSED.
- 결함: 20쪽 등 수식 토큰 leak — root3(√ 미렌더), rm bar(overline leak), primeF, cdotscdots(⋯⋯).
- 수정(컨트리뷰터): tokenizer glued-split 확장(root/sqrt·GEQ/LEQ digit, prime, longest_keyword_prefix GLUE_SAFE allowlist, over/atop ≤2자) + parser decoration 위임/대소문자 fallback.
- ⚠️ PR 본문 범위 축소 기술(#1204-E·관계연산자·대문자 미고지). allowlist 제한으로 회귀 0이나 본문 정정 요청 예정.
- 메인테이너 보정: GLUE_SAFE 에 cdots/ldots/vdots/ddots 추가(cdotscdots leak) + test_dots_glued_split. 모호 4자 dots 제외.
- 검증: test --tests **1907 passed 0 failed** / 20쪽 root3 7→0·bar 5→0·prime 1→0 / 전체 23쪽 정량 회귀 0(전부 leak→정상) / cdotscdots 14·18·19·20쪽→0. **작업지시자 시각 판정 통과(20쪽 SVG↔PDF)**.
- 보고서: pr_1208_report.md / 검토서: pr_1208_review.md.

## PR #1213 (그림/표/도형 textFlow roundtrip 보존, @Martinel2 첫 기여) — 완료

- PR #1213 → **MERGED** (devel --no-ff `31ce33f0`). 연결 이슈 #1188 → CLOSED.
- 결함: textFlow(개체 좌우 텍스트 흐름)가 roundtrip 시 항상 BOTH_SIDES 로 소실. 직렬화 하드코딩 + HWP5 attr bit 24-25 미파싱 + 모델 필드 부재.
- 수정: TextFlow enum + CommonObjAttr.text_flow, HWP5 (>>24)&0x03 파싱, HWPX 5위치 파싱, 직렬화 하드코딩 제거, 비트패킹 <<24. 7파일 +200/-21.
- 권위 검증: hwp2hwpx TextHorzArrange BothSides(0)/LeftOnly(1)/RightOnly(2)/LargestOnly(3) 정확 일치. textFlow≠textWrap 구분 정확.
- 검증: CI 미실행 → 로컬 fmt/build/clippy(lib) clean / test --tests **1916 passed 0 failed** / 실샘플 hwp3-sample19 LARGEST_ONLY→IR LargestOnly 파싱 / bit 24-25 충돌 없음.
- 시각: 렌더러 text_flow 미참조 → SVG 보정 전/후 byte 동일(무회귀 실증). IR 보존 PR 이라 시각 변화 없음이 정상 — 작업지시자 roundtrip+권위 검증으로 머지 결정.
- @Martinel2 첫 기여 → 환영 코멘트. 보고서: pr_1213_report.md / 검토서: pr_1213_review.md.

## PR #1222 (HWPX 본문·표 셀 문단 id 전역 유니크, @Mireutale #1206 분리 재제출) — 완료

- PR #1222 → **MERGED** (devel --no-ff `6f9036f7`). 연결 이슈 #1134 → CLOSED.
- 배경: #1206 close 시 요청한 "id 충돌 수정만 분리 재제출" 이행. fillBrush(header.rs) 제외 확인.
- 수정: SerializeContext next_para_id() 전역 카운터, 본문·각주·셀 문단 공유 → <hp:p id> 중복 방지. context/section/table + 통합테스트 4파일 +159/-7.
- #1206 사유 처리: ②fillBrush 혼재 해소(제외), ③image_fill_mode 비대칭 해소(fillBrush 제외), ①미완성 모듈은 id 수정 "적절" 평가+분리 합의 이행. cell_split_save_corruption 손상 방지 방향.
- 충돌(CONFLICTING): #1213(textFlow) 테스트와 table.rs tests 인접 충돌 → 양쪽 8건 보존 해소.
- 검증: CI 미실행 → 로컬 fmt/build/clippy(lib) clean / test --tests **1921 passed 0 failed** / id 단위 4건 + 통합 para_ids_unique_across_body_and_table. 시각 무관(XML id).
- @Mireutale 첫 기여 + 메인테이너 안내 따라 분리 재제출 → 정중한 감사 코멘트. 보고서: pr_1222_report.md / 검토서: pr_1222_review.md.

## PR #1226 (수식 LEFT-RIGHT 그룹 뒤 첨자 결합 |x|^3, @twoLoop-40 첫 기여) — 완료

- PR #1226 → **MERGED** (devel --no-ff `211a483d` + 회귀 테스트 후속). 연결 이슈 없음(KICE 시험지 수식 제보).
- 결함: parse_command LEFT 분기가 parse_left_right()를 try_parse_scripts 없이 직접 반환 → |x|^3 의 ^3 가 orphan Superscript{base:Empty}. 다른 primary 는 모두 try_parse_scripts 경유.
- 수정: try_parse_scripts(node) 위임(+4/-1). 메인테이너 회귀 테스트 left_right_group_binds_trailing_script 추가.
- base=main(우리 규칙 devel) → 변경 무충돌이라 devel 로 머지 + PR base 안내 코멘트.
- 검증: fmt/build/clippy(lib) clean / test --tests **1922 passed 0 failed** / AST 전후(base Empty→Paren) / SVG '3' y 17.40→11.20(superscript) / 회귀 x^2·|x|_3·|x|^2_3 정상. **작업지시자 시각 판정 통과**.
- @twoLoop-40 첫 기여 → 환영. 보고서: pr_1226_report.md / 검토서: pr_1226_review.md.

## PR #1207 (exam_social 중첩 표 셀 붙여넣기 경로 보존, @postmelee) — 완료 (메인테이너 처리)

- PR #1207 → **MERGED** (devel --no-ff `e91385ae`). 연결 이슈 #1198 → CLOSED.
- 결함: 중첩 표 안쪽 셀(성명 칸) 붙여넣기가 바깥 셀로 해석. 붙여넣기 API 가 단일 셀 4파라미터만 받아 cellPath 표현 불가.
- 수정: cellPath 기반 pasteInternalInCellByPath/pasteHtmlInCellByPath native+WASM 추가, text_editing 중첩 path 지원, TS cellPath.length>1 라우팅. 기존 단일 셀 API 보존(opt-in 하위 호환). 13파일 +996/-239.
- 검증: fmt/build/clippy(lib) clean / test --tests **1924 passed 0 failed** / issue_1198 통합 2건 + issue_850 회귀 3건 / rhwp-studio 빌드 / CI green. **작업지시자 동작 테스트 통과(성명 칸 붙여넣기→안쪽 셀 삽입)**. WASM 빌드(신규 API 노출 확인).
- @postmelee 핵심 컨트리뷰터. 보고서: pr_1207_report.md / 검토서: pr_1207_review.md. copy source path API 는 후속 이슈.

## PR #1220 (HWP5 wrap=Square 호스트 본문 커서 전진 — 답안↔문제 겹침, @planet6897) — 완료

- PR #1220 → **MERGED** (devel --no-ff `351868ec`). 연결 이슈 #1218 → CLOSED.
- 결함: 3-09월_교육_통합_2023 4쪽 문26 답안 ①이 문제 끝줄과 세로 겹침. wrap=Square 인라인 표가 커서를 표 높이만 전진, 호스트 본문(layout_wrap_around_paras) 미전진 → 본문>표 시 다음 단락이 표 하단에서 시작.
- 수정: layout.rs 단일 hunk(+19) — host_text_bottom = table_y_before + (text_h - last_ls) 까지 커서 전진, `> y_offset` 가드로 본문≤표 케이스 불변.
- ⚠️ double advance 트러블슈팅(square_wrap_pic_bottom) 무재발 확인: exam_science 4쪽/37items/45items 보정 전후 동일. 호스트 본문 자기 높이 기준이라 wrap-around 누적과 결합 안 함.
- 검증: fmt/build clean / test --tests **1924 passed 0 failed**(svg_snapshot/issue_546 wrap_around 회귀 포함) / 문26 ① y 906.3(겹침)→922.8(18px 분리). **작업지시자 시각 판정 통과(SVG: 4쪽 ①~⑤ 분리↔PDF / rhwp-studio 편집기 canvas 양쪽 정합)**. WASM 빌드.
- 보고서: pr_1220_report.md / 검토서: pr_1220_review.md. z-표 행 압축은 별도 이슈.

## PR #1223 (수식 포함 줄 본문 한글 압축·겹침 해소, @planet6897) — 완료

- PR #1223 → **MERGED** (devel --no-ff `9c7298ac`). 연결 이슈 #1219 → CLOSED.
- 결함: 3-09월_교육_통합_2023 6쪽 문26 인라인 수식(TAC) 포함 줄 본문 한글이 거짓 오버플로우로 음수 자간 압축(8.96px, PDF ≈12px). align=Left·자간0%.
- 원인 둘: (1) est_x/total_tac_width 가 pos<=line_end(포함) 필터로 다음 줄 선두 수식(55px) 오포함 → 거짓 오버플로우. (2) 선두 미주(Endnote) endnote_marker_x_advance + footnote_positions 이중계상.
- 수정: paragraph_layout.rs 측정을 line_tac_offsets(렌더와 동일 엄격 미만)로 통일 + start_line==0 선두 Endnote 측정 제외. 측정 코드만(렌더/모델 무변경).
- 검증: fmt/build clean / test --tests **1925 passed 0 failed** / **golden SVG 8건**(인라인 TAC/각주/목차/우측탭 회귀 0) / issue_1219 회귀 / 한글 advance 8.96→11.93px(정상 줄 12.0 불변). **작업지시자 SVG 시각 판정 통과**. WASM 빌드.
- 보고서: pr_1223_report.md / 검토서: pr_1223_review.md. 글리프 크기(Noto↔한컴돋움 폰트대체)는 별도 이슈.

## PR #1225 (HWP5 수식-only 셀 z-표 행 압축 수정 — tac 순서매핑, @planet6897) — 완료

- PR #1225 → **MERGED** (devel --no-ff `421529ae`). 연결 이슈 #1221 → CLOSED. (#1220 z-표 후속)
- 결함: 3-09월_교육_통합_2023 4쪽 문26 z-표 z값 "1.0"/"1.1" 겹쳐 "1.01.". z값=인라인 수식, 수식-only 셀 문단은 모든 줄 char_start=0(degenerate) → tac 줄 char-범위 매핑 붕괴(line1 모든 수식 흡수).
- 수정: paragraph_layout 빈-runs tac 블록에 index_based_tac 가드(다중 줄+줄수==tac수+모든 줄 빈 runs+char_start degenerate) 하 순서 1:1 매핑. 그 외 char-범위 유지(일반 문단 불변). +23 단일 블록.
- 검증: fmt/build clean / test --tests **1925 passed 0 failed** / golden SVG 8건 / 전체 23쪽 diff 4쪽 1곳만 변화(z-표 수식 y 925.3 겹침→913.3/925.3 세로 분리, x 정렬), 22쪽 무영향. **작업지시자 시각 판정 통과(4쪽 z-표 행 분리↔PDF)**. WASM 빌드.
- 보고서: pr_1225_report.md / 검토서: pr_1225_review.md.

## PR #1228 (표 셀 중첩 picture 복사 + 떠있는 개체 paste cascade, @johndoekim) — 완료

- PR #1228 → **MERGED** (devel --no-ff `b9341872`). 연결 이슈 #1161 → CLOSED.
- 결함: 표 셀(중첩) inline picture 복사(Ctrl+C) 미지원. 복사 native 4종 cell_path 미수신 + ImageNode 다단계 cellPath 미보유(스칼라만).
- 수정: ImageNode cell_context(TextRunNode 정합) + layout 3 chokepoint + rendering cellPath JSON 방출 + 복사 4종 cell_path/resolve_control_para + WASM 4인자화 + floating cascade(inline 제외). additive 하위호환. 28파일 +1026/-86.
- 검증: fmt/build/clippy(lib) clean / test --tests **1933 passed 0 failed**(golden SVG 회귀 0) / issue_1161 5건 / WASM 재빌드 후 tsc 0 errors. **작업지시자 동작 판정 통과(표 안 인라인 그림 복사→붙여넣기 성공)**. WASM 빌드.
- **글상자 안 그림 선택 불가** 발견 → 범위 밖(#1171 계열 hit-test). 작업지시자 지시로 별도 이슈 등록.
- 보고서: pr_1228_report.md / 검토서: pr_1228_review.md. 후속 #1227(무관 직렬화 버그).

## Task #1209 — task 1139 후속 보정: 3-11월 실전 14쪽 문22 미주 간격

- [x] 이슈 등록: [#1209](https://github.com/edwardkim/rhwp/issues/1209)
- [x] 브랜치 생성: `local/task_m100_1209`
- [x] Stage0 문서 생성: `mydocs/working/task_m100_1209_stage0.md`
- [x] Stage0 PDF/SVG/rsvg-convert 비교 산출물 생성.
- [x] Stage0 `dump-pages`/`RHWP_VPOS_DEBUG` 기반 원인 분석.
- [x] Stage0 분석 결과 기록: `pi=631 -> pi=632`에서 stale forward를 버리면서 직전 `line_spacing=1984HU`까지 사라지는 문제로 좁힘.
- [x] Stage0 커밋: `6f6a5e71` (`task 1209: Stage0 원인 분석 기록`)
- [x] Stage1 구현 스테이지 생성: `mydocs/working/task_m100_1209_stage1.md`
- [x] Stage1 구현: compact 미주 질문 제목 stale forward에서 직전 line spacing만 보존.
- [x] Stage1 추가 비교: `문23)`도 PDF/한컴 기준과 대조.
- [x] Stage1 추가 구현: 빈 spacer 뒤 새 미주 제목은 compact deep-backtrack 제외.
- [x] Stage1 재확인: 작업지시자 지적으로 17쪽 `문27)`, `문28)` 본문/이전 미주 조각 뒤 제목 간격 부족 확인.
- [x] Stage1 공통 보정: 이어진 `PartialParagraph` 뒤 compact 미주 제목에는 직전 `line_spacing`을 미주 사이 간격으로 보존하고 VPOS base도 함께 이동.
- [x] Stage1 검증: 대상 회귀 테스트와 14쪽/17쪽 rsvg-convert 산출물 확인.
- [x] Stage1 커밋: `0c503873` (`task 1209: Stage1 미주 사이 간격 보정`)
- [x] Stage2 문서 생성: `mydocs/working/task_m100_1209_stage2.md`
- [x] Stage2 1차 분석: 17쪽 `문29)` 현재 RHWP red row `y=792..802px`, PDF red row `y=813..823px`로 약 `21px` 위에 있음을 확인.
- [x] Stage2 1차 판단: task 1139/1189/1209 미주 간격 보정은 케이스별로 흩어져 있으며, `raw_unknown` 전역 가산이 아니라 “이미 반영된 기본 7mm”와 “split/rewind/stale-forward에서 손실된 gap”을 구분하는 공통 정책으로 정리해야 함.
- [x] Stage2 승인: 작업지시자가 공통 gap 보존 방향 진행 승인.
- [x] Stage2 구현: compact 미주 제목 앞에서 직전 paragraph/partial의 trailing `line_spacing`이 손실된 경우만 공통 최소 gap으로 보존.
- [x] Stage2 검증: `cargo test --test issue_1139_inline_picture_duplicate -- --nocapture` 통과(37 passed, 0 failed).
- [x] Stage2 시각 산출물: 17쪽 `문29)` RHWP red row `y=812..821px`, PDF red row `y=813..822px` 확인.
- [x] Stage2 WASM 검증: `wasm-pack build --target web --out-dir pkg` 통과.
- [x] Stage2 작업지시자 시각 판정 및 커밋 승인.
- [x] Stage2 커밋: `90eb8385` (`task 1209: Stage2 미주 제목 공통 간격 보정`, `upstream/devel` rebase 후)
- [x] Stage3 문서 생성: `mydocs/working/task_m100_1209_stage3.md`
- [x] Stage3 분석: `3-09월_교육_통합_2024-구분선아래20.hwp`, `3-09월_교육_통합_2024-미주사이20.hwp`와 각 `-2024.pdf` 비교.
- [x] Stage3 판정: 페이지 수와 shape 값은 맞지만, `구분선아래20` 후반 페이지와 `미주사이20` 후반 페이지가 PDF와 다르고 첫 미주 페이지에서 렌더 overflow 로그가 발생함.
- [x] Stage3 rebase: `upstream/devel` `73034de9` 기준으로 `local/task_m100_1209` rebase 완료.
- [x] Stage3 구현: `EndnoteSeparator`/`betweenNotes` 시각 gap을 문항별 예외가 아닌 공통 미주 모양 helper로 pagination fit budget에 반영.
- [x] Stage3 검증: `cargo test --test issue_1139_inline_picture_duplicate -- --nocapture` 통과(37 passed, 0 failed).
- [ ] Stage3 남은 시각 확인: `미주사이20` 23쪽은 페이지 수/후반 분배는 맞지만 `문29)` 상단 시작 위치가 PDF보다 낮아 추가 시각 판정 필요.
- [x] Stage3 커밋: `fb6b358b` (`task 1209: Stage3 2024 미주 모양 공통 처리`)
- [x] Stage4 문서 생성: `mydocs/working/task_m100_1209_stage4.md`
- [x] Stage4 분석: `3-09월_교육_통합_2022.hwp` 10쪽 `문12)` 배치 PDF 비교.
- [x] Stage4 추가 분석: 같은 파일 13쪽 `문19)` 그래프 주변 문단 겹침 원인 확인.
- [x] Stage4 구현: 미주 가상 문단에도 비-TAC `Square` 그림 wrap anchor를 등록해 후속 문단이 한컴/PDF처럼 좁은 `LINE_SEG` 영역을 사용하도록 보정.
- [x] Stage4 추가 구현: `Square` 그림 host 공백 줄에 TAC 수식이 있는 경우 0높이 guide 줄로 오판하지 않게 해 13쪽 `문19)` 꼬리 수식과 다음 문단의 세로 겹침 해소.
- [x] Stage4 검증: `cargo test --test issue_1139_inline_picture_duplicate -- --nocapture`, `cargo fmt --all --check`, `git diff --check`, `wasm-pack build --target web --out-dir pkg` 통과.
- [x] Stage4 작업지시자 시각 판정 및 커밋 승인.
- [x] Stage4 커밋: `ca9eaace` (`task 1209: Stage4 문19 줄 겹침 보정`)
- [x] Stage5 문서 생성: `mydocs/working/task_m100_1209_stage5.md`
- [x] Stage5 1차 분석: PR #1015 `test-image.hwp` fixture 기준으로 `자리차지=TopAndBottom(bit 1)`, `어울림=Square(bit 0)` 재확인.
- [x] Stage5 추가 확인: 다시 저장된 `test-image.hwp`는 각 배치 방식을 별도 페이지/문단의 단일 그림으로 담고 있으며, record 순서는 `TopAndBottom(자리차지) → Square(어울림) → InFrontOfText(글앞으로) → BehindText(글뒤로)`이다.
- [x] Stage5 추가 확인: 현재 `test-image.hwp/.hwpx`의 `글자처럼 취급` 그림은 `treat_as_char=true` + `BehindText`로 저장되어 있어, wrap 라벨과 TAC 플래그를 분리해서 해석한다.
- [x] Stage5 PR #1015 영향 검토: fallback 로직은 1쪽 `자리차지` 문단 `vpos=15180` 처리에 여전히 필요하므로 유지하고, 현재 fixture 구조에 맞게 코드 주석만 정정.
- [x] Stage5 진단기 보정: `dump`의 `TextWrap::TopAndBottom` 한글 라벨을 `자리차지`로 수정하고, `hwp5-anchor-trace`에 CommonObjAttr 비트 해석을 추가.
- [x] Stage5 스펙 문서 보정: 원문 표는 유지하고 `mydocs/tech/한글문서파일형식_5.0_revision1.3.md`, `mydocs/tech/hwp_spec_errata.md`에 HWP5 CommonObjAttr wrap 비트 실측 기준 보완 주석 추가.
- [x] Stage5 자리차지 렌더 보정: 비-TAC `TopAndBottom` + 문단 기준 그림/도형은 문항별 예외가 아니라 `LINE_SEG.vertical_pos`를 줄별 y 좌표로 반영하는 공통 경로로 처리.
- [x] Stage5 회귀 테스트 추가: `samples/test-image.hwp` 1쪽 자리차지 그림 하단보다 본문 줄이 위로 올라오지 않는지 확인.
- [x] Stage5 대상 재확인: 현재 화면의 `3-09월_교육_통합_2022.hwp`와 `2024-구분선아래20.hwp` 모두 8쪽 `문29)` 그림은 `pi=429 ci=21`, raw `wrap=0(Square/어울림)`이며 후반 `LINE_SEG` 폭이 줄어드는 어울림 흐름을 저장함. `TopAndBottom/자리차지`로 강제 변환하지 않음.
- [x] Stage5 구현: `Square/어울림 + VertRelTo::Para` 비-TAC 그림은 문단 시작 y가 아니라 `LINE_SEG`가 처음 좁아지는 줄의 `vertical_pos`를 그림 상단으로 삼도록 보정.
- [x] Stage5 검증: `cargo fmt --all --check`, `cargo test --test issue_1139_inline_picture_duplicate -- --nocapture`, `wasm-pack build --target web --out-dir pkg` 통과. `rsvg-convert` 산출물 시각 확인.
- [x] Stage5 커밋: `e862c8c0` (`task 1209: Stage5 어울림 그림 줄 위치 보정`, `samples/test-image2.hwp` 포함)
- [x] Stage6 문서 생성: `mydocs/working/task_m100_1209_stage6.md`
- [x] Stage6 분석: `3-09월_교육_통합_2022.hwp` 10쪽 `문12)` 제목/수식 블록이 저장 `LINE_SEG`의 안전한 mid-column VPOS 되감김을 따르지 않아 PDF보다 아래로 밀리는 문제 확인.
- [x] Stage6 구현: `HeightCursor`에 단 중간부 전용 `compact_endnote_safe_vpos_backtrack` 공통 가드를 추가해 직전 visible content를 침범하지 않는 저장 VPOS만 반영.
- [x] Stage6 회귀 방어: 같은 큰 `line_spacing`이라도 단 하단 미주 제목(`3-10월` 11쪽 `문20)`, `3-11월` 14쪽 `문23)`)은 기존 미주 사이 간격을 유지하도록 하단부에서는 새 가드를 적용하지 않음.
- [x] Stage6 검증: `cargo fmt --all --check`, `cargo test --test issue_1139_inline_picture_duplicate -- --nocapture` 통과(41 passed, 0 failed), `wasm-pack build --target web --out-dir pkg` 통과.
- [x] Stage6 시각 산출물: `output/task1209_stage6_page10_final/rhwp_page10.png`, `output/task1209_stage6_page10_final/pdf_page10.png` (`rsvg-convert` 사용).
- [x] Stage6 커밋: `e0151e37` (`task 1209: Stage6 미주 VPOS 되감김 보정`, `upstream/devel` rebase 후)
- [x] Stage7 문서 생성: `mydocs/working/task_m100_1209_stage7.md`
- [x] Stage7 범위 정정: 실제 대상은 문단 모양/글자 모양 등 모달 대화상자의 title bar 드래그 이동 불가 문제로 재확인.
- [x] Stage7 구현: `dialog-drag.ts` 공통 유틸 추가, 이동 시작 시 `position: fixed` 좌표계로 전환하도록 문단/글자 모양 및 주요 모달 대화상자에 적용.
- [x] Stage7 검증: `npm run build`, `cargo test --test issue_1139_inline_picture_duplicate -- --nocapture` 통과. rebase 후 `pkg` 타입 정의 갱신이 필요해 `wasm-pack build --target web --out-dir pkg` 후 `npm run build` 재실행 통과.
- [x] Stage7 수식 회귀 확인: closed PR `#1208`, `#1223`, `#1225`, `#1226` 범위를 `cargo test --lib renderer::equation -- --nocapture`, `cargo test --test issue_1219_equation_line_hangul_advance -- --nocapture`, `cargo test --test issue_1139_inline_picture_duplicate -- --nocapture`로 재점검.
- [x] Stage7 작업지시자 확인: 커밋 후 PR 준비 지시.
