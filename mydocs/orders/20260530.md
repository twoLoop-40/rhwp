# 2026-05-30 — 오늘 할일

## Task M100-1172 — rhwp-studio 문단모양 여백 2배 + 테두리/배경 결함 (#1172)

- [x] 작업지시자 피드백: 문단모양 왼쪽 여백 10pt → 20pt, 여백만 바꿔도 테두리 배경 생성
- [x] 이슈 #1172 등록 + assignee
- [x] 착수 점검: 열린 PR(#1170/#1159 무관) + 트러블슈팅(image_crop_scale_rule, paragraph_indent)
- [x] `local/task1172` 브랜치
- [x] 진단 (작업지시자 제공 샘플 para-001.{hwp,hwpx} + saved/para-001-s.hwp):
  - 결함 A 여백 2배: ParaShape margin IR 2× 스케일(한컴 IR2000=10pt) — build_para_properties_json ÷2 환산 누락
  - 결함 B 테두리: patternType=-1 문단에서 무늬 select '-1' 옵션 부재 → collectMods 0!=-1 오인 → fillType=solid 강제 주입 → border_fill 생성
  - 부수: HWPX winBrush faceColor="none" → Solid 오파싱
- [x] 정정:
  - formatting.rs: margin/indent 표시 hwpunit_to_px(raw/2)
  - para-shape-tab-builders.ts: 무늬 '없음' value 0→-1
  - para-shape-dialog.ts: collectMods patternType ?? 0→-1 + NaN 방어
  - header.rs: winBrush faceColor=none → FillType::None
  - tests/hwpx_to_hwp_adapter.rs: task888 카운트 1→0 (파서 단계 처리)
- [x] e2e 검증 (호스트 CDP): 여백 변경 시 mods={"marginLeft":2000} (fill 미포함)
- [x] 회귀 가드 tests/issue_1172 + 전체 --tests + fmt + clippy + tsc 통과
- [x] **작업지시자 시각 판정 통과** (여백 정상 + 테두리 미생성)
- [x] 최종 보고서 + e2e-cdp 진단 방법론 활용
- [ ] commit + devel 머지 + close #1172

## PR #1174 — filename field context 멀티 렌더러 일관화 (by @postmelee, closes #1144)

- [x] PR 정보 확인 — +160/-1 3파일(wasm_api.rs/wasm-bridge.ts/issue_1144.rs), CI pass, BEHIND
- [x] 검토: set_file_name → invalidate_page_tree_cache (stale filename cache 방지) + Studio setter 전파. 회귀 5개(새 fixture 없이 command API)
- [x] 충돌 점검: auto-merge clean (3파일 무충돌)
- [x] 검증: 빌드 + issue_1144 5(native-skia 포함) + 전체 --tests + fmt
- [x] 승인 → devel 머지(804b4ae9) + PR 코멘트 + close #1144 + 보고서 archives

## PR #1175 — external image reference discovery contract (by @postmelee, closes #1142)

- [x] PR 정보 확인 — +225/-30 2파일(wasm_api.rs/issue_1142.rs), CI pass, BEHIND
- [x] 검토: getExternalImageReferences 구조화 API(discovery만), index-first loaded 판정, basename 호환. 순수 opt-in
- [x] 충돌 점검: auto-merge clean (wasm_api.rs #1144 와 다른 영역)
- [x] 검증: 빌드 + issue_1142 5 + 전체 --tests + fmt
- [x] 승인(권장안) → devel 머지(a19d8e8d) + PR 코멘트 + close #1142 + 보고서 archives

## Task M100-1166 — HWPX 가로 편집 용지 무시 결함 (#1166, bug)

- [x] 이슈 #1166 확인(기등록) + assignee + 수행/구현계획서 승인
- [x] 진단: HWPX landscape 무시(parser:234) + 직렬화 하드코딩. 권위 매핑 WIDELY=세로/NARROWLY=가로 (hwplib)
- [x] Stage 1: parse_page_pr landscape 매핑 + RED 테스트 (파싱만으로 렌더 자동 정합 — PageDef.landscape + page.rs swap 기존)
- [x] Stage 2: HWPX 직렬화 IR 치환 + HWP5 직렬화 attr bit0 동기화 (작업지시자 지적 HWPX→HWP 저장 보존)
- [x] Stage 3: 통합 검증(--tests + native-skia 32 + fmt + clippy) + wasm + 작업지시자 동작 판정 통과
- [x] 최종 보고서 + issue_1166 6 테스트(파싱 3 + round-trip 3)
- [ ] commit + devel 머지 + close #1166

## Task M100-1179 — Rust 테스트 경고 정리 (#1179)

- 이슈: [#1179](https://github.com/edwardkim/rhwp/issues/1179)
- 브랜치: `local/task_m100_1179`
- 기준: 최신 `upstream/devel`
- 상태: PR #1180 생성 완료

### 진행 항목

- [x] 작업지시자 경고 로그 확인
- [x] 이슈 #1179 생성
- [x] 최신 `upstream/devel` 기준 새 브랜치 생성
- [x] Stage1 문서 생성
- [x] 경고 재현
- [x] 경고 제거 수정
- [x] 검증
- [x] 커밋 및 PR 준비

### 현재 판단

- 대상 경고는 테스트 실패가 아니라 `cargo test renderer::height_cursor::tests::compact_endnote -- --nocapture` 실행 중 출력되는 warning 6개다.
- 수정 범위는 테스트/진단 코드의 attribute, 네이밍, unused result 정리로 제한했다.
- 검증: `cargo fmt --all --check`, `cargo test renderer::height_cursor::tests::compact_endnote -- --nocapture` warning 없이 통과.
- PR: [#1180](https://github.com/edwardkim/rhwp/pull/1180)

## Task #1139 — `3-09월_교육_통합_2022.hwp` 20쪽 하단 미주 흐름 보정

- 이슈: [#1139](https://github.com/edwardkim/rhwp/issues/1139)
- 브랜치: `local/task_m100_1139`
- 상태: PR #1178 생성 완료
- assignee: 현재 `jangster77` 계정 권한 제한으로 미지정 상태를 유지한다. `ReplaceActorsForAssignable` 오류가 예상되므로 assignee 지정 명령은 시도하지 않는다.

### 진행 항목

- [x] 이슈 #1139 상태 확인
- [x] 열린 PR 확인
- [x] Stage27 기준 커밋 확인
- [x] Stage28 문서 확인
- [x] PDF 기준 파일 확인: `pdf/3-09월_교육_통합_2022.pdf`
- [x] PDF 19/20/21쪽과 RHWP SVG/dump 재추출
- [x] 작업지시자 해결 진행 지시 확인
- [x] PDF 23쪽 단일 비교 산출물 생성: `output/task1139_stage28_page23_compare/`
- [x] 19→20쪽 미주 분배 원인 보정
- [x] 23쪽 split 미주 내부 빈 줄 TAC Picture 누락 보정
- [x] 회귀 테스트 추가
- [x] Rust/WASM 검증
- [x] Stage28 보고서 갱신
- [x] 작업지시자 23쪽 시각 검증 완료 확인
- [x] Stage28 커밋
- [x] Stage29 문서 생성
- [x] Stage29 현상황 커밋
- [x] Stage30 문서 생성
- [x] Stage30 머릿말 불일치 분석 및 보정
- [x] Stage30 Rust/WASM 검증
- [x] 작업지시자 Stage30 시각 검증 완료 확인
- [x] Stage30 커밋
- [x] `upstream/devel` rebase 동기화
- [x] Stage31 문서 생성

### 현재 판단

- PDF 기준 20쪽은 `문27)`만 문제가 아니라, 19쪽 마지막 단에서 문29 풀이 끝부분이 RHWP보다 더 들어가서 20쪽 시작점 자체가 RHWP보다 뒤쪽이다.
- RHWP 20쪽은 `pi=1020`부터 시작하지만, PDF 기준 20쪽은 `g'(2)` 계산 이후부터 시작한다.
- 우선 19쪽 마지막 단에서 문29 후반 `pi=1020/1021` 수준의 작은 tail이 다음 쪽으로 밀리는 조건을 좁게 보정한다.
- PDF 23쪽 단일 비교의 핵심 누락은 `pi=1175` line 10의 `Picture ci=20`이다. split된 partial paragraph 내부의 "빈 줄 + TAC Picture" line을 `paragraph_layout`이 건너뛰던 문제를 보정했다.
- 작업지시자가 23쪽 시각 검증 완료를 확인했으므로 Stage28 변경분을 커밋한 뒤, `3-09월_교육_통합_2023.hwp` 4쪽 문26 겹침 문제를 다음 스테이지로 시작한다.

## Task #1139 Stage29 — `3-09월_교육_통합_2023.hwp` 4쪽 문26 겹침

- 기준 커밋: `ea0b39c8` (`task 1139: Stage28 23쪽 split 그림 렌더 보정`)
- 대상 파일: `samples/3-09월_교육_통합_2023.hwp`
- 기준 PDF: `pdf/3-09월_교육_통합_2023.pdf`
- 상태: Stage29 커밋 완료

### 진행 항목

- [x] Stage28 커밋 이후 새 스테이지 문서 생성
- [x] 대상 HWP/PDF 파일 존재 확인
- [x] 작업지시자 Stage29 문제 해결 진행 지시 확인
- [x] 4쪽 PDF/RHWP 비교 산출물 생성
- [x] 4쪽 `dump-pages` 분석
- [x] 문26 겹침 원인 분리
- [x] 회귀 테스트 추가
- [x] Rust/WASM 수정 및 검증
- [x] Stage29 보고서 갱신
- [x] 12쪽 PDF/RHWP 추가 비교
- [x] 13쪽 PDF/RHWP 추가 비교
- [x] 작업지시자 12쪽/13쪽 문제 해결 지시 확인
- [x] 12쪽/13쪽 미주 단 분배 원인 분리
- [x] 12쪽/13쪽 후속 보정 및 검증
- [x] 작업지시자 현상황 커밋 지시
- [x] Stage29 커밋

### 현재 판단

- 4쪽 `문26)` 겹침은 `pi=258 ci=5` Square wrap 표가 문단 첫 줄 y에 붙어 렌더된 것이 원인이다.
- 해당 문단의 LineSeg는 마지막 줄만 폭이 줄어들어 있어, 표는 후반 줄 오른쪽에 붙어야 한다.
- 표의 시각 y만 LineSeg anchor로 내리고, 선택지 흐름은 기존 y를 유지하는 좁은 보정을 적용했다.
- `cargo fmt --all --check`, `cargo build`, `cargo test --test issue_1139_inline_picture_duplicate -- --nocapture`, `git diff --check`를 통과했다.
- 12쪽 추가 비교 결과 RHWP는 오른쪽 단 상단에 이전 풀이 tail이 남고 `문14)` 시작이 PDF보다 아래로 밀린다. 오른쪽 단 하단 `pi=625..634`에서 overflow도 발생한다.
- 13쪽 추가 비교 결과 RHWP는 첫머리에 이전 페이지 tail이 남아 PDF보다 시작점이 늦고, 왼쪽 단 `pi=656/657`, 오른쪽 단 `pi=695` overflow가 발생한다.
- 12쪽/13쪽 원인은 `pi=605` TAC 도형 포함 미주 문단의 formatter 높이 과충전, 새 미주 묶음 보호 규칙의 과도한 단 넘김, 빈 host TAC Picture의 y 이중 진행이었다.
- 보정 후 12쪽 왼쪽 단에 `문14)` 제목/첫 풀이가 남고, 13쪽은 `pi=637` 그래프부터 시작한다. 12쪽/13쪽 SVG export와 debug overlay export에서 `LAYOUT_OVERFLOW`는 발생하지 않았다.
- `wasm-pack build --target web --out-dir pkg`는 2026-05-30 21:26 작업지시자 재실행과 Codex 재실행에서 모두 통과했다. 산출물 경로는 `/Users/tsjang/Cloud/Devel/rhwp/pkg`.
- Stage29는 `6b4d6e3e` (`task 1139: Stage29 2023 문항 겹침 및 미주 분배 보정`)로 커밋했다.

## Task #1139 Stage30 — `3-09월_교육_통합_2022.hwp` 머릿말 불일치

- 기준 커밋: `6b4d6e3e` (`task 1139: Stage29 2023 문항 겹침 및 미주 분배 보정`)
- 대상 파일: `samples/3-09월_교육_통합_2022.hwp`
- 기준 비교: 한컴오피스 표시 화면 및 필요 시 `pdf/3-09월_교육_통합_2022.pdf`
- 상태: 커밋 완료, rebase 동기화 완료

### 진행 항목

- [x] Stage29 커밋 이후 새 스테이지 문서 생성
- [x] 작업지시자 머릿말 불일치 보고 확인
- [x] 1쪽 PDF/RHWP 비교 산출물 생성
- [x] 머릿말 영역 dump 추출
- [x] header/page/master/shape anchor 원인 분리
- [x] 회귀 테스트 추가
- [x] Rust/WASM 수정 및 검증
- [x] Stage30 보고서 갱신
- [x] 작업지시자 시각 승인

### 현재 판단

- 1쪽 상단 머릿말 불일치는 제목/회차 표가 header 본문 여백 9mm 기준으로 배치되어 한컴/PDF의 paper-based 쪽 테두리 왼쪽 간격 7mm보다 약 7.56px 오른쪽으로 밀린 것이 원인이다.
- 로고 Picture는 별도 경로로 이미 기준에 가까워 전체 머릿말 이동 대신 머릿말 표 레이아웃 기준 영역만 `PageBorderFill.spacing_left/right` 기준으로 보정했다.
- 비교 산출물은 `output/task1139_stage30_header_page1/`에 갱신했다.
- `cargo fmt --all --check`, `cargo build`, `cargo test --test issue_1139_inline_picture_duplicate -- --nocapture`, `git diff --check`, `wasm-pack build --target web --out-dir pkg`를 통과했다.

## Task #1139 Stage31 — rhwp-studio 미주 및 미주 모양 기능 추가

- 기준 커밋: `46b5be3c` (`task 1139: Stage30 머릿말 표 기준 보정`)
- 대상: rhwp-studio 입력 메뉴 미주 삽입 및 미주 모양 대화상자
- 상태: 구현 및 검증 완료

### 진행 항목

- [x] Stage30 커밋
- [x] `upstream/devel` 동기화
- [x] merge 동기화 커밋을 rebase 형태로 정리
- [x] Stage31 문서 생성
- [x] 각주 메뉴/WASM API 경로 확인
- [x] 현재 HEAD 각주 관련 회귀 테스트 확인
- [x] `upstream/devel` 메뉴 로그 비교
- [x] 회귀 여부 1차 결론 정리
- [x] 작업지시자 Stage31 목적 변경 확인
- [x] 한컴 도움말 미주/미주 모양 기능 기준 확인
- [x] 미주 삽입 native/WASM API 추가
- [x] 미주 모양 조회/적용 native/WASM API 추가
- [x] rhwp-studio 메뉴/툴바 미주 명령 연결
- [x] rhwp-studio 미주 모양 대화상자 추가
- [x] 미주 삽입 직후 미주 내용 편집 모드 진입 연결
- [x] 주석 편집 모드 전용 도구상자와 닫기 버튼 추가
- [x] Rust/WASM/Studio 검증
- [x] Stage31 커밋
- [x] PR 본문 초안 및 생성 명령 준비
- [x] PR #1178 생성
- [x] PR #1178 제한 사항 보강
- [x] PR #1178 Build & Test 실패 원인 수정

### 현재 판단

- Stage30 이후 `upstream/devel` 동기화는 최종적으로 rebase 형태로 정리했다. 임시 merge 결과 `ca4d27bb`는 `backup/task_m100_1139-merge-ca4d27bb`에 보존했다.
- 작업지시자가 보고한 `[footnote] pos/result` 콘솔 로그는 `result: {ok: true}`를 반환하는 성공 디버그 로그다.
- `git blame`과 `upstream/devel` 비교 결과 해당 `console.log` 두 줄은 초기 커밋부터 존재했고, Task #1139 변경이 추가한 에러 로그가 아니다.
- 현재 HEAD에서 `issue_1058_new_footnote_inner_para_contract`, `issue_598_footnote_marker_nav` 테스트는 통과했다.
- 새 문서에서 `insert_footnote_native(0,0,0)` 2회 호출 후 렌더 트리에 `FootnoteMarker`가 생성되지 않는 결함은 현재 HEAD와 `upstream/devel`(`a5a3e0e5`)에서 동일하게 재현된다.
- 원인 형태: 삽입 후 본문 문단이 `text=""`, `char_offsets=[]`, `controls=4`, `char_count=33` 상태로 남아 각주 control anchor가 없어 `composer`/`paragraph_layout`의 마커 생성 경로에 도달하지 못한다.
- 결론: 보고된 콘솔 로그 자체와 새 문서 각주 마커 미표시 문제는 Task #1139 회귀가 아니라 upstream에도 존재하는 기존 결함이다. 후속 수정은 별도 이슈/스테이지로 다루는 것이 맞다.
- 변경된 Stage31 목적은 한컴 도움말 기준 미주 기능 구현이다. 미주는 본문 위치에 번호를 남기고 미주 내용은 문서 끝 또는 구역 끝에 모이며, 미주 모양은 번호 서식/장식 문자/구분선/여백/번호 매기기/위치를 구역 속성으로 설정한다.
- 한컴 도움말 기준 미주 실행 직후 커서는 미주 내용 위치로 이동하고 `주석` 탭이 나타나며, 본문 복귀는 `<Shift+Esc>` 또는 `닫기`로 수행한다.
- 구현 결과: 새 빈 문서에서 미주 삽입 시 `Control::Endnote`와 미주 내용 `AutoNumberType::Endnote` 문단을 만들고, 본문/미주 내용에는 기본 `1)` 마커가 렌더된다.
- 미주 모양 API는 `SectionDef.endnote_shape`를 대상으로 번호 서식, 앞/뒤 장식 문자, 구분선, 구분선 위/아래, 미주 사이, 번호 매기기, 문서/구역 끝 위치를 조회/적용한다.
- 미주 모양 적용 시 기존 미주의 본문 번호와 미주 내용 번호도 함께 재번호/재동기화하여 앞 장식 문자, 뒤 장식 문자, 시작 번호가 즉시 반영된다.
- rhwp-studio는 미주/각주 삽입 직후 기존 note editing 경로로 진입하고, 한컴처럼 `주석` 전용 도구상자에 `각주`, `미주`, `각주/미주 모양`, `닫기` 버튼을 표시한다.
- 검증: `cargo fmt --all --check`, Stage31 대상 테스트 4개, 전체 `issue_1139_inline_picture_duplicate` 테스트 28개, `issue_598_footnote_marker_nav` 테스트 4개, `cargo build`, `npm --prefix rhwp-studio run build`, `git diff --check`, `wasm-pack build --target web --out-dir pkg` 통과.
- 추가 UI 검증: 로컬 Chrome headless에서 `미주` 클릭 후 주석 도구상자 표시, `121212` 입력, `닫기` 클릭 후 주석 모드 종료를 확인했다.
- PR: [#1178](https://github.com/edwardkim/rhwp/pull/1178)
- 작업지시자 피드백에 따라 PR 본문에 미주 처리가 아직 일반 완성 기능이 아니며, 정상 처리 확인 범위가 두 개 시험문제 케이스에 한정된다는 제한 사항을 명시했다.
- PR #1178 Build & Test 실패 원인은 `examples/diag_1139_para_shape.rs`가 현재 IR 모델과 맞지 않는 오래된 필드(`ShapeObject.paragraphs`, `LineSeg.text_length`)를 참조한 것이다. 글상자 문단 접근과 line 길이 계산을 현재 모델에 맞게 수정했다.
