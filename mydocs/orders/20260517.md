# 2026-05-17 — 오늘 할일

## Task #952 — hwp3-sample16 외곽선 밖 내용 돌출 (PR #956)

본질 분석 결과 3개 별개 회귀로 분리.

- [x] Issue 1 (페이지 외곽선 paper/body) — `paper_based = true` fix + RHWP_DEBUG_PAGE_BORDER (PR #956)

## Task #957 — sample16 page 18 본문 다음 페이지 밀림 (원 #952 Issue 2) (PR #958)

- [x] 빈 caption phantom advance 정정 + RHWP_DEBUG_TAC_CURSOR 영구화
- [x] sample16 page 18 본문 같은 페이지 표시 ✓ 한컴 viewer 정합

## Task #959 — HWP5 시험지 page 1 문9 vertical 처짐 (원 #952 Issue 3) (PR #961)

- [x] Issue 등록 + branch + 수행/구현 계획서 + 승인
- [x] Stage 1 — Shape pi=69 ci=0 (non-TAC TopAndBottom column picture) 가 +274px advance 확정
- [x] Stage 2 — Fix C 선정 (horz_rel_to=Column + col 외부 emit 시 advance skip)
- [x] Stage 3 — Fix C 적용 + 시험지 page 1 단위 검증 (문9 정상 위치)
- [x] Stage 4 — 회귀 검증 (cargo test 1288 + 시험지 4종 + exam 정상)
- [x] Stage 5 — 한컴 PDF 정합 + 최종 보고서

## Task #960 — 시험지 page 2 문14 cases formula off-by-one (원 #952 Issue 4) (PR #963)

- [x] 본 task #959 의 page 2 시각 검증 중 발견 + Issue [#960](https://github.com/edwardkim/rhwp/issues/960) 등록
- [x] Stage 1 — char_offsets gap 분석 영역 영역 cases (ci=3) position 30 (`\n`) 매핑 확정 (RHWP_DEBUG_PARA_TAC)
- [x] Stage 2 — Fix 선정 (has_line_break line 마지막 run 의 end-position TAC 포함)
- [x] Stage 4 — 회귀 검증 (cargo test 1288 + LAYOUT_OVERFLOW 41→41 + exam/sample 회귀 0)
- [x] Stage 5 — 한컴 PDF 정합 (cases formula y 329→352 line 1) + 최종 보고서

## Task #962 — 시험지 page 2 보기 textbox inline equation scramble (원 #952 Issue 5) (PR #964)

- [x] 본 task #960 의 page 2 시각 검증 중 발견 + Issue [#962](https://github.com/edwardkim/rhwp/issues/962) 등록
- [x] Stage 1 — SVG 분석 영역 영역 보기 textbox equation 12개 (Set 1 paragraph_layout 정상 + Set 2 shape_layout legacy fallback duplicate) 확정
- [x] Stage 2 — Fix 선정 (shape_layout Equation branch emit 전 get_inline_shape_position 확인 → paragraph_layout 등록 시 duplicate 차단)
- [x] Stage 4 — 회귀 검증 (cargo test 1288 + LAYOUT_OVERFLOW 325→325 + exam/sample 회귀 0)
- [x] Stage 5 — 한컴 PDF 정합 (equations 12→6, ㄱ/ㄴ/ㄷ) + 최종 보고서

## Task #965 — WMF SetTextAlign vertical bits 파싱 버그 (PR #918 Stage 33-A 포팅) (PR #966)

- [x] Issue #965 등록 + branch + 수행/구현 계획서 + 승인
- [x] Stage 1 — `mode & VTA_TOP(=0)` 항상-true 버그 root cause 확정 (set_text_align)
- [x] Stage 2 — Fix 선정 (PR #918 Stage 33-A: v_bits 0x0018 mask + baseline y shift 정합)
- [x] Stage 4 — 회귀 검증 (cargo test 1288 + WMF sample14/4 PNG diff <1%)
- [x] Stage 5 — sample16 page 18 WMF 박스 한컴 viewer 정합 + 최종 보고서
- [x] PR #918 (CLOSED, +5082) 거대 PR 의 root cause ~60 lines 만 단독 포팅

## 후속

- 원 issue #952 의 5 분리 결함 모두 완결 (Issue 1/2/3 PR #961 close + #960 PR #963 + #962 PR #964)
- PR #968 (HWP3 sample18 빈 paragraph + 쪽나누기) 후속 진행 예정

---

## M100 — PR 처리 (메인테이너, 5/17~18 사이클)

| PR | 타스크 | 상태 | 비고 |
|----|--------|------|------|
| **PR #956 (closes #952 Issue 1)** | fix — 쪽 테두리 paper-based outline 강제 (#920 비트 해석 회귀 정정) | **완료 (옵션 A — 1 commit cherry-pick + no-ff merge `b31e38ff`, 작업지시자 시각 판정 통과)** | 컨트리뷰터 @jangster77 (Taesup Jang) — **24+ 사이클** 핵심 컨트리뷰터 (HWP3/WMF/page border) — **연속 5 PR 1번째** (#956 → #958 → #961 → #963 → #964). **사전 작업 — 원격 동기화** (작업지시자 지시): 본 세션 5/11 작업 후 정체, origin/devel 영역 영역 5/12~17 PR #919~#954 누적 → 본 세션 5/11 작업 (PR #786~#818 + v0.7.11) origin 반영 확인 + devel 3자 동기화 (`local/devel`=`devel`=`origin/devel`=`016e694c`, +19 commits FF) + WASM 4.4 MB 빌드. `feedback_release_sync_check` 권위 사례 강화. 본질: 페이지 외곽선 paper/body 잘못 분류 회귀 — 회귀 source `4bb11289` (#920) `paper_based = (attr & 0x01) == 0` 비트 반전 (bisect: task877 baseline paper-based ✓ → #920 body-based ⚠️ → 현재 devel layout.rs:764 회귀 잔존). 진단: 5+ samples 한컴 viewer 실측 — attr 0/1, textBorder PAPER/CONTENT 양쪽 다 paper-based, bit 0 은 outline 위치 결정 비트 아님. 회귀 history: task877 (`!= 0`) → #920 (`== 0`) → 본 PR (`true` 모든 sample 정합). 정정 (`src/renderer/layout.rs` +16/-2): `paper_based = true` 강제 + `RHWP_DEBUG_PAGE_BORDER` 진단 영구화 + 회귀 history 코멘트 영구 보존. 추가 (13 files): 시험지 fixture 8 (HWP/HWPX) + 한컴 2022 권위 PDF 4 + 문서 4. 본 PR 범위 외 (별도 task 분리, PR 본문 명시): Issue 2 (sample16 page 18 본문 밀림, typeset multi-TAC cursor over-advance) + Issue 3 (시험지 문9 vertical 처짐, HWP5 column) — 부분 해결 + 명확한 분리 (archive/task936 "9회 시도 + 5회 revert" 대조 교훈). 본 환경 cherry-pick `bb3f40ad` 충돌 0건 (CLEAN). 결정적 검증: cargo test --release --lib **1288 passed, 0 failed** + cargo clippy --release -D warnings 통과 + **광범위 sweep 7 fixture / 169 페이지 / 169 same / 0 diff** (기존 fixture 회귀 부재, sweep fixture 영역 영역 sample16/시험지 미포함 → 작업지시자 시각 검증 영역 영역 핵심 게이트) + WASM 4.4 MB 재빌드. **작업지시자 시각 판정** ✅ 통과 (sample16 HWP3/HWP5/HWPX page 17 + 시험지 3-09월/3-11월 page 1 외곽선 paper-based, 한컴 2022 PDF 권위). CI ✅ Build & Test + CodeQL (js-ts/python/rust) + Canvas visual diff. Issue #952 OPEN 유지 (Issue 2/3 별도 task 분리). 검토 보고서: `mydocs/pr/archives/pr_956_review.md`. 처리 보고서: `mydocs/pr/archives/pr_956_report.md`. **`feedback_contributor_cycle_check` 정합** — @jangster77 24+ 사이클. **`feedback_release_sync_check` 권위 사례 강화** — PR 작업 전 원격 동기화 선행 (5/11→5/17 +19 commits FF). **`feedback_image_renderer_paths_separate` 정합** — layout.rs page border 분기 단일. **`feedback_hancom_compat_specific_over_general` 권위 사례 강화** — bit 0 추측 해석 (task877/#920) → 5+ sample 한컴 실측 영역 영역 `true` 강제 (추측보다 실측). **`feedback_diagnosis_layer_attribution` 권위 사례 강화** — 회귀 source `4bb11289` (#920) bisect 정확 + Issue 1/2/3 분리 진단. **`feedback_pr_supersede_chain` 정합** — task877 → #920 (회귀 도입) → **#956** (회귀 정정) (c) 패턴. **`reference_authoritative_hancom` 정합** — 시험지 한컴 2022 PDF 권위 영구 보존 + 회귀 fixture. **`feedback_v076_regression_origin` 정합** — #920 추측 bit 해석 → 작업지시자 환경 회귀 → 한컴 실측 영역 영역 정정 패턴. |
| **PR #958 (closes #957, #952 Issue 2)** | fix — 빈 caption phantom advance 정정 (sample16 page 18 본문 밀림 해소) | **완료 (옵션 A — 1 commit cherry-pick + orders 충돌 수동 해결 + no-ff merge `0b630773`, 작업지시자 시각 판정 통과)** | 컨트리뷰터 @jangster77 — **24+ 사이클** 핵심 컨트리뷰터 — **연속 5 PR 2번째** (#956 ✅ → #958 → #961 → #963 → #964). 본질: Issue #957 (Issue #952 Issue 2 분리 task) — sample16 page 18 "나. 주요 과업내용" 후 본문 (pi=395~401) 다음 페이지 밀림. Root cause (RHWP_DEBUG_TAC_CURSOR 추적): pi=394 ci=1 picture 빈 caption (dir=Bottom width=0 paras=1 text="") 영역 영역 phantom +430.6px 누적 → pi=395 body 외 영역 emit. 정정 (`src/renderer/layout.rs` :3491): `caption_is_empty` 가드 (caption 모든 paragraph 무의미 문자 ≤U+001F/U+FFFC + controls.is_empty()) + Bottom 한정 → result_y advance skip. RHWP_DEBUG_TAC_CURSOR 진단 영구화 (PR #956 RHWP_DEBUG_PAGE_BORDER 패턴 정합). 영역 좁힘: 빈 caption + Bottom 한정 — non-empty/None/Top 영향 없음. **본 환경 충돌 수동 해결**: `mydocs/orders/20260517.md` (본 환경 PR #956 처리 섹션 + PR #958 Task #952/#957 작업 일지 양측 보존 통합) + `src/renderer/layout.rs` (PR #956 `paper_based=true` :770 + PR #958 `caption_is_empty` :3491 다른 영역 auto-merge — 양립 확인) + task_m100_957* 8 신규 추가. 본 환경 cherry-pick `ee079382` 충돌 1건 수동 해결. 결정적 검증: cargo test --release --lib **1288 passed, 0 failed** + cargo clippy --release -D warnings 통과 + **광범위 sweep 7 fixture / 169 페이지 / 169 same / 0 diff** (회귀 부재, sweep fixture 영역 영역 sample16 미포함 → 작업지시자 시각 검증 핵심 게이트) + WASM 4.4 MB 재빌드. **작업지시자 시각 판정** ✅ 통과 (sample16 page 18 본문 같은 페이지 정상, 한컴 viewer page 16 정합 + sample14/10/11/13 + PR #956 page border 회귀 부재). CI ✅ Build & Test + CodeQL (js-ts/python/rust) + Canvas visual diff. Issue #957 close + Issue #952 OPEN 유지 (Issue 3 잔존). 검토 보고서: `mydocs/pr/archives/pr_958_review.md`. 처리 보고서: `mydocs/pr/archives/pr_958_report.md`. **`feedback_contributor_cycle_check` 정합** — @jangster77 24+ 사이클 (연속 5 PR 2번째). **`feedback_image_renderer_paths_separate` 정합** — layout.rs caption advance 단일. **`feedback_hancom_compat_specific_over_general` 정합** — 빈 caption + Bottom 한정 가드 (케이스별 명시, 일반화 없음). **`feedback_diagnosis_layer_attribution` 권위 사례 강화** — RHWP_DEBUG_TAC_CURSOR 추적 영역 영역 pi=394 ci=1 phantom +430.6px 정확 진단 + Issue #952 → Issue 1/2/3 분리. **`feedback_pr_supersede_chain` 정합** — archive/task936 (미완 시도) + PR #918 (close) → **#958** (정확 정정) (c) 패턴. |
| **PR #961 (closes #959, #952 Issue 3)** | fix — horz_rel_to=Column picture column 외부 emit 시 cursor advance skip (시험지 page 1 문9 정합) | **완료 (옵션 A — 1 commit cherry-pick + orders 충돌 수동 해결 + no-ff merge, 작업지시자 시각 판정 통과)** | 컨트리뷰터 @jangster77 — **24+ 사이클** 핵심 컨트리뷰터 — **연속 5 PR 3번째** (#956 ✅ → #958 ✅ → #961 → #963 → #964). 본질: Issue #959 (Issue #952 Issue 3 분리 task) — 시험지 (3-11월) page 1 우측 단 문9 ~250px 처짐. Root cause (RHWP_DEBUG_TAC_CURSOR 추적): Shape pi=69 ci=0 (horz_rel_to=Column, h_offset=79.5mm, Center) 영역 영역 pic_emit_x=767 > col_area right=759.7 → picture 좌측 edge column 외부, 한컴 PDF 우측 단 picture 미표시. 그럼에도 cursor 274px advance → 문9 처짐. 정정 (`src/renderer/layout.rs` :3537): `saved_y_offset` — horz_rel_to=Column picture pic_emit_x 가 col_area 우측 초과 시 result_y = saved_y_offset (advance skip, :3566). 영역 좁힘: horz_rel_to=Column + col 외부 한정 — col 내부/Paper/Page/Para/TAC 영향 없음. **본 환경 충돌 수동 해결**: `mydocs/orders/20260517.md` (본 환경 PR #956/#958 처리 표 + PR #961 Task #959/#960 작업 일지 양측 보존 통합). layout.rs auto-merge 성공 — PR #956 `paper_based=true` :770 + PR #958 `caption_is_empty` :3491 + PR #961 `saved_y_offset` :3537 **3 정정 양립 확인** (RHWP_DEBUG_TAC_CURSOR :2022 PR #958 머지 영역 영역 동일 영역 영역 자동 병합). 시험지 fixture 8 + PDF 4 영역 영역 PR #956 머지 영역 영역 동일 content (devel 보존). 본 환경 cherry-pick 충돌 1건 (orders) 수동 해결. 결정적 검증: cargo test --release --lib **1288 passed, 0 failed** + cargo clippy --release -D warnings 통과 + **광범위 sweep 7 fixture / 169 페이지 / 169 same / 0 diff** (회귀 부재) + WASM 4.4 MB 재빌드. **작업지시자 시각 판정** ✅ 통과 (시험지 3-11월 page 1 문9 y=805 정합, 한컴 PDF 권위 + 시험지 4종 + exam_kor p18 Task #722 + PR #956/#958 회귀 부재). CI ✅ Build & Test + CodeQL (js-ts/python/rust) + Canvas visual diff. Issue #959 close + **Issue #952 close** (Issue 1/2/3 모두 해결). 잔존: Issue #960 (page 2 multi-line equation off-by-one, pre-existing, 별도 task). 검토 보고서: `mydocs/pr/archives/pr_961_review.md`. 처리 보고서: `mydocs/pr/archives/pr_961_report.md`. **`feedback_contributor_cycle_check` 정합** — @jangster77 24+ 사이클 (연속 5 PR 3번째). **`feedback_image_renderer_paths_separate` 정합** — layout.rs column picture advance 단일. **`feedback_hancom_compat_specific_over_general` 정합** — horz_rel_to=Column + col 외부 한정 가드 (케이스별 명시). **`feedback_diagnosis_layer_attribution` 권위 사례 강화** — RHWP_DEBUG_TAC_CURSOR 추적 영역 영역 pi=69 ci=0 dy=274 정확 진단 + Issue #952 → Issue 1/2/3 + #960 분리. **`feedback_pr_supersede_chain` 정합** — Issue #952 통합 → #956 (Issue 1) → #958 (Issue 2) → **#961 (Issue 3)** + #960 (신규 분리). **`reference_authoritative_hancom` 정합** — 시험지 한컴 PDF (pdf/3-11월) 권위 page 1 문9 y=805 기준. |
| **PR #963 (closes #960, #952 Issue 4)** | fix — line break 직전 inline TAC control line 매핑 정정 (시험지 page 2 cases formula off-by-one 해소) | **완료 (옵션 A — 본질 commit cherry-pick + orders 충돌 수동 해결 + no-ff merge `415b9d8d`, 작업지시자 시각 판정 통과 + 한컴 2020 직접 확인)** | 컨트리뷰터 @jangster77 — **24+ 사이클** 핵심 컨트리뷰터 — **연속 5 PR 4번째** (#956 ✅ → #958 ✅ → #961 ✅ → #963 → #964). 본질: Issue #960 (Issue #952 Issue 4, PR #961 page 2 검증 중 발견) — 시험지 (3-11월) page 2 문14 (pi=117) cases formula 가 line 0 (y=329) 에 emit → header text overlap. Root cause (RHWP_DEBUG_PARA_TAC 추적): char_offsets gap 분석 영역 영역 cases (ci=3) → position 30 (= `\n`). compose_lines line 1 chars [23,30) 에서 position 30 제외 → is_last_run && pos == end 만 허용 → line 1 (last line 아님) 누락 → line 0 (header) emit → overlap. 정정 (`src/renderer/layout/paragraph_layout.rs` :1730): `allow_end_tac = is_last_run || (comp_line.has_line_break && is_last_run_of_line(run_idx))` — has_line_break line 마지막 run end-position TAC 포함. RHWP_DEBUG_PARA_TAC 진단 영구화. 기존 인프라 (is_last_run_of_line :1525, has_line_break) 활용. 영역 좁힘: has_line_break + end-position 한정 — has_line_break 없는 line / 일반 TAC 영향 없음. **본 환경 충돌 수동 해결**: `mydocs/orders/20260517.md` (`git checkout --ours` 본 환경 PR #956/#958/#961 처리 표 보존 + Task #960/#962 작업 일지 갱신). paragraph_layout.rs auto-merge — devel 변경 부재, PR #956/#958/#961 은 layout.rs, 본 PR 은 layout/paragraph_layout.rs (**다른 파일, 4 정정 양립**). devel merge commit (`d690b878`/`8626a8b1`) cherry-pick 제외 — 본질 `90fb1684` 만. task_m100_960* 9 신규 추가. 시험지 fixture/PDF 미포함 (PR 본문 명시, PR #956/#961 영역 영역 추가, 중복 방지). 본 환경 cherry-pick `11b6d419` 충돌 1건 (orders) 수동 해결. 결정적 검증: cargo test --release --lib **1288 passed, 0 failed** + cargo clippy --release -D warnings 통과 + **광범위 sweep 7 fixture / 169 페이지 / 168 same / 1 diff (exam_math_017.svg)**. **⚠️ exam_math page 18 sweep diff** — inline equation line 매핑 변화 (AFTER equation `translate(366.84,301.65)` 추가, BEFORE 텍스트 `translate(85.52,343.83)` 다른 line). PR 본문 영역 영역 "exam_math 회귀 0" 명시였으나 본 환경 diff 1건 검출. **작업지시자 한컴 2020 에디터 직접 확인 영역 영역 의도된 정정 확정** — 이전 equation 잘못된 line (y=343.8) emit → 정정 후 올바른 line (y=301.7), 시험지 page 2 cases 와 동일 본질. 회귀 아님, 추가 정합 효과. WASM 4.4 MB 재빌드. **작업지시자 시각 판정** ✅ 통과 (시험지 page 2 cases y=352 한컴 PDF 정합 + exam_math p18 한컴 2020 직접 확인 의도 확정 + 시험지 4종/exam/sample 회귀 부재 + PR #956/#958/#961 회귀 부재). CI Build & Test pending (devel merge 재트리거, 본 환경 자기 검증 보완). Issue #960 close. 잔존: Issue #962 (page 2 보기 textbox scramble, pre-existing, 별도 task). 검토 보고서: `mydocs/pr/archives/pr_963_review.md`. 처리 보고서: `mydocs/pr/archives/pr_963_report.md`. **`feedback_contributor_cycle_check` 정합** — @jangster77 24+ 사이클 (연속 5 PR 4번째). **`feedback_image_renderer_paths_separate` 정합** — paragraph_layout.rs TAC line 매핑 단일 (layout.rs 와 다른 파일). **`feedback_hancom_compat_specific_over_general` 정합** — has_line_break + end-position 한정 가드. **`feedback_diagnosis_layer_attribution` 권위 사례 강화** — RHWP_DEBUG_PARA_TAC + char_offsets gap 분석 영역 영역 pi=117 position 30 (`\n`) off-by-one 정확 진단. **`feedback_visual_judgment_authority` 권위 사례 강화** — sweep diff 1건 (exam_math p18) 영역 영역 작업지시자 한컴 2020 에디터 직접 확인 영역 영역 의도/회귀 최종 결정 (sweep ≠ 자동 회귀 판정, 작업지시자 시각 판정 권위). **`feedback_pr_supersede_chain` 권위 사례 강화** — Issue #952 (Issue 1/2/3) → #956/#958/#961 + Issue #960 (Issue 4, PR #961 검증 발견) → **#963** + #962 (신규 분리). **`reference_authoritative_hancom` 정합** — 시험지 한컴 PDF (pdf/3-11월) page 2 cases y=352 + 작업지시자 한컴 2020 에디터 직접 확인 (exam_math p18). |
| **PR #964 (closes #962, #952 Issue 5)** | fix — 글상자 내부 inline equation duplicate emit 차단 (시험지 page 2 보기 textbox scramble 해소) | **완료 (옵션 A — 본질 commit cherry-pick + orders 충돌 수동 해결 + no-ff merge `808f419e`, 작업지시자 시각 판정 통과)** | 컨트리뷰터 @jangster77 — **24+ 사이클** 핵심 컨트리뷰터 — **연속 5 PR 5번째 — 마지막** (#956 ✅ → #958 ✅ → #961 ✅ → #963 ✅ → #964 ✅). 본질: Issue #962 (Issue #952 Issue 5, PR #963 page 2 검증 중 발견) — 시험지 (3-11월) page 2 문14 <보기> textbox inline 수식 각각 2번 emit → scramble. Root cause (SVG 분석): 보기 textbox 영역 영역 equation 12개 (6×2): Set 1 (정상, gap 위치) paragraph_layout inline TAC + Set 2 (duplicate, textbox 좌측 edge) shape_layout 두번째 loop legacy fallback. paragraph_layout 가 현재 textbox 내부 inline TAC 정상 처리 → 중복 emit. 정정 (`src/renderer/layout/shape_layout.rs` :1620): Equation branch emit 전 `get_inline_shape_position` (equiv_cell_ctx — parent_cell_path + textbox entry) 확인 → paragraph_layout 등록 시 `inline_x += eq_w` 만 (duplicate 차단), 미등록 시 legacy fallback else 유지. 영역 좁힘: paragraph_layout 등록 case 한정 — legacy fallback / Shape/Picture/Table / standalone 영향 없음. **본 환경 충돌 수동 해결**: `mydocs/orders/20260517.md` (`git checkout --ours` 본 환경 PR 처리 표 보존 + Task #962 작업 일지 갱신). shape_layout.rs auto-merge (devel 변경 부재). PR #956/#958/#961 (layout.rs :770/:3491/:3537) + #963 (paragraph_layout.rs :1730) + 본 PR (shape_layout.rs :1620) **5 정정 모두 다른 파일 양립 확인**. devel merge commit (`93dfe0a8`/`3d1cdf31`) cherry-pick 제외 — 본질 `682875fe` 만. task_m100_962* 8 신규 추가. 시험지 fixture/PDF 미포함 (PR 본문 명시, 중복 방지). 본 환경 cherry-pick `371fd2be` 충돌 1건 (orders) 수동 해결. 결정적 검증: cargo test --release --lib **1288 passed, 0 failed** + cargo clippy --release -D warnings 통과 + **광범위 sweep 7 fixture / 169 페이지 / 169 same / 0 diff** (회귀 부재, sweep fixture 영역 영역 시험지 미포함 → 작업지시자 시각 검증 핵심 게이트) + WASM 4.4 MB 재빌드. **작업지시자 시각 판정** ✅ 통과 (시험지 page 2 보기 textbox equations 12→6, ㄱ. h(1)=3 / ㄴ. 함수 h(x)는... / ㄷ. 함수 g(x)가... 한컴 PDF 정합 scramble 해소 + 시험지 4종/exam/sample 회귀 부재 + PR #956/#958/#961/#963 회귀 부재). CI ✅ Build & Test + CodeQL (js-ts/python/rust) + Canvas visual diff. Issue #962 close + **원 Issue #952 의 5 분리 결함 완결** (#956/#958/#961/#963/#964). 검토 보고서: `mydocs/pr/archives/pr_964_review.md`. 처리 보고서: `mydocs/pr/archives/pr_964_report.md`. **`feedback_contributor_cycle_check` 정합** — @jangster77 24+ 사이클 (연속 5 PR 5번째 마지막). **`feedback_image_renderer_paths_separate` 권위 사례 강화** — shape_layout.rs (legacy fallback) vs paragraph_layout.rs (정상) 두 경로 동시 emit duplicate 정확 진단. **`feedback_hancom_compat_specific_over_general` 정합** — paragraph_layout 등록 case 한정 가드 (legacy fallback else 유지). **`feedback_diagnosis_layer_attribution` 권위 사례 강화** — SVG 분석 영역 영역 Set 1 + Set 2 12개 (6×2) duplicate 정확 진단. **`feedback_pr_supersede_chain` 권위 사례 강화** — Issue #952 (5 분리 결함) → #956/#958/#961 + #960 (#963) + #962 (#964) 검증 중 결함 발견 5-연쇄 완결. **`feedback_visual_judgment_authority` 정합** — sweep fixture 미포함 영역 영역 작업지시자 시각 판정 핵심 게이트 (PR #963 exam_math p18 선례). **`reference_authoritative_hancom` 정합** — 시험지 한컴 PDF (pdf/3-11월) 권위 page 2 보기 textbox ㄱ/ㄴ/ㄷ 기준. |

---

## 연속 5 PR (@jangster77) 완결 총평

원 Issue #952 (1 통합 issue) → 5 분리 결함 → 부분 해결 + 명확한 분리:

| PR | Issue | 본질 | 진단 도구 | 머지 |
|----|-------|------|----------|------|
| #956 | #952 Issue 1 | page border paper/body | bisect (`4bb11289`) | `b31e38ff` |
| #958 | #957 (Issue 2) | sample16 p18 빈 caption phantom | RHWP_DEBUG_TAC_CURSOR | `0b630773` |
| #961 | #959 (Issue 3) | 시험지 p1 문9 column picture advance | RHWP_DEBUG_TAC_CURSOR | `586e3cc0` |
| #963 | #960 (Issue 4) | 시험지 p2 cases off-by-one | RHWP_DEBUG_PARA_TAC | `415b9d8d` |
| #964 | #962 (Issue 5) | 시험지 p2 보기 textbox duplicate | SVG 분석 | `808f419e` |

- 환경변수 진단 도구 (RHWP_DEBUG_*) 영구화 — 차후 재회귀 추적 인프라
- 5 정정 모두 다른 파일/영역 (layout.rs ×3 + paragraph_layout.rs + shape_layout.rs) — 정확한 결함 분리 + 양립
- 매 PR 작업지시자 시각 판정 + cargo test 1288 + sweep — 결정적 검증 일관
- archive/task936 "9회 시도 + 5회 revert" 대조 — 부분 해결 + 명확한 분리 방법론 입증
- exam_math p18 (PR #963) sweep diff 영역 영역 작업지시자 한컴 2020 직접 확인 의도 확정 — `feedback_visual_judgment_authority` 권위
| **PR #966 (closes #965, ports PR #918 Stage 33-A)** | fix — WMF SetTextAlign vertical bits 파싱 정정 + baseline y shift 정합 (WMF 박스 외부 텍스트 표시 해소) | **완료 (옵션 A — 본질 commit cherry-pick + orders 충돌 수동 해결 + no-ff merge `235e049c`, 작업지시자 시각 판정 통과)** | 컨트리뷰터 @jangster77 — **24+ 사이클** 핵심 컨트리뷰터 — **연속 5 PR #956~#964 완결 후 추가 2개 중 #966 1번째** (#968 후속). 본질: Issue #965 — sample16 page 18 WMF 다이어그램 (주전산센터 목표시스템 구성안) 박스 내부 한글 텍스트 ("PE6450", "기록서버", "Windows 서버군") 박스 외부 벗어남. Root cause (`src/wmf/converter/svg/mod.rs` :2208 set_text_align): `mode & VTA_TOP(=0x0000) == 0x0000` 항상 true → BASELINE/BOTTOM mode 도 VTA_TOP 오매핑 → +ascent shift → baseline 박스 하단 걸침. WMF [MS-WMF] 2.1.2.18 (TA_TOP=0x0000/TA_BOTTOM=0x0008/TA_BASELINE=0x0018) spec 정합. 정정 (svg/mod.rs 3 영역 ~60 lines): set_text_align (:2208) `v_bits = mode & 0x0018` 마스킹 + 우선순위 BASELINE→BOTTOM→TOP + ext_text_out (:811) baseline y shift 정합 (VTA_BASELINE=0/BOTTOM=-em*0.2/TOP=+ascent, font.height<0 잘못된 보정 제거) + text_out (:1545) META_TEXTOUT 동일 보정 (PR #918 미포함, 본 PR 추가). **PR #918 supersede**: PR #918 (CLOSED 5/16, **+5082/-74 거대 PR**, 다양한 부작용 — LibreOffice emfio 포팅/WASM RasterPlayer/nested SVG inline embed/woff2 base64 제거/DX byte-aware/POLYPOLYGON fill-rule) → Stage 33-A root cause ~60 lines 만 단독 포팅. **본 환경 충돌 수동 해결**: `mydocs/orders/20260517.md` (`git checkout --ours` 본 환경 PR 처리 표 보존 + Task #965 작업 일지 갱신). svg/mod.rs **auto-merge** — devel PR #860/#864 (5/16 `735d3057` EMF/WMF image 렌더) 변경 영역 영역 PR #966 정정 (3 영역) 다른 라인 → 자동 병합, devel 함수 (ext_text_out:790/text_out:1534/set_text_align:2185) 보존 + PR #966 정정 양립 확인. devel merge commit 4개 cherry-pick 제외 — 본질 `88291fdb` 만. task_m100_965* 8 신규 추가. 본 환경 cherry-pick `1a890422` 충돌 1건 (orders) 수동 해결. 결정적 검증: cargo test --release --lib **1288 passed, 0 failed** + cargo clippy --release -D warnings 통과 + **광범위 sweep 7 fixture / 169 페이지 / 169 same / 0 diff** (회귀 부재, sweep fixture 영역 영역 WMF 다이어그램 미포함 → 작업지시자 시각 검증 핵심 게이트) + WASM 4.4 MB 재빌드. **작업지시자 시각 판정** ✅ 통과 (sample16 p18 WMF 박스 내부 한글 텍스트 정상 위치 한컴 viewer 정합 + WMF sample14/4 + devel PR #860/#864 EMF/WMF image 렌더 회귀 부재 + 비-WMF sample 회귀 부재). CI ✅ Build & Test + CodeQL (js-ts/python/rust). Issue #965 close. 검토 보고서: `mydocs/pr/archives/pr_966_review.md`. 처리 보고서: `mydocs/pr/archives/pr_966_report.md`. **`feedback_contributor_cycle_check` 정합** — @jangster77 24+ 사이클 (연속 5 PR 완결 후 추가 #966/#968). **`feedback_image_renderer_paths_separate` 정합** — WMF converter svg/mod.rs 단일 (Canvas2D 동일 SVG 정합 개선). **`feedback_hancom_compat_specific_over_general` 정합** — WMF [MS-WMF] 2.1.2.18 spec 정합 (추측 아닌 spec 기반). **`feedback_diagnosis_layer_attribution` 권위 사례 강화** — `mode & VTA_TOP(=0) == 0` 항상-true 버그 root cause 정확 진단. **`feedback_pr_supersede_chain` 권위 사례 강화** — PR #918 (CLOSED, +5082 거대 PR 다양한 부작용) → #966 (root cause ~60 lines 단독 포팅 + text_out 추가) (a) 패턴. **`feedback_small_batch_release_strategy` 권위 사례 강화** — 5082 lines 거대 PR → 60 lines root cause 분리 (작은 단위 회전 입증). **`reference_authoritative_hancom` 정합** — sample16 page 18 WMF 박스 한컴 viewer 정합 기준. |
