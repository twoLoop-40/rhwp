# 오늘 할일 - 2026년 5월 9일

## M100 — v1.0.0 조판 엔진 (PR 처리 — 5/8 사이클의 연속)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #694 Task #688** | `samples/table-vpos-01.hwpx` p.5 nested 표 외부 셀 height 미반영 — 외부 표 height 57.72px / 권위 778.8px (closes #688) | **완료 (4 commits cherry-pick + 메인테이너 충돌 해결 통합 정정 머지 `f3534293`)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 30+ 사이클 핵심 컨트리뷰터 (Layout/페이지네이션). 본질: layout_table + height_measurer 두 곳 1×1 wrapper unwrap (`flat_map(...).find_map(...)` → 첫 nested 만 추출, paragraphs ≥ 2 케이스 두 번째 nested 누락). PR 정정: 4 조건 가드 (1×1 단일 셀 + paragraphs.len()==1 + controls.len()==1 nested table + visible text 없음). **메인테이너 충돌 해결 통합 정정**: PR #681 (Task #680, 자료 박스 외곽 테두리) 와 동일 분기 충돌. PR #694 `controls.len() == 1` 가드 그대로 적용 시 `exam_social.hwp` pi=15 (paragraphs=1, controls=2 — 정렬 마커 + nested 6×3) 영역에서 PR #681 외곽선 분기 회귀 누락. `controls.len() == 1` 가드 제거 + `paragraphs.len() == 1` 만 본질 가드 보존 + `find_map` 으로 다른 control 무시. 두 곳 (table_layout.rs + height_measurer.rs) 동일. 결정적 검증 1166 lib + svg_snapshot 8/8 + issue_nested_table_border 1/1 (PR #681 회귀 가드) + clippy clean. 광범위 sweep 7 샘플 170 페이지 same=170 / diff=0. 시각 대상 직접 측정: table-vpos-01.hwpx p.5 SVG 20,516 → **133,816 bytes** (×6.5, nested 11×3 그리드 복원) / exam_social.hwp p.1 **byte-identical** (PR #681 외곽선 보존). WASM 빌드 4,594,114 bytes. **작업지시자 시각 판정 ★ 통과** ("svg 5 페이지 시각 판정 통과") — PDF 권위본 `pdf/table-vpos-01-2022.pdf` 5쪽 정합. **별건 결함 발견**: 도형 2개 미출력 — 본 PR 정정으로 인한 회귀 아닌 별개 결함 (작업지시자 확정). 본 PR 범위 외, 새 이슈 등록 예정. 검토 보고서: `mydocs/pr/archives/pr_694_review.md`. 처리 보고서: `mydocs/pr/archives/pr_694_report.md`. **`feedback_pr_supersede_chain` 권위 사례 강화** — PR #681 (외곽 테두리) → PR #694 (1×1 unwrap 정밀화) 동일 분기 충돌 → 메인테이너 통합 정정 신규 패턴. **`feedback_hancom_compat_specific_over_general` 권위 사례** — `controls.len() == 1` 과도한 정밀화 → `paragraphs.len() == 1` 본질 가드만 보존. **`feedback_image_renderer_paths_separate` 권위 사례** — table_layout.rs + height_measurer.rs 두 경로 동일 정정. **`feedback_visual_judgment_authority` 권위 사례 강화** — 결정적 검증 + sweep 통과 후 시각 판정에서 ★ 통과 + 별건 결함 발견 영역 (cell-clip 확장 + nested 표시 후 가시화). |
| **PR #693 Task #685+#689** | rhwp-studio 그리드 모드 click 좌표 단일 컬럼 가정 + getPageAtY X 무시 일괄 정정 (closes #685, #689) | **완료 (옵션 A — 8 commits 단계별 보존 cherry-pick + no-ff merge `b784c585`)** | 컨트리뷰터 @johndoekim (johndoe) — **두 번째 사이클** (PR #645 후속). PR #645 (Task #595) Stage 3 e2e assert 강화에서 발견된 본 영역 결함의 합본 정정. 본질: (1) Issue #685 — `virtual-scroll.ts` 그리드 모드 `pageLefts[i] = marginLeft + col*(pw+gap)` 와 `input-handler-mouse.ts` 14곳의 단일 컬럼 가정 공식 `(scrollContent.clientWidth - pageDisplayWidth) / 2` 부정합 → click 좌표 ±수백 px 어긋남. (2) Issue #689 — `getPageAtY(docY)` Y-only loop 가 row-shared `pageOffsets[i]` 환경에서 항상 last page idx 만 반환 → non-last col click 시 last col 페이지로 cursor 처리. 정정: `virtual-scroll.ts` 헬퍼 2개 추가 (`getPageLeftResolved`, `getPageAtPoint`) + 5 파일 일괄 치환 (getPageAtPoint 18곳 + getPageLeftResolved 24곳). 부수 변경: `updateCaretDuringDrag` 함수 삭제 + 호출처 `updateCaret` 변경 (PR 본문 미명시, 시각 판정에서 영향 부재 확정). 본 환경 cherry-pick 8 commits 충돌 1건 (5/8 orders add/add — 본 환경 표 보존 + 컨트리뷰터 Task 본문 별도 섹션 통합). 결정적 검증 lib 1166 + 통합 ALL GREEN + clippy clean + tsc clean + npm build 정상. **e2e grid-mode-click-coord ALL PASS 22/0** (zoom=0.5/0.25/1.0 모든 col CORRECT click → cursor.rectPageIdx 정합 + 헬퍼 동치성 max delta 0.0000px). 정량 측정 PR 본문 정합: zoom=0.5 col 0/1 ±285.6px / zoom=0.25 col 0/4 ±581.3px / zoom=1.0 baseline 0px. WASM 빌드 4,594,114 bytes. **작업지시자 시각 판정 ★ 통과** ("시각 판정 통과입니다"). 검토 보고서: `mydocs/pr/archives/pr_693_review.md`. 처리 보고서: `mydocs/pr/archives/pr_693_report.md`. **`feedback_contributor_cycle_check`** 정합 — @johndoekim 두 번째 사이클 정확 표현. **`feedback_pr_supersede_chain` 권위 사례** — PR #645 (Task #595) → PR #693 (Task #685+#689 후속 sweep) 신규 사례 — 동일 컨트리뷰터 후속 사이클 본질 분리 패턴. **`feedback_image_renderer_paths_separate` 권위 사례** — 5 파일 일괄 sweep 정합 (input-handler-mouse + input-handler + input-handler-table + input-handler-picture + input-handler-connector). **`feedback_visual_judgment_authority` 권위 사례 강화** — e2e 22/0 + 시각 판정 ★ 통과 양측 정합. |
| **PR #695** | feat — ir-diff `--summary` / `--max-lines` 출력 가드 추가 (closes 명시 없음 — PR #659/Task #653 후속 보강) | **완료 (옵션 A — 1 commit cherry-pick + no-ff merge `2ca126c3`)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 30+ 사이클 핵심 컨트리뷰터. PR #659 (Task #653, ir-diff 표 속성/컨트롤 식별 비교) 머지 후 보강. 본질: 광범위 회귀 검증 시 ir-diff 출력량 폭증 (aift.hwpx vs aift.hwp 1437 라인) → 카테고리별 카운트 + 출력 라인 제한 모드 추가. 정정 (`src/main.rs::ir_diff`): (1) `--summary` 옵션 — 카테고리별 차이 카운트만 출력 (paragraph 헤더 + 개별 차이 생략). (2) `--max-lines <N>` 옵션 — 출력 라인 N 제한 + truncation 마커, 합계 라인 가드 외부 보존. 두 매크로 (`emit_header!` + `emit_diff!`) 함수 내부 도입 + 기존 `println!` 6곳 매크로 치환 (비교 로직 무변경). 카테고리 추출: `":"` 앞쪽 prefix 의 `"]"` 이후 (예: `controls[N].xxx` → `xxx`). summary 모드 BTreeMap 누적 후 count 내림차순 → 알파벳 정렬. 회귀 가드 테스트 신규 3건 (`tests/ir_diff_summary_mode.rs`) — `no_flags_preserves_full_output` 가 옵션 부재 시 기존 형식 100% 보존 직접 입증. 본 환경 cherry-pick 충돌 0건. 결정적 검증 lib 1166 + 통합 ALL GREEN + 신규 ir_diff_summary_mode **3/3 PASS** + clippy clean. 수동 검증 PR 본문 정합: `--summary` 45 카테고리 출력 (260 char_shapes count + 69 ml + 61 indent + ...) / `--max-lines 20` truncation 마커 + `=== 비교 완료: 차이 707 건 ===` 합계 보존 / 옵션 부재 1436 라인 보존. **시각 판정 게이트 면제** — UI 무관 CLI 옵션 추가, env-var-checked 본질 영역의 PR #649/#650 면제 패턴 정합. 검토 보고서: `mydocs/pr/archives/pr_695_review.md`. 처리 보고서: `mydocs/pr/archives/pr_695_report.md`. **`feedback_visual_regression_grows` 권위 사례** — UI 무관 CLI 옵션 추가의 시각 판정 게이트 면제 정합. **`feedback_pr_supersede_chain` 권위 사례** — PR #659 (Task #653) → PR #695 (출력 가드 후속 보강) 신규 사례 — 동일 컨트리뷰터의 후속 사이클 본질 분리 패턴. **`feedback_hancom_compat_specific_over_general` 정합** — 출력 가드만 추가 (영향 좁힘), 비교 로직 무변경. |
| **PR #699** | samples — aift/kps-ai PDF 변환본을 기본 인쇄(1장/쪽)로 갱신 (closes 명시 없음 — PDF 권위 자료 갱신) | **완료 (옵션 A — 5 commits 단계별 보존 cherry-pick + no-ff merge `bbc44035`)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 30+ 사이클 핵심 컨트리뷰터. 본질: 이전 PR #670 (한글 2022 PDF 변환본 추가) 의 6 PDF 가 모아찍기 2-up 또는 1쪽만 인쇄되어 페이지 수 어긋남 → 한글 2022 기본 인쇄 (1장/쪽) 로 재출력. 갱신: pdf/aift-2022.pdf 37→**74 페이지** / pdf/kps-ai-2022.pdf 39→**77** / pdf/hwpx/aift-2022.pdf 37→**74** / pdf/hwp-multi-001-2022.pdf 5→**9** / pdf/KTX-2022.pdf 14→**27** / pdf/basic/shortcut-2022.pdf 1→**7**. 본 환경 cherry-pick 충돌 0건. 결정적 검증: pdfinfo 실측 6 PDF 모두 PR 본문 수치 정합 (74/77/74/9/27/7) + cargo test --release lib 1166 + 통합 ALL GREEN, failed 0 + 코드 무변경 → 회귀 영향 0 확정. **본 환경 rhwp 출력과의 정합 영향**: ✅ 정합 회복 2건 (hwpx/aift.hwpx rhwp 74 = PDF 74 / KTX.hwp rhwp 27 = PDF 27) + 잔존 어긋남 4건 (aift.hwp +3 / kps-ai.hwp +2 / hwp-multi-001.hwp +1 / basic/shortcut.hwp +3 — 별개 결함, 본 PR 범위 외). **시각 판정 게이트 면제** — PDF 권위 자료 갱신, 코드 무변경 (UI/렌더링 무관). 검토 보고서: `mydocs/pr/archives/pr_699_review.md`. 처리 보고서: `mydocs/pr/archives/pr_699_report.md`. **`reference_authoritative_hancom` 권위 사례** — 한글 2022 PDF 권위 자료 갱신 (PR #670 영구 보존 영역의 정정). **`feedback_pdf_not_authoritative` 정합** — 한컴 한글 2022 편집기 PDF 만 정답지 가능 (1-up 기본 인쇄로 정확). **`feedback_visual_regression_grows` 권위 사례** — page-mismatch sweep 의 권위 비교 신뢰성 회복 (거짓 어긋남 2건 해소). |
| **PR #706 Task #700** | 셀 paragraph cut 위치 vpos 정합 — compute_cell_line_ranges cum 절대 동기화 (closes #700, closes #697) | **완료 (옵션 A — 5 commits cherry-pick + golden SVG 갱신 + no-ff merge `fce2d870`)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 30+ 사이클 핵심 컨트리뷰터. **PR supersede 패턴**: PR #701 (Task #697) close → PR #706 (Task #697 + #700 통합) — 컨트리뷰터 자체 결정. 본질: `samples/inner-table-01.hwp` cell[11] cell-internal split 시 cum 누적 metric 가 한컴 LINE_SEG.vpos 누적과 ~50px 어긋남 → paragraph cut 부정합 (p[17] '- 전사 데이터 수집/유통체계 구축' 누락). 정정 (`compute_cell_line_ranges`): (1) Task #697 — vpos 리셋 검출 (cell_first_vpos==0 + cur_first_vpos<prev_end_vpos 시 cum=abs_limit). (2) Task #700 — 정상 누적 시 vpos 절대 동기화 (target_cum>cum 일 때만 cum=target_cum, 전진 보장). 부수 (table_partial.rs): split row 미분할 cell valign 보존 (is_in_split_row && cell_was_split). **CI Build & Test FAILURE** — `tests/svg_snapshot.rs::form_002_page_0` mismatch (golden SVG `12a6cbcc...` vs PR 출력 `672c78c6...`). **메인테이너 직접 시각 판정 ★ 통과** — BEFORE (devel, PR #662 정정 영역, 26 글자 표시) vs AFTER (PR #706, 26 글자 누락) 비교 + PDF 권위본 (`pdf/hwpx/form-002-2022.pdf` page 1) 정합 — **AFTER 가 한컴 정답지 정합 확정**. **golden SVG 갱신** (`tests/golden_svg/form-002/page-0.svg` md5 12a6cbcc → 672c78c6, -26 LOC) — 메인테이너 추가 commit `78e38e51`. **부수 사실**: PR #662 (Task #656) 의 form-002 정정 본질 자체가 잘못된 영역 — 26 글자가 한컴 PDF page 1 본문 영역 외부 영역에 위치 (한컴 권위, 표시되지 않는 영역이 정합). 후속 별도 분석 가능성. 결정적 검증 lib 1166 + 통합 ALL GREEN + svg_snapshot 8/8 (form-002 갱신 후 통과) + clippy clean. WASM 빌드 4,596,712 bytes. 검토 보고서: `mydocs/pr/archives/pr_706_review.md`. 처리 보고서: `mydocs/pr/archives/pr_706_report.md`. **`feedback_visual_judgment_authority` 권위 사례 신규** — CI FAILURE + 결정적 검증 vs 작업지시자 시각 판정 충돌 영역 + **시각 판정 권위 영역**으로 golden 갱신. **`feedback_pr_supersede_chain` 권위 사례** — PR #701 close → PR #706 supersede 컨트리뷰터 자체 결정 신규 패턴. **`feedback_visual_regression_grows` 권위 사례 신규** — golden SVG 가 회귀 가드 영역 영역 부정확 (PR #662 잘못된 정정 보존) → golden 갱신. **`feedback_close_issue_verify_merged` 정합** — Issue #700/#697 자체 close 영역 의 PR 머지 정합 회복. |
| **PR #707 Task #703** | BehindText/InFrontOfText 표 본문 흐름 누락 정정 (closes #703) | **완료 (옵션 A — 3 commits 단계별 보존 cherry-pick + no-ff merge `e3484101`)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 30+ 사이클 핵심 컨트리뷰터. 본질: `pagination/engine.rs:976-981` 의 BehindText/InFrontOfText 표 가드가 메인 pagination 인 `typeset.rs::typeset_table_paragraph` 분기에 미반영 → `place_table_with_text` 의 `cur_h += pre_height + table_total_height` 가 BehindText/InFrontOfText 표에도 적용되어 본문 흐름 누적 발생 → trailing 항목 다음 페이지 밀림. 정정 (`src/renderer/typeset.rs:1403`, +13 LOC): `Control::Table` 분기 진입 시 `text_wrap` 가드 (InFrontOfText \| BehindText) → `PageItem::Shape` push + continue. `pagination/engine.rs:976-981` 와 의미 정합. 영향 좁힘 (text_wrap 두 변형만). 본 환경 cherry-pick 충돌 0건. 결정적 검증 lib **1167** (+1 단위 테스트 paginator vs typeset 동치성) + 통합 ALL GREEN + clippy clean + issue_703 1/3 PASS (2 ignored — Issue #704 별개) + svg_snapshot form_002_page_0 PASS (PR #706 영역 보존). **본 환경 직접 페이지 수 측정** (PR 본문 정합 입증): samples/basic/calendar_year.hwp 2 → **1 페이지** (한컴 PDF 권위 정합) / samples/table-ipc.hwp 11 → **10 페이지** (한컴 PDF 권위 정합, 부수 효과). 광범위 sweep (PR 본문) 196 샘플 회귀 0, 정합 +2. **시각 판정 게이트 면제** — 결정적 검증 + sweep + 직접 측정 모두 통과. 분리된 후속 Issue #704 OPEN (TopAndBottom TAC + 각주 환경 0.84 px borderline 결함, 별개 본질). 검토 보고서: `mydocs/pr/archives/pr_707_review.md`. 처리 보고서: `mydocs/pr/archives/pr_707_report.md`. **`feedback_hancom_compat_specific_over_general` 정합** — text_wrap 두 변형만 가드 (영향 좁힘). **`feedback_process_must_follow` 권위 사례** — TDD Stage 1 RED → Stage 2 GREEN → Stage 3 sweep 절차 정합 + 후속 Issue #704 분리. **`feedback_image_renderer_paths_separate` 권위 사례** — typeset.rs + pagination/engine.rs 두 경로 동치성 단위 테스트 명시. |
| **PR #710 Task #702** | shortcut.hwp 다단 정의 후속 갱신 누락 정정 (closes #702) | **완료 (옵션 A — 2 commits 단계별 보존 cherry-pick + no-ff merge `63d38737`)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 30+ 사이클 핵심 컨트리뷰터. 본질 (2 영역): (1) Distribute 다단의 짧은 컬럼 vpos-reset 임계값 — `pv > 5000` 미달 (vpos=3000) → column-advance 미발동 → 6항목 1단 적층. (2) Page/Column break + 새 ColumnDef 미적용 — MultiColumn break 만 ColumnDef 적용 → shortcut.hwp p2 [쪽나누기]+1단 / [단나누기]+2단 배분 패턴에서 col_count 가 이전 zone 값 유지 → 페이지 분기 폭주. 정정 (`src/renderer/typeset.rs` +50/-6): ColumnType 추적 (`current_zone_column_type` 필드) + vpos-reset trigger Distribute 한정 완화 (`pv > 0`) + Page/Column break + 새 ColumnDef 검출 후 zone 재정의 (process_multicolumn_break 또는 force_new_page + ColumnDef). **영향 좁힘**: Normal (NEWSPAPER) 다단 → 기존 `pv > 5000` 유지 → Task #321/#418/#470 회귀 차단. 단일 단 → 기존 유지. 본 환경 cherry-pick 충돌 0건 (auto-merging typeset.rs). 결정적 검증 lib 1167 + 통합 ALL GREEN + clippy clean + **issue_702 2/2 PASS** (회귀 가드) + exam_eng_multicolumn PASS (Normal 다단 회귀 차단) + issue_418 PASS + svg_snapshot 8/8 (form-002 보존). **본 환경 직접 페이지 수 측정**: samples/basic/shortcut.hwp **10 → 8 페이지** (PDF 권위 7, +1쪽 Issue #708 잔존). **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅. WASM 빌드 4,604,562 bytes. **시각 판정 게이트 면제** — 작업지시자 결정 ("컨트리뷰터가 잔존 결함은 별도 이슈로 분리했으니 PR 체리픽해서 마무리"), 결정적 검증 + 회귀 가드 + sweep 회귀 0 + 후속 결함 분리 정합. 분리된 후속: Issue #708 OPEN (1쪽 시프트 잔존, pi=94 bare [단나누기] last col), Issue #709 OPEN (부수 시각 결함 4건). 검토 보고서: `mydocs/pr/archives/pr_710_review.md`. 처리 보고서: `mydocs/pr/archives/pr_710_report.md`. **`feedback_hancom_compat_specific_over_general` 권위 사례** — Distribute 한정 임계값 완화 (영향 좁힘). **`feedback_process_must_follow` 권위 사례 강화** — 컨트리뷰터의 fix 시도 회귀 발견 시 rollback + Issue #708/#709 별도 task 분리 정합. **`feedback_visual_judgment_authority` 권위 사례** — 잔존 결함 분리 + 결정적 검증 통과 → 시각 판정 면제 합리. |
| **PR #711 Task #705** | 한컴 호환 셀 안 PageHide 컨트롤 본질 정정 (closes #705) | **완료 (옵션 A — 7 commits 단계별 보존 cherry-pick + 메인테이너 가드 갱신 + no-ff merge `2bc982cb`)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 30+ 사이클 핵심 컨트리뷰터. **PR supersede 영역**: PR #638 (Task #634, src 무변경 회귀 가드만 머지) / PR #640 (Task #637 분석) / PR #641 (Task #639 cover-style 휴리스틱 폐기, 작업지시자 권고) close → PR #711 본질 정정 supersede. 본질 결함 3건: (1) `pagination/engine.rs:519-544` + `typeset.rs:2120` — page_hides 수집이 본문 paragraph 만 → 셀 안 PageHide 무시 (두 페이지네이션 경로 양분 정합, `collect_pagehide_in_table` 재귀 함수 추가). (2) `layout.rs:411-422` — `build_page_background()`/`build_page_borders()` 호출에 `hide_fill`/`hide_border` 가드 부재 → 6 필드 모두 가드 정합화. (3) `main.rs:1665-1670` — dump 셀 안 controls 매칭에서 PageHide 분기 부재. **CI Build & Test FAILURE** — `test_634_gukrip_page3_shows_page_number` (count=3 기대 → 0 실제). 본 환경 직접 재현 + 한컴 PDF 권위본 직접 측정 (`pdf/2022년 국립국어원 업무계획-2022.pdf` page 3 마지막 줄 `- 1 -`). **메인테이너 직접 시각 판정 ★ 통과** — 한컴 편집기 [감추기] 다이얼로그 6 필드 영역 + page 3 쪽번호 영역 직접 확인 — **PR #711 정답지 정합 확정**. **메인테이너 가드 갱신 commit `3253f7d0`** — test_634_gukrip_page3 (count=3 → count=0, rhwp 의 한컴 부정합 행위 보존 → 한컴 권위 정합 의도 변경). 결정적 검증 lib **1173** (+6 test_705 신규) + 통합 ALL GREEN + clippy clean. 광범위 sweep 7 fixture / 170 페이지 / **diff 2** (aift p2/p3 의도된 변경, 다른 6 fixture 회귀 0). WASM 빌드 4,606,179 bytes. Stage 0 198 sample sweep — 셀 안 PageHide 13건 / 6 샘플 (PR #640 H2 측정 누락 영역의 정량 확인). 영향 샘플: aift / 2022 국립국어원 / KTX / kps-ai / tac-img-02 hwp+hwpx. 검토 보고서: `mydocs/pr/archives/pr_711_review.md`. 처리 보고서: `mydocs/pr/archives/pr_711_report.md`. **`feedback_visual_judgment_authority` 권위 사례 강화** — CI FAILURE + test_634 가드 영역 vs 작업지시자 시각 판정 충돌 → **시각 판정 권위로 가드 갱신** (PR #706 form-002 패턴 정합 영역의 두 번째 사례). **`feedback_pr_supersede_chain` 권위 사례 강화** — 다중 PR (#638/#640/#641) close → 본질 정정 supersede 패턴. **`feedback_visual_regression_grows` 권위 사례 신규** — 회귀 가드 영역 의 의도 변경 (count=3 → count=0) — rhwp 의 한컴 부정합 행위 보존 영역 → 한컴 권위 정합 의도 변경. **`feedback_image_renderer_paths_separate` 권위 사례** — 두 페이지네이션 경로 (engine.rs + typeset.rs) 동기 정정 (`collect_pagehide_in_table` 재귀 함수). **`feedback_rule_not_heuristic` 정합** — PR #641 cover-style 휴리스틱 폐기 영역 의 본질 정정. |
| **PR #714 Task #712** | wrap=TopAndBottom 음수 vert_offset 표 침범 정정 (closes #712) | **완료 (옵션 A — 6 commits 단계별 보존 cherry-pick + no-ff merge `f4663fe0`)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 30+ 사이클 핵심 컨트리뷰터. 본질: `HwpUnit=u32` 영역 의 signed 캐스트 누락 결함. `vertical_offset` 음수 (예: -1796 HU) 의 unsigned 비트표현 = `0xFFFFF8FC` = `4294965500u32` 영역에서 `> 0` 게이트 통과 → 후속 `as i32` 캐스트 영역 음수 적용 → 표가 위로 점프 → 직전 인라인 표 영역 침범. 비-Partial 경로 (`table_layout.rs:1069+`) 영역에는 `raw_y.max(y_start)` 클램프 영역으로 음수 무력화. **Partial 경로** (`table_partial.rs:59-78`) 영역에는 클램프 부재 영역 → 결함 노출. 영향 샘플: `samples/2022년 국립국어원 업무계획.hwp` 페이지 31 영역 (작업지시자 안내 영역, 본 환경 동적 탐색 정합) — pi=585 (1×3 인라인 TAC 제목 표) + pi=586 (12×5 일정 표 vert=문단 -1796 HU 음수) 영역 의 침범 ~15.94 px → 정정 후 침범 0 px. 정정 (signed 비교 14 라인): table_partial.rs (Partial 경로 본질) + layout.rs (비-Partial 경로 게이트 동기) 영역 의 `(vertical_offset as i32) > 0` 변환. 영향 좁힘 (`feedback_hancom_compat_specific_over_general` 정합) — `is_continuation=true` / `vertical_offset >= 0` / 비-TopAndBottom wrap / TAC 표 / 비-Para vert_rel_to 영역 무영향. 본 환경 cherry-pick 충돌 0건 (auto-merging layout.rs + table_partial.rs). 결정적 검증 lib **1173** + 통합 ALL GREEN + clippy clean + **issue_712 1/1 PASS** (회귀 가드, **동적 페이지 탐색** 영역 — PR #644/#711 머지 영역 페이지네이션 변동 영역 자동 적응) + svg_snapshot 8/8 (form-002 PR #706 영역 보존) + test_705 6/6 (PR #711 영역 보존) + test_634 8/8 (가드 갱신 후 보존). **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅. WASM 빌드 4,606,183 bytes. **본 환경 직접 페이지 탐색**: pi=585/586 동시 등장 페이지 = page 31 (작업지시자 안내 정합). **작업지시자 시각 판정 ★ 통과** ("시각 판정 통과입니다") — BEFORE/AFTER SVG 비교 + 한컴 PDF 권위본 정합. 검토 보고서: `mydocs/pr/archives/pr_714_review.md`. 처리 보고서: `mydocs/pr/archives/pr_714_report.md`. **`feedback_image_renderer_paths_separate` 권위 사례** — Partial / 비-Partial 두 경로 동기 정정 (signed 캐스트). **`feedback_visual_regression_grows` 권위 사례** — 동적 페이지 탐색 영역 의 회귀 가드 — 페이지네이션 변동 영역 견고 영역 의 신규 패턴. **`feedback_process_must_follow` 정합** — TDD Stage 0/1 → 1 RED → 2-3 GREEN → 4-5 sweep → 6 보고서 절차 정합. |
| **PR #715 Task #713** | 분할 표 orphan sliver(<25px) 행 단위 push (closes #713) | **완료 (옵션 A — 5 commits 단계별 보존 cherry-pick + no-ff merge `d4efc29a`)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 30+ 사이클 핵심 컨트리뷰터. 본질: `samples/2022년 국립국어원 업무계획.hwp` 12×5 일정 표 (pi=586, RowBreak) row 8 영역 의 **17.6 px 인트라-로우 분할** 결함. 한컴은 페이지 끝 영역 의 작은 sliver 영역 부재 영역 의 행 단위 push 가 한컴 권위. **본 환경 직접 재현**: BEFORE page_index=30 rows=0..**9** split_end=**17.6 px** (orphan sliver) → AFTER page_index=30 rows=0..**8** split_end=0 px / page_index=31 rows=**8**..12 cont=true (row 8 전체 영역 다음 페이지 push). 정정 (`src/renderer/typeset.rs:1931+`, +5 LOC + 주석): `MIN_TOP_KEEP_PX = 25.0` 가드 추가 — `avail_content_for_r >= MIN_TOP_KEEP_PX` 영역 의 orphan 가드. **임계값 25 px 결정 근거**: 본 결함 (17.6 px) 차단 / synam-001 p23 정합 (27.3 px) 보존 / 기타 정합 분할 (93/437/510 px) 보존. **영향 좁힘** (`feedback_hancom_compat_specific_over_general`): avail_content_for_r >= 25 px 영역 의 분할 / 비-RowBreak 표 / 비-Partial 분할 영역 무영향. 활성 경로 (typeset.rs) 만 정정 — fallback 경로 (pagination/engine.rs) 영역 부재 (RHWP_USE_PAGINATOR=1 영역 미설정 영역 위험 매우 낮음). 본 환경 cherry-pick 충돌 0건 (auto-merging typeset.rs). 결정적 검증 lib **1173** + 통합 ALL GREEN + clippy clean + **issue_713 1/1 PASS** (회귀 가드, **동적 페이지 sweep** 영역) + issue_712 PASS (PR #714 영역 보존) + svg_snapshot 8/8 (form-002 PR #706 영역 보존). **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅. WASM 빌드 **4,607,734 bytes**. **작업지시자 시각 판정 ★ 통과** ("웹 에디터에서 메인테이너의 시각 판정 통과입니다") — page 31 row 8 sliver 해소 + page 32 row 8 전체 push 정합. 검토 보고서: `mydocs/pr/archives/pr_715_review.md`. 처리 보고서: `mydocs/pr/archives/pr_715_report.md`. **`feedback_hancom_compat_specific_over_general` 권위 사례** — 25 px 임계값 영역 의 영향 좁힘 (17.6 ↔ 27.3 px 사이). **`feedback_visual_regression_grows` 권위 사례 강화** — 동적 페이지 sweep 영역 의 회귀 가드 (PR #714 패턴 정합 영역의 두 번째 사례). **`feedback_process_must_follow` 정합** — TDD Stage 0 → 1 RED → 2 가설 → 3 GREEN → 4-5-6 절차 정합. |
| **PR #718 Task #661** | 드래그 선택 중 커서/스크롤 튐 정정 (closes #661) | **완료 (옵션 A — 5 commits 단계별 보존 cherry-pick + 메인테이너 통합 정정 + no-ff merge `95eea5f9`)** | 컨트리뷰터 @postmelee (Taegyu Lee) — 6+ 사이클 (PR #645/#664 동일 컨트리뷰터). 본질: rhwp-studio 영역 의 텍스트 드래그 선택 중 커서/스크롤 튐 결함. 기존 영역: mousemove 마다 hit-test → cursor focus 이동 → caret 갱신 + scrollCaretIntoView() 호출 → 선택 focus 영역 의 다른 페이지/문단 순간 이동 시 스크롤 컨테이너 가 caret rect 추적 → 사용자 드래그 위치 ↔ 문서 스크롤 함께 튐. 정정 (드래그 중 caret 갱신 + 스크롤 책임 분리): (1) caret 영역 — 선택 상태 표시만 갱신 (scrollCaretIntoView 부재). (2) 스크롤 — 포인터 edge 감지만 담당 (RAF + edge 48 px + step 2~20 px). (3) 편집 영역 외부 드래그 — document-level mousemove 등록/해제. (4) 포인터 좌표 저장 + 스크롤 발생 시 동일 좌표로 hit-test 재계산. 신규 헬퍼 (`input-handler.ts` +128 LOC): startTextSelectionDrag / stopTextSelectionDrag / updateTextSelectionDragPointer / updateTextSelectionDragFromPointer / hitTestFromClientPoint / updateTextSelectionDragAutoScroll / runTextSelectionDragAutoScroll / updateCaretDuringDrag. `input-handler-mouse.ts` (+6/-9): 3 곳 isDragging=true → startTextSelectionDrag(e) + mousemove 분기 hit-test 직접 호출 → updateTextSelectionDragFromPointer 래퍼 + mouseup → stopTextSelectionDrag. **CONFLICTING (DIRTY)** — base = `de1c2d00` (5/8) ↔ devel HEAD = `93d6c6a7` (PR #693/#664 머지 후) 영역 의 충돌 3건. **메인테이너 통합 정정** (3건 해결): (a) `input-handler-mouse.ts:1107` mousemove 분기 — PR #718 영역 의 updateTextSelectionDragFromPointer 영역 채택. (b) `input-handler.ts:980` hitTestFromClientPoint — PR #718 영역 의 clientX/Y parameter + devel 영역 의 PR #693 (Task #685+#689) `getPageAtPoint` (그리드 모드 click 좌표 정합) 영역 통합. (c) `input-handler.ts:1648` updateCaretDuringDrag — PR #718 영역 의 scrollCaretIntoView 부재 본질 + devel 영역 의 PR #664 (Task #658) `caret.updateLive` (깜박임 타이머 유지 본질) 영역 통합. 6번째 commit "Align drag caret update with devel" — 메인테이너 통합 정정 영역에 이미 포함되어 빈 commit skip. 결정적 검증 lib 1173 + 통합 ALL GREEN + clippy clean + tsc clean + npm build 정상. WASM 빌드 4,607,734 bytes. 회귀 가드 e2e (`drag-selection-autoscroll.test.mjs` +86 LOC): 70줄 문서 + 첫 줄 → 하단 edge 드래그 / scrollTop 0→1529 / hasSelection / focus 문단 69 / highlight 70. **작업지시자 시각 판정 ★ 통과** ("웹 에디터에서 블럭 드래그 선택 기능 통과입니다") — 드래그 안정성 / 자동 스크롤 / 편집 영역 외부 드래그 / 선택 focus 정합 / 그리드 모드 정합 / 깜박임 타이머 유지 / 다른 PR (#693/#706/#711/#714/#715) 회귀 부재 모두 정합. 분리된 후속 Issue #717 OPEN (PR #725 별도 처리). 검토 보고서: `mydocs/pr/archives/pr_718_review.md`. 처리 보고서: `mydocs/pr/archives/pr_718_report.md`. **`feedback_pr_supersede_chain` 권위 사례 강화** — PR #664 (Task #658) `updateLive` + PR #693 (Task #685+#689) `getPageAtPoint` 영역과 PR #718 영역 의 충돌 영역 → 메인테이너 통합 정정 (PR #694/#706/#711 패턴 정합). **`feedback_image_renderer_paths_separate` 권위 사례** — 두 영역 동기 정정 + 다른 PR 영역 본질 보존. **`feedback_contributor_cycle_check` 정합** — @postmelee 6+ 사이클 핵심 컨트리뷰터 정확 표현. |
| **PR #719 Task #716** | 빈 paragraph fix_overlay push 차단 (page 1 LAYOUT_OVERFLOW_DRAW) (closes #716) | **완료 (옵션 A — 7 commits 단계별 보존 cherry-pick + no-ff merge `b8defa26`)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 30+ 사이클 핵심 컨트리뷰터 (누적 23 머지). 본질: Task #9 의 `fix_overlay_active` push 영역 의 빈 paragraph (text_len=0) push 영역으로 의미 없는 drift +20 px (pi=1 +8 + pi=3 +12) 누적 → page 1 마지막 줄 (pi=15 line=2) 영역 컬럼 하단 +20.15 px 초과 시각 cropping. **본 환경 직접 재현**: BEFORE `LAYOUT_OVERFLOW_DRAW: pi=15 line=2 overflow=20.1px` + `LAYOUT_OVERFLOW: para=15 overflow=31.3px` → AFTER `LAYOUT_OVERFLOW_DRAW` 메시지 **부재** ★ + `LAYOUT_OVERFLOW` 11.3px (trailing ls 잔존, Task #452 영역). Root cause 영역 의 흐름: 수행계획서 가설 (음수 line_spacing 미반영) → Stage 2 instrument 실측 영역 의 line advance 영역 ls 가산 정확 확인 → drift 진원지 = Task #9 push 의 빈 paragraph 영역 (pi=1 +8 + pi=3 +12 = +20 px ≈ overflow 20.15 px 99.3% 일치). 정정 (`src/renderer/layout.rs:1562-1582`, +13 LOC): `is_empty_para` 가드 영역 추가 — `text.is_empty()` 또는 control/FFFC 만. `!is_fixed && !is_empty_para` 조건 영역 push 적용. **영향 좁힘** (`feedback_hancom_compat_specific_over_general`): Task #9 텍스트 paragraph push 의도 (TAC 표 위 침범 차단) 그대로 유지 + fix_overlay_active 유지 + 빈 paragraph 만 차단. 본 환경 cherry-pick 충돌 0건 (auto-merging layout.rs). 결정적 검증 lib **1173** + 통합 ALL GREEN + clippy clean + **issue_716 1/1 PASS** (회귀 가드 — body bbox + TextLine bbox 영역 max_bottom <= body_bottom + 0.5 px) + svg_snapshot 8/8 (form-002 PR #706 영역 보존) + issue_712 PASS (PR #714 영역) + issue_713 PASS (PR #715 영역). **광범위 sweep 7 fixture / 170 페이지 / 회귀 0** ✅. PR 본문 광범위 sweep (169 샘플): LAYOUT_OVERFLOW_DRAW 187→185 (−2 정정) + LAYOUT_OVERFLOW 279→277 (−2 부수 개선 table-vpos-01 hwp/hwpx) + 페이지 수 변동 0 + panic 0. **시각 판정 게이트 면제** — 결정적 검증 + 광범위 sweep + 본 환경 직접 측정 (LAYOUT_OVERFLOW_DRAW 부재 확증) 모두 통과. 검토 보고서: `mydocs/pr/archives/pr_719_review.md`. 처리 보고서: `mydocs/pr/archives/pr_719_report.md`. **`feedback_hancom_compat_specific_over_general` 권위 사례** — is_empty_para 가드 영역 영향 좁힘 (Task #9 의도 유지). **`feedback_process_must_follow` 권위 사례 강화** — 정적 분석 가설 영역 의 Stage 2 실측 영역 가설 갱신 영역의 절차 정합. **`feedback_visual_judgment_authority` 정합** — 결정적 검증 + sweep + 직접 측정 통과 영역 → 시각 판정 면제 합리. |

## 메인테이너 본인 영역 — 5/9

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **Task #724 (PR 신규 예정)** | hwp3-sample5-hwp5.hwp paragraph 441 wrap zone 매칭 실패 + HWP3 파서 IR 정합 정정 (closes #724) | **완료 (Stage 1~9 정정, PR 신규 생성 예정)** | 메인테이너 (Taesup Jang) 본인 영역. PR #723 (Task #722) 후속. **Stage 1~3**: HWP5 변환본 anchor host cs=0 caption-style + 다음 paragraph cs>0 wrap zone 매칭 실패 → typeset.rs 매칭 가드 (`anchor_image_match` image expected_cs 정확 일치) + Task #321 vpos-reset 가드의 wrap_around 강제 종료 (anchor cs=0 한정). **Stage 4~6**: 작업지시자 핵심 지적 ("HWP3 파서가 잘못해석해서 IR 로 잘못 전달하는 것 아닌가? composer.rs 에서 처리하면 너무 예외") — HWP3 파서 후처리 영역 wrap_zone 끝 line sw=0 강제 → 한컴 HWP5 변환본 정합 sw=col_area_width=51024. **Stage 7~9**: HWP3 파서 page break flag 처리 (빈 paragraph 가드 2 곳) + force_vpos_reset (vpos reset 보존) + wrap_zone 비활성 sw=0 정합화 (paragraph 189 ls[3~6], 190/191 등 페이지 break 후 paragraph). HWP3 파서 단일 영역 정정 (composer/layout 미수정, CLAUDE.md HWP3 파서 규칙 정합). 결정적 검증 1166 lib + clippy clean + 광범위 sweep 209 fixture DIFF 0. 시각 판정: HWP3 native 페이지 4/8/9/16/22 + HWP5 변환본 페이지 16/22 + PR #723 영역 (페이지 8/27/48) 모두 한컴 PDF 권위 정합 ★. **`project_hancom_lineseg_behavior` 권위 사례 강화** — HWP3 native 의 sw=0 인코딩 패턴이 한컴 HWP5 변환본 IR (sw=51024) 정합 위반의 본질. HWP3 파서 정합화로 정정. **`feedback_visual_judgment_authority` 권위 사례** — 작업지시자 본질 지적 ("HWP3 파서 결함") 으로 본 환경 결함의 정확한 위치 (HWP3 파서 vs composer/layout) 결정. **HWP3 파서 ↔ Document IR 표준 정합 권위 사례** — composer/layout 정정 영역 회피, HWP3 파서 영역 정합화로 본질 도달. 최종 결과 보고서: `mydocs/report/task_m100_724_report.md`. 단계별 보고서 6개. |

## 작업 메모

### 본 사이클 (5/9) 패턴

- PR #694 (Task #688) — 4 commits cherry-pick + 메인테이너 충돌 해결 통합 정정 머지. PR #681 영역 외곽 테두리 분기 보존하며 PR #694 의 본질 가드 (`paragraphs.len() == 1`) 만 적용. `feedback_hancom_compat_specific_over_general` (과도한 정밀화 회피) 권위 사례 강화.
- PR #693 (Task #685+#689) — 8 commits 단계별 보존 cherry-pick + no-ff merge `b784c585` (옵션 A). 그리드 모드 click 좌표 한컴 호환 정정 (헬퍼 2개 + 5 파일 일괄 sweep). e2e 22/0 + 시각 판정 ★ 통과. @johndoekim 두 번째 사이클 PR (PR #645 후속).
- PR #695 — 1 commit cherry-pick + no-ff merge `2ca126c3` (옵션 A). ir-diff CLI `--summary` / `--max-lines` 출력 가드 추가. 회귀 가드 3/3 + 수동 검증 ALL PASS. **시각 판정 면제** (UI 무관). PR #659 (Task #653) 후속 보강.
- PR #699 — 5 commits 단계별 보존 cherry-pick + no-ff merge `bbc44035` (옵션 A). PDF 권위 자료 6개 1-up 인쇄로 갱신 (2-up/1쪽만 인쇄 영역 → 정상). 정합 회복 2건 (hwpx/aift + KTX) + 잔존 어긋남 4건 (별건). **시각 판정 면제** (PDF 권위 자료 갱신, 코드 무변경). PR #670 후속 정정.
- PR #706 (Task #697 + #700 통합) — 5 commits cherry-pick + golden SVG 갱신 + no-ff merge `fce2d870` (옵션 A). PR #701 supersede. **CI FAILURE 영역 의 작업지시자 시각 판정 ★ 통과 — 정답지 정합 확정**. golden SVG 가 회귀 가드 영역 부정확 (PR #662 의 잘못된 정정 보존) 영역 영역 갱신. PR #662 (Task #656) form-002 정정 본질 자체 부정확 사실 — 후속 별도 분석 가능성. `feedback_visual_judgment_authority` 권위 사례 신규 (CI 결정적 검증 vs 시각 판정 충돌, 시각 판정 권위).
- PR #707 (Task #703) — 3 commits 단계별 보존 cherry-pick + no-ff merge `e3484101` (옵션 A). BehindText/InFrontOfText 표 본문 흐름 누락 정정 (typeset.rs +13 LOC, pagination/engine.rs 와 의미 정합). lib 1166 → **1167** (+1 단위 테스트 paginator vs typeset 동치성). 직접 측정 정합 회복: calendar_year 2→1 / table-ipc 11→10. 196 샘플 sweep 회귀 0. **시각 판정 면제** (결정적 검증 + sweep + 직접 측정 모두 통과). 분리된 후속 Issue #704 OPEN.
- PR #710 (Task #702) — 2 commits cherry-pick + no-ff merge `63d38737` (옵션 A). shortcut.hwp 다단 정의 후속 갱신 누락 정정 (typeset.rs +50/-6, ColumnType 추적 + Distribute 임계값 완화 + Page/Column break ColumnDef 재정의). 직접 측정 정합 회복: shortcut.hwp **10 → 8 페이지** (PDF 권위 7, +1쪽 Issue #708 잔존). 7 fixture / 170 페이지 sweep 회귀 0. **시각 판정 면제** (작업지시자 결정 — 잔존 결함 분리 정합). 분리된 후속 Issue #708/#709 OPEN. WASM 4,604,562 bytes.
- PR #711 (Task #705) — 7 commits cherry-pick + 메인테이너 가드 갱신 (`3253f7d0` test_634_gukrip_page3 count=3→0) + no-ff merge `2bc982cb` (옵션 A). PR #638/#640/#641 close → 본질 정정 supersede. 한컴 호환 셀 안 PageHide 본질 정정 3 결함 (engine.rs + typeset.rs + layout.rs + main.rs). **CI FAILURE 영역의 작업지시자 시각 판정 ★ 통과 — 정답지 정합 확정** (한컴 PDF page 3 `- 1 -` 정합). lib 1167 → **1173** (+6 test_705 신규). sweep 7 fixture / 170 페이지 / diff 2 (aift p2/p3 의도된 변경). PR #706 form-002 패턴 정합 영역 의 두 번째 사례 (시각 판정 권위로 가드 갱신).
- PR #714 (Task #712) — 6 commits 단계별 보존 cherry-pick + no-ff merge `f4663fe0` (옵션 A). HwpUnit=u32 영역 의 signed 캐스트 누락 결함 영역 정정 (signed 비교 14 라인, 두 경로 동기). 국립국어원 업무계획.hwp 페이지 31 영역 의 pi=586 12×5 일정 표 영역 의 pi=585 침범 ~15.94 px → 0 px. 본 환경 cherry-pick 충돌 0건. lib 1173 + 통합 ALL GREEN + issue_712 1/1 + sweep 7 fixture / 170 페이지 / 회귀 0. WASM 4,606,183 bytes. **작업지시자 시각 판정 ★ 통과**. 회귀 가드 영역 의 동적 페이지 탐색 영역 (PR #644/#711 머지 영역 페이지네이션 변동 영역 자동 적응) 신규 패턴.
- PR #715 (Task #713) — 5 commits 단계별 보존 cherry-pick + no-ff merge `d4efc29a` (옵션 A). 분할 표 orphan sliver (<25 px) 행 단위 push 정정 (typeset.rs +5 LOC, MIN_TOP_KEEP_PX=25 가드). 국립국어원 업무계획.hwp 페이지 31 영역 의 row 8 sliver 17.6 px → 0 px (row 8 전체 영역 page 32 push). 본 환경 직접 재현 + 정정 입증. 임계값 25 px 영역 영역 의 영향 좁힘 (17.6 ↔ 27.3 px 사이). 본 환경 cherry-pick 충돌 0건. lib 1173 + 통합 ALL GREEN + issue_713 1/1 + sweep 7 fixture / 170 페이지 / 회귀 0. WASM 4,607,734 bytes. **작업지시자 시각 판정 ★ 통과** (웹 에디터). 동적 페이지 sweep 영역 의 회귀 가드 (PR #714 패턴 정합 영역의 두 번째 사례).
- PR #718 (Task #661) — 5 commits cherry-pick + 메인테이너 통합 정정 + no-ff merge `95eea5f9` (옵션 A). rhwp-studio 영역 의 드래그 선택 중 커서/스크롤 튐 정정 (input-handler.ts +128 LOC + input-handler-mouse.ts +6/-9, 드래그 중 caret 갱신 + 스크롤 책임 분리). 충돌 3건 영역 의 메인테이너 통합 정정: PR #693 (Task #685+#689) `getPageAtPoint` 영역 + PR #664 (Task #658) `caret.updateLive` 영역과 PR #718 영역 의 충돌 영역 → 양립 영역 통합. lib 1173 + 통합 ALL GREEN + tsc clean + npm build 정상. WASM 4,607,734 bytes. **작업지시자 시각 판정 ★ 통과** (웹 에디터, "블럭 드래그 선택 기능 통과"). @postmelee 6+ 사이클 (PR #645/#664 동일 컨트리뷰터). 분리된 후속 Issue #717 OPEN.
- PR #719 (Task #716) — 7 commits 단계별 보존 cherry-pick + no-ff merge `b8defa26` (옵션 A). 빈 paragraph fix_overlay push 차단 정정 (layout.rs +13 LOC, is_empty_para 가드). hongbo p1 LAYOUT_OVERFLOW_DRAW 20.1 px → 부재 ★ (본 환경 직접 측정 + 정정 입증). 영향 좁힘: Task #9 텍스트 push 의도 유지 + 빈 paragraph 만 차단. 본 환경 cherry-pick 충돌 0건. lib 1173 + 통합 ALL GREEN + issue_716 1/1 + sweep 7 fixture / 170 페이지 / 회귀 0. PR 본문 광범위 sweep 169 샘플: LAYOUT_OVERFLOW_DRAW 187→185 (−2) + LAYOUT_OVERFLOW 279→277 (−2) + 페이지 수 변동 0. **시각 판정 면제** (결정적 검증 + 광범위 sweep + 직접 측정 통과). 정적 분석 가설 영역 의 Stage 2 실측 영역 가설 갱신 영역 절차 정합 (`feedback_process_must_follow` 권위 사례 강화).

### 후속 이슈

- **Issue #726 (5/9 신규 등록)** — `samples/table-vpos-01.hwpx` p.5 nested 11×3 그리드 안 4대 그룹 구분 도형 2개 SVG 미출력. 본 PR (Task #688) 정정 후 nested 11×3 그리드 표시 + cell-clip 확장으로 가시화. 본 PR 정정으로 인한 회귀 아님 (작업지시자 확정). 본 환경 IR 권위 (`rhwp dump`) — pi=34 셀[18] (r=8, cs=3) 다각형 1개 존재 + 셀[6]/셀[13] 영역 ctrls=0 (파서/IR 누락 가능성). SVG 출력 측정 — polygon 0건. 두 결함 후보: (a) SVG renderer 다각형 미출력, (b) HWPX 파서 다각형 1개 누락.
- **Issue #727 (5/9 신규 등록)** — `samples/table-vpos-01.hwpx` p.5 PUA U+F02B1~F02BC 매핑 결함. 한컴 PDF 권위 (`pdf/table-vpos-01-2022.pdf`) 5쪽 12 추진과제 번호는 **사각형 안 숫자 1~12** 인데 본 환경은 **원문자 ①~⑫** 로 매핑 (PR #600 / Task #509 도입), 10~12 는 두부. PR #600 의 mel-001 / kps-ai 정합 검증 정합 재검토 필요 가능성 — 동일 PUA 코드가 문서별 다른 의도일 가능성. 본 PR 정정 후 nested 11×3 그리드 표시로 가시화된 별건. 작업지시자 권위 판정: 사각형 안 숫자가 한컴 정답지.
- **Issue #731 (5/9 신규 등록)** — `rhwp-studio` 영역 의 입력 메뉴 영역 의 **수식 (Equation) 신규 입력 기능 미구현**. 본문 캐럿 위치 영역 에 새 수식 객체 영역 삽입 영역 누락. 기존 `insert:equation-edit` (선택된 수식 객체 더블클릭 영역의 편집) / `EquationEditorDialog` / WASM `renderEquationPreview`+`getEquationProperties`/`setEquationProperties` / Rust `Control::Equation` 모델 영역 모두 구현 영역. 신규 영역: `index.html` 입력 메뉴 영역 의 수식 항목 + WASM `insertEquation` API + `insert:equation` command + Ctrl+N+M 단축키 (한컴 호환). 우선순위 Medium-High. PR #714 시각 판정 영역 별건 보고.
