# 2026-06-13 오늘 할일

## PR #1400 — 이미지 선택 후 Shift+방향키 크기 조절 (#1231, oksure)

- [x] PR 정보 확인 — base `devel`, head `oksure:contrib/feat-1231-shift-arrow-resize`
- [x] 로컬 검증 — `cargo build --release`, `cargo test --release --lib`, `cargo test --profile release-test --tests`, `cargo fmt --check`, `rhwp-studio npm test` 통과
- [x] 검토 문서 `mydocs/pr/pr_1400_review.md` 작성
- [x] 검토 문서 커밋 `eaad7faa`를 PR head에 push해 PR diff에 포함
- [x] GitHub Actions 재확인 — `Build & Test`, CodeQL, Canvas visual diff 통과
- [x] PR merge — merge commit `6e7c7165`
- [x] 이슈 #1231 auto-close 미동작 확인 후 수동 close
- [x] PR 감사 코멘트 등록
- [x] 검토 문서 `mydocs/pr/archives/pr_1400_review.md`로 이동

## PR #1399 — createEmpty 기본 A4 구역/빈 문단 생성 (#1386, oksure)

- [x] PR 정보 확인 — base `devel`, head `oksure:contrib/fix-1386-create-empty-section`
- [x] 로컬 검증 — `cargo fmt --check`, `cargo build --release`, `cargo test --release --lib`, `cargo test --profile release-test --tests`, `wasm-pack build --target web --out-dir pkg` 통과
- [x] 검토 문서 `mydocs/pr/archives/pr_1399_review.md` 작성
- [x] 검토 문서/오늘할일 커밋을 PR head에 push해 PR diff에 포함
- [ ] GitHub Actions 재확인 — 문서 커밋 push 후 `Build & Test`, CodeQL, Canvas visual diff 확인
- [ ] PR merge 준비 완료 보고
- [ ] PR merge — 작업지시자 승인 후 진행
- [ ] 이슈 #1386 auto-close 여부 확인
- [ ] PR 감사 코멘트 등록
