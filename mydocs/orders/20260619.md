# 2026-06-19 오늘 할일

## 원격 동기화

- [x] `origin` fetch — local/devel·devel 모두 `588be823` 정합 (앞0/뒤0)
- [x] WASM 빌드 (Docker) — rhwp v0.7.15, pkg/ 정상

## #1434 — 누름틀 안내문(Direction) 한컴 바인딩 결함 (HWP 저장 호환)

- [x] 이슈 확인 + assignee 확인(edwardkim 기지정) + 열린 PR 확인 (겹침 없음)
- [x] `local/task1434` 브랜치 생성 (`local/devel` 기준)
- [x] 트러블슈팅 사전 검색 — 선행 기록 없음 (기존 누름틀 작업은 편집 UX, 저장 호환 신규)
- [x] 근본 원인 진단 (한컴 권위 샘플 field-01/form-01 command 대조)
  - 한컴 원본: `Clickhere:set:48:Direction:wstring:6:여기에 입력 HelpState:wstring:0:  ` (공백 2개, Name 키 없음)
  - rhwp `build_clickhere_command`: ① **Name 키를 command에 포함**(한컴은 CTRL_DATA에만) ② trailing **공백 1개**(한컴 2개) ③ **set 길이=inner 전체**(한컴 inner_len−1)
  - 이름이 command에 끼면 set 길이·구조가 한컴 파서 기대와 어긋나 Direction 바인딩 실패
- [x] 수행계획서 `mydocs/plans/task_m100_1434.md` — 승인 (`aa18893c`)
- [x] 구현계획서 `mydocs/plans/task_m100_1434_impl.md` (3단계) — 승인 대기
  - 추가 발견: wasm_api 수정 경로는 이미 name="" 전달(정상), field_query 삽입 경로만 name을 command에 포함(결함). build_clickhere_command 시그니처 (guide,memo,name)→(guide,memo) 단순화
- [x] 1단계: build_clickhere_command 한컴 포맷 정정(Name 제거+공백2개+set−1) + 호출처 2곳 정합 + 단위 테스트 4건 — control 7 passed/field_query 4 passed, 한컴 샘플 dump 불변 — 승인 대기
- [x] 2단계: round-trip 통합 테스트(저장→재파싱 guide/memo/name 보존 + Name 부재) 2/2 passed, CI급 전체 그린·fmt·clippy 0, 한컴 판정 샘플 산출(output/poc/task1434) — 승인 대기
- [x] 3단계: 한컴 2020/2022 편집기 바인딩 판정 **통과**(작업지시자 시각 판정) + 트러블슈팅 + 최종 보고서 — 승인
- [x] merge(`local/devel`→`devel` `13ecafae`) + push + #1434 close (origin/devel 포함 검증)

## #1439 — 드래그&드롭 로컬 파일 로딩 보안 게이트 + 모달 확인 대화상자

- [x] 이슈 등록(#1439) + assignee 지정(edwardkim) + 열린 PR 확인 (겹침 없음)
- [x] `local/task1439` 브랜치 생성 (`local/devel` 기준)
- [x] 트러블슈팅 사전 검색 — 드롭 보안 선행 기록 없음
- [x] 현황 조사: drop 핸들러(main.ts:364)가 확인·안내 없이 즉시 loadFile. 상태바 안내는 로딩/저장모드일 뿐 보안 고지 아님. unsaved-changes-dialog가 확인 대화상자 참조 모델(showAsync Promise 패턴)
- [x] 수행계획서 `mydocs/plans/task_m100_1439.md` (3단계) — 승인 (`40dbc38c`)
- [x] 구현계획서 `mydocs/plans/task_m100_1439_impl.md` (3단계, 크롬 확장 모드 동작 보장 포함) — 승인 대기
  - 확장/웹 공통: 드롭·대화상자 모두 main.ts 같은 페이지 컨텍스트, 확장도 standalone 탭(/rhwp/)이라 ModalDialog 정상. 드롭은 dataTransfer.files라 file:// 권한 무관
- [x] 1단계: drop-confirm-dialog.ts 신규(ModalDialog showAsync Promise<boolean>) + main.ts drop 핸들러 게이트(loadFile/이미지배치 전 확인) — tsc exit 0 — 승인 대기
- [x] 2단계: npm run build(확장/PWA) 성공·산출물 게이트 포함, e2e 인프라 pixelmatch/pngjs 설치, unsaved-guard e2e 전부 PASS(회귀 0) — 승인 대기
- [x] 3단계: drop-confirm e2e 신규(3 TC 전부 PASS) + 보안 가이드 드롭 게이트 규칙 + 최종 보고서 — 승인
- [x] merge(`local/devel`→`devel` `9fa266b8`) + push + #1439 close (origin/devel 포함 검증)

## PR #1441 — task 1440: 온새미로 그림 어울림 및 문단 테두리 정합

- [x] PR 생성 확인: https://github.com/edwardkim/rhwp/pull/1441
- [x] base/head 확인: `edwardkim/rhwp:devel` ← `edwardkim/rhwp:task_m100_1440`
- [x] mergeable 확인: `MERGEABLE`, GitHub Actions 대기 상태
- [x] 로컬 필수 검증 완료
  - `cargo build --release`
  - `cargo test --release --lib` (`1842 passed; 0 failed; 6 ignored`)
  - `cargo test --profile release-test --tests`
  - `cargo fmt --check`
  - `cargo clippy --all-targets -- -D warnings`
  - `npm --prefix rhwp-studio run build`
  - `cargo test --test issue_1440_onsamiro_picture_wrap` (`4 passed`)
- [x] 리뷰 문서 작성: `mydocs/pr/archives/pr_1441_review.md`
- [x] 리뷰 처리 계획 작성: `mydocs/pr/archives/pr_1441_review_impl.md`
- [ ] 리뷰 문서/오늘할일 커밋을 PR head에 push
- [ ] GitHub Actions 재확인
- [ ] merge 후 #1440 close 여부 확인

## PR #1442 — task 493: 셀 보호 속성 보존과 입력 차단 UX 구현

- [x] PR 생성 확인: https://github.com/edwardkim/rhwp/pull/1442
- [x] base/head 확인: `edwardkim/rhwp:devel` ← `edwardkim/rhwp:task_m100_493`
- [x] merge state 확인: `BLOCKED` (GitHub Actions 대기)
- [x] 원격 PR head 동기화: `upstream/task_m100_493` fetch 후 fast-forward (`63879223`)
- [x] 로컬 필수 검증 완료
  - `cargo build --release`
  - `cargo test --release --lib` (`1842 passed; 0 failed; 6 ignored`)
  - `cargo test --profile release-test --tests`
  - `cargo fmt --check`
  - `cargo clippy --all-targets -- -D warnings`
  - `wasm-pack build --target web --out-dir pkg`
  - `npm --prefix rhwp-studio run build`
  - `cargo test --test issue_493_cell_attrs`
  - `cargo test --test issue_493_hwpx_cell_field_name`
  - `cargo test --test issue_258_clickhere_form_mode`
  - `cargo test set_cell_field_text_updates_text_metadata --lib`
- [x] 작업지시자 시각 검증 완료: 보호 셀 UX, 표/셀 속성 진입, 대화상자 탭 전환 크기 유지
- [x] 리뷰 문서 작성: `mydocs/pr/archives/pr_1442_review.md`
- [x] 리뷰 처리 계획 작성: `mydocs/pr/archives/pr_1442_review_impl.md`
- [x] 리뷰 문서/오늘할일 커밋을 PR head에 push
- [ ] GitHub Actions 재확인
- [ ] merge 후 #493 close 여부 확인
