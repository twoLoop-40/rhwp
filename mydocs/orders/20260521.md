# 오늘 할일 - 2026년 5월 21일

## M100 — 이슈 처리

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #986 | 가로 방향 문서의 비글자취급 표 밀림 현상 수정 | Draft PR 생성 | 기본/fallback 1페이지 정합, 전체 lib release 및 회귀 세트 통과, PR #1051 |

## M100 — PR 처리 (메인테이너)

### PR #1026 — fix(text_measurement): 좁은 구두점 (U+2018/U+2019/U+2027) 폭 분류 + native/WASM 동기화 (재제출, 외부 기여 @HaimLee-4869, Refs PR #1021)

- [x] PR 정보 + 사이클 점검 (@HaimLee-4869 4번째 기여, 1차 5/20 옵션 B 보류 후 KTX 회귀 가드 + rebase 후 재제출)
- [x] 이전 검토 결과 확인 (`mydocs/pr/archives/pr_1026_review.md` + `pr_1026_report.md`) — 1차 보류 사유: KTX 페이지번호 +10px 회귀
- [x] 재제출 변경 사항 분석 (PR 본문 "C8+C9 소스 로직 변경 없음" — base rebase + KTX 회귀 가드 + golden 갱신만)
- [x] devel 동기화 (`ac6aeed4`) — PR base = `bbd38e85` (PR #1033 머지 후), origin/devel = `ac6aeed4` (PR #1034 머지 후)
- [x] 코드 본질 분석 — text_measurement.rs +26/-2 (1차와 동일 H1+H2+H3) + tests/issue_874_ktx_toc_page_number_right_align.rs +98 (신규 KTX 가드) + golden 갱신 issue-157/issue-617 (issue-267 KTX 보존)
- [x] 재검토 문서 `pr_1026_review_v2.md` → 작업지시자 옵션 A 승인 (1차 회귀 정정 + 회귀 가드 영구화)
- [x] cherry-pick `cb53dd94` (author @HaimLee-4869 보존, 충돌 없음)
- [x] 검증 (`feedback_push_full_test_required` 정합): cargo test --release --lib **1319 passed** + 통합 모두 passed + cargo test --test issue_874_ktx_toc_page_number_right_align **1 passed** (KTX 페이지번호 정합 정량 입증, max_x < 695.0 단언) + cargo test --test svg_snapshot **8/8 passed** (issue_267_ktx_toc_page 포함, KTX golden 보존) + fmt --all clean
- [x] sweep 7 fixtures (BEFORE `ac6aeed4` ↔ AFTER): **32 diff = 모두 본 PR 본질**:
  - KTX 9 diff (휴먼명조 U+2018 정합, **목차 페이지번호 영역 변동 없음 BEFORE = AFTER**)
  - hwp3-sample16-hwp5 1 + exam_kor 17 + aift 5 (HY신명조 narrow 정합, golden issue-617 영역)
  - hwp3-sample16 / exam_math / biz_plan **diff=0** (무영향)
  - 페이지 수 회귀 부재 (모든 fixture BEFORE = AFTER)
- [x] 산출물 `output/poc/pr1026/{before,after}/` 규약 배치 (총 550 SVGs)
- [x] WASM 빌드 (Docker, **4.89MB**, PR #1034 와 동일 — 본 PR 코드 변경 작음) + rhwp-studio/public 동기화
- [x] **작업지시자 시각 판정 통과** (KTX 페이지번호 정합 유지 + 휴먼명조 narrow 정합 한컴 정답지 정합)
- [x] no-ff merge `3d9bf994` → devel push (`3d9bf994`)
- [x] PR #1026 close
- [x] 검토 문서 archives 이동 (v2), 임시 브랜치 (pr1026-test / pr1026-review) 정리

핵심: @HaimLee-4869 **1차 보류 PR 재제출 모범 사례**. 1차 검토 (5/20)
시 KTX 페이지번호 +10px 회귀 (`feedback_visual_judgment_authority`
작업지시자 "이게 틀린겁니다" 판정) 로 옵션 B 보류 → 작성자가 (1) base
rebase (2) **KTX 회귀 가드 영구화** `tests/issue_874_ktx_toc_page_number_right_align.rs`
(98 lines, max_x < 695.0 단언) (3) issue-267 KTX golden 보존 후
재제출. 본 환경 검증 결과 회귀 가드 통과 + svg_snapshot 8/8 보존 +
KTX 목차 페이지번호 BEFORE=AFTER 좌표 동일 → 1차 보류 사유 완전 정정.

**1차 검토 회귀 원인 재정정**: 1차 보고서 `mydocs/pr/archives/pr_1026_report.md`
의 "H2 분기 순서 변경" 추정이 잘못된 분석으로 판명. 진짜 원인은 1차
cherry-pick + UPDATE_GOLDEN 일괄 갱신 (issue-267 KTX golden 도 함께
변경) 또는 통째-파일 교체로 PR #1021 코드 유실 (본 PR 의 KTX 회귀
가드 코드 본문 명시: "stale 브랜치를 통째-파일 교체로 통합하면서 PR
#1021 코드가 유실되어 이 +10px 이탈이 재현된 사례"). 본 PR 재제출이
회귀 가드 영구화로 진짜 원인 차단.

`feedback_pr_supersede_chain` 권위 사례 — 1차 보류 → 회귀 가드 영구화
+ rebase → 재제출의 완전한 사이클. 작성자가 1차 검토 권위
(`feedback_visual_judgment_authority`) 를 받아들이고 회귀 가드로
정정하는 모범적 supersede 패턴.

`feedback_image_renderer_paths_separate` 정합 — native (`EmbeddedTextMeasurer`)
+ WASM (`WasmTextMeasurer::wasm_internals::measure_char_width_hwp`)
두 path 동시 fix (H1+H2 native + H3 WASM). 직전 PR #1021 (단일-run
RIGHT + leader) 패턴 일관.

`feedback_hancom_compat_specific_over_general` 정합 — U+2018/U+2019/
U+2027 narrow 분기 + `glyph_w >= em_size` 조건 (정상 DB 값 영향 0).
함초롬바탕/Pretendard/맑은 고딕 무영향.

본 PR 본질 (PR 본문 명시):
- 휴먼명조 U+2027 → 0.5em → 0.3em (도청 공직기강 page 1 "선제적‧적극적", "갑질‧비위" 가운뎃점 한컴 정합)
- HY중고딕/HY신명조/HY견명조 U+2018/U+2019 동일 효과
- WASM Canvas path 동기화로 rhwp-studio 시각 정합

별도 PR 권장 영역 (PR 본문 명시):
- U+00B7 ('·' Middle Dot) 본문 영역 시각 NG (Task #630 trade-off, context-aware 처리 후속)
- U+2014 / U+2016 등 다른 General Punctuation block
- HY family fallback 메트릭 보정 (별도 트랙)

보고서: `mydocs/pr/archives/pr_1026_review_v2.md`

---

### PR #1047 — fix: CharShape start_pos 를 UTF-16 stream offset 으로 해석 (외부 기여 @HaimLee-4869, closes #915, **PR #913 revert**)

- [x] PR 정보 + 사이클 점검 (@HaimLee-4869 5번째 기여 — PR #1020/#1021/#1026 시리즈 후속)
- [x] devel 동기화 (`c40a6a27`) — PR base = `ac6aeed4` (PR #1034 머지 후), origin/devel = `c40a6a27` (PR #1026 머지 후 docs)
- [x] **PR #913 (closes #884, @planet6897, commit `9f81351a`) 이력 확인** — visible char index 해석 도입 + "작업지시자 시각 판정 ★ 통과" 명시
- [x] 코드 본질 분석 — composer.rs split_by_char_shapes 의 start_pos lookup 을 `char_offsets.iter().position(|&off| off >= cs.start_pos)` 로 변경 (UTF-16 stream offset 해석) + fallback `find_active_char_shape_visible` → `find_active_char_shape` (line_stream_start 사용)
- [x] 한컴 2024 글자모양 패널 실측 4 케이스 권위 검토:
  - table-in-tbox p1 footer "충남중부권지사장": HY수평선B 16pt (PR #913 의 26pt 추정 오진, 굵은 글꼴 + 로고 배치 육안 착시)
  - **table-in-tbox p2 "충남중부권지사": 정상** (PR #913 시 1.33px = #915 결함) ⭐
  - KTX 목차 "Ⅰ": 휴먼명조 16pt (PR #913 시 18.7pt)
  - KTX 목차 "사업 개요": HY헤드라인M 15pt (PR #913 시 18.7pt)
- [x] 검토 문서 `pr_1047_review.md` → 작업지시자 옵션 A 승인 (PR #913 revert 수용 + 시각 판정)
- [x] cherry-pick `d84e7f52` (author @HaimLee-4869 보존, 충돌 없음)
- [x] 검증 (`feedback_push_full_test_required` 정합): cargo test --release --lib **1319 passed** + 통합 모두 passed (0 FAILED) + fmt --all --check clean
- [x] **회귀 가드 4 통과**:
  - `issue_884_charshape_diagnostic` **2/2 passed** (단언 반전, footer "충" HY수평선B 검증 — 한컴 실측 기반)
  - `issue_915_charshape_cell_font_size` (신규) **1/1 passed** (한글 음절 < 5px 부재 휴리스틱)
  - `issue_874_ktx_toc_page_number_right_align` (PR #1026) **1/1 passed** (KTX 페이지번호 양립)
  - `svg_snapshot` **8/8 passed** (issue-157/267/617/677 골든 4 갱신 + table_text/form_002/aift/render_deterministic 보존)
- [x] sweep 8 fixtures (BEFORE `c40a6a27` ↔ AFTER 277 SVGs): 53 diff (광범위 stream offset 정합) — table-in-tbox 2 + KTX 3 + hwp3-sample16 3 + hwp3-sample16-hwp5 2 + exam_kor 19 + exam_math 20 + aift 4 / biz_plan **diff=0** / 페이지 수 회귀 부재
- [x] **정량 입증**: table-in-tbox p2 한글 음절 `font-size="1.3333"` → `font-size="16"` (#915 본질 해소)
- [x] 산출물 `output/poc/pr1047/{before,after}/` 규약 배치 (총 554 SVGs)
- [x] WASM 빌드 (Docker, **4.89MB**) + rhwp-studio/public 동기화
- [x] **작업지시자 시각 판정 통과** (PR #913 ★ 통과 재고, 한컴 2024 패널 실측 4 케이스 권위 인정)
- [x] no-ff merge `8ba28089` → devel push (`8ba28089`)
- [x] PR #1047 close + 이슈 #915 close
- [x] 검토 문서 archives 이동, 임시 브랜치 (pr1047-test / pr1047-review) 정리

핵심: **PR #913 (closes #884) 명시적 revert** — `feedback_visual_judgment_authority`
모범 사례. PR #913 의 visible char index 해석이 "작업지시자 시각 판정
★ 통과" 로 머지됐으나, 한컴 2024 글자모양 패널 실측 4 케이스가 정량적
권위 (`reference_authoritative_hancom` — Windows 한컴 편집기 1차 권위)
로 stream offset 해석이 정답임을 입증. 시각 판정 ★ 통과 ≠ 절대 권위,
**측정 도구 우위**.

PR #913 의 오진 원인 3 가지:
1. **footer "26pt" 육안 착시** — HY수평선B 굵은 글꼴 + 로고 옆 배치로
   인한 시각 추정 오류 (실제 16pt, 한컴 패널 실측)
2. **path 불일치 미인지** — composer 만 해석 B 로 바꿔서 paragraph_layout
   / line_breaking 와 불일치 상태였음 (4 경로 중 1 경로만 변경)
3. **#915 결함 유발** — 인라인 제어자가 문단 *중간* 에 있는 문단에서
   start_pos 가 char_offsets gap 만큼 부풀려져 char_shape 통째 누락
   (table-in-tbox p2 "충남중부권지사" 1.33px)

`feedback_pr_supersede_chain` 권위 사례 — PR #913 (오진 정합) → PR
#1047 (실측 정정) 의 supersede 패턴. 두 다른 컨트리뷰터 (@planet6897
→ @HaimLee-4869) 간 해석 충돌이 한컴 패널 실측 권위로 해소.

`feedback_diagnosis_layer_attribution` — composer ↔ paragraph_layout/
line_breaking 의 path 불일치를 정확 식별. 본 PR 이 4 경로 단일화로
path 일관성 복원.

회귀 가드 영구화 (4 가드):
- issue_884 단언 반전 (한컴 실측 기반)
- issue_915 신규 (한글 음절 5px 휴리스틱)
- issue_874 양립 (KTX PR #1026)
- svg_snapshot 골든 4 갱신 + 무회귀

비범위 / 후속:
- PR #913 머지 (PR #884 closes) 의 cherry-pick 원본 영역 (mydocs) 정리 (별도 cleanup task 후보)

보고서: `mydocs/pr/archives/pr_1047_review.md`

---

## Task M100-919 — 글상자 한컴 UX 정합 (안 표 셀 + Esc escape stack, closes #919, 메인테이너)

- [x] Issue #919 (글상자 안쪽 클릭 진입 불가) + assignee @edwardkim + milestone v1.0.0 + `local/task919` 브랜치
- [x] Stage 0 사전 진단 — hit_test_native + RenderNodeType + studio onDblClick + 표 UX (`isTableBorderClick` / `enterTableObjectSelection`) 패턴 식별
- [x] 작업지시자 보조 정보 수집 (3 라운드):
  - "글상자가 먼저 선택됩니다" — 한컴 UX 와 다른 1차 클릭 가로채기
  - "글상자안에 이미지나 표가 존재하는 경우 현재 접근 방식으로는 선택할 수 없습니다 / 한컴에서는 글상자 BBox 테두리를 선택할 경우만 글상자가 선택되도록 하고 있습니다"
  - "rhwp의 표안쪽 셀 선택과 외부 경계선 선택, 그리고 내부 진입상태에서 esc 키를 누르면 표가 선택되는 UX 를 글상자에도 동일하게 적용"
- [x] 수행 계획서 v2 `mydocs/plans/task_m100_919.md` (표 UX 동일 적용) → 작업지시자 승인
- [x] Stage 1 정밀 진단 보고서 `mydocs/working/task_m100_919_stage1.md` — 9 hit_test 시나리오 + 4 코드 갭 정확 식별
- [x] 구현 계획서 `mydocs/plans/task_m100_919_impl.md` (5 단계) → 작업지시자 승인
- [x] **Stage 2.1 Native**: TextBoxBboxInfo 신규 + collect_runs Rectangle/Ellipse/Path 분기 + getShapeBBox API + hit_test 우선순위 정정 (hit_cell → clicked_cell → textbox_hit → hit_body) + 0.5 inline Shape 분기에서 글상자 fall-through (break)
- [x] **Stage 2.2 Studio**: `isShapeBorderClickByRef` (sec/ppi/ci, getShapeBBox + 5px tolerance) + `findShapeByOuterClick` + 클릭 흐름 정정 (외곽 경계선만 객체 선택, 내부 즉시 cursor 진입, 외곽 근처 객체 선택, picHit shape 분기 외곽만 객체 선택)
- [x] **Stage 2.3 Studio**: 글상자 객체 선택 상태에서 같은 글상자 내부 클릭 → 즉시 텍스트 편집 진입
- [x] 작업지시자 1차 시각 판정 통과 — 글상자 진입 동작 정합
- [x] **작업지시자 추가 지적**: "글상자 내에 위치한 표의 셀 hitTest까지 성공입니다. 여기서 esc 키를 누르면 표가 선택되어야 하는데 글상자가 선택됩니다"
- [x] **Stage 2.5 추가 정정 — Esc 우선순위**:
  - `input-handler-keyboard.ts`: nestingDepth >= 2 + isTextBox 면 표 객체 선택 우선
  - `cursor.ts enterTableObjectSelection`: cellPath.length >= 2 면 isTextBox 상태에서도 허용
  - `cursor.ts moveOutOfSelectedTable`: 글상자 안 표 → 외부 이동 시 isTextBox 유지 (escape stack 자연 전이)
- [x] 콘솔 에러 `renderTableObjectSelection 실패: controls[2]가 표가 아닙니다` 진단 → **Stage 2.5b**:
  - `cursor_nav.rs resolve_table_by_path`: path 중간 항목 Shape (글상자) 면 text_box.paragraphs traverse, 5 ShapeObject variants 지원
- [x] **Stage 2.4 회귀 가드** `tests/issue_919_textbox_hit_test.rs` 5/5 통과:
  - 글상자 안 텍스트 hit / 빈 영역 hit / 외부 hit (회귀) / getShapeBBox bbox / 글상자 안 표 셀 cellPath 두 항목
- [x] 검증 (`feedback_push_full_test_required` 정합): cargo test --release --lib **1319 passed** + 통합 모두 passed (issue_852 / 874 / 915 / 1008 / 919 모두) + fmt --all clean + TypeScript tsc 0 errors
- [x] WASM 빌드 (Docker, **4.90MB**, PR #1047 4.89MB + getShapeBBox API 추가) + rhwp-studio/public 동기화
- [x] **작업지시자 시각 판정 통과** (2 라운드):
  - 글상자 외곽 경계선 / 내부 클릭 / 안 표 셀 hit / 객체→편집 전환
  - Esc escape stack: 안 표 객체 선택 → 글상자 안 본문 → 글상자 객체 선택 → 본문 자연 전이
- [x] 최종 보고서 `mydocs/report/task_m100_919_report.md`
- [ ] no-ff merge + devel push + 이슈 #919 close
- [ ] 임시 브랜치 정리

핵심: **글상자 한컴 UX 정합** — 표 UX 패턴 (`isTableBorderClick` /
`enterTableObjectSelection` / Esc 처리) 을 글상자에 일반화하여 적용. 한컴
한글 편집기 동일 동작 (외곽 경계선 5px 안에만 객체 선택, 내부 클릭은
즉시 텍스트 편집 진입, Esc 는 가장 안쪽 컨테이너부터 escape stack 으로
전이). 7 코드 갭 (cursor_rect / wasm_api / mouse handler / border helper
/ keyboard / cursor / cursor_nav) 정확 식별 + 정정.

**작업지시자 시각 판정 권위 게이트** (`feedback_visual_judgment_authority`):
- 1차 (글상자 진입) 통과 후 작업지시자 추가 지적 (안 표 셀 → Esc) 받고 Stage 2.5 추가 정정
- 콘솔 에러 진단으로 resolve_table_by_path 의 path traverse 일반화 추가 정정
- 2차 (Esc escape stack) 통과로 본 task 본질 완료

**escape stack 한컴 UX 정합** — 글상자 안 표 셀 위치에서 Esc 연타:
1. 안 표 객체 선택 (표 핸들)
2. cursor → 글상자 안 본문 (isTextBox 유지)
3. 글상자 객체 선택 (글상자 핸들)
4. cursor → 본문

회귀 가드 5/5 영구화 — 다른 글상자 보유 HWP 문서 (`samples/table-in-tbox.hwp`
포함) 의 한컴 UX 정합 유지.

비범위 / 후속:
- 머리말/꼬리말 안 글상자 (본 task 본문만)
- 글상자 안 글상자 중첩 재귀 (본 PR 일부 지원: cellPath traversal)
- 도형 (직사각형/원) 의 hit_test 동일 UX (본 task 는 글상자 한정)

보고서: `mydocs/report/task_m100_919_report.md`

---

### PR #1036 — Task #1035: HWP3 vs HWP5 변환본 페이지 alignment fix (37.5% → 93.75%) (외부 기여 @jangster77, closes #1035)

- [x] PR 정보 + 사이클 점검 (@jangster77 26+번째 기여 — PR #1031/#1034 HWP3 시리즈 후속)
- [x] PR base = `a52859de` (PR #1031 머지 후, 5 commits behind origin/devel `bc5683ff` = Task #919 머지 후) — multi-merge PR head 라 cherry-pick 직접 머지 불가
- [x] **PR #1009 (closes #1007, close 이력) 분석** — sample16-hwp5 +1 over-split (64 → 65) 회귀 사유 + 본 PR 정정 시도 (aux_trigger 제거 + threshold 0.85 → 0.95)
- [x] 코드 본질 분석 — engine.rs +71/-1 (cross-paragraph vpos reset 감지, is_hwp3_variant 가드, 조건 4개) + typeset.rs +106/-5 (동일 로직 두 경로 정합) + pagination.rs +5 + rendering.rs +3/-1 (PaginationOpts.is_hwp3_variant) + tests/issue_1035_alignment.rs (회귀 가드)
- [x] 검토 문서 `pr_1036_review.md` → 작업지시자 옵션 A 승인 (본질 11 파일 cherry-pick squash 적용)
- [x] **Stage 1 squash commit** `eeeba245` (author Taesup Jang 보존, 11 파일 = 4 코드 + 1 test + 6 문서)
- [x] **Stage 2 CI 패턴 검증** (`feedback_push_full_test_required` 정합): cargo test --release --lib **1319 passed** + cargo test --test issue_1035_alignment **1/1 passed** + cargo test --tests passed + clippy + fmt --check clean
- [x] **Stage 3 광범위 sweep — 10 fixture BEFORE/AFTER 페이지 수 모두 일치** (회귀 부재):
  - hwp3-sample16: 64, hwp3-sample16-hwp5.hwp: 64 (의도된 alignment 변경), hwp3-sample16-hwp5.hwpx: 69
  - hwp3-sample: 16, hwp3-sample10-hwp5: 763, hwp3-sample11-hwp5: 151
  - exam_kor: 20, aift: 74, biz_plan: 6, KTX: 27 → **모든 fixture BEFORE = AFTER**
- [x] 산출물 `output/poc/pr1036/{before,after}/` 규약 배치 (총 2444 SVGs)
- [x] WASM 빌드 (Docker, **4.90MB**) + rhwp-studio/public 동기화
- [x] **작업지시자 시각 판정 통과** (alignment 24/64 → 60/64 93.75% 입증, 일반 fixture 회귀 부재)
- [x] no-ff merge `402e0ce6` → devel push (`402e0ce6`)
- [x] PR #1036 close + 이슈 #1035 close
- [x] 검토 문서 archives 이동 (`pr_1036_review.md` + `pr_1036_report.md`), 계획서 archives (`task_m100_1035*.md`)

핵심: **PR #1009 (close) → PR #1036 (narrow 정정)** supersede 패턴 모범
사례 (`feedback_pr_supersede_chain`). PR #1009 의 `aux_trigger` (empty
bridge 휴리스틱) + `high_threshold 0.85` 가 sample16-hwp5 에서 +1
over-split (64 → 65) 회귀 유발 → close. 본 PR 이 (1) aux_trigger 제거
(2) high_threshold 0.95 narrow 로 정정. 결과: HWP3 vs HWP5 변환본 페이지별
첫 paragraph alignment 24/64 (37.5%) → 60/64 (93.75%).

`feedback_hancom_compat_specific_over_general` 정합 — `is_hwp3_variant`
가드 (일반 HWP 무영향) + threshold 0.95 (보수적) + curr_first_vpos <
1500 HU — case-specific narrow. 자연 paginator break 영역 외 제외.

`feedback_diagnosis_layer_attribution` — PR #1009 회귀 원인 (aux_trigger
false positive + threshold 0.85 가 자연 break 침범) 정확 식별 + 정정.

`feedback_image_renderer_paths_separate` 정신 — engine.rs + typeset.rs
두 경로 동일 로직 정합 (paginator 경로 + typeset 경로).

회귀 가드 영구화: `tests/issue_1035_alignment.rs::hwp3_sample16_hwp5_page_count_64`
— sample16-hwp5 64 유지 단언.

본질 11 파일 squash 적용으로 PR base 차이 (5 commits behind) 우회 —
PR #1026/#1032/#1033/#1034/#1047/#919 와 자동 양립.

잔존 / 후속 (PR 본문 명시):
- 미정합 4 페이지 (p21 등) + p23 overflow 의 본질 = HWP5 변환본 paragraph
  height 가 HWP3 의 약 2배 (font/spacing metric 차이) — 별도 issue 등록 권고

보고서: `mydocs/pr/archives/pr_1036_review.md` + `pr_1036_report.md`

---

### PR #1040 — Task #1037: HWP5 변환본 ParaShape unit normalize + Dialog 한컴 정합 fix (외부 기여 @jangster77, closes #1037)

- [x] PR 정보 + 사이클 점검 (@jangster77 30번째 기여 — PR #1031/#1034/#1036 직후 HWP3 sample16 정합 시리즈 마무리)
- [x] PR base = `bbd38e85` (PR #1033 머지 후, multi-merge 상태) → origin/devel `402e0ce6` (PR #1036 머지 후 + #59efa47e docs) — 본질 3 코드 파일만 cherry-pick 가능
- [x] PR #1036 (Task #1035) 잔존 본질 분석 — alignment 60/64 달성 후 미정합 4 페이지 + p23 overflow 의 본질 = HWP5 변환본 paragraph height 2×
- [x] 코드 본질 분석 — parser/mod.rs +16 (is_hwp3_variant 직후 ParaShape /= 2 normalize) + style_resolver.rs +5/-4 (variant_div 4 → uniform 2) + formatting.rs +20/-1 (build_para_properties_json raw_ps 직접 + variant 분기)
- [x] 검토 문서 `pr_1040_review.md` → 작업지시자 옵션 A 승인
- [x] **Stage 1 squash commit** `fd124092` (author Taesup Jang 보존, 10 파일 = 3 코드 + 7 문서)
- [x] **Stage 2 CI 패턴 검증** (`feedback_push_full_test_required` 정합): cargo test --release --lib **1319 passed** (PR #1036 회귀 가드 issue_1035 양립) + cargo test --release --tests **FAILED 0** + clippy --release --lib -D warnings clean + fmt --all --check clean
- [x] **Stage 3 광범위 sweep — 10 fixture BEFORE/AFTER `diff -rq` = 0** (전체 1254 SVG 완전 동일, **rendering 무변동 PR 본문 명시 정확 입증**):
  - 10 fixtures (hwp3-sample16 / hwp3-sample16-hwp5.hwp/.hwpx / hwp3-sample / hwp3-sample10-hwp5 / hwp3-sample11-hwp5 / exam_kor / aift / biz_plan / KTX) 페이지 수 모두 BEFORE = AFTER
- [x] 산출물 `output/poc/pr1040/{before,after}/` 규약 배치 (총 2508 SVGs)
- [x] WASM 빌드 (Docker, **4.90MB**, PR #1036 동일 — 본 PR 본질이 dialog UI 만) + rhwp-studio/public 동기화
- [x] **작업지시자 시각 판정 통과** (Dialog 4 필드 한컴 정합: 왼쪽 40 / 오른쪽 10 / 내어쓰기 20 / 문단위 8.5, HWP3 + HWP5 변환본 모두 정합)
- [x] no-ff merge `c6fb7f26` → devel push (`c6fb7f26`)
- [x] PR #1040 close + 이슈 #1037 close
- [x] 검토 문서 archives 이동 (`pr_1040_review.md` + `pr_1040_report.md`), 계획서 archives (`task_m100_1037*.md`)

핵심: **HWP3 sample16 정합 시리즈 마무리** (@jangster77 PR
#1031/#1034/#1036/#1040 4 PR 시리즈). 본 PR 은 **parser 단계 normalize**
로 한컴 변환기 ParaShape 2× quirk 를 데이터 진입 시점에 일괄 보정 →
후속 모든 컴포넌트 (dialog / rendering / dump) 가 일관된 값 사용. Task
#1001 의 case-specific `variant_div=4` 보정 제거 + uniform `=2` 단순화.

**rendering 무변동 정량 입증** — 10 fixture BEFORE/AFTER `diff -rq` = 0.
본 PR 본질은 dialog UI 표시만 영향이고 SVG/페이지 출력은 동일. PR #1036
alignment 60/64 + 페이지 수 64 완전 보존.

**Dialog 4 필드 한컴 정합** — sample16 p452/p97 "(3) 원격지 재해복구센터
(DR: Disaster Recovery) 구축" paragraph 에서 한컴 정답지 vs rhwp HWP3
vs rhwp HWP5 변환본 4 필드 (왼쪽 40 / 오른쪽 10 / 내어쓰기 20 / 문단위
8.5) 완전 정합 (PR 본문 명시).

`feedback_pr_supersede_chain` 정합 — PR #1036 (Task #1035) 잔존 본질 →
PR #1040 (Task #1037) 후속 패턴. Task #1035 Stage 4 보고서 명시 "HWP5
변환본 paragraph height 2× 별도 issue 등록 권고" 가 본 PR 로 해결.

`feedback_hancom_compat_specific_over_general` 정합 — `is_hwp3_variant`
가드 (일반 HWP 무영향) + parser 단계 case-specific normalize.

`feedback_image_renderer_paths_separate` 정신 — parser 단계 단일 normalize
로 모든 후속 path 일괄 정합 (path 일관성 확보).

Stage 4 negative result (PR 본문 명시, 잔존 후속):
- p23 외곽선 overflow 별도 issue 등록 권고
- HWP5 변환본 page_break_before 100% 손실 별도 issue 등록 권고
- Dialog 회귀 가드 (`tests/issue_1037_dialog.rs`) 후속 PR 권고

보고서: `mydocs/pr/archives/pr_1040_review.md` + `pr_1040_report.md`

---

## Task M100-1052 — 글상자 안 각주 본문 누락 정정 — typeset.rs ShapeTextBox 각주 수집 추가 (closes #1052, 메인테이너)

- [x] 트러블슈팅 사전 검색 (`feedback_search_troubleshootings_first`) — `2010_01_06_footnote_line_spacing.md` 무관 확인
- [x] 기존 이슈 검색 — 동일 이슈 없음 (#483 각주 multi-paragraph / #696 글상자 표/문단 / #919 cellPath / #974 그림 / #1050 hwpx2hwp 저장 모두 별도)
- [x] Stage 0 사전 진단 — 결함 본질 식별:
  - HWPX section0.xml 확인: 글상자 안 footNote #1 + 본문 footNote #2
  - rhwp SVG: 글상자 안 각주 마크 "1)" 표시 + 페이지 하단 각주 영역 "1) 글상자 내부 각주" **누락**
  - 본문 각주 "2) 일반 문단내 각주" 정상 (회귀 부재 가능)
  - 한컴 PDF (`pdf-large/hwpx/footnote-tbox-01.pdf`) 정답지 확인
- [x] Issue #1052 등록 + assignee=@edwardkim + milestone v1.0.0 + branch `local/task1052`
- [x] 수행 계획서 `mydocs/plans/task_m100_1052.md` → 작업지시자 승인
- [x] 구현 계획서 `mydocs/plans/task_m100_1052_impl.md` → 작업지시자 승인 (typeset.rs:1278 직후 / 기본 fixture sweep)
- [x] **Stage 1 정밀 진단** (`mydocs/working/task_m100_1052_stage1.md`):
  - `rendering.rs:1548` 코드 경로 분기 확인 — TypesetEngine 이 main (default), engine.rs 는 `RHWP_USE_PAGINATOR=1` env opt-in fallback
  - Task #993 명시 ("레거시 Paginator는 분할 표 컷을 생산하지 않음 — TypesetEngine 컷 모델로 대체")
  - 본질 위치 확정 — typeset.rs:1278 직후
- [x] **Stage 2 구현** (`mydocs/working/task_m100_1052_stage2.md`):
  - `src/renderer/typeset.rs` +27 라인 (engine.rs:1376-1398 동등 미러링)
  - FootnoteSource::ShapeTextBox push + estimate_footnote_height + add_footnote_height
  - 정량 입증: HWPX/HWP SVG 텍스트 element 42 → 51 (+9), "1) 글상자 내부 각주" 페이지 하단 추가
- [x] **Stage 3 회귀 가드 + sweep + WASM** (`mydocs/working/task_m100_1052_stage3.md`):
  - 회귀 가드 `tests/issue_1052_footnote_in_textbox.rs` (88 라인, 4 tests, **4/4 passed**) — `svg_text_sequence` 헬퍼로 글자 단위 분리 SVG sub-string 단언
  - **광범위 sweep 9 fixtures, BEFORE/AFTER diff=2** (footnote-tbox-01 HWPX+HWP 만 변동 = 의도된 본질 정정, 나머지 7 fixture 회귀 부재)
  - CI 패턴: cargo test --release --lib **1319 passed** + --tests FAILED 0 + clippy + fmt clean (`feedback_push_full_test_required` 정합)
  - WASM Docker 빌드 (**4.90 MB**) + rhwp-studio/public 동기화
- [x] **작업지시자 시각 판정 통과** (HWPX + HWP 양쪽 페이지 하단 각주 영역 한컴 PDF 정합)
- [x] 최종 보고서 `mydocs/report/task_m100_1052_report.md`
- [x] no-ff merge `a0391ee8` → devel push (`a0391ee8`)
- [x] 이슈 #1052 close
- [x] 계획서 archives (`mydocs/plans/archives/task_m100_1052*.md`)

핵심: **Shape (글상자) 컨테이너 안 컨트롤 traverse 누락 시리즈
(#696/#919/#974) 의 footNote 영역 연장**. Task #993 이후 TypesetEngine
이 main paginator (default) 가 된 후 누락된 ShapeTextBox 각주 수집
보완. engine.rs (legacy, RHWP_USE_PAGINATOR=1 env opt-in) 는 이미
처리 (line 1376-1398) — `feedback_image_renderer_paths_separate`
정합 두 paginator 경로 동기화 누락 사례.

**본질 정정 영역 한정** — `get_footnote_paragraphs`
(picture_footnote.rs:1084-1104) + `layout_footnote_area` 의
`FootnoteSource::ShapeTextBox` 통일 처리는 이미 완비, **push 만
typeset.rs 에 추가하면 해결**. 코드 변경 +27 라인.

**정량 입증**:
- HWPX/HWP SVG 텍스트 element 42 → 51 (+9): "1) 글상자 내부 각주" 페이지 하단 추가
- 한컴 PDF 정답지 (`pdf-large/hwpx/footnote-tbox-01.pdf`) 완전 정합
- 9 fixture sweep diff=2 (footnote-tbox-01 만 변동, 회귀 부재)

`feedback_image_renderer_paths_separate` 정합 — engine.rs / typeset.rs
두 paginator 경로 동기화 누락 사례 정확 식별 + 정정.

`feedback_diagnosis_layer_attribution` 정합 — Stage 1 진단으로 본질
위치 (typeset.rs default 경로) 정확 식별. SVG export 자기 검증 +
한컴 PDF 정답지 + XML 직접 확인의 3 단계 진단.

`feedback_search_troubleshootings_first` 정합 — 트러블슈팅 사전 검색
(`footnote_line_spacing.md` 무관 확인) + 기존 이슈 검색 (#483/#696/#919/
#974/#1050 모두 본질 다름).

`feedback_assign_issue_before_work` 정합 — 이슈 등록 직후 assignee 지정.

비범위 / 후속:
- 머리말/꼬리말 안 각주 (본 sample 범위 외)
- 미주 (Endnote) 의 Shape 내부 처리 (별도 검토)
- table-in-tbox 안 표 셀 안 각주 (별도 가드 필요 시 후속)

보고서: `mydocs/report/task_m100_1052_report.md`

---

### PR #950 — HWPX fragment paste support (외부 기여 @dragonnite1221-lgtm, **close**)

- [x] PR 정보 + 사이클 점검 — PR #880 머지 (5/15 08:04 `6c42ee9b`) → 1시간 후 revert (`1579dc8f`) → 5/17 PR #950 follow-up 재제출
- [x] 작업지시자 5/18 사전 우려 (용어 "양식 부품" 기계적 + 프론트엔드 의존성 거버넌스) 파악
- [x] 검토 문서 `mydocs/pr/pr_950_review.md` 작성 → 작업지시자 "수정요청 전제 검토" 승인
- [x] 메인테이너 hands-on 테스트 준비 (`test-pr950` 임시 브랜치, devel 머지 충돌 없음, 네이티브 1377 passed + WASM Docker 빌드 + rhwp-studio TypeScript 빌드 통과)
- [x] **작업지시자 hands-on UX 결함 2건 발견** (메인테이너 첫 테스트 1~2분 만에):
  - **B1**: 빈 HWPX 문서 (`samples/hwpx/blank_hwpx.hwpx`) 에서 `after_para_idx 1 out of range` 오류 — frontend ↔ Rust semantic 불일치
  - **B2**: 확인 버튼 무동작 — `YangsikPartsDialog.onConfirm()` 이 `return false;` 명시적 무시. 사용자가 자연스럽게 사용할 수 있는 시나리오 아님
- [x] 작업지시자 결정 — **"이러한 UX 는 사용자가 자연스럽게 사용할 수 있는 시나리오가 아닙니다. 컨트리뷰터에게 문제를 제기하고 해결되면 다시 PR 요청해달라고 부탁. 해당 PR 은 클로즈."**
- [x] PR #950 close 코멘트 게시 (재제출 시 권고 사항 정리: B1+B2 정정 + M1 비공개 task 참조 제거 + M2 `cross_document_migrate.rs` 분리 + R1 `yangsik` 명명 일관화)
- [x] PR #950 close + 임시 테스트 브랜치 정리 + 검토/보고서 archives 이관 + 커밋 (`807e9209`)

핵심: PR #880 잔존 6 항목 중 코드 측은 대부분 성실 반영 (예시 fragment
추가, HWP5 비호환 guard, 카탈로그 보안 강화) 되었고 핵심 기능 (raw
HWPX fragment paste primitive) 설계 자체는 잘 되어 있음. 그러나 메인
테이너 첫 테스트에서 즉시 발견되는 end-to-end UX 결함 2건 — 작업
지시자가 PR #880 close 시 강조한 "end-to-end testable by maintainer"
게이트 본질적 미충족. 컨트리뷰터 self-validation (cargo test +
fragment_paste_integration 3 건 통과) 과 실제 사용자 시나리오 검증의
격차 — `feedback_visual_judgment_authority` 권위 사례. close 후
재제출 권고.

`feedback_pr_comment_tone` 정합 — 차분 + 사실 중심 톤, close 코멘트도
코드 측 설계 양호 + PR #880 잔존 항목 성실 반영 인정. 누적 기여자라
`feedback_first_pr_courtesy` 불해당.

보고서: `mydocs/pr/archives/pr_950_review.md` + `pr_950_report.md`

---

### PR #1039 — HWPX slash/backSlash 형태 enum 파싱 분리 (외부 기여 @planet6897, closes #1038)

- [x] PR 정보 + CI 전체 pass 확인 + 단일 본질 commit (`3d5c0ead`) + planet6897 = 누적 컨트리뷰터 (PR #221 OLE/Chart/EMF, #587 HWP 5.0 spec swap)
- [x] 책임 분리 설계 검증 — slash/backSlash `type` = 방향/형태 enum (NONE/CENTER/CENTER_BELOW/CENTER_ABOVE/기타) vs `<hh:diagonal>` = 선 종류·굵기·색. HWPX spec 1:1 정합
- [x] 신규 헬퍼 `parse_slash_shape_code` (enum → 3비트 방향 코드) + `set_diagonal_attr_bits` 3비트 정보 완전 보존 (기존 1비트 축소 해소)
- [x] 신규 단위 테스트 4건 확인 — enum 매핑 + 비트 설정 + #1038 회귀 가드 + 비회귀 보장 (slash NONE + diagonal SOLID 정상)
- [x] 검토 문서 `mydocs/pr/pr_1039_review.md` 작성 → 작업지시자 승인 요청
- [x] **시각 검증 보조 수행** — `samples/hwpx/aift.hwpx` 점검 결과 slash CENTER 0 건 (NONE 177), PR 본문이 가리키는 `samples/2. 인공지능(AI) ...` 파일은 컨트리뷰터 로컬 비공개 — 본 환경 `samples/` 전체 sweep 결과 #1038 본질 재현 샘플 없음 (`tac-img-02.hwpx` 1건은 slash CENTER + diagonal SOLID 보유 = 비회귀 샘플)
- [x] 메인테이너 hands-on (devel 기준) — `tac-img-02.hwpx` p5 대각선 rhwp-studio 정상 표시 확인 (비회귀 게이트)
- [x] **작업지시자 결정 — 시각 판정 면제** (정량 게이트 3 조건 동시 만족: 결정적 측정 "검정 대각선 3→0" + 회귀 가드 단위 테스트 동봉 + parser-only scope)
- [x] 최종 보고서 `mydocs/pr/pr_1039_report.md` → 작업지시자 머지 승인
- [x] cherry-pick `3d5c0ead` author (Jaeook Ryu) 보존 → `74177634`
- [x] 검증: `cargo fmt --check` ✅ / `cargo test --release --lib` 1323 passed 0 failed (이전 1308 + PR 신규 단위 테스트 4건, 회귀 0)
- [x] 검토/보고서 archives 이관 + 커밋 (`61e55da1`)
- [x] PR #1039 close + 이슈 #1038 명시 close (cherry-pick 은 GitHub 자동 close 미발동, `feedback_close_issue_verify_merged` 정합으로 `git branch --contains` 검증 후 close)

핵심: HWPX spec 의 두 요소 책임 분리 (방향/형태 enum vs 선 정의) 가
명료. 기존 `parse_border_line_type_code` 가 slash "CENTER" 를 선 종류
파서에 잘못 넘겨 `Solid(1)` 폴백시킨 사고 정정. 본 PR 이 **"정량 게이트
충족 시 시각 판정 면제 가능" 첫 권위 사례** — 결정적 측정 (before→
after 정량) + 회귀 가드 단위 테스트 동봉 + parser-only scope 3 조건
동시 만족 시 메인테이너 hands-on 게이트 면제 가능 패턴.

`feedback_hancom_compat_specific_over_general` 정합 — 책임 분리 = 구조
가드 (측정 의존 분기 없음). `feedback_small_batch_release_strategy`
정합 — 단일 commit/파일 (소스), 소형. 신규 룰 후보 (별도 정리 task)
"정량 게이트 충족 시 시각 판정 면제" 패턴.

보고서: `mydocs/pr/archives/pr_1039_review.md` + `pr_1039_report.md`

---

## Task M100-1050 — [hwpx2hwp] 각주 저장 한컴 호환 — HWPX → IR → HWP contract 완전 정합 (closes #1050, 메인테이너)

- [x] 트러블슈팅 사전 검색 — `hwpx2hwp-rule.md` + `task178_*` 정독 (`project_hwpx_to_hwp_adapter_limit` 메모리 룰)
- [x] Stage 0 사전 진단:
  - 한컴 열기 성공 + 각주 출력 비정상 (작업지시자 판정)
  - 자기 라운드트립: 글상자 안 각주 본문 누락 (rhwp 도 자기 정합 미달)
  - IR 단 footnote 보존됨 → 본질은 record level
  - CTRL_FOOTNOTE size=6 (정답지 20) 14 byte 누락 + LIST_HEADER size=6 (정답지 16) 10 byte 누락
- [x] Issue #1050 + assignee=@edwardkim + milestone v1.0.0 + branch `local/task1050` (이슈는 기존, assignee 추가)
- [x] 수행 계획서 + 구현 계획서 → 작업지시자 옵션 **B + C 채택** (Footnote + Endnote 동시 + 한컴 14 byte 의미 규명 우선)
- [x] **Stage 1 contract 규명** (`hwplib` 권위, `/home/edward/vsworks/shwp/hwplib`):
  - CTRL_FOOTNOTE 16 byte payload (number UInt4 + before/after WChar + numberShape UInt4 + instanceId UInt4)
  - LIST_HEADER for Footnote 16 byte (paraCount + property + 8 byte zero padding)
  - 다중 fixture (footnote-tbox-01 + footnote-01 + 2010-01-06) raw byte 확인
- [x] **Stage 2 model + parser + serializer 정정**:
  - src/model/footnote.rs Footnote/Endnote +4 필드
  - src/parser/control.rs CTRL_FOOTNOTE 점진 파싱
  - src/serializer/control.rs 16 byte payload + LIST_HEADER 16 byte 형식
  - src/parser/hwpx/section.rs::parse_ctrl_footnote/endnote suffixChar/instId 매핑
- [x] **Stage 3 회귀 가드 (초기 4개) + sweep + WASM**
- [x] **Stage 3.5 작업지시자 1차 시각 판정 결함 대응**:
  - HWPX `<hp:footNotePr>` 의 `<hp:noteLine length="-1">` `<hp:autoNumFormat suffixChar=")">` 파싱 누락 식별
  - parse_sec_pr_children 에 footNotePr/endNotePr 분기 + parse_note_pr_children 헬퍼 신규
- [x] **Stage 4a FootnoteShape 추가 필드 파싱** (noteLine type/width/color + noteSpacing betweenNotes/belowLine/aboveLine + numbering newNum)
- [x] **Stage 4-pivot 작업지시자 핵심 통찰** ("HWP 코드를 참조해서 처리"):
  - 임시 진단 테스트 (HWP IR vs HWPX IR 전수 비교) → 본질 식별
  - HWP body_text.rs:326 의 AUTO_NUMBER (0x12) 처리: char_offsets.push(pos) + text.push(' ') + pos += 16
  - HWPX parser 의 autoNum 분기 → text_parts.push("\u{0012}") + visual_text 조립 단계에 HWP 동등 처리
  - serializer/body_text.rs::serialize_para_text 에 AutoNumber placeholder 검출 분기 (text==' ' + offset==prev_end + next>=+8 + controls 에 AUTO_NUMBER/FOOTNOTE)
  - 정량 입증: HWP 정답지 PARA_TEXT byte **3 케이스 완전 동일** (ORACLE = HWP-roundtrip = HWPX→HWP-v5)
- [x] **작업지시자 시각 판정 통과** (2 sample):
  - footnote-tbox-01.hwpx: "이제 잘 열리고 정확하게 한컴편집기에서 조판됩니다."
  - footnote-01.hwpx: "성공입니다."
- [x] 회귀 가드 7/7 통과 (Stage 6.1 보강 — footnote-01.hwpx + footnote 안 char_offsets contract 단언)
- [x] CI 패턴 (`feedback_push_full_test_required`): 1319 lib + 통합 + clippy + fmt clean
- [x] **광범위 sweep 10 fixtures diff=4** (HWPX 출처 2 fixture 만 변동 = 의도된 본질 정정, HWP 출처 8 fixture 회귀 부재)
- [x] WASM Docker 빌드 (**4.91 MB**) + rhwp-studio/public 동기화
- [x] 최종 보고서 `mydocs/report/task_m100_1050_report.md`
- [x] no-ff merge → devel push (`e0afbbfd`, rebase 후)
- [x] 이슈 #1050 close
- [x] 계획서 archives (`mydocs/plans/archives/task_m100_1050*.md`)

핵심 — **작업지시자 통찰의 결정성**: Stage 1~3 의 부분 정정 (FootnoteShape +
CTRL_FOOTNOTE size + LIST_HEADER size) 만으로는 한컴 조판 미흡 (각주 영역
인식 못 함). 작업지시자 통찰 "HWP → IR → HWP 라운드트립이 한컴 호환을
이미 보장하므로 HWPX → IR 만 HWP → IR 과 동일한 IR 을 생성하면 자동으로
한컴 호환" 이 **광범위 한컴 호환 영역의 정확한 접근**.

Stage 4-pivot 의 HWP IR vs HWPX IR 전수 비교 → AUTO_NUMBER inline 컨트롤의
PARA_TEXT contract 누락 (char_offsets [0, 8, 9, ...] 첫 jump 8) 식별 →
HWPX parser 의 autoNum → \u{0012} + visual_text 조립의 HWP 동등 처리 +
serializer 의 placeholder 검출 분기 정정.

`project_hwpx_to_hwp_adapter_limit` 정합 + **단순 어댑터 한계 돌파** —
Task #178 의 광범위 한컴 거부 영역의 일부 (각주 영역) 를 HWP IR contract
oracle 방식으로 정합. `hwpx2hwp-rule.md` 의 contract unit 다수 추가:
- CTRL_FOOTNOTE 16 byte payload
- footnote LIST_HEADER 16 byte 형식
- FootnoteShape 16 필드
- **AUTO_NUMBER inline 컨트롤 PARA_TEXT contract** (8 cu + placeholder + jump 8)
- HWPX `<hp:footNotePr>` ↔ HWP FOOTNOTE_SHAPE 매핑

`feedback_visual_judgment_authority` 모범 사례 — Stage 0 작업지시자 판정
("각주 출력 비정상") → Stage 3.5 1차 정정 ("개선되지 않았습니다") →
Stage 4-pivot 작업지시자 통찰 + 정정 → 통과. 다중 라운드 시각 판정 게이트.

후속:
- FOOTNOTE_SHAPE tuple=2 (endnote shape) payload hash 1건 잔여 — endnote noteLine 매핑 추가 별도 issue 권고
- 미주 (Endnote) 보유 sample 의 한컴 시각 검증 (본 task 에서 Footnote 와 동일 contract 적용)
- 광범위 HWPX → HWP 호환 (Task #178 영역) — 본 task 범위 외

보고서: `mydocs/report/task_m100_1050_report.md`

---

## Task M100-1058 — 글상자 LIST_HEADER 13 byte 한컴 contract 정합 (Task #1050 후속, closes #1058, 메인테이너)

- [x] Issue #1058 등록 + assignee=@edwardkim + milestone v1.0.0 + branch `local/task1058`
- [x] Stage 0 사전 진단 — 정답지 vs rhwp 저장본 hwp5-inventory-diff:
  - 잔여 차이 식별: **글상자 LIST_HEADER size 33 → 20 (13 byte 누락)** + PARA_HEADER #0 (1 byte hash) + FOOTNOTE_SHAPE tuple=2 (endnote, task 외)
  - 글상자 LIST_HEADER raw byte 비교 → 마지막 13 byte 모두 zero (정답지) vs 부재 (rhwp)
- [x] 수행 계획서 + 구현 계획서 → 작업지시자 옵션 **B + B 채택** (13 byte 의미 완전 규명 + 광범위 sweep)
- [x] **Stage 1 hwplib 권위 + 구조 완전 규명** (`hwplib::ForTextBox::listHeader`):
  - TextBox LIST_HEADER 33 byte 구조 = paraCount(4) + property(4) + margin × 4 (8) + textWidth(4) + zero(8) + editableAtFormMode(4) + fieldName flag(1)
  - 한컴 default: zero(8) + 0(editable) + 0(fieldName flag) → 13 byte 모두 zero
- [x] **Stage 2 serializer 정정** (`src/serializer/control.rs::serialize_text_box_if_present`, +10 라인):
  - raw_list_header_extra 비어 있을 때 (HWPX 출처) 한컴 default 13 byte zero 적용
  - HWP 출처는 기존 raw 보존 → 회귀 부재
- [x] **Stage 2 정량 입증** — TextBox LIST_HEADER size 20→33 + raw byte byte-by-byte 정답지 동일
- [x] **Stage 3 회귀 가드 + 광범위 sweep + WASM**:
  - tests/issue_1058_textbox_list_header.rs (4 tests, 4/4 passed) — size=33 + HWP roundtrip + Task #1050 양립 + byte contract
  - 광범위 sweep 14 fixtures (변환본 4종 hwp3-sample-hwp5 + footnote/table 글상자 + 일반 4종) 1143 SVG **diff=0** (직접 export 회귀 부재)
  - CI 패턴: cargo test --release --lib **1323 passed** + tests FAILED 0 + clippy + fmt clean
  - WASM Docker 빌드 **4.91 MB** + rhwp-studio/public 동기화
- [x] Task #1050 회귀 가드 7/7 양립 입증
- [x] **작업지시자 한컴 한글 2020 시각 판정 통과** — 신규 각주 추가 시 본문 다단계 목록 "1.1.1.1.1.1." **부여 안 됨**
- [x] 최종 보고서 `mydocs/report/task_m100_1058_report.md`
- [x] no-ff merge `f2a7561e` → devel push (`f2a7561e`)
- [x] 이슈 #1058 close
- [x] 계획서 archives (`mydocs/plans/archives/task_m100_1058*.md`)

핵심: **작업지시자 통찰 (Task #1050 Stage 4-pivot "HWP IR oracle 방식")
동일 적용** — "정답지와 저장 버전과 차이를 통해 추론" 으로 본질 영역 (글상자
LIST_HEADER 13 byte 누락) 정확 식별. `hwplib::ForTextBox::listHeader` 권위로
13 byte 의 정확한 의미 (zero 8 + editableAtFormMode 4 + fieldName flag 1)
완전 규명.

**한컴편집기 동작 메커니즘 해석** — 글상자 LIST_HEADER size=33 보다 짧으면
한컴이 글상자 안 paragraph 를 본문 list 의 일부로 잘못 해석 → 신규 paragraph
추가 시 본문 다단계 목록 번호 (1.1.1.1.1.1.) 자동 부여. 33 byte 정합 후
정상 처리.

`feedback_diagnosis_layer_attribution` 정합 — record-level diff 로 본질
위치 정확 식별. `feedback_hancom_compat_specific_over_general` — case-specific
한컴 contract default 보존 (raw_list_header_extra 우선 + HWPX default fallback).
`feedback_self_verification_not_hancom` — rhwp 자기 정합 통과 ≠ 한컴 호환,
작업지시자 한컴 직접 검증 게이트 통과.

`project_hwpx_to_hwp_adapter_limit` 정합 + 단순 어댑터 한계 돌파의 **2번째
contract unit 추가** (Task #1050 의 footnote contract + 본 task 의 TextBox
LIST_HEADER contract). hwpx2hwp-rule.md 의 contract 축적 원칙 정합.

후속:
- 본문 PARA_HEADER #0 break_type/num_char_shapes 차이 (별도 영역)
- FOOTNOTE_SHAPE tuple=2 (endnote) 1건 잔여
- 미주 (Endnote) 의 한컴 시각 검증
- 광범위 HWPX → HWP 호환 (Task #178 영역)

보고서: `mydocs/report/task_m100_1058_report.md`
