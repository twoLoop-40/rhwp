# 오늘 할일 - 2026년 6월 22일

## PR #1460 - 글자처럼 해제 그림 재흐름 보정 (jangster77)

- [x] PR 생성
  - base/head: `devel` ← `task_m100_1459`
  - 상태: open, draft 아님
  - 작성 시점 merge 상태: `CLEAN`
  - 관련 이슈: `Closes #1459`
- [x] PR 변경 검토
  - 비-TAC `TopAndBottom` 그림이 먼저 흐름을 예약하고 남은 TAC 그림이 한컴처럼 재배치되도록 보정
  - 비-TAC 그림/도형/표를 renderer composer inline control slot과 cursor stop에서 제외
  - TAC 해제 속성 변경 직후 텍스트 없는 그림 문단의 `LineSeg` 재구성
  - PR CI에서 재현된 #1198 nested cell paste split offset 회귀 보정
- [x] 로컬 검증 완료
  - `cargo fmt`
  - `cargo fmt --check`
  - `git diff --check`
  - `cargo test --profile release-test tac_toggle_true_to_false_restores_empty_picture_para_line_seg -- --nocapture`
  - `cargo test --profile release-test test_identify_inline_controls_table -- --nocapture`
  - `cargo test --profile release-test --test issue_1459_topbottom_picture_reflow -- --nocapture`
  - `cargo test --profile release-test --test issue_1452_saved_caret -- --nocapture`
  - `cargo test --profile release-test --test issue_1139_inline_picture_duplicate issue_1293_equation_control_is_not_always_treat_as_char -- --nocapture`
  - `cargo test --profile release-test --test issue_1139_inline_picture_duplicate issue_1139_endnote -- --nocapture`
  - `cargo test --profile release-test --lib`
  - `cargo test --profile release-test --tests`
  - `cargo build --release`
  - `cargo test --release --lib`
  - `cargo test --doc`
  - `cd rhwp-studio && npx tsc --noEmit`
  - `cd rhwp-studio && npm test`
  - `wasm-pack build --target web --out-dir pkg`
  - `cargo clippy --all-targets -- -D warnings`
- [x] PR 리뷰 문서 준비
  - `mydocs/pr/archives/pr_1460_review.md`
  - `mydocs/pr/archives/pr_1460_review_impl.md`
- [x] GitHub Actions 현재 PR head 기준 통과 확인
  - head: `e80e9f534b0d1c179eeff1bb5447b359e0a3c50a`
  - Build & Test: pass
  - Canvas visual diff: pass
  - CodeQL: pass
- [x] review 문서와 오늘할일 문서 commit + PR head remote push
- [ ] GitHub Actions 최신 head 기준 통과 확인
- [ ] merge
- [ ] 이슈 #1459 close 확인 및 후속 코멘트

## PR #1475 - 스타일 적용과 표 캡션/모양복사 보정 (jangster77)

- [x] PR 생성
  - base/head: `devel` ← `task_m100_1470`
  - 상태: open, draft 아님
  - 문서 작성 시점 merge 상태: `MERGEABLE`, `BLOCKED`
  - 관련 이슈: `Closes #1470`
- [x] PR 변경 검토
  - 스타일 왼쪽 여백 15 입력값이 30으로 바뀌는 배율 회귀 보정
  - 스타일 줄간격 적용 뒤 문단 `LineSeg`/페이지네이션 재흐름 보정
  - 스타일 적용/편집 시 직접 글자 서식 보존
  - 표 만들기 상세 옵션의 너비/높이/글자처럼 취급 전달 보정
  - TAC 표 캡션 동반 렌더 중복 방지
  - 표 캡션 `표 1`, `표 2`, `표 3` prefix와 삭제 뒤 번호 재정렬 보정
  - PR #1446 모양 붙여넣기 명시 경로 회귀 복구 및 회귀 테스트 추가
- [x] 로컬 검증 완료
  - `cargo fmt && cargo test --release issue_1470_table_caption --lib`
  - `cargo test --release issue_1470 --lib`
  - `cargo build --release`
  - `cargo test --release --lib`
  - `cargo test --profile release-test --tests`
  - `cargo fmt --check`
  - `cargo clippy --all-targets -- -D warnings`
  - `wasm-pack build --target web --out-dir pkg`
  - `cd rhwp-studio && npx tsc --noEmit`
  - `cd rhwp-studio && npm test`
  - `git diff --check`
  - 사용자 직접 수동 검증 완료
- [x] PR 리뷰 문서 준비
  - `mydocs/pr/archives/pr_1475_review.md`
  - `mydocs/pr/archives/pr_1475_review_impl.md`
- [x] review 문서와 오늘할일 문서 commit + PR head remote push
- [ ] GitHub Actions 최신 head 기준 통과 확인
- [ ] merge
- [ ] 이슈 #1470 close 확인 및 후속 코멘트

## PR #1477 - 플랫폼별 메뉴 단축키 표시 보정 (jangster77)

- [x] PR 생성
  - URL: https://github.com/edwardkim/rhwp/pull/1477
  - base/head: `devel` ← `jangster77:task_m100_1476`
  - 상태: open, draft 아님
  - 문서 작성 시점 merge 상태: `MERGEABLE`
  - 관련 이슈: `Closes #1476`
- [x] PR 변경 검토
  - 상단 메뉴 단축키 표시를 `CommandRegistry.shortcutLabel` 기준으로 동기화
  - macOS 표시를 긴 `Command`/`Option` 텍스트 대신 `⌘`, `⌥`, `⇧` Apple 기호로 보정
  - Windows/Linux 계열에서는 기존 `Ctrl`/`Alt` 표시 유지
  - 보기/서식 메뉴의 누락 `shortcutLabel` 보강
  - `pr_review_workflow.md`에 merge 후 원격 PR head 브랜치 삭제 절차 추가
- [x] 프론트 로컬 검증 완료
  - `cd rhwp-studio && node --test tests/menu-shortcut-labels.test.ts` (6 passed)
  - `cd rhwp-studio && npx tsc --noEmit`
  - `cd rhwp-studio && npm test` (116 passed)
  - `git diff --check`
  - 변경 범위가 `rhwp-studio` 프론트와 문서에 한정되어 cargo/clippy 전체 검증은 수행하지 않음
- [x] IAB / Pages / 작업지시자 시각 검증 완료
  - IAB: 상단 메뉴 8개, 단축키 항목 63개, `Ctrl`/`Alt`/`Command`/`Option` 텍스트 잔여 0개 확인
  - Pages: https://jangster77.github.io/rhwp/
  - Pages run: https://github.com/jangster77/rhwp/actions/runs/27946414455
  - 작업지시자 Windows 시각 검증 완료
- [x] PR 본문 준비
  - `mydocs/report/task_m100_1476_pr_body.md`
- [x] PR 리뷰 문서 준비
  - `mydocs/pr/archives/pr_1477_review.md`
  - `mydocs/pr/archives/pr_1477_review_impl.md`
- [x] review 문서와 오늘할일 문서 commit + PR head remote push
- [ ] GitHub Actions 최신 head 기준 통과 확인
- [ ] merge
- [ ] 이슈 #1476 close 확인 및 후속 코멘트

## M100 — v1.0.0 편집 기반

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #1471 | Chrome 다운로드 filename 인터셉터 전역 부작용 수정 | PR 준비 | PR #1480 생성, self-merge review 문서 추가 |

## Task #1481 - 표 열 추가 후 일반 표 높이 축소 회귀 (jangster77)

- [x] 이슈 등록
  - URL: https://github.com/edwardkim/rhwp/issues/1481
  - 제목: 표 열 추가 후 일반 표 높이가 납작해지는 회귀
  - assignee: `jangster77`
- [x] 열린 PR/중복 이슈 확인
  - open PR 중 직접 충돌 후보 없음
  - 같은 증상의 open issue 없음
- [x] 작업 브랜치 준비
  - base: `upstream/devel`
  - branch: `task_m100_1481`
- [x] 사전 원인 후보 정리
  - 일반 `createTable()`은 셀 height `282HU`, 표 `common.height` `1282HU * 행수`로 생성
  - 열 추가/삭제 후 `Table::update_ctrl_dimensions()`가 `get_row_heights()` 합으로 `common.height`를 다시 써 표 높이가 축소될 수 있음
  - `784e0063` 이후 `self.common.height` 동기화로 화면 즉시 반영 가능성이 커짐
  - PR #1446 Stage 19의 common width/height 복원 패턴을 구조 편집 경로에 응용 가능
- [x] 계획 문서 준비
  - `mydocs/plans/task_m100_1481.md`
  - `mydocs/plans/task_m100_1481_impl.md`
- [x] 작업지시자 승인 후 Stage 1 구현 시작
  - `mydocs/working/task_m100_1481_stage1.md`
- [x] Stage 1 구현
  - 열 추가: `Table::insert_column()`에서 표 외곽 height 보존
  - 칸 지우기: `Table::delete_column()`에서 표 외곽 height 보존
  - 회귀 테스트: `issue_1481_insert_column_keeps_create_table_height`
  - 회귀 테스트: `issue_1481_delete_column_keeps_create_table_height`
- [x] 한컴 도움말 기준 줄/칸 추가·지우기 대표 기능 보강
  - `Alt+Insert`: `줄/칸 추가하기` 대화상자
  - `Alt+Delete`: `줄/칸 지우기` 대화상자
  - 추가 방향 4종과 줄/칸 수 1~63 반복 적용
  - 삭제 대상 `줄`/`칸` 선택 적용
- [x] Stage 1 로컬 검증
  - `cargo test --release issue_1481 --lib` (2 passed)
  - `cargo test --release table --lib` (254 passed, 3 ignored)
  - `cd rhwp-studio && npx tsc --noEmit`
  - `cd rhwp-studio && node --test tests/shortcut-map.test.ts tests/menu-shortcut-labels.test.ts` (12 passed)
  - `cd rhwp-studio && npm test` (118 passed)
- [x] `cargo fmt --check`
- [x] `wasm-pack build --target web --out-dir pkg`
- [x] `git diff --check`
- [x] 작업지시자 확인 후 커밋
  - `554dce58 task 1481: 표 줄칸 편집 회귀 보정`
- [x] Stage 2 구현 시작
  - `mydocs/working/task_m100_1481_stage2.md`
- [x] 한컴 메뉴 표기/단축키 복원
  - 상단 표 메뉴를 `줄/칸 추가하기(I)... Alt+Insert`, `줄/칸 지우기(E)... Alt+Delete` 대표 항목으로 정리
  - 상단 표 메뉴의 개별 `위쪽에 줄 추가하기`, `칸 지우기` 항목 제거
  - 표 셀 우클릭 컨텍스트 메뉴도 `줄/칸 추가하기(I)...`, `줄/칸 지우기(E)...` 대표 항목으로 정리
  - 단축키 표시는 #1477 기준에 따라 macOS `⌥Insert`/`⌥Delete`, Windows/Linux `Alt+Insert`/`Alt+Delete`
  - macOS `Option+Insert`가 `Help`/`code=Insert`로 들어오는 이벤트 변형 매핑 추가
- [x] Stage 1 누락 회귀 보정
  - `위쪽에 줄 추가하기`/줄 삭제 후 일반 표 외곽 height가 셀 저장 height 합으로 납작해지는 경로 보정
  - 회귀 테스트: `issue_1481_insert_row_keeps_create_table_display_height`
  - 회귀 테스트: `issue_1481_delete_row_keeps_create_table_display_height`
- [x] Stage 2 로컬 검증
  - `cargo fmt --check`
  - `cargo test --release issue_1481 --lib` (4 passed)
  - `cd rhwp-studio && npx tsc --noEmit`
  - `cd rhwp-studio && node --test tests/navigation-keymap.test.ts tests/menu-shortcut-labels.test.ts tests/shortcut-map.test.ts` (30 passed)
  - `cd rhwp-studio && npm test` (120 passed)
  - `wasm-pack build --target web --out-dir pkg`
  - IAB: 상단 표 메뉴/표 셀 우클릭 메뉴 한컴식 대표 항목 및 플랫폼별 단축키 표시 확인
  - `git diff --check`
- [x] Stage 2 작업지시자 확인 후 커밋
  - 커밋: `be3702d6 task 1481: 줄칸 메뉴와 행 높이 회귀 보정`
- [x] Stage 3 구현 시작
  - `mydocs/working/task_m100_1481_stage3.md`
- [x] macOS `Option+Insert` 입력 이벤트 진단
  - 작업지시자 확인: `Opt+Insert`는 동작하지 않음
  - 작업지시자 확인: `Opt+Enter`는 처음부터 줄/칸 추가 대화상자를 열지만, 이는 `Opt+Insert` 해결로 보지 않음
  - IAB 실제 `Opt+Insert`: 페이지에는 `AltLeft` keydown/keyup만 도달, `Insert`/`Help`/`Enter` 이벤트 없음
  - IAB 합성 `Alt+Insert`: `key=Insert`, `code=Insert`, `altKey=true`로 도달하고 줄/칸 추가 대화상자 정상 표시
  - 결론: Stage 2 앱 매핑은 정상이며, 현재 macOS/IAB 물리 입력은 브라우저 페이지까지 `Insert` 이벤트를 전달하지 않음
- [x] Stage 3 후속 방향 결정
  - macOS 줄/칸 추가 단축키를 `Option+Enter`로 변경
  - 작업지시자 Windows 확인 결과 `Alt+Insert`도 동작하지 않아 Windows/Linux 줄/칸 추가 단축키도 `Alt+Enter`로 변경
- [x] Stage 3 구현
  - 플랫폼 공통 `Alt/Option+Enter` → `table:insert-row-col`
  - 메뉴/팔레트 표시: macOS `⌥Enter`, Windows/Linux `Alt+Enter`
  - 줄/칸 추가·지우기 대화상자 닫힘 후 편집 textarea 포커스 복원
  - `Option+Enter → Esc → Option+Enter`, `Option+Delete → Esc → Option+Delete` 반복 입력 회귀 보정
- [x] Stage 3 로컬 검증
  - `cd rhwp-studio && node --test tests/shortcut-map.test.ts tests/navigation-keymap.test.ts tests/menu-shortcut-labels.test.ts` (30 passed)
  - `cd rhwp-studio && npx tsc --noEmit`
  - `cd rhwp-studio && npm test` (120 passed)
- [x] Stage 3 Pages 배포
  - URL: https://jangster77.github.io/rhwp/
  - run: https://github.com/jangster77/rhwp/actions/runs/27957003127
  - ref: `task_m100_1481`
  - 결과: Build/Deploy success
- [x] Stage 3 작업지시자 확인 후 커밋
  - `d3ce087f task 1481: macOS 줄칸 단축키 보정`
  - `1a706395 task 1481: 줄칸 추가 단축키 통일`
- [x] PR 생성
  - URL: https://github.com/edwardkim/rhwp/pull/1482
  - base/head: `devel` ← `task_m100_1481`
  - 문서 갱신 시점 참고 상태: open, draft
  - 관련 이슈: `Closes #1481`
- [x] 최신 `upstream/devel` 기준 rebase
  - base: `af1b78eb`
  - 오늘할일 문서 충돌은 #1471/#1480 M100 기록과 #1481 기록을 모두 보존하는 방식으로 해결
- [x] Stage 4 하단선 resize 회귀 보정
  - `mydocs/working/task_m100_1481_stage4.md`
  - 표 생성 직후 맨 아랫줄 resize 시 표시 height가 셀 저장 height 합으로 붕괴하는 경로 보정
  - 회귀 테스트: `issue_1481_resize_bottom_row_keeps_create_table_display_height`
- [x] Stage 4 로컬 검증
  - `cargo test --release issue_1481_resize --lib` (1 passed)
  - `cargo test --release issue_1481 --lib` (5 passed)
  - `cargo fmt --check`
  - `wasm-pack build --target web --out-dir pkg`
  - `git diff --check`
- [x] Stage 4 commit
  - rebase 후 커밋: `ee80a390 task 1481: 표 resize 높이 회귀 보정`
- [x] Stage 5/6 표 생성 직후 탈출 회귀 보정
  - `mydocs/working/task_m100_1481_stage6.md`
  - 자리차지 표 앞 조판부호 위치 `Enter`는 표 위가 아니라 표 아래 빈 문단으로 이동
  - 마지막 셀 `Tab`은 한컴처럼 아래쪽 새 줄 자동 추가 후 새 줄 첫 셀로 이동
  - 일반 빈 문단과 `blank2010.hwp` 템플릿 첫 문단에서 표 생성 시 표 위 생성 경로 빈 줄 제거
  - 첫 셀 위/왼쪽 이동 시 표 밖 첫 조판부호 위치로 탈출 가능하도록 회귀 테스트 추가
  - clippy `needless_return` 보정: `src/document_core/queries/cursor_nav.rs`
- [x] PR head 히스토리 재작성
  - 기존 PR review 문서 전용 커밋 제거
  - 현재 로컬 브랜치 기준 이전 PR review 문서 커밋는 포함되지 않음
  - 원격 `upstream/task_m100_1481`은 최종 push 시 `--force-with-lease`로 갱신 예정
- [x] 전체 로컬 검증 재수행
  - `cargo build --release`: 통과
  - `cargo test --release --lib`: 통과 (1923 passed, 6 ignored)
  - `cargo test --profile release-test --tests`: 통과
  - `cargo fmt --check`: 통과
  - `git diff --check`: 통과
  - `cargo test --test svg_snapshot`: 통과 (8 passed)
  - `cargo clippy --all-targets -- -D warnings`: 통과
  - `cargo test --doc`: 통과 (0 passed, 1 ignored)
  - `cd rhwp-studio && npx tsc --noEmit`: 통과
  - `cd rhwp-studio && npm test`: 통과 (124 passed)
  - `wasm-pack build --target web --out-dir pkg`: 통과
- [x] PR 리뷰 문서 재작성
  - `mydocs/pr/archives/pr_1482_review.md`
  - `mydocs/pr/archives/pr_1482_review_impl.md`
- [ ] review 문서와 오늘할일 문서 포함 최종 commit
- [ ] 기존 PR #1482 head remote push
- [ ] PR 본문 최신 검증 결과로 갱신
- [ ] GitHub Actions 최신 head 기준 통과 확인
- [ ] merge
- [ ] 이슈 #1481 close 확인 및 후속 코멘트
