# 오늘 할일 - 2026년 5월 13일

## M100 — v1.0.0 조판 엔진 (PR 처리 — 5/13 사이클)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #819 (Ref #260)** | feat — Ctrl+Backspace + Cmd+Backspace | **완료 (옵션 B)** | @oksure. devel 흡수 + 충돌. 신규 가치 2건 직접 구현. 시각 판정 ★. merge `e8cb19cb`. |
| **PR #820 (Ref #616)** | fix — SVG 폰트 폴백 세리프 인식 | **완료 (cherry-pick 2 commits)** | @oksure. CI 전 통과. 시각 판정 통과. merge `00aefd93`. |
| **PR #821 (Ref #260)** | feat — Ctrl+Delete 단어 전방 삭제 | **완료 (옵션 B)** | @oksure. devel 흡수 + 충돌. 시각 판정 ★. merge `b07e5c48`. |
| **PR #822** | feat — Ctrl+ㄷ 한글 IME 매핑 | **완료 (1줄 추가)** | @oksure. devel 흡수. 동작 통과. merge `8bd85777`. |
| **PR #828** | feat — 모양 복사/붙이기 | **close (옵션 C)** | @oksure. 한컴 정합 미충족 (선택 대화상자 필요). 후속 PR 가이드 안내. |
| **PR #829 (closes #585)** | fix — 중고딕 font-weight 500 | **완료 (cherry-pick 3 commits)** | @oksure. CI 전 통과. 시각 판정 ★. merge `e71e5c57`. |
| **PR #830 (closes #307)** | feat — 이미지 드래그 앤 드롭 삽입 | **완료 (cherry-pick 2 commits)** | @oksure. CI 전 통과. 동작 판정 ★ (macOS). merge `c5a357d9`. |
| **PR #832 (closes #824/#825/#826)** | fix — sample11.hwp 머리말 3건 | **완료 (GitHub diff cherry-pick)** | @jangster77. fork tree 문제로 GitHub diff 방식. 시각 판정 ★. merge `6f87893f`. |
| **PR #834 (closes #833)** | feat — 다른 이름으로 저장 + cancel fallback | **완료 (GitHub diff cherry-pick)** | @jangster77. 동작 판정 ★. merge `0aedfbb6`. |
| **PR #835 (closes #831)** | fix — 이미지 회전/대칭 + BBox 회전 | **완료 (cherry-pick + 메인테이너 보강)** | @oksure + 메인테이너 BBox 보강. 동작 판정 ★. merge `3aac78c7`. |
| **PR #840** | feat — GlyphRun layer variant contract | **완료 (cherry-pick squash)** | @seo-rii. additive 변경. 시각 판정 ★. merge `249e5653`. |
| **PR #843 (closes part #842)** | fix — shortcut.hwp 정합성 3건 | **완료 (GitHub diff cherry-pick)** | @planet6897. SVG before/after + PDF 권위 비교. 시각 판정 ★. merge `f35902b0`. |
| **PR #845** | feat — SwiftUI native read bindings | **완료 (cherry-pick, base main→devel 보정)** | @ubermensch1218 (rhwp 첫 PR). Swift 4/4 통과. ios/devel 동기화 + 아이폰 빌드 성공. 예제 + README 문서화. merge `03a3f0d0`. |

## 작업 메모

### 5/13 사이클 요약
- **처리 PR 13건** (머지 12 + close 1)
- **컨트리뷰터 5명**: @oksure (7건), @jangster77 (2건), @seo-rii (1건), @planet6897 (1건), @ubermensch1218 (1건, 첫 PR)
- **신규 컨트리뷰터 1명**: @ubermensch1218 (강정석, Swift binding)
- **메모리 동기화**: mydocs/manual/memory 덤프 39파일 → 내 메모리 일괄 동기화 (12→51항목)
- **어휘 피드백 적용**: "본질/정합 영역" 등 기계적 반복 표현 억제

### 시각 판정 게이트 정립
- PR #820에서 시각 판정 없이 머지한 프로세스 위반 발견 → 이후 모든 PR에서 머지 전 작업지시자 시각/동작 판정 게이트 준수
- SVG before/after 비교 (PR #843) + 웹 에디터 동작 테스트 + PDF 권위 자료 대비 패턴 정립

### ios/devel 동기화
- devel 492 커밋 → ios/devel 머지 (충돌 3건 해결: Cargo.toml + search_query.rs + orders)
- 아이폰(iPhone 12 Pro) 빌드 + 설치 성공
- PR #845 Swift 예제 + README 문서화 추가
