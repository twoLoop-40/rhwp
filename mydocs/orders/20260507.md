# 오늘 할일 - 2026년 5월 7일

## M100 — v1.0.0 조판 엔진 (PR 처리 — v0.7.10 후 사이클 계속)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **PR #620 Task #618** | Picture flip/rotation 누락 회귀 정정 — Task #519 정정 재적용 (closes #618) | **완료 (핀셋 cherry-pick 머지 + push, PR #620/Issue #618 close + 옵션 B 후속 권유)** | 외부 컨트리뷰터 @planet6897 (Jaeuk Ryu) / Jaeook Ryu (commit author). v0.7.10 후 네 번째 처리 PR (5/7 첫 처리). PR mergeable=MERGEABLE 표시지만 PR base 22 commits 뒤 (5/6 등록 후 본 사이클 #578/#629/#611 처리분 누적), 본질 cherry-pick (`7ac87770` 4 파일 +10/-4) 충돌 0 (auto-merge layout.rs / paragraph_layout.rs 깨끗 통과). **본질 결함 (회귀 origin)**: Task #519 fix commit (`7ead89d`) 와 묶음 머지 (`a7e43f9`, "Task #517/#518/#519/#520/#521/#523/#528") 가 본 환경 (edwardkim/rhwp) devel 에 부재 — 본 환경 직접 검증 (`git merge-base --is-ancestor`) 으로 100% 정합 확정. **회귀 origin 메커니즘**: 컨트리뷰터 fork 의 묶음 머지 `a7e43f9` 가 PR #527 (CLOSED, "Layout 리팩터링 Phase 0~2 + 옛한글 PUA + Square wrap fixes") 으로 본 환경에 올라왔으나 close 처리됨 → 묶음 머지가 본 환경 devel 에 도달 못 함. 이후 개별 task 중 #521 (PR #564) + #528 (PR #592) 만 cherry-pick 으로 적용됐고 **#517 / #518 / #519 / #520 / #523 누락 상태**. **본질 정정**: 4 파일 6 ImageNode 생성 지점에 `transform: extract_shape_transform(&pic.shape_attr)` 재적용 — `layout.rs:2851` (TAC Picture / col_node) + `picture_footnote.rs:117,328` (layout_picture / layout_floating_picture) + `paragraph_layout.rs:1834,2108,2218` (TAC 인라인 3 사이트) + `table_cell_content.rs:644` (셀 내부 Picture, 명시적 `ShapeTransform::default()` → `extract_shape_transform` 교체). **`extract_shape_transform` 헬퍼는 `utils.rs:109` 에 그대로 살아있음** (devel 에서 `shape_layout.rs:530` 1곳에서만 사용 중) → **신규 단위 변환 코드 0줄** (`feedback_rule_not_heuristic` 정합). **케이스별 명시 가드** — 4 파일 6 ImageNode 생성 지점만 명시 정정, 다른 영역 무영향 (`feedback_hancom_compat_specific_over_general` 정합). 검증: cargo test --lib --release **1140 passed** (회귀 0) / svg_snapshot 6/6 / issue_546 1 / issue_554 12 / clippy 0 / build --release / Docker WASM **4,590,537 bytes** (PR #611 baseline 4,590,307 +230 — 4 파일 transform 호출 추가 정합) / rhwp-studio npm run build TypeScript 타입 체크 통과 + dist 빌드 (`rhwp_bg-DaB-SIXg.wasm` 4,590,537 정합). **광범위 페이지네이션 회귀 sweep**: `samples/` 폴더 전체 **167 fixture (161 hwp + 6 hwpx, PR #611 의 3개 권위 샘플 포함) / 1,687 페이지 / 페이지 수 차이 0**. **정량 측정 (PR 본문 100% 재현)**: `samples/exam_eng.hwp` page 4 BEFORE 0 → AFTER 1 `<g transform=>` 래퍼 (transform 값 `translate(1602.426666666667,0) scale(-1,1) translate(0,2217.373333333333) scale(1,-1)` PR 본문 명세 **정확 일치**), byte 차이 +107 bytes. page 1/2/3/5/6/7/8 byte-identical (회귀 0). **메인테이너 시각 판정 ★ SVG + web 양쪽 통과** — 작업지시자 평가: "svg 는 시각 판정 통과!" + "웹 에디터도 시각 판정 통과입니다." 권위 영역: page 4 — Q28 박스 종이-말림(curl) 데코레이션 그림 (`bin_id=2`, `flip=(h=true,v=true)`, `rot=0`) 박스 좌상단 정상 위치 출력. cherry-pick author Jaeook Ryu 보존 (`95b228e`, committer edward). devel merge `c80d227` + push 완료. PR #620 close + 한글 댓글 등록 + Issue #618 수동 close (closes #618 키워드는 cherry-pick merge 로 자동 처리 안 됨, 안내 댓글 등록). **옵션 B 후속 권유 댓글 등록** — 묶음 머지 `a7e43f9` 의 다른 task (#517 / #518 / #523) 누락 가능성 + Task #520 (PR #627 별도 영역) 표 명시 + 컨트리뷰터에게 후속 PR 권유 + 본 환경 직접 점검 옵션 분리 안내. 처리 보고서: `mydocs/pr/archives/pr_620_report.md`. 검토 보고서: `mydocs/pr/archives/pr_620_review.md`. **`feedback_close_issue_verify_merged` 메모리 권위 영역 강화** — Task #519 close 시 정정 commit 의 devel 머지 검증 미수행 → 동일 결함 재발 (Issue #618) 의 권위 케이스로, 본 PR 의 정확한 진단 + 재적용 패턴 우수. **회귀 origin 정확 진단 (`git merge-base --is-ancestor` 본 환경 직접 재현) + `extract_shape_transform` 헬퍼 재사용 (신규 변환 코드 0줄) + 케이스별 명시 가드 + 회귀 위험 영역 좁힘 + 시각 판정 SVG/web 양쪽 일관 정합 + 옵션 B 후속 권유 결합 패턴 모두 정합.** |
| **PR #642 Task #598** | rhwp-studio 본문 각주 마커 이동 및 삭제 (closes #598) | **완료 (합본 cherry-pick 머지 + push, PR #642/Issue #598 close)** | 활발한 컨트리뷰터 @postmelee (Taegyu Lee, meleeisdeveloping@gmail.com) — 13 PR 누적 (MERGED 5 + CLOSED 7 + 본 PR), 4/16~5/6 약 3주 사이클. v0.7.10 후 다섯 번째 처리 PR (5/7 두 번째). PR mergeable=CONFLICTING 표시지만 본 환경 직접 검증 결과 **mydocs/orders/20260506.md add/add 충돌만 발생**, src 영역 충돌 0. **본질 결함 (Issue #598 권위 영역)**: 크롬 확장 사용자 별점 리뷰 요청 — rhwp-studio editor 의 본문 각주 마커 hit test 미구현 + 커서 이동 시 마커 건너뜀 + Delete/Backspace 분기 부재 + 삭제 API 부재 + 확인창 부재. 한컴 정합 UX 5 영역 (hit test → 각주 영역 커서 이동 / 인라인 cursor unit / Delete-Backspace 양방향 동일 확인창 / 번호 재계산 / Undo). **본질 정정 (5 commits 합본 squash, 16 파일 +959/-34)**: (1) `src/wasm_api.rs` (+48) WASM API 신규 — `hitTestBodyFootnoteMarker` / `getFootnoteAtCursor` / `deleteFootnote` (+ native 영역 동일 함수), (2) `src/document_core/commands/footnote_ops.rs` (+176) `delete_footnote` 핵심 로직 — 본문 footnote control 제거 + ctrl_data_records 동기화 + char_offsets/char_count 보정 + 번호 재계산 + reflow/pagination/cache 무효화, (3) `src/document_core/queries/cursor_rect.rs` (+145/-4) 본문 각주 마커 hit test + `marker_pos` 왼쪽/오른쪽 caret 인라인 cursor unit, (4) `src/model/paragraph.rs` (+37/-11) `delete_text_at()` UTF-16 길이 보정 + `insert_text_at()` inline control 앞 삽입 조건 정정, (5) rhwp-studio TypeScript 4 파일 (+135/-2) — `wasm-bridge.ts` bridge 메서드 + `types.ts` 타입 신규 + `input-handler-mouse.ts` 마커 클릭 시 각주 편집 모드 진입 + `input-handler-text.ts` Delete/Fn+Delete 마커 앞 + Backspace 마커 뒤 분기 + 동일 `showConfirm("각주 삭제", "각주를 삭제하시겠습니까?")` 호출 + `SnapshotCommand` Undo 통합, (6) 보조 — `composer.rs` / `paragraph_layout.rs` (-8 LOC, 각주 영역 정합 보강) + `event.rs` 신규 footnote delete event + `helpers.rs` 보강. **회귀 차단 가드**: `tests/issue_598_footnote_marker_nav.rs` (+194, 4 통합 테스트) + `rhwp-studio/e2e/footnote-delete-confirm.test.mjs` (+186, Puppeteer e2e). 검증: cargo test --lib --release **1141 passed** (회귀 0) / **issue_598_footnote_marker_nav 4/4 passed** / svg_snapshot 6/6 / clippy 0 / build --release / `rhwp-studio npm run build` TypeScript 타입 체크 통과 + dist 빌드 (`index-CKsYNtwg.js` 691,386 / `rhwp_bg-DN7QfwxB.wasm` 4,587,318) / Docker WASM **4,587,318 bytes** (PR #620 baseline 4,590,537 **-3,219 bytes** — 각주 영역 정합 보강 + LLVM 최적화 합산 정합). **광범위 페이지네이션 회귀 sweep**: 167 fixture / 1,687 페이지 / 차이 0. **메인테이너 시각 판정 ★ 4 권위 영역 모두 통과 (web 환경 + `samples/footnote-01.hwp` 권위 샘플)** — 작업지시자 평가: "각주 hitTest 판정 성공 / 각주 캐럿 이동 판정 성공 / 각주 앞에서 del 키 판정 성공 / 각주 뒤에서 backspace 키 판정 성공". 권위 샘플: `samples/footnote-01.hwp` (32,768 bytes, 본 환경 git tracked 영구 보존 영역). cherry-pick author postmelee 보존 (`17434e9`, committer edward). devel merge `30afaae` + push 완료. PR #642 close + 한글 댓글 등록 + Issue #598 수동 close (closes #598 키워드는 cherry-pick merge 로 자동 처리 안 됨, 안내 댓글 등록). 후속 영역 (셀/글상자 내부 각주) 별도 후속 task 후보 명시. 처리 보고서: `mydocs/pr/archives/pr_642_report.md`. 검토 보고서: `mydocs/pr/archives/pr_642_review.md`. **`feedback_assign_issue_before_work` 메모리 권위 영역 강화** — Issue #598 의 작업지시자 직접 assignee 지정 (@postmelee) 으로 외부 컨트리뷰터의 일차 방어선 정합. **`reference_authoritative_hancom` 정합** — 한컴 권위 영역 인지 명시 (PR 본문) + 작업지시자 직접 시각 판정 영역 정합. **DTP 엔진 (`project_dtp_identity`) web editor 영역 정합 강화** — v1.0.0 진입 직전 핵심 사용성 영역 (크롬 확장 사용자 별점 리뷰 요청) 본질 정합. **WASM API 신규 정합 (3 메서드 + native 영역 동일 함수) + HWP IR 표준 직접 사용 (footnote control / ctrl_data_records / char_offsets) + 단일 컴포넌트 확인 다이얼로그 + SnapshotCommand Undo 통합 + 회귀 차단 가드 (4 통합 테스트 + Puppeteer e2e + 권위 샘플 영구 보존) + 활발한 컨트리뷰터 5 commits 후속 보강 통합 패턴 모두 정합.** |
| **PR #601 Task #594** | 복수 제목행 반복 시 2행 이상 출력 정정 (closes #594) | **완료 (옵션 A-2 합본 cherry-pick 머지 + push, PR #601/Issue #594 close + Issue #652 신규 등록)** | 활발한 컨트리뷰터 @oksure (Hyunwoo Park, oksure@gmail.com) — 5번째 사이클 PR (PR #581/#582/#583/#600 + 본 PR). v0.7.10 후 여섯 번째 처리 PR (5/7 세 번째). PR mergeable=MERGEABLE / BEHIND (PR base 134 commits 뒤). **본질 정정 (`table_partial.rs` +22/-12, 2 commits squash)**: `render_rows.push(0)` 단일 행 하드코딩 → `is_header` 셀 동적 수집 (`header_rows`) + 분할 페이지 반복 렌더링. 4 분기 가드 (`is_continuation` + `repeat_header` + `start_row > 0` + `r < start_row`) 로 회귀 위험 좁힘. `seen` boolean vec O(1) 멤버십 (Copilot review #4 응답). **Copilot review 4 코멘트 자체 검토 응답 정합**: #1 header_rows scope (`r < start_row` 가드 정합) + #2 pagination height (`MeasuredTable.header_row_flags` 후속 영역) + #3 regression test (메인테이너 영역 권유) + #4 O(N) 최적화 (`seen` boolean 적용). 검증: cargo test --lib --release **1141 passed** (회귀 0) / clippy 0 / build --release / Docker WASM **4,588,023 bytes** (PR #642 baseline 4,587,318 +705 — header_rows Vec allocation 정합) / `rhwp-studio npm run build` 통과 (`index-BywcUMYq.js` 691,386 / `rhwp_bg-BAk_YtfR.wasm` 4,588,023). **광범위 페이지네이션 회귀 sweep**: 167 fixture / 1,687 페이지 / 차이 0. **정량 측정 (회귀 0 입증)**: PR 본문 명시 권위 샘플 — `synam-001.hwp` 35 페이지 / `aift.hwp` 77 페이지 모두 byte-identical (회귀 0 정합 입증). **시각 발현 영역 정밀 분석 (`examples/inspect_pr601.rs` 신규 진단 스크립트)**: aift.hwp 의 모든 표 sweep 결과 — is_header 부재 85개 + 단일 제목행 3개 (47 페이지 영역의 pi=579/581/584 모두 단일 제목행) + **다중 제목행 표 2개** (s2 pi=147 7×4 + s2 pi=745 9×14) — 한 페이지에 완전히 들어감, **분할 미발현**. → 본 환경 fixture 의 다중 제목행 + 분할 표 발현 영역 부재. **옵션 A-2 진행 정합** — 회귀 0 + 결정적 검증 + 본질 정합성 입증으로 머지, 시각 발현 영역은 후속 task 영역 (Copilot review #2/#3 + Issue #652) 분리. cherry-pick author Hyunwoo Park 보존 (`0059557`, committer edward). devel merge `6d9849a` + push 완료. PR #601 close + 한글 댓글 등록 + Issue #594 수동 close. 처리 보고서: `mydocs/pr/archives/pr_601_report.md`. 검토 보고서: `mydocs/pr/archives/pr_601_review.md`. **PR 검토 영역에서 작업지시자 시각 발견 — 47 페이지 (page_num=41) 의 pi=578/579/581 vpos 부풉 + 표 오버랩 조판 붕괴 현상** → **신규 Issue #652 등록** (aift.hwp/aift.hwpx 표 조판 영역 결함). 한컴 2010/2020 PDF 권위 정답지 = **74 페이지** / 본 환경 HWPX = **74 페이지 (한컴 정합) ✅** / 본 환경 HWP5 = 77 페이지 (+3 부풉) → 결함 영역 좁힘 (HWP5 영역의 vpos 누적 결함). 단위 해석 자체는 정합 (HU → mm → px 변환 정확). 권위 자료 영역 도입: `samples/hwpx/aift.hwpx` git tracked + `samples/hwpx/aift-2020.pdf` (작업지시자 PDF 재작성 대기, 1 page 1 본문 영역). **옵션 A-2 + 후속 결함 영역 분리 + 권위 자료 영역 도입 + 활발한 컨트리뷰터 자체 검토 응답 정합 + DTP 엔진 영역 강화 패턴 모두 정합.** |
| **Issue #658** | rhwp-studio 마우스 드래그 텍스트 선택 하이라이트가 페이지 밖으로 튀고 버벅임 | **검증 완료 (최종 승인 대기)** | `local/task658` 브랜치. 원인: 줄 경계 offset에서 selection rect 시작/끝 cursor hit bias 미분리 + `SelectionRenderer` div 전량 삭제/재생성. 정정: `CursorBias::{Leading,Trailing}` 기반 native rect 정합화 + 셀 경로 방어 + `SelectionRenderer` div pool 재사용 + 드래그 중 `updateCaretDuringDrag()` 경량 경로. 검증: `issue_658_text_selection_rects` 2/2, `cargo test --lib --release` 1141 passed / 0 failed / 2 ignored, `rhwp-studio npm run build` 통과, `inspect_658_selection` 관찰 대상 모두 `overflow_count=0`, 웹 계측 maxRight 956.6px <= pageWidth 1028.0px + selection div pool 재사용 확인. 작업지시자 시각 판정: "드래그가 텍스트를 넘어가는 문제는 해결". 후속 별도 이슈 #661 등록: 드래그 중 커서/스크롤 위치 튐. |
| **Issue #661 (신규)** | rhwp-studio 텍스트 드래그 선택 중 커서와 스크롤 위치가 튀는 현상 | **OPEN (후속 task)** | #658 직접 테스트 중 발견된 별도 증상. selection rect overflow와 분리. 의심 영역: `input-handler-mouse.ts` rAF 내부 원본 `MouseEvent` 재해석, `updateCaretDuringDrag()`의 `scrollCaretIntoView()` 호출, `CursorState.updateRect()` pageIndex mismatch 폴백, Rust `getCursorRect*`의 page hint 없는 후보 페이지 순회. 재현: `samples/exam_social.hwp`, web 120%, 2/4쪽 구역 2/2 하단 좌측 박스 드래그. |
| **Issue #652 (신규)** | aift.hwp/aift.hwpx 표 조판 영역 결함 의심 — HWP5 영역 vpos 누적 결함 (+3 페이지 부풉) | **OPEN (M100, 후속 task)** | PR #601 검토 영역에서 작업지시자 발견. 한컴 2010/2020 PDF 권위 정답지 74 페이지 vs 본 환경 HWP5 77 페이지 (+3 부풉). HWPX 영역 (74 페이지) 은 한컴 정합 ✅, **HWP5 영역만 좁은 결함**. 47 페이지 (page_num=41) 의 pi=578/579/581 vpos 부풉 + 표 오버랩 조판 붕괴 현상 (작업지시자 시각 발견). 단위 해석 자체는 정합 (HU → mm → px 변환 정확) — 결함은 HWP5 의 vpos / paragraph 누적 y / 표 위 빈 공간 처리 영역. 권위 자료: `samples/hwpx/aift.hwpx` (74 페이지 한컴 정합 변환본) + `samples/hwpx/aift-2020.pdf` (한컴 2010/2020 PDF 정답지, 재작성 대기). 마일스톤 M100. assignee 미지정 (외부 기여 환영). |
| **PR #659 (#653)** | ir-diff 표 속성 / 컨트롤 식별 비교 추가 (closes #653) | **완료 (3 commits 보존 cherry-pick 머지 + push, PR #659/Issue #653 close)** | 활발한 컨트리뷰터 @oksure (Hyunwoo Park) — 6번째 사이클 PR. v0.7.10 후 일곱 번째 처리 PR (5/7 네 번째). PR mergeable=MERGEABLE / BEHIND 5 commits. **메인테이너 → 컨트리뷰터 선구현 패턴**: 메인테이너 5/5 직접 등록 (Issue #653, HWPX 시리얼라이제이션 영역 작업 중) → 외부 컨트리뷰터 5/7 같은 일자에 선구현. **본질 영역**: `src/main.rs` ir-diff 명령 (+140/-3) — `control_tag` (Control 22 variant 매핑) + `diff_table` (page_break/repeat_header/cell_spacing/border_fill_id/outer_margin/CommonObjAttr) + `diff_common_obj` (treat_as_char/text_wrap/size/v_offset/h_offset/vert_rel/horz_rel) + `LINE_SEG` 8 필드 확장 (vpos/th/bl/ls/cs 신규). **Copilot review 4 코멘트 자체 검토 응답 정합** (마지막 commit `eb97d9b`): border_fill → border_fill_id / voff/hoff → v_offset/h_offset / if-let → single match / help text 정정. 검증: cargo test --lib **1141 passed** (회귀 0) / clippy --bin rhwp + --lib 통과 / build --release / 진단 도구 영역 (코어 IR/렌더/직렬화 무영향). **결정적 게이트웨이 검증** (작업지시자 지정 — `samples/hwpx/aift.hwpx` page 1 ir-diff): BEFORE 차이 255 건 → AFTER 259 건 (+4 신규 식별) — `ctrl[0] type: A=cold vs B=secd` (HWPX SectionDef 정합 안 됨, Issue #653 본질 정확 재현) + `ctrl[1/2] type` 신규 검출. 전체 sweep (aift.hwpx vs aift.hwp): 650 → 707 건 (+57 신규) — `tbl page_break: A=CellBreak vs B=RowBreak` 다수 (Issue #652 본질 #1 재현) + `tbl 속성 차이 48 건` + `pic 속성 차이 4 건` + `ctrl type 차이 4 건`. **메인테이너 시각 판정 ★ 통과** — 작업지시자 평가: "시각 판정 통과입니다.". cherry-pick author Hyunwoo Park 보존 (`cf1296b`+`65817f2`+`ed13aa2`). devel merge `0d8b0fe` + push 완료. PR #659 close + 한글 댓글 등록 + Issue #653 수동 close. 후속 권유: `cells` / `is_header` 비교 영역 (PR #601 다중 제목행 분할 영역과 연관, 별도 PR 환영). 처리 보고서: `mydocs/pr/archives/pr_659_report.md`. 검토 보고서: `mydocs/pr/archives/pr_659_review.md`. **메인테이너 → 컨트리뷰터 선구현 패턴 (5/5 등록 → 5/7 흡수, 메인테이너 사이클 단축 + 공동체 비전 정합) + Copilot 자체 검토 응답 정합 + 결정적 게이트웨이 BEFORE/AFTER 정량 검증 + Issue #652 본질 영역 추가 검출 + 활발한 컨트리뷰터 6번째 PR 흡수 패턴 모두 정합.** |
| **PR #602 (#449)** | rhwpDev 디버깅 툴킷 — showAllIds / search / findNearest (closes #449) | **완료 (2 commits 보존 cherry-pick 머지 + push, PR #602 close + Issue #449 reopen, 게이트웨이 검증 결함 두 건 → 후속 PR 권유)** | 활발한 컨트리뷰터 @oksure (Hyunwoo Park) — 7번째 사이클 PR. v0.7.10 후 여덟 번째 처리 PR (5/7 다섯 번째). PR mergeable=MERGEABLE / BEHIND 148 commits. **본질 영역**: `rhwp-studio/src/core/rhwp-dev.ts` (+111 신규) + `rhwp-studio/src/main.ts` (+3, `initRhwpDev(wasm)` 호출) — `window.rhwpDev` 전역 객체 (showAllIds/search/findNearest/help). **Copilot review 7 코멘트 자체 검토 응답 정합 100%** (마지막 commit `284abc7`). 검증: cargo test --lib **1141 passed** / cherry-pick 충돌 0 / **Docker WASM 재빌드 v0.7.10** (본 환경 v0.6.0 → v0.7.10, `renderPageToCanvasFiltered` 부재 결함 동시 해소) / rhwp-studio dev 서버 (vite v8.0.10) 7700 포트. **메인테이너 게이트웨이 검증** (작업지시자 권위 자료 `samples/aift.hwp` web 환경 시각 판정): `rhwpDev.search("사업계획")` 정합 ✅ + `rhwpDev.showAllIds(0)` 결함 ⚠️ — **본질 결함 두 건 식별** (작업지시자 직접 지적). cherry-pick author Hyunwoo Park 보존. devel merge `9e97072` + push. **PR #602 close 유지** + **Issue #449 reopen**. **컨트리뷰터에게 메인테이너 판정 게이트웨이 방식 + 결함 두 건 + 후속 PR 권유 댓글** ([#issuecomment-4395273056](https://github.com/edwardkim/rhwp/pull/602#issuecomment-4395273056)). **결함 #1 (paragraph 식별 부정확)**: `getPageTextLayout` 의 `runs` 가 본문 + 표/글상자 내부 paragraph 평탄화 → `secIdx`/`paraIdx` 키 충돌 (본문 paragraph 0 "  * 사업계획서..." 가 아닌 셀 내부 paragraph "※ " 표시) — Issue #449 본질 (DOM 깊은 곳) 영역 충돌. **결함 #2 (다중 매치 미처리)**: `wasm.searchText` 단일 매치 — Issue #449 본질 (AI 환각 Fallback) 영역 충돌. 처리 보고서: `mydocs/pr/archives/pr_602_report.md`. 검토 보고서: `mydocs/pr/archives/pr_602_review.md`. **메인테이너 판정 게이트웨이 방식의 권위 사례** — 결정적 검증만으로는 본질 결함 식별 부족, 권위 자료 시각 판정에서 결함 두 건 식별 + 컨트리뷰터에게 게이트웨이 방식 + 자체 검증 패턴 안내. `feedback_no_pr_accumulation` + `feedback_per_task_pr_branch` + `feedback_visual_judgment_authority` + `feedback_assign_issue_before_work` 모두 정합. |
| **PR #609 Task #604** | Document IR 표준 정합화 (HWP3/HWP5/HWPX 3개 포맷 동일 페이지 일치, closes #604) | **완료 (옵션 A 11 commits 단계별 cherry-pick 머지 + push, PR #609/Issue #604 close)** | 컨트리뷰터 @jangster77 (Taesup Jang, tsjang@gmail.com) — PR #589 직후 동일 컨트리뷰터의 후속 영역. v0.7.10 후 처리 PR (5/7). PR mergeable=MERGEABLE / BEHIND (PR base 9 commits 뒤). **본질 영역**: PR #589 머지 후 시각 판정 중 발견된 `hwp3-sample5.hwp` page 4 결함 정정 + Document IR 표준 명문화. **Stage 1~D-2 11 commits 단계별 분리 보존** (rebase 으로 hash 재생성, author Taesup Jang 보존): Stage 1 IR 표준 + `is_in_wrap_zone()` helper / Stage 2 typeset `wrap_anchors` 메타데이터 채널 / Stage 2b `wrap_precomputed` 필드 제거 + HWP3 후처리 30 LOC 청산 (PR #589 잔존 부채) / Stage 3 HWP3 파서 wrap zone pgy 가드 단방향화 (Issue #604 결함 정정) / Stage 4 광범위 회귀 검증 / Stage 5 B-2 줄간격 100% 정합 (Stage A+D 에서 revert) / Stage 6 paper_images z-order 정합 / Stage A+D HWP5 IR 표준 정밀 재진단 + acc_section_vpos 누적 / Stage D-2 HWP5 IR 표준 8 항목 정정 / Stage D-2 보완 paragraph 내 line wrap vpos reset / Stage D-2 문서. **신규 IR 표준 명문화** (`mydocs/tech/document_ir_lineseg_standard.md` +195 + 3 분석 자료 +824) — 향후 wrap zone 영역 정합 인프라. **권위 자료 영구 보존**: `samples/hwp3-sample5-hwp5-v2018.hwp` (296KB) + `samples/hwp3-sample5-hwp5-v2024.hwp` (285KB) git tracked (작업지시자 결정). 검증: cargo test --lib --release **1155 passed** (PR #668 흡수 후 baseline, 회귀 0) / svg_snapshot 6/6 / issue_546 1/1 / issue_554 12/12 / clippy 0 / build --release / Docker WASM **4,598,892 bytes** (PR #601 baseline 4,588,023 +10,869 — wrap_anchors + 8 항목 정정 누적 정합) / `rhwp-studio npm run build` TypeScript 타입 체크 + dist 빌드 (`index-DMU370fc.js` 691,386 / `rhwp_bg-BMG1HwDs.wasm` 4,598,892). **광범위 페이지네이션 회귀 sweep**: BEFORE 232 / AFTER 234 fixture (신규 v2018/v2024 +2), 231 fixture 페이지 수 차이 **0** (회귀 0). `hwp3-sample.hwp` 16/16, `hwp3-sample4.hwp` native 39/39 (변동 없음, PR 본문 표 의 "36" 은 HWP5 변환본 영역으로 issue_554 통과), `hwp3-sample5.hwp` 64/64. **메인테이너 시각 판정 ★ 통과 (한컴 2010 + 한컴 2022 편집기 권위 정답지)** — `output/svg/pr609_after/hwp3-sample5/` 64 페이지 (Issue #604 page 4 + 8/16/22/27 + 43) + `output/svg/pr609_after/hwp3-sample4/` 39 페이지 + 잔존 영역 (별도 task scope) 까지 직접 확인 영역 통과. **분기 처리 (다른 컴퓨터 push 와 동시성 영역)**: 본 환경 cherry-pick 진행 중 다른 컴퓨터에서 PR #602/#659/#668 처리 + push → origin/devel 분기 발견 → `local/devel` 에서 `git rebase origin/devel` 2회 진행 (`db07c3c` 흡수 1차 + `a86dcf7` 흡수 2차) → `devel` 의 이전 hash (rebase 전) 영역 정리 (`git reset --hard origin/devel`) → 새 hash 영역 ff-merge → push 완료 (`feedback_release_sync_check` 정합). devel 머지 commits: `29d1587` ~ `3228016` (linear, 11 commits). PR #609 close + 한글 댓글 등록 + Issue #604 수동 close (closes #604 키워드 hash 재생성으로 자동 처리 안 됨). 처리 보고서: `mydocs/pr/archives/pr_609_report.md`. 검토 보고서: `mydocs/pr/archives/pr_609_review.md`. **`feedback_release_sync_check` 권위 사례 강화** — 다른 컴퓨터 push 와 본 환경 작업의 동시성 영역에서 분기 발견 즉시 보고 + rebase + ff-merge 처리 패턴. **`reference_authoritative_hancom` 정합** — PR 본문의 "HWP5 v2024 정합" 표는 컨트리뷰터의 비교 기준이며 본 환경 권위 영역 아님 (한컴 편집기 만 권위 정답지). **`feedback_close_issue_verify_merged` 정합** — Issue #604 close 시 본 PR 머지 검증 + 수동 close. **`project_hwpx_to_hwp_adapter_limit` 진전** — HWP3/HWP5/HWPX 3개 포맷 영역 정합 영역 진전. PR #589 잔존 부채 (wrap_precomputed IR 플래그) 청산 + HWP5 IR 표준 8 항목 정정 + Document IR 표준 명문화 + 11 stages 단계별 분리 보존 + 회귀 0 + 시각 판정 ★ 통과 패턴 모두 정합. |
| **PR #670** | samples: 한글 2022 PDF 변환본 199개 + 변환 자동화 스크립트 + 본 환경 정합 보강 (이슈 미연결, 권위 자료 영역) | **완료 (옵션 D 변형 cherry-pick + CLAUDE.md/pdf/README.md 보강 + push, PR #670 close)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 4번째 사이클 PR (PR #629/#620/#578 직후). v0.7.10 후 처리 PR (5/7). PR mergeable=MERGEABLE / BEHIND. **본질 영역**: samples/ 폴더 207개 → 한글 2022 OCX 자동화 PDF 변환 → 199 성공 (96.1%) → `pdf/` 폴더 영구 추가 + 변환 자동화 스크립트 (`_convert.ps1` / `_watchdog.ps1` / `_convert.log`). 48MB 영구 추가. **본 환경 정합 영역의 본질 정정 영역** (작업지시자 통찰 영역): "맥과 리눅스에서 개발하는 컨트리뷰터의 경우 PDF 가 오히려 더 정확합니다." → 한컴 편집기 영역 부재 환경 (맥/리눅스) 에서 한글 2020/2022 PDF 가 권위 정답지 영역 가능. **외부 컨트리뷰터 다양성 인프라 영역** (`project_dtp_identity` 영역 강화). **메모리 룰 영역 갱신** (`reference_authoritative_hancom` + `feedback_pdf_not_authoritative`): 컨트리뷰터 환경별 권위 영역 분리 — Windows + 한컴 편집기 1차 / macOS·Linux 한글 2020/2022 PDF 1차 / 한글 2010 PDF 등급 미달 / 한컴 뷰어·외부 변환·macOS 인쇄 모든 환경 정답지 아님. **본 환경 정합 보강 commit `12aa99c`**: (1) `pdf/README.md` 신규 작성 (+155 LOC) — 폴더 구조 + 명명 규약 + 권위 등급 + 변환 자동화 스크립트 안내, (2) `CLAUDE.md` 갱신 (+33/-1) — 예제 폴더 영역 + PDF 권위 자료 명명 규약 표 + PDF 권위 등급 (컨트리뷰터 환경별) 추가. **폴더 구조 영역 결정**: `pdf/` (한글 2022, 본 PR) + `pdf-2020/` (예정, 추가 환영) + `pdf-2010/` (예정, 등급 미달 영역으로 영구 보존 영역 부재). 명명 규약 `{stem}-{버전}.pdf` 일관 영역. 검증: cargo test --lib --release **1155 passed** (회귀 0, PDF binary 만 추가 영역으로 src 영향 0) / clippy 0 / build --release. cherry-pick author Jaeook Ryu 보존 (`69a7078`, committer edward) + 본 환경 정합 보강 commit edward (`12aa99c`). devel push `9216781..12aa99c`. PR #670 close + 한글 댓글 등록 (한글 2020 추가 변환 환영 + 한글 2010 정답지 등급 미달 영역 분리 + 변환 자동화 스크립트 영역의 가치 명시). 처리 보고서: `mydocs/pr/archives/pr_670_report.md`. 검토 보고서: `mydocs/pr/archives/pr_670_review.md`. **`reference_authoritative_hancom` + `feedback_pdf_not_authoritative` 메모리 룰 갱신 — 본 사이클 (5/7) 의 통찰 영역 정합 + 외부 컨트리뷰터 다양성 인프라 영역의 본질 영역 강화 + 향후 컨트리뷰터 증가 시 한컴 프로그램 부재 환경 영역에서 기여 가능 영역 확보 패턴 모두 정합.** |
| **PR #636 Task #630** | aift p4 목차 `·` 포함 라인 `(페이지 표기)` 8.67px 좌측 이탈 정정 + Issue #635 흡수 (closes #630 + #635) | **완료 (옵션 A 8 commits cherry-pick + push, b534dc9 skip, PR #636/Issue #630/#635 close)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 9번째 사이클 PR. v0.7.10 후 처리 PR (5/7). PR mergeable=CONFLICTING / DIRTY (PR base 95 commits 뒤). **본질 결함 (Issue #630)**: aift.hwp 4페이지 목차 영역의 `·` (U+00B7 MIDDLE DOT) 포함 라인의 `(페이지 표기)` 가 정확히 8.67 px (반각 1자) 좌측 이탈 — `is_halfwidth_punct` 가 U+00B7 영역을 강제 반각 처리, 한컴 저장 시점 측정값은 전각 기반으로 `tab_extended[0]` 영역이 전각 기준 산출 → 8.67 px 차이. **본질 정정 (TDD 5 단계 + Stage 6 + Stage 6 보강)**: Stage 1 베이스라인 측정 (164 fixture / 1614 페이지) → Stage 2 단위 테스트 RED (test_630 신규) → Stage 3 정정 1 (`·` 측정 통일, `is_halfwidth_punct` 에서 U+00B7 제거, 단일 룰) → **Stage 4 정정 2 시도 → 회귀 발견 → 철회** (HWP5 ext[0] 영역의 LEFT fallback 영역이 인코딩 의도와 정합 영역 발견, 합성 데이터 단위 테스트 함정 학습 영역) → Stage 5 광범위 회귀 검증 + 골든 SVG 갱신 → **Stage 6 Issue #635 흡수** (inline_tabs RIGHT + leader 본문 우측 끝 클램프, 3 곳 — Embedded/Wasm/estimate_text_width) → **Stage 6 보강 WasmTextMeasurer 정합** (작업지시자 "wasm 에서만 나타나는 현상" 피드백 반영, web canvas 영역). **`feedback_hancom_compat_specific_over_general` 권위 사례 강화 누적** — `is_halfwidth_punct` 에서 U+00B7 만 제외 (단일 룰, 케이스별 명시) + RIGHT + leader 케이스 신설 (구조적 가드, 측정 의존 없음). 본 사이클의 권위 사례 누적: PR #621 + PR #622 + PR #632 + 본 PR. **`feedback_image_renderer_paths_separate` 정합** — Stage 6 보강 영역의 권위 사례 (Embedded + Wasm 양쪽 경로 정정). **거버넌스 영역 본 환경 명명 규약 정합**: `task_m100_630*` 영역 — PR #622/#627/#632 패턴 정합. **Co-Authored-By Claude Opus 4.7** 영역 명시 (8 commits 모두) — PR #627/#668 패턴 정합. **충돌 처리**: `mydocs/orders/20260506.md` add/add 충돌 → `git checkout --ours` 본 환경 영역 보존 정합 / `b534dc9` (orders only commit) skip — src 변경 부재. 검증: cargo test --lib --release **1157 passed** (1156 + test_630 신규 정합, 회귀 0) / svg_snapshot 7/7 (issue_147 + issue_267 골든 갱신) / issue_546 1/1 / issue_554 12/12 / issue_418 1/1 / issue_501 1/1 / **issue_630 1/1** / clippy 0 / build --release / Docker WASM **4,572,439 bytes** (PR #632 baseline 4,577,370 -4,931 bytes — 산식 영역 정정 + LLVM 최적화 누적) / rhwp-studio/public/{rhwp_bg.wasm,rhwp.js} 갱신 (vite dev server web 영역). **권위 영역 100% 일치 (본 환경 직접 측정)**: aift p4 목차 영역의 `(페이지 표기)` 모든 라인 (`·` 포함 6 라인 + 미포함 라인) `x="605.4533..."` 동일 위치 (PR 본문 spread 0.00 정확 일치) / `)` 끝 718.12 px (본문 우측 끝, PDF 0.11 mm 정합) / SVG 우측 마진 -0.01 mm. **메인테이너 시각 판정 ★ 통과** — 작업지시자 평가: "시각 판정 통과했습니다." 권위 영역: aift p4 목차 영역의 `·` 포함 6 라인 + 미포함 라인 모두 본문 우측 끝까지 정렬 + KTX p1 leader 도트 영역 회귀 부재. **회귀 차단 가드 영구 보존**: `tests/issue_630.rs::test_630_aift_p4_toc_paren_alignment` 신규 + `text_measurement::tests::test_630_middle_dot_full_width_in_registered_font` 신규. **TDD 흐름 영역 권위 패턴**: Stage 1 RED → Stage 4 회귀 발견 → 철회 (학습 영역) → Stage 6 Issue #635 흡수 → Stage 6 보강 WASM 정합. cherry-pick author Jaeook Ryu + Co-Authored-By Claude 8 commits 모두 보존 (`1a31c5a` Stage 1 + `e7d80fd` Stage 2 + `393d5f7` Stage 3 + `069de4e` Stage 4 + `27e7dd9` Stage 5 + `e75c974` 최종 보고서 + `cb78efc` Stage 6 + `1aa48fc` Stage 6 보강, committer edward, b534dc9 orders only commit skip). devel push `565f9b1..1aa48fc`. PR #636 close + 한글 댓글 등록 + Issue #630 수동 close + **Issue #635 수동 close (Stage 6 흡수)**. 처리 보고서: `mydocs/pr/archives/pr_636_report.md`. 검토 보고서: `mydocs/pr/archives/pr_636_review.md`. **TDD 5 단계 + Stage 4 회귀 발견 → 철회 + Stage 6 Issue #635 흡수 + Stage 6 보강 WASM 정합 + 본 환경 명명 규약 (m100) 정합 + 권위 영역 100% 일치 + 메인테이너 시각 판정 ★ 통과 + 2 Issue close + `feedback_hancom_compat_specific_over_general` + `feedback_image_renderer_paths_separate` 권위 사례 강화 누적 + Co-Authored-By Claude 패턴 정합 영역 모두 정합.** |
| **PR #632 Task #631** | HWP 권위값 더블체크로 vpos-reset 인접 line 보존 (closes #631) | **완료 (옵션 B 5 commits cherry-pick + push, samples/aift.pdf 제외, PR #632/Issue #631 close)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 8번째 사이클 PR. v0.7.10 후 처리 PR (5/7). PR mergeable=CONFLICTING / DIRTY (PR base 89 commits 뒤). **본질 결함 (Issue #631)**: aift.hwp page 18 (=PDF page 12) 의 pi=222 단락 첫 2줄 영역의 페이지 영역 회귀 — `LAYOUT_DRIFT_SAFETY_PX = 10.0` 영역이 두 곳 (typeset.rs:1046 + 1074) 에서 차감 → 합계 20 px 보수 마진 영역으로 본문 잔여 59.2px → 39.2px 축소 → 2줄 합 (41.6) 수용 안 됨 → line 1 다음 페이지 밀림. **Task #332 stage4b 영역의 알려진 회귀 (aift pi=222) 종결**. **본질 정정 (단 1곳, +18/-1)**: src/renderer/typeset.rs::typeset_paragraph 줄 단위 분할 루프에 HWP 권위값 더블체크 추가 — (1) 다음 줄 vertical_pos == 0 (HWP 페이지 경계 신호) + (2) 현재 줄 vpos+lh ≤ body_available_height (HWP 좌표상 본문 안) 동시 성립 시 break 우회. 조건 1 영역이 매우 좁아 회귀 위험 영역 최소. **`feedback_hancom_compat_specific_over_general` 권위 사례 강화 누적** — 본 사이클의 vpos-reset 영역 권위 사례 누적: PR #621 (다중 줄 가드, line_segs.len() >= 2) + PR #622 (다단 vpos-reset, col_count > 1 && li > cursor_line && vertical_pos == 0) + 본 PR (vpos-reset 인접 line 보존 더블체크) — 모두 구조적 판단 영역 + HWP 인코딩 신호 영역 존중 영역. **거버넌스 영역 본 환경 명명 규약 정합**: `task_m100_631*` 영역 — PR #622/#627 패턴 정합. **옵션 B 분리 영역**: `samples/aift.pdf` 영역 (1.7 MB, PR commit `2ad3bfa`) 영역 제외 — 본 환경 `pdf/aift-2022.pdf` (PR #670 영구 보존 영역, 한글 2022 PDF, 1,767,558 bytes, SHA `8cbfb6c2...`) 영역과 다른 SHA 영역 (1,723,119 bytes, SHA `b42c25d8...`) 영역 충돌 영역 → 본 환경 명명 규약 정합 영역으로 분리. **충돌 처리**: `mydocs/orders/20260506.md` add/add 충돌 (Stage 4) → `git checkout --ours` 본 환경 영역 보존 정합 (PR #622/#627 패턴). 검증: cargo test --lib --release **1156 passed** (회귀 0) / svg_snapshot 7/7 / issue_546 1/1 / issue_554 12/12 / issue_418 1/1 / issue_501 1/1 / clippy 0 / build --release / Docker WASM **4,577,370 bytes** (PR #627 baseline 4,578,751 -1,381 bytes — 권위값 더블체크 영역 + LLVM 최적화 누적) / rhwp-studio/public/{rhwp_bg.wasm,rhwp.js} 갱신 (vite dev server web 영역). **권위 영역 100% 일치 (본 환경 직접 측정)**: page 18 pi=222 lines=0..2 vpos=67980..0 [vpos-reset@line2] / page 19 pi=222 lines=2..4 vpos=0..1920 [vpos-reset@line2] — PR 본문 명세 정확 일치. **컨트리뷰터의 광범위 회귀 검증**: 155 샘플 / 1,255 페이지 / 부정적 회귀 0건 / 1,300+ 누락 콘텐츠 복구 (hwpctl_API_v2.4 +460 text + hwpspec +855 text 등). **시각 판정 영역 스킵** (작업지시자 결정) — 결정적 검증 + 권위 영역 100% 일치 + 광범위 sweep 회귀 0 영역 통과로 시각 판정 영역 스킵 정합. cherry-pick author Jaeook Ryu 보존 (`51c22a6` Stage 1 + `7127ded` Stage 2 구현계획 + `5fdc096` Stage 2 정정 + `e415f62` Stage 3 + `e098562` Stage 4, committer edward, samples/aift.pdf 영역 commit `2ad3bfa` 제외). devel push `ff13a08..e098562`. PR #632 close + 한글 댓글 등록 + Issue #631 수동 close. 처리 보고서: `mydocs/pr/archives/pr_632_report.md`. 검토 보고서: `mydocs/pr/archives/pr_632_review.md`. **HWP 권위값 더블체크 (구조적 가드) + 광범위 회귀 검증 (1,300+ 누락 콘텐츠 복구) + 본 환경 명명 규약 (m100 + samples/aift.pdf 제외) 정합 + 권위 영역 100% 일치 + Task #332 stage4b 알려진 회귀 영역 종결 + `feedback_hancom_compat_specific_over_general` 권위 사례 강화 누적 패턴 모두 정합.** |
| **PR #627 Task #624** | exam_science p2 7번 글상자 ㉠ 사각형 y 회귀 정정 — Task #520 부분 회귀 복원 (closes #624) | **완료 (옵션 A 5 commits 단계별 cherry-pick + push, PR #627/Issue #624 close)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 7번째 사이클 PR (PR #629/#620/#578/#670/#621/#622 직후). v0.7.10 후 처리 PR (5/7). PR mergeable=CONFLICTING / DIRTY (PR base 83 commits 뒤). **회귀 출처**: PR #561 cherry-pick (`3de0505`) 영역에서 Task #520 정정 누락 (3 라인) — 메인테이너 conflict resolution 영역의 누락 영역 (PR #620 Task #519 누락 영역과 동일 패턴). **본질 결함 (Issue #624)**: `samples/exam_science.hwp` page 2 7번 문제 글상자 (pi=33 ci=0, 1x1 Table) 안 p[1] 단락의 ㉠ 사각형 (Control::Shape, treat_as_char=true, ls[1] 위치) 이 Line 1 영역 (y=213.95) 에 잘못 그려져 본문 텍스트 "분자당 구성 …" 위에 겹침. **본질 정정 (3 라인, +9/-2)**: src/renderer/layout/table_layout.rs — (1) Picture 분기 tac_img_y 산식: `seg.vertical_pos` → `seg.vertical_pos - first_vpos` (이중 합산 회귀 해소), (2) Shape 분기 shape_area.y: `para_y_before_compose` → `tac_img_y` (target_line 기반 위치), (3) Shape 분기 layout_cell_shape para_y 인자: 동일. **회귀 발생 4 조건 분석 (PR 본문)**: (a) cell 안 paragraph + (b) multi-line + (c) target_line > 0 + (d) first_vpos > 0 동시 성립 시 가시화. exam_science p[1] 만 (c) AND (d) 동시 성립 — 광범위 sweep 결과 (1/1496) 와 정확 부합. **edge case 안전성**: first_vpos=0 / target_line=0 / single-line / wrap=Square/InFrontOfText/TopAndBottom 모두 무회귀 확정. **TDD 흐름 영역 권위 패턴**: Stage 0 (수행/구현 계획서) → Stage 1 (TDD RED 통합 테스트, y=213.95 회귀 재현) → Stage 2 분석 (영향 범위 + edge case + 후속 진단, 작업지시자 "분석만 계속 진행" 영역 정합) → Stage 2 정정 (본질 3 라인) → Stage 3 (광범위 sweep + 최종 보고서). **거버넌스 영역 본 환경 명명 규약 정합**: `task_m100_624*` 영역 — PR #622 패턴 정합 → 그대로 cherry-pick. **Co-Authored-By Claude Opus 4.7** 영역 명시 (5 commits 모두) — PR #668 패턴 정합. **충돌 처리**: `mydocs/orders/20260506.md` add/add 충돌 (Stage 3) → `git checkout --ours` 본 환경 영역 보존 정합. 검증: cargo test --lib --release **1156 passed** (1155 + test_624 신규 정합, 회귀 0) / **test_624_textbox_inline_shape_y_on_line2_p2_q7 1/1 (RED → GREEN)** / svg_snapshot 7/7 / issue_546 1/1 / issue_554 12/12 / issue_418 1/1 / issue_501 1/1 / clippy 0 / build --release / Docker WASM **4,578,751 bytes** (PR #626 baseline 4,578,641 +110 bytes — 산식 영역 정정 영역의 미세 영역) / rhwp-studio/public/{rhwp_bg.wasm,rhwp.js} 갱신 (vite dev server web 영역). **권위 영역 100% 일치 (본 환경 직접 측정)**: ㉠ rect y="235.41333..." (PR 본문 235.413 정확 일치) + ㉠ text y="251.45333..." (PR 본문 251.453 정확 일치) + Δ +21.47 px = ls[1].vpos − ls[0].vpos / 75 정확 일치. **컨트리뷰터의 광범위 sweep**: 158 fixture / 1,496 페이지 / 의도된 정정 1 페이지 (0.067%) / 회귀 0 (1,495 페이지 byte-identical). **회귀 차단 가드 영구 보존**: `src/renderer/layout/integration_tests.rs::test_624_textbox_inline_shape_y_on_line2_p2_q7` (+58 LOC, y 좌표 [230, 240] 범위 검증 영역). **메인테이너 web editor 시각 판정 ★ 통과** — 작업지시자 평가: "웹 에디터에서 7번 문제 시각 판정 통과입니다." 권위 영역: page 2 7번 글상자 ㉠ 사각형 영역 Line 2 (y=235.41) 정상 위치 + 본문 "분자당 구성 …" 위 겹침 부재. cherry-pick author Jaeook Ryu + Co-Authored-By Claude 5 commits 모두 보존 (`fc8675c` Stage 0 + `dc94a55` Stage 1 + `e042a15` Stage 2 분석 + `f8085c8` Stage 2 정정 + `c55ee3c` Stage 3, committer edward). devel push `868e6df..c55ee3c`. PR #627 close + 한글 댓글 등록 + Issue #624 수동 close. **`feedback_close_issue_verify_merged` 권위 사례 강화 누적** — 본 사이클의 권위 사례 누적 영역: PR #620 (Task #519 누락) + 본 PR #627 (Task #520 누락) = PR #561 cherry-pick base diff 점검 누락 영역의 권위 영역 사례 영역. 처리 보고서: `mydocs/pr/archives/pr_627_report.md`. 검토 보고서: `mydocs/pr/archives/pr_627_review.md`. **TDD 흐름 영역의 권위 패턴 (Stage 1 RED → Stage 2 정정 → Stage 3 GREEN) + 회귀 차단 가드 영구 보존 + 본 환경 명명 규약 (m100) 정합 + 권위 영역 100% 일치 + 메인테이너 web editor 시각 판정 ★ 통과 + Co-Authored-By Claude 영역 명시 + `feedback_close_issue_verify_merged` 권위 사례 강화 누적 패턴 모두 정합.** |
| **PR #626** | render: replay equations in native Skia PNG output (Follow-up to #599, P5 단계) | **완료 (옵션 A 3 commits 단계별 cherry-pick + push, PR #626 close)** | 컨트리뷰터 @seo-rii (Seohyun Lee, me@seorii.page) — PR #599 직후 follow-up 컨트리뷰터. v0.7.10 후 처리 PR (5/7). PR mergeable=MERGEABLE / BEHIND (PR base 79 commits 뒤). **본질 영역**: PR #599 (P4: PNG 게이트웨이 + Skia 한글 fallback + char-fallback + --font-path + VLM 옵션 + export-png CLI) 의 follow-up — native Skia PNG 렌더링 경로의 수식 replay 영역. P4 까지 `PaintOp::Equation` 영역이 native Skia 경로에서 placeholder 박스로만 그려졌던 영역 → 실제 equation layout tree (`EquationNode.layout_box`) 영역의 Skia canvas 영역 직접 replay. **본질 정정 (5 files, +854/-5)**: (1) `src/renderer/skia/equation_conv.rs` 신규 (+717 LOC) — equation layout node → Skia draw call 변환 영역, 12 노드 종류 영역 지원 (fraction / atop / sqrt / superscript / subscript / matrix / limit / bracket / paren / decoration / font style), (2) `src/renderer/skia/renderer.rs` (+130/-3) — `PaintOp::Equation { bbox, .. }` placeholder 영역 → render_equation 호출 + bbox 폭 차이 영역의 x축 scale 영역 정합 + 색상 + font size 반영, (3) `src/renderer/skia/mod.rs` (+1) — equation_conv 모듈 등록, (4) README 양쪽 (한국어 + 영어) +3/-1 — P5 단계 명시 (native Skia equation replay) + 후속 영역 (raw-svg / form / native CanvasKit) 명시. **opt-in feature 영역** (`native-skia`) — 기본 빌드 영역 영향 부재, WASM 영역 외. 검증: cargo test --lib --release (default) **1155 passed** (회귀 0) / cargo test --features native-skia skia --lib --release **22 passed** (`renders_equation_layout_as_colored_ink` + `renders_atop_equation_layout_as_colored_ink` 신규 테스트 정합) / cargo clippy default + native-skia 0 warning / cargo build --release --features native-skia --bin rhwp 통과 (export-png CLI 영역) / Docker WASM 빌드 **4,578,641 bytes** (PR #622 baseline 동일 — native-skia 영역은 WASM 외 정합). **메인테이너 PNG 시각 판정 ★ 통과** — 작업지시자 평가: "png 시각판정 통과입니다." 권위 영역: `samples/exam_math.hwp` (20 페이지) → `output/png/pr626_after/exam_math/` (1.5 MB) — 수식 영역 (분수/atop/sqrt/superscript/matrix/limit/bracket 등) placeholder 박스 부재 + 실제 colored ink 영역 렌더 영역 정합. cherry-pick author seorii 보존 (`067a18b` 본질 + `dac1caa` docs + `2f6918a` atop fix, committer edward). devel push `8a8c4f1..2f6918a`. PR #626 close + 한글 댓글 등록. 연결 이슈 부재 (Follow-up to #599, closes 키워드 부재) — Issue close 영역 부재. **본 PR 의 정체성**: opt-in feature 영역의 작은 단위 회전 (P4 → P5 단계) + PR #599 의 follow-up + DTP 엔진 (`project_dtp_identity`) 의 native PNG 렌더링 영역 본질 영역 강화 + VLM/Vision 영역 사용 흐름의 PNG 출력 품질 보강. **후속 영역 (PR 본문 명시)**: P6 후보 — raw-svg replay / form replay / native CanvasKit replay + 별도 이슈 영역 — VLM preset 확장 (#613) / PNG DPI metadata (#614). 처리 보고서: `mydocs/pr/archives/pr_626_report.md`. 검토 보고서: `mydocs/pr/archives/pr_626_review.md`. **opt-in feature 영역의 작은 단위 회전 + PR #599 follow-up 단계별 진행 + 신규 native-skia 테스트 영역 영구 보존 + 메인테이너 PNG 시각 판정 ★ 통과 + DTP 엔진 본질 영역 강화 + `feedback_small_batch_release_strategy` 영역 정합 패턴 모두 정합.** |
| **PR #622 Task #619** | 다단 paragraph 내 vpos-reset 미처리 정정 (closes #619) | **완료 (옵션 A 3 commits 단계별 cherry-pick + push, PR #622/Issue #619 close)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 6번째 사이클 PR (PR #629/#620/#578/#670/#621 직후). v0.7.10 후 처리 PR (5/7). PR mergeable=CONFLICTING / DIRTY (PR base 75 commits 뒤). **본질 결함 (Issue #619)**: `samples/21_언어_기출_편집가능본.hwp` 페이지 8 우측 단 마지막 줄 (pi=181 line 8) 17.1 px overflow 영역 — `pi=181 line_segs[8].vertical_pos = 0` 영역은 HWP 가 line 8 을 다음 단/페이지 최상단에 배치하도록 인코딩한 vpos-reset 신호 영역. 활성 `TypesetEngine` partial-split 루프가 문단 *내부* line.vertical_pos=0 신호를 인식하지 않음 (문단 *간* vpos-reset 만 처리). **본질 정정 (단 1곳, 다단 한정 구조적 가드, +10 LOC)**: `src/renderer/typeset.rs::typeset_paragraph` partial-split 루프에 `if st.col_count > 1 && li > cursor_line && para.line_segs.get(li).map(|s| s.vertical_pos == 0).unwrap_or(false) { break; }` 추가. 다단 한정 (`col_count > 1`) — 단일 단의 partial-table split (issue #418) 회귀 차단. `li > cursor_line` 가드 — 무한 루프 방지. **`feedback_hancom_compat_specific_over_general` 권위 사례 강화 누적** — 다단 한정 가드는 구조적 판단 (col_count + cursor_line + vertical_pos 영역, 측정 의존 없음). PR #621 다중 줄 가드 + 본 PR 다단 vpos-reset 가드 = 본 사이클의 권위 사례 누적 영역. **거버넌스 영역 본 환경 명명 규약 정합**: 본 PR 의 `task_m100_619*` 영역은 본 환경 명명 규약 정합 영역 (PR #621 m07→m100 정정 영역과 다른 영역) → 그대로 cherry-pick. **충돌 처리**: `mydocs/orders/20260506.md` add/add 충돌 영역 → `git checkout --ours` 영역 본 환경 영역 보존 정합. 검증: cargo test --lib --release **1155 passed** (회귀 0) / svg_snapshot 7/7 / issue_546 1/1 / issue_554 12/12 / **issue_418 1/1** (단일 단 partial-table 회귀 차단 영역 정합) / issue_501 1/1 / clippy 0 / build --release / Docker WASM **4,578,641 bytes** (PR #621 baseline 4,606,564 **-27,923 bytes**) / rhwp-studio/public/{rhwp_bg.wasm,rhwp.js} 갱신 (vite dev server web 영역). **권위 영역 100% 일치 (본 환경 직접 측정)**: 페이지 8 단 1 pi=181 lines=0..8 vpos=77316..0 [vpos-reset@line8] / 페이지 9 단 0 pi=181 lines=8..13 vpos=0..7264 [vpos-reset@line8] / LAYOUT_OVERFLOW_DRAW 17.1px 사라짐. **컨트리뷰터의 회귀 가드 sweep**: 10개 회귀 가드 샘플 (exam_eng/exam_kor/exam_science/exam_math/exam_social/hwp-multi-001/hwp-multi-002/k-water-rfp/kps-ai/aift) LAYOUT_OVERFLOW + 페이지 분포 변경 전후 완전 동일. **메인테이너 web editor 시각 판정 ★ 통과** — 작업지시자 평가: "웹 에디터에서 시각 판정 통과되었습니다." 권위 영역: 페이지 8 우측 단 마지막 줄 line 8 정상 분할 + 페이지 9 좌측 단 첫 줄 line 8 정상 배치 (한컴 2020 PDF 권위 정답지 정합 — `pdf/21_언어_기출_편집가능본-2022.pdf` PR #670 영구 보존 영역). cherry-pick author Jaeook Ryu 보존 (`20e660c` Stage 1 + `a7eb171` Stage 2/3 + `2d20cc0` Stage 4, committer edward). devel push `d58217b..2d20cc0`. PR #622 close + 한글 댓글 등록 + Issue #619 수동 close. **잔여 영역 (PR 본문 §Remaining)**: page=7 col=1 pi=181 PartialParagraph + Shape 2.4 px overflow — 본 PR 영역 외 별도 이슈 분리 후보 영역. 처리 보고서: `mydocs/pr/archives/pr_622_report.md`. 검토 보고서: `mydocs/pr/archives/pr_622_review.md`. **다단 한정 구조적 가드 + 회귀 가드 영구 보존 + 본 환경 명명 규약 (m100) 정합 + 권위 영역 100% 일치 + 메인테이너 web editor 시각 판정 ★ 통과 + `feedback_hancom_compat_specific_over_general` 권위 사례 강화 누적 패턴 모두 정합.** |
| **PR #621 Task #617** | 표 셀 padding shrink 휴리스틱 다중 줄 가드 (closes #617) | **완료 (옵션 B src+가드만 cherry-pick + 거버넌스 영역 명명 규약 정합 + push, PR #621/Issue #617 close)** | 컨트리뷰터 @planet6897 (Jaeuk Ryu) — 5번째 사이클 PR (PR #629/#620/#578/#670 직후). v0.7.10 후 처리 PR (5/7). PR mergeable=CONFLICTING / DIRTY (PR base 71 commits 뒤). **본질 결함 (Issue #617)**: `exam_kor.hwp` 16/27/36번 보기/자료 박스에서 본문이 셀 테두리에 닿던 시각 오류 — `shrink_cell_padding_for_overflow` 가 양쪽 정렬 한국어 본문의 자연 폭을 과대 추정하여 정상 padding (=850 HU ≈ 3 mm) 을 1 px 까지 축소. **본질 정정 (단일 룰 구조적 가드)**: `paragraphs.iter().any(|p| p.line_segs.len() >= 2)` 검사 추가 — 다중 줄 단락이 있는 셀은 HWP 가 자간 분배 / 줄바꿈을 마친 상태이므로 자연 폭 추정으로 다시 깎으면 오버 페인팅. 단일 줄 좁은 셀 (table-text / form-002 수치 셀 등) 은 종전 휴리스틱 유지 (회귀 0). 함수 시그니처 `paragraphs: &[Paragraph]` 인자 추가, 호출처 4곳 (`table_cell_content.rs`, `table_partial.rs`, `table_layout.rs` ×2) 동시 수정. **`feedback_hancom_compat_specific_over_general` 권위 사례 강화** — 다중 줄 가드는 구조적 판단 (line_segs 개수, 측정 의존 없음). **cherry-pick simulation 영역의 학습 영역**: `/tmp/pr621_test` 임시 clone 영역에서는 통과했으나 본 환경의 `git checkout local/pr621 -- src/...` 영역에서 컴파일 에러 6 건 (E0061, paragraph_layout.rs:634 16→17 인자 + render_tree.rs:771 3→4 인자 시그니처 PR base 시점 이후 변경 영역) 발생 → `git cherry-pick 5e9d996` 으로 재진행 → 3-way merge 영역으로 base 시점 이후 영역 변경 흡수 → 컴파일 통과. **외부 PR 처리 영역에서 `git cherry-pick` 영역 사용 (`git checkout` 영역 회피) 학습 영역**. **거버넌스 영역 본 환경 명명 규약 정합**: PR 본문의 `task_m07_617*` (m07 = v0.7.x 약어) → `task_m100_617*` (m100 = v1.0.0 마일스톤 정합) 일괄 정정 — git mv + sed 일괄 치환 + PR 두 번째 commit `79ed72b` 영역의 task_m07_617_report.md 영역 추출 + task_m100_617_report.md 영역 신규 추가. PR #629 / PR #668 패턴 정합. 검증: cargo test --lib --release **1155 passed** (회귀 0) / svg_snapshot **7/7** (issue_617 신규 포함) / issue_546 1/1 / issue_554 12/12 (`task554_no_regression_exam_kor` 통과) / issue_501 1/1 (Task #501 vertical padding 영역 회귀 0) / clippy 0 / build --release / Docker WASM **4,606,564 bytes** (PR #609 baseline 4,598,892 +7,672 — 다중 줄 가드 + paragraphs 시그니처 추가) / `rhwp-studio npm run build` TypeScript 타입 체크 + dist 빌드 (`rhwp_bg-CblmEDdh.wasm` 4,606,564). **메인테이너 시각 판정 ★ 통과** (한컴 2010 + 한컴 2022 편집기 권위 정답지 + vite dev server web 환경) — 작업지시자 평가: "시각판정 통과입니다." 권위 영역: exam_kor.hwp 페이지 6 (16번 보기) / 페이지 9 (27번 보기) / 페이지 17 (36번 자료) 본문 좌·우 padding 약 3mm 복원 + 다른 페이지 단일 줄 좁은 셀 미회귀. **회귀 차단 가드 영구 보존**: `tests/svg_snapshot.rs::issue_617_exam_kor_page5` 신규 + `tests/golden_svg/issue-617/exam-kor-page5.svg` (1893 LOC). cherry-pick author Jaeook Ryu 보존 (`2f28866`, committer edward) + 거버넌스 commit edward (`ab146d6`) + public commit edward (`b23468c` rhwp-studio/public/rhwp.js 갱신, vite dev server web 영역). devel push `08972a8..b23468c`. PR #621 close + 한글 댓글 등록 + Issue #617 수동 close. 처리 보고서: `mydocs/pr/archives/pr_621_report.md`. 검토 보고서: `mydocs/pr/archives/pr_621_review.md`. **단일 룰 구조적 가드 + 회귀 차단 가드 영구 보존 + 본 환경 명명 규약 정합 + cherry-pick simulation 학습 영역 (git cherry-pick vs git checkout) + 컨트리뷰터의 5번째 사이클 PR + 시각 판정 ★ 통과 + `feedback_hancom_compat_specific_over_general` 권위 사례 강화 패턴 모두 정합.** |
| **PR #668 (#660)** | Neumann ingest 파이프라인 v2 — JSON 스키마 + Rust 빌더 + Skill (closes #660) | **완료 (4 commits squash cherry-pick 머지 + push, PR #668/Issue #660 close)** | **rhwp 첫 PR 컨트리뷰터** @metahan88-droid (GitHub 가입 2025-09-29). v0.7.10 후 아홉 번째 처리 PR (5/7 여섯 번째). PR mergeable=MERGEABLE / BEHIND. **거버넌스 정합 영역 우수** (PR #629 권위 자료 패턴과 동등 또는 진보) — 하이퍼-워터폴 단계별 산출물(stage1/2/3 + e2e + v2) + AI 페어프로그래밍 명시 (Co-Authored-By Claude Opus 4.7) + 자가 검증/수정 (e2e hotfix 2건 + v2 redesign auto_number 명시 필드 도입으로 휴리스틱 제거). **본질 영역 (신규 4개 모듈)**: `tools/rhwp-ingest/schema/ingest_schema_v1.json` JSON Schema v7 (+118) + `src/parser/ingest/{mod,schema}.rs` serde 모델 (+223) + `src/document_core/builders/{mod,exam_paper}.rs` Document IR 빌더 (+374) + `src/main.rs build-from-ingest` CLI (+108) + `.claude/skills/rhwp-exam-ingest/SKILL.md` + helpers 4종 (+467). **본질 cherry-pick (PR #629 패턴 정합)** — src + .claude/skills + tools/rhwp-ingest 영역만, mydocs 거버넌스 산출물 7파일(plans/working×5/report) cherry-pick에서 제외. single squash commit `80535a3` author 보존 (`한 <han@han-ui-Macmini.local>`). 검증: cargo test --release --lib **1155 passed** (parser::ingest 4 + builders 10 신규 포함, 회귀 0) / cargo test --release 전체 GREEN / clippy 신규 경고 0 / build --release / Docker WASM **4,598,886 bytes** (PR #629 시점 4,590,307 +8,579 — parser/ingest + builders + main.rs CLI + serde_json 추가 정합) / e2e 라운드트립: sample_minimal.json → HWPX 5,356 bytes (문제 3개 / 문단 21개, ① ~ ⑤ 평문 보존). **광범위 회귀 sweep**: aift/exam_kor/exam_math/exam_science/aift.hwpx 195 페이지 export-svg 모두 정상. **메인테이너 시각 게이트웨이 (rhwp-studio 웹 에디터)** — `output/pr668/sample_minimal.hwpx` 시각 확인. **발견 사항**: 1번 문제 두 번째 줄("환경 오염은 ... 영향을 미친다.")이 SVG 렌더에서는 1줄, WASM 렌더(웹 에디터)에서는 본문 폭(~150mm)에 맞춰 2줄로 줄바꿈 — 두 렌더 결과 불일치. **원인**: `src/document_core/builders/exam_paper.rs:97-106` `make_text_para()` 헬퍼가 모든 문단에 단일 LineSeg(`segment_width=50000`, `text_start=0`) + 줄나눔 사전 미계산 → SVG는 단일 LineSeg 신뢰 / WASM은 자체 layout 재계산. **작업지시자 시각 판정 ★ 머지 통과** — 본 PR은 Neumann 본 작업 1단계 골격이고, 시각 정합성은 후속 이슈 #665 (placement 4모드 + ParaShape 시험지 표준 + Picture/BinData IR 빌드)에서 본격 해결. devel merge `9fc8f32` + push. PR #668 close + 한글 댓글 등록 (rhwp 첫 PR 환영 + 거버넌스 정합 인정 + cherry-pick 영역 분류 + LineSeg 발견 사항 + 후속 영역 안내) + Issue #660 수동 close. 처리 보고서: `mydocs/pr/pr_668_report.md`. 검토 보고서: `mydocs/pr/pr_668_review.md`. **컨트리뷰터 등록 후속 이슈 #665/#666/#667 영역** — 마일스톤 **v1.0.0** + assignee **@metahan88-droid** 일괄 지정 + 안내 코멘트 등록 완료 (작업지시자 결정). **PR #629 권위 자료 패턴 정합 사례** — 외부 컨트리뷰터 거버넌스 산출물 영역(`mydocs/`)과 메인테이너 산출물 영역의 본질적 분리 정책의 두 번째 권위 적용 케이스. `feedback_first_pr_courtesy` + `feedback_visual_judgment_authority` + `feedback_rule_not_heuristic` 모두 정합. **rhwp 첫 PR로서 매우 인상적인 사례** — 향후 외부 컨트리뷰터 거버넌스 정합 모범 사례. |
| **Task #677 (메인테이너)** | 복학원서.hwp PDF 정합 결함 — pi=16 인라인 표 라인 누적 + U+F081C 채움 문자 폭 + 워터마크 효과 미적용 (closes #677) | **완료 (5 stages 본질 정정 + 회귀 가드 + stream/devel PR 진행)** | 메인테이너 자체 등록 task (5/7 두 번째 메인테이너 task — Task #617 PR #621 cherry-pick 후). **본질 결함 3개**: (1) **pi=16 PartialParagraph y 누적 결함** — 인라인 TAC 표 (`tac=true`, block-TAC 분류) Table item 이 y_offset 을 표 바닥까지 누적 후 후속 PartialParagraph 가 동일 paragraph 의 line 1 을 그대로 상속받아 표 높이 이중 누적 → 273.1px 단 하단 초과 → 접수증 영역 + "고 려 대 학 교 총 장 귀 하" 큰 제목 영역 콘텐츠 손실. **본질 정정** (`layout.rs:2120-2147`): TAC 표 보유 paragraph 의 PP (start_line>0) 진입 시 `y_offset` 을 `LineSeg.vpos` 정합 위치로 리셋 + `y_offset = y_offset.max(pp_y_out)` 누적. 조건 가드 3개 (start_line>0 / TAC 표 보유 / para_start_y 등록). (2) **U+F081C HWP PUA 채움 문자 폭 결함** — 본 fixture 의 ComposedLine cl[0] 가 99 chars × U+F081C 채움 문자 보유, `compute_tac_leading_width` 가 `estimate_text_width` default fallback (`font_size * 0.5`) 으로 99×6.65 = 658px 의 leading width 가산 → 3×3 접수증 표가 col_left + 658 = x=716px (body 우측 끝) 으로 배치 → 본문 우측 외곽. **본질 정정** (`text_measurement.rs` `char_width` 클로저 5 사이트): U+F081C 시각 폭 0 (한컴 PDF 정합, fillers 가 표 너비만큼 채워져 표가 fillers 영역 위에 시각적으로 겹쳐 column-left 출력 패턴). (3) **한컴 워터마크 모드 변환 미적용** — HWP IR `effect=GrayScale, brightness=-50, contrast=70, watermark=custom` 저장값 그대로 적용 → 어두운/고대비 본문 가림. **본질 정정 (3차 — 작업지시자 시각 판정 피드백 반영)** (`svg.rs:1082-1097` + `web_canvas.rs:418-432` 양쪽 동기): 1차 한컴 표준 프리셋 (70, -50) 강제 → "이미지가 너무 흐림" / 2차 (\|abs\|, -\|abs\|) = (50, -70) → "반투명해야 + 더 진해져야" / 3차 저장값 그대로 brightness/contrast 적용 + opacity 0.17 반투명 영역 (작업지시자 단계별 피드백 반영 — 0.5 → 0.35 → 0.25 → 0.17 약하게 정정). **검증**: cargo test --release --lib **1155 passed** (회귀 0) / cargo test --release GREEN / svg_snapshot **8/8** (issue_677 신규 + 7 기존 byte-identical) / issue_546 1/1 / issue_554 12/12 / issue_598_footnote_marker_nav 4/4 / issue_501 1/1 / clippy lib 0 / Docker WASM 빌드 + Studio TypeScript+vite 빌드 success. **광범위 sweep**: 162+ HWP/HWPX fixture (총 1,964 페이지) 페이지 수 회귀 0. U+F081C 보유 fixture 1개만 (본 fixture) / 워터마크 보유 fixture 1개만 → 다른 fixture 영향 0. **회귀 차단 가드 영구 보존**: `tests/svg_snapshot.rs::issue_677_bokhakwonseo_page1` 신규 + `tests/golden_svg/issue-677/bokhakwonseo-page1.svg` (414KB). **시각 정합** (PNG 1600×2263 ↔ `pdf/복학원서-2022.pdf`): 복학원서(학부) / 5×4 표 / 본인은 휴학으로... / I have taken leave... (font-weight=bold) / 본인 + 접수자 box / **고 려 대 학 교 총 장 귀 하** (BEFORE 가려져 안 보임) / 분리선 / 복 학 원 서 접 수 증 / Filing Receipt / 대학/학번/성명 / 위 학생의... / The above student's... / 년 월 일 + ㊞ / ※ 군필자 + ※ Those who completed... / 고려대학교 워터마크 모두 정합 출력. **잔존 영역 (별도 task 후보)**: (1) 굵은 폰트처리 영역 — 본 fixture cell paragraph CharShape 모두 `bold=false` (HWP IR), PDF 굵은 외양은 한컴 한양견명조/한양신명조 디자인 굵기 자체 → **폰트 가용성 영역의 결함, 본 task 의 layout/워터마크 본질과 다른 본질** (작업지시자 별도 task 분리 승인). (2) body-clip width 1619.92 (시각 영향 없음, 코드 위생). (3) 워터마크 표준 프리셋 외 사용자 customization 영역 (다른 워터마크 fixture 발견 시 검토). **변경 LOC**: src 4 파일 +82/-4 + tests +14 (svg_snapshot 가드) + 골든 414KB + mydocs 8 LOC (수행/구현/Stage 1~5/최종 보고서). **메인테이너 시각 판정 ★ 통과** — 작업지시자 단계별 피드백 (이미지 흐림 → 반투명 + 진해져야 → opacity 약하게) 모두 반영, 4차 시각 판정 후 stream/devel PR 진행 승인. 처리 보고서: `mydocs/report/task_m100_677_report.md`. 진단/정정 단계별 보고서: `mydocs/working/task_m100_677_stage{1,2,3,4,5}.md`. **`feedback_visual_judgment_authority` 권위 사례 — 작업지시자 단계별 시각 피드백 반영 + 단계별 정정 + 단계별 승인 패턴 + `feedback_image_renderer_paths_separate` SVG/Canvas 양쪽 동기 정정 + `feedback_hancom_compat_specific_over_general` 케이스별 명시 가드 (U+F081C 1개 / TAC 표 1개 fixture) + `feedback_rule_not_heuristic` HWP IR 표준 직접 사용 패턴 모두 정합.** |

## 작업 메모

### PR #620 본질
- **`feedback_close_issue_verify_merged` 메모리 권위 영역의 권위 케이스** — Task #519 close 시 정정 commit (`7ead89d`) 이 임시 브랜치에만 있고 devel 미머지 → 동일 결함 재발 (Issue #618). PR #527 (CLOSED) 묶음 머지 `a7e43f9` 가 본 환경 devel 도달 못 한 후속 영역.
- **본 환경 직접 검증 정합** — `git merge-base --is-ancestor 7ead89d devel` 부재 + `git merge-base --is-ancestor a7e43f9 devel` 부재. PR 본문 진단 100% 재현.
- **`extract_shape_transform` 헬퍼 재사용** — `utils.rs:109` 에 살아있는 헬퍼 그대로 활용, 신규 단위 변환 코드 0줄. `feedback_rule_not_heuristic` 정합.
- **회귀 위험 영역 좁힘** — page 4 만 의도된 정정 (+107 bytes), 다른 7 페이지 byte-identical + 광범위 sweep 167 fixture / 1,687 페이지 차이 0.
- **시각 판정 SVG + web 양쪽 일관 정합** — Picture transform 영역이 SVG/Canvas (web editor) 양쪽 동일 영향. `feedback_image_renderer_paths_separate` 영역에서도 정합.

### 회귀 방지 후속 영역 — 묶음 머지 a7e43f9 의 다른 task 영역 점검
| Task | 본 환경 정합 상태 |
|------|--------------|
| #517 | ❓ 누락 가능성 (별도 후속 task 영역) |
| #518 | ❓ 누락 가능성 (별도 후속 task 영역) |
| #519 | ✅ **PR #620 재적용 완료 (본 PR)** |
| #520 | 🔄 **PR #627 별도 영역** (다음 검토 단계) |
| #521 | ✅ PR #564 cherry-pick 정합 |
| #523 | ❓ 누락 가능성 (별도 후속 task 영역) |
| #528 | ✅ PR #592 cherry-pick 정합 |

→ 컨트리뷰터에게 후속 PR 권유 (옵션 B 댓글 등록) + 본 환경 직접 점검 옵션 분리 안내.

### PR #642 본질
- **본 사이클 두 번째 web editor (rhwp-studio TypeScript) 본질 정정 PR** (이전 PR #611 에 이어). DTP 엔진 (`project_dtp_identity`) web editor 영역 정합 강화.
- **활발한 컨트리뷰터 @postmelee 13 PR 누적** — 4/16 PR #167 부터 본 PR 까지 약 3주 사이클 (Firefox/Chrome 확장, Safari, Native bridge API, 브라우저 확장 보강 등 다양한 영역).
- **Issue #598 권위 영역 (크롬 확장 사용자 별점 리뷰)** — 한컴 정합 UX 5 영역 모두 본질 정정 + 시각 판정 ★ 4 권위 영역 통과.
- **`feedback_assign_issue_before_work` 메모리 권위 영역 강화** — 작업지시자 직접 assignee 지정으로 외부 컨트리뷰터 일차 방어선 정합.
- **회귀 차단 가드 영구 보존** — 4 통합 테스트 + Puppeteer e2e + `samples/footnote-01.hwp` 권위 샘플 (32KB) 영구 보존.
- **WASM API 영역 분리** — `hitTestBodyFootnoteMarker` / `getFootnoteAtCursor` / `deleteFootnote` (+ native 영역 동일 함수) 명료한 영역.
- **단일 컴포넌트 확인 다이얼로그** — `showConfirm()` Delete/Backspace 양쪽 동일 호출, Issue #598 명세 정합.

### 다음 영역 — 13 잔존 PR 처리
- 5/6 신규 PR (9건): #644, #641, #638, #636, #632, #627 (Task #520 별도 영역, 본 PR 처리 후 우선 검토 권장), #626, #622, #621
- 5/5 잔존 (4건, v0.7.11 영역 후보): #601, #602, #607 (검토 중 — 컨트리뷰터 응답 대기), #609

### v0.7.11 PATCH 사이클 영역
v0.7.10 (5/6) 후 PATCH 회전 운영 패턴 (`feedback_small_batch_release_strategy`) — 본 사이클 (5/6+5/7) PR 처리 누적에 따라 결정. 5/7 첫 처리 PR 진행 중.
## M100 — synam-001.hwp 분할 표 마지막 줄 클립 (Task #485)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| **M100 #485** | synam-001.hwp 15·20·21 페이지 분할 표 셀 마지막 줄 클립핑 (Task #431 잔여) | **완료** | 작업지시자 승인 (Stage 1~3 단계별). 본질: layout 측 `compute_cell_line_ranges` 의 **두 개 분리된 버그**. **Bug-1 (out-of-order)**: inner break 가 outer 루프를 빠져나오지 않아 셀 마지막 단락(line_spacing 제외로 line_h 작아짐)이 abs_limit 안에 fit 하여 시각 순서 역전 + 클립 (p15: pi=84 line_h=14.667 / p20: pi=169 line_h=13.333). **Bug-2 (boundary epsilon)**: `>` boundary 케이스에서 ~0~2px 차이로 fit 하면 cell-clip-rect bottom 침범 (p21: pi=108 gap=0.947). **정정**: `limit_reached` 플래그 + outer 차단 + `SPLIT_LIMIT_EPSILON=2.0px`. 후보 A (typeset split_end_limit) / 후보 D (vpos correction) 미적용 — 본질이 layout 측에서 충분히 정정됨. 검증: cargo test 1199 passed, 전 샘플 (155개) export OK, kps-ai.hwp Task #362 영역 보존, aift.hwp 분할 표 정상. 산출물: `mydocs/plans/task_m100_485.md` + `_impl.md` + `mydocs/working/task_m100_485_stage{1,2a,2b,3}.md` + `mydocs/report/task_m100_485_report.md`. 변경 파일: `src/renderer/layout/table_layout.rs` 단독 (3 hunk). |

## 후속

- 이슈 #485 GitHub 코멘트 추가 (페이지 20·21 검증 케이스 + 정정 영역 + close 권고)
- `local/task485` → `local/devel` merge
- 후속 작업 이슈 #656 등록 (typeset/layout 모델 통일)
