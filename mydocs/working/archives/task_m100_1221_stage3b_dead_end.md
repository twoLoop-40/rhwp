# Stage 3b 보고 — Task M100 #1221 (렌더 경로 추적 막힘)

**브랜치**: `local/task1221`
**결과**: 추가 추적했으나 **z값 렌더 경로 미특정 → 막힘**. 소스 무변경(전부 revert).

## 이번에 instrument 해 배제한 경로

1. `paragraph_layout.rs:3008` (셀 인라인 수식 EquationNode) — 페이지4 셀 수식 11개 로깅됐으나 **z값(1.0/1.1/1.2/1.3) 없음**, y=150~553 (다른 셀). z값 미포함.
2. `table_partial.rs:1095` (분할 표 셀 수식, `eq_y=para_y_before_compose`) — 페이지4 에서 **0건**. z표는 이 경로 미사용.

→ z표 값은 **위 두 수식 경로 어디에도 안 나타남**. 제3의 경로(또는 z값이 수식이 아닌 텍스트/필드)일 가능성.

## 누적 배제 정리 (가설 좁힘)

- 텍스트 줄 위치/centering: **정렬됨** (무죄).
- line-height 클램프: **무효**.
- 표준/분할 셀 수식 렌더 경로: **z값 미통과**.
- 유력 후보(미확인): 어울림 host 인라인 표 셀이 `layout_partial_paragraph`(layout_wrap_around_paras 경유) 또는 별도 셀-수식 경로로 렌더되며, 그 경로에서 수식 y 가 줄 인덱스 무시(문단 시작 y 고정)일 가능성.

## 솔직한 결론

광범위 instrument 에도 **z값 실제 렌더 경로를 특정하지 못함**. 본 세션에서 안전한 수정 불가. 막힌 지점과 배제 목록을 남겨 전용 사이클에서:
1. `render_equation_svg`/`TextLine` emit 시점에 **좌표+내용 동시 로깅**으로 z값 노드의 생성 위치(함수) 직접 포착.
2. 그 함수의 수식/텍스트 y 가 줄 인덱스를 반영하는지 점검.

소스 변경 없음. #1221 미해결 — 전용 사이클 재착수 필요.
