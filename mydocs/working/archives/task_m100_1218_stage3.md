# Stage 3 조사보고서 — Task M100 #1218 (z-표 행 압축)

**단계**: z-표 행 압축 조사
**브랜치**: `local/task1218`

## 증상

4쪽 문26 우측 z-표: z-열이 P-열과 **세로 정렬 어긋남** — 첫 데이터 행 z 비어 보이고 "1.0"/"1.1" 겹침("1.01"). P-열(0.3413~0.4032)은 정상.

## 조사 (고해상 렌더 + dump)

- z-열 셀(cell[2]): **4개 값이 3개 문단**(p[0]=2줄 "1.0"/"1.1", p[1]="1.2", p[2]="1.3")에 들어 있고, p[0] ls[0] **lh=825 (< 폰트 900)**.
- P-열 셀(cell[3]): 4개 값이 4개 문단, 각 lh=900.
- 셀 콘텐츠는 `table_layout.rs:calc_cell_paragraphs_content_height` 로 높이 산출 후 `valign=Center` 로 `(inner_h - content_h)/2` 중앙 정렬.
- z-열/P-열의 **문단 구성·줄높이가 달라(3 vs 4 문단, lh 825 vs 900) 콘텐츠 높이·중앙정렬 오프셋이 어긋나** 두 열의 행이 세로로 안 맞고, p[0] 의 lh=825 로 "1.0"/"1.1" 글자가 겹침.

## 판단: 별개 사안으로 분리 권장

- 수정 지점이 **표 셀 내부 세로 레이아웃**(`table_layout.rs` 셀 콘텐츠 높이 산출 + valign 중앙정렬) — Stage 2(본문 흐름, layout.rs)와 **다른 서브시스템**.
- 이 경로 변경은 **모든 표 셀 렌더링에 영향** → 광범위 회귀 위험(svg_snapshot 다수 표 샘플). 안전한 수정은 셀 콘텐츠 높이 산출과 실제 줄 렌더 줄높이의 정합(특히 stored lh<font 케이스, 다중행 문단)을 별도로 검증해야 함.
- 본 이슈(#1218)의 **주 증상(① 답안↔문제 겹침)은 Stage 2 에서 해소**. z-표 행 압축은 성격·위험도가 달라 **별도 이슈로 분리**하여 신중히 다루는 것을 권장.

## 결론

#1218 = Stage 2 수정으로 종결 권장. z-표 행 압축 → 신규 이슈(표 셀 세로정렬/줄높이 정합)로 분리.
