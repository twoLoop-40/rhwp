# Task #142 시각 검증 — 발견된 버그 목록

exam_math.hwp 20페이지 시각 검증 중 발견된 문제 기록.

## 해결 완료

| 페이지 | 문제 | 상태 |
|--------|------|------|
| 전체 | 수식-텍스트 겹침 (TAC 너비 부정확) | ✅ 해결 (composer.rs 레이아웃 산출값 사용) |
| 1 | `f'(1)` → `f'  (1)` prime 폭 과다 | ✅ 해결 (Unicode 폭 추정 세분화) |
| 2 | `cos(π/2+θ)=-1/5` 전체가 하나의 분수로 처리 | ✅ 해결 (OVER 중위 연산자 재설계) |
| 1 | `lim` 폰트 크기가 비정상적으로 큼 | ✅ 해결 (font_size_from_box 버그 수정) |
| 전체 | 수식 y축 정렬 — 텍스트 baseline과 불일치 | ✅ 해결 (layout_box.baseline 기준 배치) |
| 전체 | 분수 baseline — Row 내 다른 요소와 겹침 | ✅ 해결 (axis_height 기반 baseline 계산) |
| 전체 | 분수선 SVG y좌표 오류 | ✅ 해결 (baseline - axis_height) |

## 미해결

| 페이지 | 문제 | 설명 |
|--------|------|------|
| 1 | `sqrt{3} of {5}` 중괄호 인덱스 | ✅ 파서 수정 완료, 렌더링 미확인 |
| 2 | ③ 위에 수식 `0`이 겹침 | 탭+TAC 조합 위치 계산 오류. 선택지 `① -5 ② -√5 ③ 0 ④ √5 ⑤ 5`에서 ③과 수식0 사이 간격 3.6px로 너무 좁음 |

| ? | `log_{2}` 밑 첨자가 log에서 떨어짐 | ✅ 해결 (try_parse_scripts에서 Thin 공백 건너뛰기) |
| ? | 적분 ∫의 상한/하한이 기호 위/아래 중앙에 배치됨 | ✅ 해결 (적분을 nolimits 스타일로 변경) |
| ? | sin/cos/tan 등 함수의 첨자 위치 | ✅ 해결 (Thin 공백 건너뛰기로 함수 첨자 정상 파싱) |
| ? | `bar{rm AD it}` 중괄호 `}` 텍스트 출력 | ✅ 해결 (parse_single_or_group에서 RBrace 처리) |
| 2 | ③ 위에 수식 `0`이 겹침 | ✅ 해결 (inline_tabs 동기화) |
| 3 | `log` 뒤 빈 공간 | `log `{1} over {sqrt{10}}`에서 Thin 공백이 log와 분수 사이에 렌더링됨. 한컴에서는 간격 없음 |
| 3 | 분수 분모가 분수선에 너무 가까움 | `{1} over {sqrt{10}}`에서 √10이 분수선에 밀착. 한컴에서는 분수선-분모 간 여백이 더 넓음. FRAC_LINE_PAD(0.15) 조정 필요 |
| 공통 | 함수명(log,sin,cos) 뒤 Thin 공백이 과대 | 한컴에서는 함수명-인자 사이 간격이 매우 작지만, rhwp에서 Thin 공백(0.17×fs)이 그대로 렌더링됨 |
| 공통 | 적분 ∫ 상한/하한 배치 오류 | ✅ 해결 (MathSymbol BIG_OP_SCALE + 적분 전용 SubSup 배치) |
| 공통 | 함수명 뒤 Thin 공백 | ✅ 해결 (파서에서 함수 뒤 ` 소비) |
| 6 | CASES+EQALIGN에서 `&&` 이중 탭 미처리 | ✅ 해결 (CASES/EQALIGN 파서에서 && → Tab 처리) |
| 8 | CASES 수식 내용 오버래핑 | 한글+수학 혼합 긴 조건 텍스트가 포함된 CASES에서 행 내용이 겹침 |
| 9 | `rm P it left(...)` 파싱 오류 | ✅ 해결 (FontStyle 뒤 구조 명령어 body 먹힘 방지) |
| 16,20 | 문단 내 수식+텍스트 조판 겹침 | 큰 수식(∑, 분수, lim, 쌍곡선)의 TAC 높이가 줄 높이에 반영되지 않아 인접 줄과 겹침. 복수 페이지에서 발생 |

## 미검증 페이지

| 페이지 | 상태 |
|--------|------|
| 1~2 | 검증 완료 |
| 3 | 검증 중 (log 빈 공간) |
| 4~20 | 미검증 |
