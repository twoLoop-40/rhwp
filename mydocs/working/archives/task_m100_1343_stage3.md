# Stage 3 완료보고서 — Task #1343 (시각 검증)

## 검증 방법

```bash
rhwp export-svg "samples/3-09월_교육_통합_2024-구분선아래20구분선위20.hwpx" -p 15 \
  -o output/svg/task1343/
```

16페이지(0-기준 15) SVG 내보내기 → PNG 변환 → 문24 수식 영역 확대 확인.

## 결과

### 두부(▦) 소거
- SVG 내 `U+E04D` 잔존 카운트: **0** (변환 전: 2건)

### 시각 정합
문24 `③` 조건부 확률 수식이 정상 렌더됨:
- `P(A|B) = P(A∩B) / P(B)` — 조건부 막대 `|` 정상
- `P(B|A) = P(A∩B) / P(A)` — 두 번째 조건부 막대도 정상
- 두부(▦) 박스 완전 소거, 기존 단일 `|` 기호와 동일 형태로 렌더

### 권위 자료 비교
한글 2022 편집기 PDF(`pdf/`)의 동일 수식과 시각적으로 동일한 `|` 막대 위치/형태 확인.

## 산출물
- `output/svg/task1343/..._016.svg`
- 크롭 검증 이미지(`crop_q24c.png`) — 문24 수식 영역

판정: **통과**. 이슈 #1343 증상 해소.
