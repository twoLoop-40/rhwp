# Stage 3 보고서 — Task #1258: 구현안 B 시도 + 진단 (방향 재판단 필요)

- **이슈**: edwardkim/rhwp#1258
- **단계**: Stage 3 / 5 (정규화 적용 시도)
- **결과**: **B 단순 적용 실패 → 코드 복원(green).** 구조적 진단 확보.

## 1. 시도한 변경

`paragraph_layout.rs:4509` 다줄 미주 문제경계 trailing 을 `0.0` → `line_spacing_px`(단일줄 4523·pagination 정합).
S8 은 유지(Stage 2 계획대로 "#1246 자연 no-op" 기대).

## 2. 결과 — issue_1139 8건 회귀 (이중가산)

`issue_1139_inline_picture_duplicate`: 46 → **38 passed, 8 failed**. 실패 전부 미주 between-notes gap:
#1139 q15, #1189(2023 q29 / 2022oct), #1209(p17×2, **p14 q22**), #1256 q12, #1261 q8.

문22 실측: y **484.3 → 510.77px** (약 +26.5px=between_notes 만큼 더 내려감), pi=645 overflow 15.3px 발생.
gap 이 26.5 → **52.9px(정확히 2배)** = between_notes **이중가산**.

## 3. 핵심 진단 — Stage 2 가설 반증

> Stage 2 가설: "render 가 trailing 을 포함하면 stored_gap_px 가 ≈0 이 아니게 되어 #1246 이 자연 no-op."
> **반증됨.**

`#1246` 조건: `stored_gap_px = result - y_offset ∈ (-0.5, 4.0)`. 이는 **상대값**이다.
render 변경으로 y_offset(=prev 커서 끝)이 내려가면, vpos 기반 `result` 도 (base shift 로) 같이 내려가
**gap 은 여전히 ≈0 유지** → #1246 이 **계속 발화** → render(4509) + #1246 이 between_notes 를 **두 번** 더함.

| | prev_content_bottom | render 4509 trailing | #1246 min-gap | 문22 y |
|--|--------------------|---------------------|---------------|--------|
| baseline | 457.87 | 0 (제로) | +26.5 | 484.3 ✓ |
| B 단순 | 457.87 | +26.5 | +26.5(여전히 발화) | 510.8 ✗ |

## 4. 결론 — B 는 분리 불가, 결합 변경 필요

option B 는 "render 먼저, S8 나중 제거"로 **분리 불가**. 같은 변경에서:
1. `paragraph_layout.rs:4509` trailing 포함, **그리고**
2. height_cursor `#1246` min-gap **제거**(또는 add 비활성), **그리고**
3. 다줄에도 base-shift 보장(단일줄 `#1256/#1261` 가드의 다줄 확장/통합)

을 **동시에** 해야 한다. 즉 #1248 조사가 예측한 "#1246(가산) → #1256형(이중방지) 가드로 수렴".

## 5. 가치 재판단 (정직)

- 결합 변경 후에도 결과는 **gap 제공 위치 이동**(height_cursor → render) + **여전히 base-shift 가드 필요**.
  순(net) 특례 감소는 불확실(#1246+#1256 통합으로 1개 감소 가능성 ~ 동일 수 유지).
- 첫 단순 시도에서 8건 회귀 → 위험 실재. #1248 조사의 "즉시 가치 낮음·위험 중간" 평가가 **실측으로 확인**됨.
- 즉 A 정규화는 **구조적 대청소가 아니라 측면 이동(lateral move)** 에 가깝다.

## 6. 선택지 (작업지시자 판단)

| 안 | 내용 |
|----|------|
| **중단 (권장)** | 진단 확보로 #1258 목적(통일 가능성 실증) 달성. 결합 변경의 이득이 위험 대비 불확실 → 현 S8 유지·문서화·종료 |
| 결합 B 강행 | 4509 + #1246 제거 + base-shift 다줄 확장 동시 변경. 고노력, 순 이득 불확실 |
| 재범위 | C 영역(forward 캡) 제외하고 더 좁은 정규화만 탐색 |

## 승인 요청

방향 결정 요청. **중단** 시 Stage 3 진단을 최종 보고서로 정리하고 종료.
