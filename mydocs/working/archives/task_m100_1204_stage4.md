# Stage 4 완료보고서 — Task M100 #1204

**단계**: 시각 검증
**브랜치**: `local/task1204`

## 렌더 검증 (20쪽)

`rhwp export-svg samples/3-09월_교육_통합_2022.hwpx -o output/svg/t1204/ -p 19`

leak 토큰 잔존 검사 (SVG grep):

| 토큰 | 수정 전 | 수정 후 |
|------|--------|--------|
| `root3` | 7 | **0** |
| `>bar<` | 5 | **0** |
| `>prime` | (있음) | **0** |
| `primeF` | (있음) | **0** |

rsvg 래스터(우측 상단, 문24·25):
- 문24) `(2a, √3)`, `2ax/a² - √3 y = 1`, `2/(√3 a)`, `1/√3`, `-√3` — √ 모두 렌더.
- 문25) `F̄'F̄ = √(AF'² - AF²) = √(5²-3²) = 4`, `a > √5`, `P̄F̄ + P̄F̄' = 2a` — overline·√ 모두 렌더.

## 권위 자료 대조

한글 2022 PDF 20쪽과 시각 정합 (√·overline 표현 일치).

## 비고

- A(root/sqrt)·B(rm bar)·C(prime) 모두 해소.
- 문26·15 도 동일 토큰(√/함수) 기반으로 leak 0 확인.
