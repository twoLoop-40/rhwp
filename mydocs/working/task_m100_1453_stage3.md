# Task M100 #1453 단계별 완료보고서 — 3단계: 14종 시각판정 산출물 + 비교 분석

- 이슈: #1453
- 브랜치: `local/task1453`
- 단계: 3/3 (시각판정 산출물 + 정답지 대조)
- 작성일: 2026-06-21

## 산출물

`output/poc/chart_c1a/{종류}/` (gitignore):
- `{종류}.svg` — rhwp export-svg (hwpx, hwp 둘 다)
- `rhwp.png` — rhwp 렌더 SVG → PNG (rsvg-convert, z=1.5)
- `hancom.png` — `pdf/chart/{경로}-2022.pdf` 정답지 → PNG (pdftoppm, 110dpi)

7종 × (rhwp/hancom) PNG + 14 SVG 생성 완료.

## 정답지(한컴 2022 PDF) 대조표

> 판정 기준: **placeholder 제거 + 데이터·기하 정확** = C1a 완료(#1431 Track C). 픽셀 parity 비목표.
> 환경: macOS (한컴 편집기 미접근) → `pdf/chart/`(한컴 2022) 1차 정답지.

| 종류 | OOXML→라우팅 | 데이터·기하 | rhwp(2D 근사) | 한컴 정답지 | 차이 (분류) |
|------|------|:---:|------|------|------|
| 3차원묶은세로막대형 | bar3DChart/col→Column | ✅ 일치(4×3) | 평면 묶은 세로막대 | 3D 입체 세로막대 | 3D→평면(C1a 근사), 제목·팔레트·범례(갭1-3) |
| 3차원누적세로막대형 | bar3DChart/col→Column | ✅ 값 일치 | **묶은**(grouped) 세로막대 | 3D **누적** 세로막대 | 3D→평면 + **누적 미반영**(선재 렌더러 갭) |
| 3차원묶은가로막대형 | bar3DChart/bar→Bar | ✅ 일치 | 평면 묶은 가로막대 | 3D 입체 가로막대 | 3D→평면, 갭1-3 |
| 3차원누적가로막대형 | bar3DChart/bar→Bar | ✅ 값 일치 | **묶은** 가로막대 | 3D **누적** 가로막대 | 3D→평면 + **누적 미반영** |
| 3차원원형 | pie3DChart→Pie | ✅ 비율 일치 | 평면 원형 | 3D 입체 원형 | 3D→평면(C1a 근사), 제목·팔레트·범례 |
| 원형대원형 | ofPieChart→Pie | ✅ 전 데이터 표시 | 단일 원형(4조각) | 주 원형 + 보조 원형 | ofPie 보조플롯 미표현(C1a 근사→단일원형) |
| 원형대가로막대형 | ofPieChart→Pie | ✅ 전 데이터 표시 | 단일 원형(4조각) | 주 원형 + 보조 막대 | ofPie 보조플롯 미표현(C1a 근사→단일원형) |

## 핵심 관찰

1. **placeholder 0건** — 7종 14파일 모두 "차트 (미지원)" 제거, 막대/원형 정상 렌더. (목표 달성)
2. **데이터·기하 정확** — 카테고리·시리즈 개수·값·원형 분할 비율이 정답지와 일치.
3. **차이 분류**:
   - (a) **의도된 C1a 2D 근사**: 3D 입체→평면, ofPie 보조플롯→단일 원형. (계획 명시, C2 후속)
   - (b) **기존 스타일 4갭**(#1431 측정값): 제목 누락 / 팔레트 초록-우선(한컴 파랑-우선) / 범례 하단(한컴 우측) / Y축. → **C1c** 범위.
   - (c) **누적(stacked) 미반영**: 누적형이 묶은형으로 렌더 — C1a 라우팅과 무관한 **선재 렌더러 한계**(2D 누적형에도 동일). 별도 후속 후보.
4. C1a 범위(라우팅) 안에서 **회귀·신규 결함 없음**. (b)(c)는 본 작업이 만든 게 아니라 7종이 렌더되며 드러난 기존 갭.

## 전체 검증

```
$ cargo test                  # 전체 스위트: 2469 passed; 0 failed (RC=0) — 회귀 없음
$ cargo build / clippy --lib  # 통과 / ooxml_chart 무경고
$ cargo test --test issue_1453_chart_3d_ofpie_routing  # 14파일 가드 통과 (1 passed)
```

## 작업지시자 시각판정 요청

`output/poc/chart_c1a/{종류}/rhwp.png` ↔ `hancom.png` 비교로 **2D 근사 수용선** 확정 요청:
- 3D→평면, ofPie→단일원형 근사가 C1a 수용 범위인지
- 누적 미반영(c)을 C1a에서 다룰지(별도 후속으로 둘지)
