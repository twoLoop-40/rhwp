# task 1293 stage119 - 2022-10 p14 잔여 분류

## 목적

stage118 전체 sweep에서 `2022-10` p14가 `flagged=1/18`로 남았다.

stage117 이후 question/tail 후보는 사라졌고, 현재 p14는 `line/equation/large` 후보만 남았다.
이 stage에서는 이것이 공식 미주 간격 계산 문제인지, 수식/large ink 비교 지표의 잔여인지 분류한다.

## 시작 기준

- 브랜치: `local/task_m100_1293`
- 시작 커밋: `2f694ec9 task 1293: stage117 이후 sweep 재집계`
- 대상:
  - `2022-10` p14

## 조사 계획

1. 이전 stage 문서에서 `2022-10` 처리 기록을 확인한다.
2. `output/task1293_stage118_full_sweep/2022-10`의 p14 flagged JSON과 question flow를 확인한다.
3. equation overlap/large ink 후보가 실제 배치 문제인지, 자동 비교 지표의 남은 noise인지 분류한다.

## 조사 결과

- 이전 Stage51/Stage52 기준에서 `2022-10`은 핵심 게이트 0까지 정리된 적이 있다.
- stage118 p14 최신 상태:
  - page/frame overflow 없음: `rhwp_outside_frame_pixels=0`, `pdf_outside_frame_pixels=0`
  - question marker drift 없음
  - render-tree frame tail overflow 없음
  - red marker drift는 후보 flags에 포함되지 않으며, marker count는 RHWP/PDF 모두 5개이다.
  - 남은 flags: `equation_text_overlap`, `line_band_drift`, `column_line_band_drift`,
    `large_ink_region_drift`
- question flow:
  - p14 문28/문29/문30 및 우단 문23/문24/문25의 page/column 흐름은 PDF와 동일하다.
  - 문23/문24는 각각 약 15px/41px y 차이가 있지만, 문항 page/column drift는 없다.
- equation overlap 후보:
  - 후보는 1개뿐이다.
  - `equation_line_pi=775`, text line `"[EQ]에서 [EQ]"`
  - `text_pi=777`, text `", "`, text line `"[EQ], [EQ]이므로"`
  - overlap은 `9px`, `overlap_ratio=0.5`로 작은 쉼표 glyph bbox와 직전 수식 bbox가 걸린다.
- `compare_014.png`, `annotated_014.png` 육안 확인:
  - p14의 미주 제목 흐름과 하단 frame은 맞는다.
  - 자동 후보는 우단 중하단의 수식/쉼표 근처를 가리킨다.
  - PDF 대비 큰 차이는 문항/미주 간격이 아니라 수식 박스와 glyph bbox의 세부 위치/검출 차이다.

## 판단

- `2022-10` p14는 공식 `구분선 위/아래/미주 사이` 모델의 추가 불일치로 보기 어렵다.
- 남은 후보는 수식과 짧은 텍스트 bbox의 9px 교차 및 large ink region coarse matching 차이로 분류한다.
- 코드 수정은 하지 않는다.

## 검증 계획

문서 분류 stage이면 `git diff --check`만 수행한다. 코드 수정이 필요하면 targeted sweep을 수행한다.

## 검증 결과

- `git diff --check`: 통과
