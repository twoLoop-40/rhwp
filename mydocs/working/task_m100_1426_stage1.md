# Task M100 #1426 1단계 완료보고서 — 테두리 상태 규칙 구현

- 이슈: #1426
- 브랜치: `local/task1426`
- 작성일: 2026-06-21
- 단계: 1단계 — 테두리 상태 규칙 구현

## 1. 작업 범위

`rhwp-studio/src/ui/page-border-dialog.ts`의 쪽 테두리/배경 대화상자에서
미리보기 방향 버튼 클릭 경로를 켜기 전용 동작에서 상태 기반 토글 동작으로 변경했다.

이번 단계는 최종 상호작용 규칙을 코드에 반영하는 범위다. `테두리 사용 안 함` 체크 상태,
개별 방향, 전체 버튼, `선 모양 바로 적용`이 모두 같은 방향별 상태 모델을 사용하도록 정리했다.

## 2. 변경 내용

| 항목 | 변경 |
|------|------|
| 방향 목록 | `ALL_SIDES` 상수 추가 |
| 개별 버튼 | `toggleSide()` 추가, 켜짐이면 `noneBorder()`로 해제하고 꺼짐이면 `currentBorder()` 적용 |
| 전체 버튼 | `toggleAllSides()` 추가, 사방 모두 켜짐이면 전체 해제하고 일부라도 꺼져 있으면 전체 적용 |
| 상태 판정 | `isSideActive()` 추가, `borderEdits[side].type !== 0` 기준 |
| 체크박스 동기화 | `syncBorderNoneCheck()` 추가, 활성 방향이 없으면 `테두리 사용 안 함` 체크 |
| none 체크 동작 | `handleBorderNoneChange()`와 `clearAllSides()` 추가, 체크 시 내부 방향 상태 전체 해제 |
| 즉시 적용 | 선 종류/굵기/색 변경 시 현재 켜진 방향에만 `currentBorder()` 적용 |
| 기존 적용 경로 | `applyToSides()`도 변경 후 `syncBorderNoneCheck()`를 사용하도록 정리 |

## 3. 최종 조작 규칙

| 조작 | 규칙 |
|------|------|
| 개별 방향 버튼 | 해당 방향이 켜져 있으면 해제, 꺼져 있으면 현재 선 속성으로 적용 |
| 전체 버튼 | 네 방향이 모두 켜져 있으면 전체 해제, 하나라도 꺼져 있으면 전체 적용 |
| `테두리 사용 안 함` 체크 | 네 방향 내부 상태를 모두 해제하고 preview 선을 숨김 |
| none 상태에서 개별 방향 클릭 | 전체 상태를 되살리지 않고 클릭한 방향만 적용 |
| none 상태에서 전체 클릭 | 한 번에 네 방향을 모두 적용 |
| `선 모양 바로 적용` | 현재 켜진 방향에만 선 속성 변경 반영 |

## 4. 검증 결과

| 명령 | 결과 |
|------|------|
| `cd rhwp-studio && npm run build` | 통과 |
| `git diff --check` | 통과 |

`npm run build`에서 Vite chunk size 경고가 출력되었으나 빌드는 정상 완료되었다.
해당 경고는 번들 크기 안내이며 이번 변경의 컴파일 오류는 아니다.

## 5. 남은 작업

2단계에서 다음 작업을 진행한다.

1. `page-border-toggle.test.mjs` E2E 회귀 테스트를 추가한다.
2. 개별 버튼 2회 클릭, 전체 버튼 2회 클릭, 위쪽만 켠 상태의 선 속성 변경을 검증한다.
3. `테두리 사용 안 함` 상태에서 전체/개별 버튼 클릭 결과를 검증한다.

## 6. 승인 요청

1단계 변경을 검토한 뒤, 2단계 진행 여부를 승인받는다.
