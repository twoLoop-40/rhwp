# task 1293 stage117 - 2022-09 기본 below rewind/title tail 보정

## 목적

stage116 전체 sweep에서 `2022-09`가 `flagged=2/23`으로 남았다.

이 stage에서는 p16/p20 후보가 task1293의 공식 미주 모양 간격 문제인지, 이전에 확인한
본문/그림/tail 잔여인지 최신 산출물 기준으로 다시 분류하고, 기본 `구분선 아래` profile에서
빠진 title-tail 조건을 보정한다.

## 시작 기준

- 브랜치: `local/task_m100_1293`
- 시작 커밋: `97f79e28 task 1293: stage115 이후 sweep 재집계`
- 대상:
  - `2022-09` p16
  - `2022-09` p20

## 조사 계획

1. 이전 stage 문서에서 `2022-09` 잔여 원인 기록을 확인한다.
2. `output/task1293_stage116_full_sweep/2022-09`의 flagged JSON, question flow, render-tree 후보를 확인한다.
3. 공식 `구분선 위/아래/미주 사이` 계산 문제인지, 본문/그림/tail/검출 noise인지 분류한다.

## 조사 결과

- 이전 Stage47은 `2022-09`의 renderer overflow 로그를 line-box tail 허용으로 정리했지만,
  최신 sweep의 p16/p20은 question-flow 비교에서 드러난 제목 tail/cascade 성격이었다.
- stage116 p16:
  - RHWP: 문24가 p16 2단 상단(`y=90.7`)으로 넘어갔다.
  - PDF: 문24가 p16 1단 하단(`y=982.8`)에 남았다.
  - debug: 문24 제목 emit에서 `cur=871.13`, `gap=26.45px`, `has_rewind=true`,
    `large_head_outside=true`, `advance_new=true`.
  - 기본 `미주 사이` gap과 제목 한 줄은 현재 단에 들어가지만, 기본 `구분선 아래` 2mm라
    stage107의 큰 below 예외에 걸리지 않았다.
- stage116 p20:
  - RHWP: 문23이 p20 2단 상단(`y=90.7`)으로 넘어갔다.
  - PDF: 문23이 p20 1단 하단(`y=1010.6`)에 남았다.
  - debug: 문23 제목 emit에서 `cur=898.85`, `gap=26.45px`, `has_rewind=true`,
    `allow_default_col_bottom=true`, `advance_new=true`.
- 첫 보정만 적용하면 p20은 해소되고 문24~문26도 PDF와 맞지만, 문27 제목과 equation-only head가
  p16 마지막 단 하단에 orphan으로 남아 새 p17 drift가 생겼다.
  - debug: 문27 제목 emit `cur=972.32`, 다음 두 equation-only head를 더하면 frame을 넘는다.

## 수정

- `src/renderer/typeset.rs`
  - 기본 `미주 사이` + visible separator + 기본값 근방 `구분선 아래` + 저장 vpos rewind에서
    공식 `미주 사이` gap과 제목 한 줄이 첫 단 하단에 들어가면 새 미주 advance를 막는다.
  - 기본값 근방 `구분선 아래`의 마지막 단에서 제목 한 줄만 들어가고 뒤따르는 equation-only
    head pair가 frame을 넘으면 다음 쪽/단으로 advance해 제목 orphan을 막는다.

## 검증 계획

코드 수정 stage로 전환했으므로 다음을 수행한다.

- `cargo fmt --check`
- `cargo build --bin rhwp`
- `python3 scripts/task1274_visual_sweep.py --target 2022-09 --target 2023-09 --target 2022-10 --target 2022-11-practice --target 2024-11-practice-above0-between7-below2 --target 2024-11-practice-above0-between7-below20 --out output/task1293_stage117_targeted_v2`
- `git diff --check`

## 검증 결과

- `cargo fmt --check`: 통과
- `cargo build --bin rhwp`: 통과
- targeted sweep `output/task1293_stage117_targeted_v2`
  - `2022-09`: `flagged=0/23`
  - `2023-09`: `flagged=0/20`
  - `2022-10`: `flagged=1/18` (p14, `line/equation/large` 후보)
  - `2022-11-practice`: `flagged=0/21`
  - `2024-11-practice-above0-between7-below2`: `flagged=0/21`
  - `2024-11-practice-above0-between7-below20`: `flagged=0/21`

## 판단

- `2022-09` p16/p20은 공식 `미주 사이` gap과 제목 한 줄이 들어가는데도 small-below profile에서
  title-tail 허용이 빠져 생긴 간격/advance 문제로 판단한다.
- stage117 보정으로 `2022-09`는 최신 sweep 기준 0이 됐다.
- `2022-10` p14는 question/tail 후보가 사라졌지만 line/equation/large 후보가 남았으므로 다음
  stage에서 별도 분류한다.
