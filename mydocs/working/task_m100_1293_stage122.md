# task 1293 stage122 - rebase 후 sweep 재검증과 정상 target 회귀 테스트

## 시작 기준

- rebase 기준: `upstream/devel` `a0a37d72`
- rebase 후 최신 커밋: `69aabe0b task 1293: 중복 sweep target 제거`
- 대상: `tests/issue_1139_inline_picture_duplicate.rs`, `mydocs/report/task_m100_1293_report.md`

## 요청

1. `upstream/devel` 기준 rebase 후 전체 visual sweep을 다시 실행한다.
2. 최종 보고서의 최종 잔여물과 동일한지 확인한다.
3. 현재 기준 정상 판정난 target을 `tests/issue_1139_inline_picture_duplicate.rs`에 회귀 테스트로 추가한다.

## 작업 계획

1. rebase 후 sweep 결과를 `output/task1293_stage122_rebase_full_sweep`에 생성한다.
2. `summary.json`의 `targets/pages/flagged/clean_targets`와 잔여 key를 보고서 기준과 비교한다.
3. 정상 판정 target 중 대표 미주 모양 샘플이 잔여 후보로 다시 잡히지 않도록 회귀 테스트를 추가한다.
4. focused 통합 테스트와 `git diff --check`를 실행한다.

## 변경 내용

- `tests/issue_1139_inline_picture_duplicate.rs`
  - 최종 sweep에서 clean인 12개 target의 page count, 공식 미주 모양 profile, 구분선 표시 여부를 고정하는 회귀 테스트를 추가했다.
- `mydocs/report/task_m100_1293_report.md`
  - rebase 후 sweep 결과를 최종 sweep으로 갱신했다.

## rebase 후 sweep 결과

- `cargo build --bin rhwp`
  - 통과
- `python3 scripts/task1274_visual_sweep.py --target all --out output/task1293_stage122_rebase_full_sweep`
  - 통과
  - `targets=15`, `pages=323`, `flagged=7`, `clean_targets=12`
  - 잔여 key:
    - `2022-10`: `1/18`
    - `2024-09-below20-above20`: `3/23`
    - `2024-11-practice-above0-between20-below2`: `3/22`
  - stage121 최종 보고서의 잔여 key와 동일하다.

## 테스트 결과

- `cargo test --test issue_1139_inline_picture_duplicate issue_1293_clean_visual_sweep_targets_keep_page_counts_and_shape_profiles -- --nocapture`
  - 통과 (`1 passed`)
- `cargo test --test issue_1139_inline_picture_duplicate issue_1293`
  - 통과 (`10 passed`)
- `cargo fmt --check`
  - 통과
- `cargo test --test issue_1139_inline_picture_duplicate`
  - 실패 (`78 passed`, `7 failed`)
  - 실패 항목은 새 회귀 테스트가 아니라 기존 #1274/#1284 strict 위치 검사다.
  - 실패 목록:
    - `issue_1274_2022_oct_page11_question20_equation_tail_stays_in_frame`
    - `issue_1274_2022_oct_page16_question30_title_keeps_first_line`
    - `issue_1284_2023_sep_page20_question30_title_stays_in_left_tail`
    - `issue_1284_2024_between20_page18_late_question_titles_match_pdf`
    - `issue_1284_2024_between20_page19_question24_continues_from_pdf_top`
    - `issue_1284_2024_between20_page21_question23_title_stays_in_left_tail`
    - `issue_1284_2024_between20_page22_23_question_tail_matches_pdf`
- `git diff --check`
  - 통과
