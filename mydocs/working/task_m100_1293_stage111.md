# task 1293 stage111 - between20/below2 p17 tail 후보 분리

## 목적

stage110에서 `2024-11-practice-above0-between20-below2` 잔여 후보는 `flagged=3/22`가 되었다.

이 stage에서는 남은 p17/p20/p21 중 p17의 `render_tree_frame_tail_overflow`와 question marker drift를 먼저 분리한다. p17이 cascade 원인이면 p21까지 함께 줄어드는지 확인한다.

## 시작 기준

- 브랜치: `local/task_m100_1293`
- 시작 커밋: `c91a4b30 task 1293: between20 rewind 제목 tail 보정`
- 대상:
  - `2024-11-practice-above0-between20-below2`

## 조사 계획

1. stage110 `flagged_pages.json`에서 p17의 question marker drift와 render-tree overflow 후보를 확인한다.
2. p17의 실제 overflow 문항이 새 미주 제목 tail, 본문 continuation, 또는 sweep 과검출인지 구분한다.
3. 수정이 필요하면 `betweenNotes=20mm` 마지막 단 tail 조건만 좁게 보정한다.

## 검증 계획

수정이 필요하면 다음 범위로 확인한다.

```bash
cargo fmt --check
cargo build --bin rhwp
python3 scripts/task1274_visual_sweep.py \
  --target 2024-11-practice-above0-between20-below2 \
  --target 2024-09-between20 \
  --target 2024-09-below20 \
  --target 2024-11-practice-above0-between7-below2 \
  --out output/task1293_stage111_targeted
git diff --check
```

CI 전체 테스트와 PR은 작업지시자 지시에 따라 수행하지 않는다.

## 조사 결과

- p17
  - 문27 marker가 RHWP `y=729.3`, PDF `y=674.6`으로 약 `+54.7px` 낮다.
  - render-tree overflow 후보는 문27 마지막 줄 `overflow=5.0px`이다.
  - marker gap에서 문26->문27 구간만 RHWP가 PDF보다 약 `55px` 크다.
  - 문26 시작 marker는 양쪽이 거의 같고, 문26 본문 높이 누적이 RHWP에서 더 커진 뒤 문27이 늦게 시작한다.
- p20
  - question marker drift는 없다.
  - `betweenNotes` marker gap은 최대 `0.5px` 차이로 사실상 맞는다.
  - 남은 후보는 문28 본문 tail overflow(`39.9px`, `61.0px`)와 line-band drift이다.
- p21
  - 문29 marker가 RHWP `y=750.6`, PDF `y=805.7`로 약 `-55.1px` 높다.
  - p20의 문28 tail/본문 흐름 차이가 다음 쪽 content bottom 후보로 이어진 성격이다.

## 판정

- p17/p20/p21 잔여 후보는 공식 `구분선 위/아래/미주 사이` 간격 모델의 추가 불일치로 보기 어렵다.
- p17은 문26 본문 높이 누적, p20은 문28 그림/수식/본문 tail, p21은 p20 이후 본문 흐름 차이로 분류한다.
- 따라서 이 stage에서는 typeset 미주 간격 계산식을 더 보정하지 않는다.

## 검증 결과

- 기준 sweep: `output/task1293_stage110_targeted`
- `2024-11-practice-above0-between20-below2`: `flagged=3/22` (p17, p20, p21)
- `2024-09-between20`: `flagged=0/24`
- `2024-09-below20`: `flagged=0/23`
- `2024-11-practice-above0-between7-below2`: `flagged=0/21`

## 다음 단계

- 전체 sweep을 다시 재집계해 Stage109/110 개선이 다른 target 총량에 어떻게 반영됐는지 확인한다.
- 다음 수정은 미주 간격 불일치로 남은 후보가 확인될 때만 진행한다.
